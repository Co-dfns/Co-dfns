:Namespace t0016

catfirst←{⍺⍪⍵}

find←{⍺⍷⍵}

gradedown←{⍒⍵}

gradeup←{⍋⍵}

indexof←{⍺⍳⍵}

intersection←{⍺∩⍵}

matinv←{⌹⍵}

matdiv←{⍺⌹⍵}

membership←{⍺∊⍵}

split←{↓⍵}

union←{⍺∪⍵}

unique←{∪⍵}

:EndNamespace
