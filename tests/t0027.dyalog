:Namespace t0027

F←{⍵}
shape←{⍴⍵}
reshape←{⍺⍴⍵}
transpose1←{⍉⍵}
transpose2←{⍺⍉⍵}
bracket←{⍺[⍵]}
gradeup1←{⍋⍵}
add←{⍺+⍵}
neg←{-⍵}
addax←{1 2 3+[⍺]⍵}
sign←{×⍵}
recip←{÷⍵}
mag←{|⍵}
floor←{⌊⍵}
ceil←{⌈⍵}
expt←{*⍵}
loge←{⍟⍵}
piti←{○⍵}
trig←{⍺○⍵}

:EndNamespace