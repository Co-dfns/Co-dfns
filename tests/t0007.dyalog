:Namespace t0007

add←{⍺+⍵} ⋄ sub←{⍺-⍵} ⋄ mul←{⍺×⍵} ⋄ div←{⍺÷⍵}
pow←{⍺*⍵} ⋄ log←{⍺⍟⍵} ⋄ res←{⍺|⍵} ⋄ min←{⍺⌊⍵}
max←{⍺⌈⍵} ⋄ leq←{⍺≤⍵} ⋄ let←{⍺<⍵} ⋄ eql←{⍺=⍵}
geq←{⍺≥⍵} ⋄ get←{⍺>⍵} ⋄ neq←{⍺≠⍵} ⋄ and←{⍺∧⍵}
lor←{⍺∨⍵} ⋄ nor←{⍺⍱⍵} ⋄ nan←{⍺⍲⍵} ⋄ cir←{⍺○⍵}
bin←{⍺!⍵} ⋄ con←{+⍵}  ⋄ neg←{-⍵}  ⋄ dir←{×⍵}
rec←{÷⍵}  ⋄ exp←{*⍵}  ⋄ nlg←{⍟⍵}  ⋄ mag←{|⍵}
pit←{○⍵}  ⋄ flr←{⌊⍵}  ⋄ cel←{⌈⍵}  ⋄ not←{~⍵}
mat←{⌷⍵}  ⋄ fac←{!⍵}

:EndNamespace