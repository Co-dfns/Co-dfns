  <section xmlns="http://docbook.org/ns/docbook" version="5.0" xmlns:xi="http://www.w3.org/2001/XInclude">
    <title>Derived Requirements</title>
    <simpara>
      The following are the requirements that were derived from the sequence
      enumeration. They will be merged into the
      <citetitle>Software Requirements</citetitle>
      at the end of specification.
    </simpara>
    <orderedlist>
      <listitem>
        <simpara>Only a single documented function is exposed from the
        compiler namespace.</simpara>
      </listitem>
      <listitem>
        <simpara>
          When given a pathname that is an invalid pathname, a
          <literal>DOMAIN ERROR</literal>
          should be signalled.
        </simpara>
      </listitem>
      <listitem>
        <simpara>
          An interrupt or break signal shall cause an immediate termination
          of the compiler with a signal
          <literal>INTERRUPT</literal>
          . Any resources in use at the time, including files, shall be
          released as is.
        </simpara>
      </listitem>
      <listitem>
        <simpara>
          An error in the LLVM system shall signal an
          <literal>INTERNAL ERROR</literal>
          .
        </simpara>
      </listitem>
      <listitem>
        <simpara>Allow for any operator to be named or parenthesized.</simpara>
      </listitem>
      <listitem>
        <simpara>
          The namespace returned by
          <function>CoDfns.Fix</function>
          should allow both calling of the functions defined in the given
          namespace script as well as rebinding them to names outside of
          the namespace in a manner consistent with normally fixed namespaces.
        </simpara>
      </listitem>
      <listitem>
        <simpara>
          Attempting to access a variable in the namespace returned by
          <function>CoDfns.Fix</function>
          that is not bound by the namespace script should signal a
          <symbol>VALUE ERROR</symbol>
          .
        </simpara>
      </listitem>
      <listitem>
        <simpara>
          Any erroneous input passed to a function of a namespace returned
          by
          <function>CoDfns.Fix</function>
          should result in the same error as would have been produced by
          giving the same input to the namespace returned by the
          <function>⎕FIX</function>
          function on the same namespace script.
        </simpara>
      </listitem>
    </orderedlist>
  </section>
