<listitem xmlns="http://docbook.org/ns/docbook" version="5.0">
  <formalpara>
    <title>Description</title>
    <para>A complete system crash occurs on an empty namespace.</para>
  </formalpara>
  <formalpara>
    <title>Analysis</title>
    <para>
      The return on
      <function>GenInit</function>
      was incorrectly passing and useless environment. In the GenInit function,
      we don't actually have any interesting elements that need to be returned,
      so we can just do a direct return and eliminate the need to make
      the call to
      <function>MkRet</function>
      .
    </para>
  </formalpara>
</listitem>
