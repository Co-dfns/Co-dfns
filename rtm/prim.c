#include "codfns.h"
#include "prim.h"

EXPORT int
DyalogGetInterpreterFunctions(void *p)
{
    return set_dwafns(p);
}

extern struct cell_func *q_signal_ibeam;
extern struct cell_func *q_dr_ibeam;
extern struct cell_func *squeeze_ibeam;
extern struct cell_func *is_simple_ibeam;
extern struct cell_func *is_numeric_ibeam;
extern struct cell_func *is_char_ibeam;
extern struct cell_func *is_integer_ibeam;
extern struct cell_func *max_shp_ibeam;
extern struct cell_func *max_shp_ibeam;
extern struct cell_func *has_nat_vals_ibeam;
extern struct cell_func *any_ibeam;
extern struct cell_moper *veach_ibeam;
extern struct cell_func *set_ibeam;
extern struct cell_func *reshape_ibeam;
extern struct cell_func *reshape_ibeam;
extern struct cell_func *shape_ibeam;
extern struct cell_func *ravel_ibeam;
extern struct cell_func *ravel_ibeam;
extern struct cell_func *ravel_ibeam;
extern struct cell_func *ravel_ibeam;
extern struct cell_func *same_ibeam;
extern struct cell_func *nqv_ibeam;
extern struct cell_func *index_ibeam;
extern struct cell_func *index_ibeam;
extern struct cell_func *index_gen_vec;
extern struct cell_func *index_gen_vec;
extern struct cell_func *disclose_ibeam;
extern struct cell_func *enclose_ibeam;
extern struct cell_func *enclose_ibeam;
extern struct cell_func *conjugate_vec;
extern struct cell_func *conjugate_vec;
extern struct cell_func *add_vec_ibeam;
extern struct cell_func *sub_vec_ibeam;
extern struct cell_func *mul_vec_ibeam;
extern struct cell_func *div_vec_ibeam;
extern struct cell_func *abs_ibeam;
extern struct cell_func *abs_ibeam;
extern struct cell_func *floor_vec_ibeam;
extern struct cell_func *floor_vec_ibeam;
extern struct cell_func *min_vec_ibeam;
extern struct cell_func *ceil_vec_ibeam;
extern struct cell_func *ceil_vec_ibeam;
extern struct cell_func *max_vec_ibeam;
extern struct cell_func *pow_vec_ibeam;
extern struct cell_func *exp_vec_ibeam;
extern struct cell_func *log_vec_ibeam;
extern struct cell_func *nlg_vec_ibeam;
extern struct cell_func *sin_vec_ibeam;
extern struct cell_func *arcsin_vec_ibeam;
extern struct cell_func *cos_vec_ibeam;
extern struct cell_func *arccos_vec_ibeam;
extern struct cell_func *tan_vec_ibeam;
extern struct cell_func *arctan_vec_ibeam;
extern struct cell_func *sinh_vec_ibeam;
extern struct cell_func *arcsinh_vec_ibeam;
extern struct cell_func *cosh_vec_ibeam;
extern struct cell_func *arccosh_vec_ibeam;
extern struct cell_func *tanh_vec_ibeam;
extern struct cell_func *arctanh_vec_ibeam;
extern struct cell_func *realpart_vec_ibeam;
extern struct cell_func *realpart_vec_ibeam;
extern struct cell_func *imagpart_vec_ibeam;
extern struct cell_func *imagpart_vec_ibeam;
extern struct cell_func *sin_vec_ibeam;
extern struct cell_func *arcsin_vec_ibeam;
extern struct cell_func *cos_vec_ibeam;
extern struct cell_func *arccos_vec_ibeam;
extern struct cell_func *tan_vec_ibeam;
extern struct cell_func *arctan_vec_ibeam;
extern struct cell_func *sinh_vec_ibeam;
extern struct cell_func *arcsinh_vec_ibeam;
extern struct cell_func *cosh_vec_ibeam;
extern struct cell_func *arccosh_vec_ibeam;
extern struct cell_func *tanh_vec_ibeam;
extern struct cell_func *arctanh_vec_ibeam;
extern struct cell_func *realpart_vec_ibeam;
extern struct cell_func *realpart_vec_ibeam;
extern struct cell_func *imagpart_vec_ibeam;
extern struct cell_func *imagpart_vec_ibeam;
extern struct cell_func *factorial_vec_ibeam;
extern struct cell_func *not_vec_ibeam;
extern struct cell_func *not_vec_ibeam;
extern struct cell_func *and_vec_ibeam;
extern struct cell_func *and_vec_ibeam;
extern struct cell_func *lor_vec_ibeam;
extern struct cell_func *lor_vec_ibeam;
extern struct cell_func *lth_vec_ibeam;
extern struct cell_func *lth_vec_ibeam;
extern struct cell_func *lte_vec_ibeam;
extern struct cell_func *lte_vec_ibeam;
extern struct cell_func *eql_vec_ibeam;
extern struct cell_func *gte_vec_ibeam;
extern struct cell_func *gte_vec_ibeam;
extern struct cell_func *gth_vec_ibeam;
extern struct cell_func *gth_vec_ibeam;
extern struct cell_func *neq_vec_ibeam;
extern struct cell_moper *identity_ibeam;
extern struct cell_moper *identity_ibeam;
extern struct cell_moper *identity_ibeam;
extern struct cell_moper *identity_ibeam;

int ptr7978(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7979(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7980(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7981(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7982(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7983(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7984(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7985(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7986(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7987(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7988(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7989(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7990(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7991(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7992(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7993(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7994(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7995(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7996(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7997(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr7998(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr7999(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8000(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8001(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8002(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8003(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8004(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8005(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8006(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8007(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8008(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8009(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8010(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8011(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8012(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8013(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8014(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8015(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8016(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8017(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8018(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8019(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8020(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8021(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8022(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8023(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8024(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8025(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8026(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8027(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8028(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8029(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8030(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8031(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8032(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8033(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8034(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8035(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8036(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8037(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8038(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8039(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8040(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8041(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8042(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8043(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8044(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8045(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8046(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8047(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8048(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8049(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8050(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8051(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8052(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8053(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8054(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8055(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8056(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8057(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8058(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8059(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8060(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8061(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8062(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8063(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8064(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8065(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8066(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8067(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8068(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8069(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8070(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8071(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8072(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8073(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8074(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8075(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8076(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8077(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8078(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8079(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8080(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8081(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8082(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8083(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8084(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8085(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8086(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8087(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8088(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8089(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8090(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8091(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8092(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8093(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8094(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8095(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8096(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8097(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8098(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8099(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8100(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8101(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8102(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8103(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8104(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8105(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8106(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8107(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8108(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8109(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8110(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8111(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8112(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8113(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8114(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8115(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8116(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8117(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8118(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8119(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8120(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8121(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8122(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8123(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8124(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8125(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8126(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8127(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8128(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8129(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8130(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8131(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8132(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8133(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8134(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8135(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8136(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8137(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8138(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8139(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8140(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8141(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8142(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8143(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8144(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8145(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8146(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8147(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8148(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8149(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8150(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8151(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8152(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8153(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8154(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8155(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8156(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8157(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8158(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8159(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8160(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8161(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8162(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8163(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8164(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8165(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8166(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8167(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8168(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8169(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8170(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8171(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8172(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8173(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8174(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8175(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8176(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8177(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8178(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8179(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8180(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8181(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8182(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8183(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8184(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8185(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8186(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8187(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8188(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8189(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8190(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8191(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8192(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8193(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8194(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8195(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8196(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8197(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8198(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8199(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8200(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8201(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8202(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8203(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8204(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8205(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8206(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8207(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8208(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8209(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8210(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8211(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8212(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8213(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8214(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8215(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8216(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8217(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8218(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8219(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8220(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8221(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8222(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8223(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8224(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8225(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8226(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8227(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8228(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8229(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8230(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8231(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8232(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8233(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8234(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8235(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8236(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8237(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8238(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8239(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8240(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8241(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8242(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8243(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8244(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8245(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int ptr8246(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int ptr8247(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);

int cdf_prim_flag = 0;

wchar_t *cdf_prim_names[] = {L"q_signal", L"q_dr", L"squeeze", L"is_simple", L"is_numeric", L"is_char", L"is_integer", L"is_bool", L"max_shp", L"has_nat_vals", L"chk_scl", L"chk_valid_shape", L"both_simple", L"both_numeric", L"both_integer", L"both_char", L"both_bool", L"any", L"assert_inrange", L"numeric", L"ambiv", L"same", L"veach", L"scalar", L"set", L"brk", L"rgt", L"lft", L"reshape", L"rho", L"cat", L"depth", L"eqv", L"nqv", L"index", L"sqd", L"index_gen", L"index_of", L"iot", L"dis", L"enclose", L"par", L"conjugate", L"add", L"sub", L"sign", L"mul", L"div", L"absolute", L"residue", L"res", L"floor_array", L"min", L"ceil_array", L"max", L"exp", L"log", L"pitimes", L"trig", L"cir", L"binomial", L"fac", L"notscl", L"without", L"not", L"logand", L"and", L"logor", L"lor", L"nan", L"nor", L"lessthan", L"lth", L"lesseql", L"lte", L"eql_vec", L"equal", L"eql", L"greatereql", L"gte", L"greaterthan", L"gth", L"firstocc", L"neq_vec", L"noteq", L"neq", L"mix", L"take", L"tke", L"split", L"drop", L"drp", L"reverse_last", L"rotate_last", L"rot", L"com", L"map", L"reduce", L"nwreduce", L"rdf", L"rpf", L"dot", L"oup", L"pow", L"jot"};

EXPORT int
cdf_prim_init(void)
{
	struct cdf_prim_loc *loc;
	void *stk[128];
	void **stkhd;
	int err;

	if (cdf_prim_flag)
		return 0;

	stkhd = &stk[0];
	cdf_prim_flag = 1;
	cdf_prim_init();

	loc = &cdf_prim;
	loc->__count = 105;
	loc->__names = cdf_prim_names;
	loc->q_signal = NULL;
	loc->q_dr = NULL;
	CHK(mk_func_box(&loc->squeeze, NULL), cleanup, L"Init mutable variable: loc->squeeze");
	CHK(mk_func_box(&loc->is_simple, NULL), cleanup, L"Init mutable variable: loc->is_simple");
	CHK(mk_func_box(&loc->is_numeric, NULL), cleanup, L"Init mutable variable: loc->is_numeric");
	CHK(mk_func_box(&loc->is_char, NULL), cleanup, L"Init mutable variable: loc->is_char");
	CHK(mk_func_box(&loc->is_integer, NULL), cleanup, L"Init mutable variable: loc->is_integer");
	CHK(mk_func_box(&loc->is_bool, NULL), cleanup, L"Init mutable variable: loc->is_bool");
	CHK(mk_func_box(&loc->max_shp, NULL), cleanup, L"Init mutable variable: loc->max_shp");
	CHK(mk_func_box(&loc->has_nat_vals, NULL), cleanup, L"Init mutable variable: loc->has_nat_vals");
	CHK(mk_func_box(&loc->chk_scl, NULL), cleanup, L"Init mutable variable: loc->chk_scl");
	CHK(mk_func_box(&loc->chk_valid_shape, NULL), cleanup, L"Init mutable variable: loc->chk_valid_shape");
	CHK(mk_func_box(&loc->both_simple, NULL), cleanup, L"Init mutable variable: loc->both_simple");
	CHK(mk_func_box(&loc->both_numeric, NULL), cleanup, L"Init mutable variable: loc->both_numeric");
	CHK(mk_func_box(&loc->both_integer, NULL), cleanup, L"Init mutable variable: loc->both_integer");
	CHK(mk_func_box(&loc->both_char, NULL), cleanup, L"Init mutable variable: loc->both_char");
	CHK(mk_func_box(&loc->both_bool, NULL), cleanup, L"Init mutable variable: loc->both_bool");
	CHK(mk_func_box(&loc->any, NULL), cleanup, L"Init mutable variable: loc->any");
	CHK(mk_func_box(&loc->assert_inrange, NULL), cleanup, L"Init mutable variable: loc->assert_inrange");
	loc->numeric = NULL;
	loc->ambiv = NULL;
	CHK(mk_func_box(&loc->same, NULL), cleanup, L"Init mutable variable: loc->same");
	CHK(mk_moper_box(&loc->veach, NULL), cleanup, L"Init mutable variable: loc->veach");
	loc->scalar = NULL;
	loc->set = NULL;
	loc->brk = NULL;
	loc->rgt = NULL;
	loc->lft = NULL;
	loc->reshape = NULL;
	loc->rho = NULL;
	loc->cat = NULL;
	loc->depth = NULL;
	loc->eqv = NULL;
	loc->nqv = NULL;
	CHK(mk_func_box(&loc->index, NULL), cleanup, L"Init mutable variable: loc->index");
	loc->sqd = NULL;
	loc->index_gen = NULL;
	loc->index_of = NULL;
	loc->iot = NULL;
	loc->dis = NULL;
	loc->enclose = NULL;
	loc->par = NULL;
	loc->conjugate = NULL;
	loc->add = NULL;
	loc->sub = NULL;
	loc->sign = NULL;
	loc->mul = NULL;
	loc->div = NULL;
	loc->absolute = NULL;
	loc->residue = NULL;
	loc->res = NULL;
	loc->floor_array = NULL;
	loc->min = NULL;
	loc->ceil_array = NULL;
	loc->max = NULL;
	loc->exp = NULL;
	loc->log = NULL;
	loc->pitimes = NULL;
	loc->trig = NULL;
	loc->cir = NULL;
	loc->binomial = NULL;
	loc->fac = NULL;
	loc->notscl = NULL;
	loc->without = NULL;
	loc->not = NULL;
	loc->logand = NULL;
	loc->and = NULL;
	loc->logor = NULL;
	loc->lor = NULL;
	loc->nan = NULL;
	loc->nor = NULL;
	loc->lessthan = NULL;
	loc->lth = NULL;
	loc->lesseql = NULL;
	loc->lte = NULL;
	CHK(mk_func_box(&loc->eql_vec, NULL), cleanup, L"Init mutable variable: loc->eql_vec");
	loc->equal = NULL;
	loc->eql = NULL;
	loc->greatereql = NULL;
	loc->gte = NULL;
	loc->greaterthan = NULL;
	loc->gth = NULL;
	loc->firstocc = NULL;
	CHK(mk_func_box(&loc->neq_vec, NULL), cleanup, L"Init mutable variable: loc->neq_vec");
	loc->noteq = NULL;
	loc->neq = NULL;
	loc->mix = NULL;
	loc->take = NULL;
	loc->tke = NULL;
	loc->split = NULL;
	loc->drop = NULL;
	loc->drp = NULL;
	loc->reverse_last = NULL;
	loc->rotate_last = NULL;
	loc->rot = NULL;
	loc->com = NULL;
	loc->map = NULL;
	CHK(mk_moper_box(&loc->reduce, NULL), cleanup, L"Init mutable variable: loc->reduce");
	CHK(mk_moper_box(&loc->nwreduce, NULL), cleanup, L"Init mutable variable: loc->nwreduce");
	loc->rdf = NULL;
	loc->rpf = NULL;
	loc->dot = NULL;
	loc->oup = NULL;
	loc->pow = NULL;
	loc->jot = NULL;

	err = 0;

	if (debug_info)
		release_array(debug_info);

	debug_info = NULL;

	if (!q_signal_ibeam)
		CHK(6, cleanup, L"[4] q_signal←╠'q_signal_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(q_signal_ibeam);
	
	release_cell(loc->q_signal);
	loc->q_signal = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!q_dr_ibeam)
		CHK(6, cleanup, L"[5] q_dr←╠'q_dr_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(q_dr_ibeam);
	
	release_cell(loc->q_dr);
	loc->q_dr = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!squeeze_ibeam)
		CHK(6, cleanup, L"[8] squeeze←╠'squeeze_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(squeeze_ibeam);
	
	release_cell(loc->squeeze->value);
	loc->squeeze->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!is_simple_ibeam)
		CHK(6, cleanup, L"[9] is_simple←╠'is_simple_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(is_simple_ibeam);
	
	release_cell(loc->is_simple->value);
	loc->is_simple->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!is_numeric_ibeam)
		CHK(6, cleanup, L"[10] is_numeric←╠'is_numeric_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(is_numeric_ibeam);
	
	release_cell(loc->is_numeric->value);
	loc->is_numeric->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!is_char_ibeam)
		CHK(6, cleanup, L"[11] is_char←╠'is_char_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(is_char_ibeam);
	
	release_cell(loc->is_char->value);
	loc->is_char->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!is_integer_ibeam)
		CHK(6, cleanup, L"[12] is_integer←╠'is_integer_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(is_integer_ibeam);
	
	release_cell(loc->is_integer->value);
	loc->is_integer->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7978, ptr7979, 1), cleanup, 
		    L"[13] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	
		k->fv[0] = retain_cell(loc->squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->is_bool->value);
	loc->is_bool->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7980, ptr7981, 0), cleanup, 
		    L"[14] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->max_shp->value);
	loc->max_shp->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!has_nat_vals_ibeam)
		CHK(6, cleanup, L"[15] has_nat_vals←╠'has_nat_vals_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(has_nat_vals_ibeam);
	
	release_cell(loc->has_nat_vals->value);
	loc->has_nat_vals->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7982, ptr7983, 0), cleanup, 
		    L"[16] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->chk_scl->value);
	loc->chk_scl->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7984, ptr7989, 1), cleanup, 
		    L"[17] chk_valid_shape←╠{╣");
	
		k->fv[0] = retain_cell(loc->has_nat_vals);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->chk_valid_shape->value);
	loc->chk_valid_shape->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7994, ptr7995, 1), cleanup, 
		    L"[22] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->is_simple);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->both_simple->value);
	loc->both_simple->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7996, ptr7997, 1), cleanup, 
		    L"[23] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->both_numeric->value);
	loc->both_numeric->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7998, ptr7999, 1), cleanup, 
		    L"[24] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->both_integer->value);
	loc->both_integer->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8000, ptr8001, 1), cleanup, 
		    L"[25] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->is_char);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->both_char->value);
	loc->both_char->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8002, ptr8003, 1), cleanup, 
		    L"[26] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->is_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->both_bool->value);
	loc->both_bool->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!any_ibeam)
		CHK(6, cleanup, L"[27] any←╠'any_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(any_ibeam);
	
	release_cell(loc->any->value);
	loc->any->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8004, ptr8005, 1), cleanup, 
		    L"[28] assert_inrange←╠{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->any);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->assert_inrange->value);
	loc->assert_inrange->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8006, ptr8007, ptr8008, ptr8009, 1), cleanup, 
		    L"[29] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->numeric);
	loc->numeric = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, ptr8010, ptr8011, ptr8012, ptr8013, ptr8014, ptr8015, ptr8016, ptr8017, 1), cleanup, 
		    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	
		k->fv[0] = retain_cell(loc->same);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->ambiv);
	loc->ambiv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!veach_ibeam)
		CHK(6, cleanup, L"[31] veach←╠'veach_ibeam'⌶⌶╣");
	
	*stkhd++ = retain_cell(veach_ibeam);
	
	release_cell(loc->veach->value);
	loc->veach->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8018, ptr8019, ptr8020, ptr8021, 5), cleanup, 
		    L"[32] scalar←╠{╣");
	
		k->fv[0] = retain_cell(loc->veach);
		k->fv[1] = retain_cell(loc->both_simple);
		k->fv[2] = retain_cell(loc->chk_scl);
		k->fv[3] = retain_cell(loc->max_shp);
		k->fv[4] = retain_cell(loc->squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->scalar);
	loc->scalar = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!set_ibeam)
		CHK(6, cleanup, L"[42] set←╠'set_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(set_ibeam);
	
	release_cell(loc->set);
	loc->set = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8022, ptr8023, 0), cleanup, 
		    L"[45] brk←╠{⍵⌷⍺}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->brk);
	loc->brk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8024, ptr8025, 0), cleanup, 
		    L"[48] rgt←╠{⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->rgt);
	loc->rgt = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8028, ptr8029, 0), cleanup, 
		    L"[51] lft←{⍵} ambiv ╠{⍺}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[51] lft←{⍵} ╠ambiv╣ {⍺}");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8026, ptr8027, 0), cleanup, 
		    L"[51] lft←╠{⍵}╣ ambiv {⍺}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[51] lft←╠{⍵} ambiv {⍺}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lft);
	loc->lft = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8030, ptr8031, 1), cleanup, 
		    L"[54] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	
		k->fv[0] = retain_cell(loc->chk_valid_shape);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->reshape);
	loc->reshape = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->reshape)
		CHK(6, cleanup, L"[55] rho←('shape_ibeam'⌶) ambiv ╠reshape╣");
	
	*stkhd++ = retain_cell(loc->reshape);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[55] rho←('shape_ibeam'⌶) ╠ambiv╣ reshape");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!shape_ibeam)
		CHK(6, cleanup, L"[55] rho←╠('shape_ibeam'⌶)╣ ambiv reshape");
	
	*stkhd++ = retain_cell(shape_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[55] rho←╠('shape_ibeam'⌶) ambiv reshape╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rho);
	loc->rho = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8032, ptr8043, 1), cleanup, 
		    L"[58] cat←╠{╣");
	
		k->fv[0] = retain_cell(loc->is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cat);
	loc->cat = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8054, ptr8057, 1), cleanup, 
		    L"[84] depth←╠{╣");
	
		k->fv[0] = retain_cell(loc->is_simple);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->depth);
	loc->depth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!same_ibeam)
		CHK(6, cleanup, L"[88] same←╠'same_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(same_ibeam);
	
	release_cell(loc->same->value);
	loc->same->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->same->value)
		CHK(6, cleanup, L"[89] eqv←depth ambiv ╠same╣");
	
	*stkhd++ = retain_cell(loc->same->value);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[89] eqv←depth ╠ambiv╣ same");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->depth)
		CHK(6, cleanup, L"[89] eqv←╠depth╣ ambiv same");
	
	*stkhd++ = retain_cell(loc->depth);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[89] eqv←╠depth ambiv same╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->eqv);
	loc->eqv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!nqv_ibeam)
		CHK(6, cleanup, L"[92] nqv←{⊃⍴⍵} ambiv ╠('nqv_ibeam'⌶)╣");
	
	*stkhd++ = retain_cell(nqv_ibeam);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[92] nqv←{⊃⍴⍵} ╠ambiv╣ ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8060, ptr8061, 0), cleanup, 
		    L"[92] nqv←╠{⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[92] nqv←╠{⊃⍴⍵} ambiv ('nqv_ibeam'⌶)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->nqv);
	loc->nqv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8062, ptr8063, 1), cleanup, 
		    L"[95] index←╠{╣");
	
		k->fv[0] = retain_cell(loc->assert_inrange);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->index->value);
	loc->index->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8064, ptr8067, 2), cleanup, 
		    L"[100] sqd←╠{⍺←⊢╣");
	
		k->fv[0] = retain_cell(loc->index);
		k->fv[1] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->sqd);
	loc->sqd = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8070, ptr8071, 1), cleanup, 
		    L"[111] index_gen←╠{╣");
	
		k->fv[0] = retain_cell(loc->chk_valid_shape);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->index_gen);
	loc->index_gen = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8072, ptr8073, 0), cleanup, 
		    L"[118] index_of←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->index_of);
	loc->index_of = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->index_of)
		CHK(6, cleanup, L"[119] iot←index_gen ambiv ╠index_of╣");
	
	*stkhd++ = retain_cell(loc->index_of);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[119] iot←index_gen ╠ambiv╣ index_of");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->index_gen)
		CHK(6, cleanup, L"[119] iot←╠index_gen╣ ambiv index_of");
	
	*stkhd++ = retain_cell(loc->index_gen);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[119] iot←╠index_gen ambiv index_of╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->iot);
	loc->iot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8074, ptr8075, 0), cleanup, 
		    L"[122] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[122] dis←('disclose_ibeam'⌶) ╠ambiv╣ {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!disclose_ibeam)
		CHK(6, cleanup, L"[122] dis←╠('disclose_ibeam'⌶)╣ ambiv {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(disclose_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[122] dis←╠('disclose_ibeam'⌶) ambiv {⎕SIGNAL 16}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->dis);
	loc->dis = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8076, ptr8077, 0), cleanup, 
		    L"[125] enclose←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->enclose);
	loc->enclose = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8078, ptr8079, 0), cleanup, 
		    L"[129] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[129] par←enclose ╠ambiv╣ {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->enclose)
		CHK(6, cleanup, L"[129] par←╠enclose╣ ambiv {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->enclose);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[129] par←╠enclose ambiv {⎕SIGNAL 16}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->par);
	loc->par = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8080, ptr8081, 1), cleanup, 
		    L"[132] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}╣");
	
		k->fv[0] = retain_cell(loc->squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->conjugate);
	loc->conjugate = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[133] add←conjugate ambiv ('add_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[133] add←conjugate ambiv ('add_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!add_vec_ibeam)
		CHK(6, cleanup, L"[133] add←conjugate ambiv (╠'add_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(add_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[133] add←conjugate ambiv (╠'add_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[133] add←conjugate ambiv ╠('add_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[133] add←conjugate ╠ambiv╣ ('add_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->conjugate)
		CHK(6, cleanup, L"[133] add←╠conjugate╣ ambiv ('add_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->conjugate);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[133] add←╠conjugate ambiv ('add_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->add);
	loc->add = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[136] sub←{0-⍵} ambiv ('sub_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[136] sub←{0-⍵} ambiv ('sub_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!sub_vec_ibeam)
		CHK(6, cleanup, L"[136] sub←{0-⍵} ambiv (╠'sub_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(sub_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[136] sub←{0-⍵} ambiv (╠'sub_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[136] sub←{0-⍵} ambiv ╠('sub_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[136] sub←{0-⍵} ╠ambiv╣ ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8082, ptr8083, 0), cleanup, 
		    L"[136] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[136] sub←╠{0-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->sub);
	loc->sub = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8084, ptr8085, 0), cleanup, 
		    L"[139] sign←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->sign);
	loc->sign = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[143] mul←sign ambiv ('mul_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[143] mul←sign ambiv ('mul_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!mul_vec_ibeam)
		CHK(6, cleanup, L"[143] mul←sign ambiv (╠'mul_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(mul_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[143] mul←sign ambiv (╠'mul_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[143] mul←sign ambiv ╠('mul_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[143] mul←sign ╠ambiv╣ ('mul_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->sign)
		CHK(6, cleanup, L"[143] mul←╠sign╣ ambiv ('mul_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->sign);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[143] mul←╠sign ambiv ('mul_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->mul);
	loc->mul = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[146] div←{1÷⍵} ambiv ('div_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[146] div←{1÷⍵} ambiv ('div_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!div_vec_ibeam)
		CHK(6, cleanup, L"[146] div←{1÷⍵} ambiv (╠'div_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(div_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[146] div←{1÷⍵} ambiv (╠'div_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[146] div←{1÷⍵} ambiv ╠('div_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[146] div←{1÷⍵} ╠ambiv╣ ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8086, ptr8087, 0), cleanup, 
		    L"[146] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[146] div←╠{1÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->div);
	loc->div = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8088, ptr8089, 1), cleanup, 
		    L"[149] absolute←╠{╣");
	
		k->fv[0] = retain_cell(loc->is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->absolute);
	loc->absolute = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8090, ptr8091, 0), cleanup, 
		    L"[154] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->residue);
	loc->residue = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->residue)
		CHK(6, cleanup, L"[155] res←absolute numeric ambiv ╠residue╣");
	
	*stkhd++ = retain_cell(loc->residue);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[155] res←absolute numeric ╠ambiv╣ residue");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[155] res←absolute ╠numeric╣ ambiv residue");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->absolute)
		CHK(6, cleanup, L"[155] res←╠absolute╣ numeric ambiv residue");
	
	*stkhd++ = retain_cell(loc->absolute);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[155] res←╠absolute numeric╣ ambiv residue");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[155] res←╠absolute numeric ambiv residue╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->res);
	loc->res = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8092, ptr8093, 1), cleanup, 
		    L"[158] floor_array←╠{╣");
	
		k->fv[0] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->floor_array);
	loc->floor_array = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[163] min←floor_array numeric ambiv ('min_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[163] min←floor_array numeric ambiv ('min_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!min_vec_ibeam)
		CHK(6, cleanup, L"[163] min←floor_array numeric ambiv (╠'min_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(min_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[163] min←floor_array numeric ambiv (╠'min_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[163] min←floor_array numeric ambiv ╠('min_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[163] min←floor_array numeric ╠ambiv╣ ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[163] min←floor_array ╠numeric╣ ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->floor_array)
		CHK(6, cleanup, L"[163] min←╠floor_array╣ numeric ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->floor_array);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[163] min←╠floor_array numeric╣ ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[163] min←╠floor_array numeric ambiv ('min_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->min);
	loc->min = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8094, ptr8095, 1), cleanup, 
		    L"[166] ceil_array←╠{╣");
	
		k->fv[0] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->ceil_array);
	loc->ceil_array = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[171] max←ceil_array numeric ambiv ('max_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[171] max←ceil_array numeric ambiv ('max_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!max_vec_ibeam)
		CHK(6, cleanup, L"[171] max←ceil_array numeric ambiv (╠'max_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(max_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[171] max←ceil_array numeric ambiv (╠'max_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[171] max←ceil_array numeric ambiv ╠('max_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[171] max←ceil_array numeric ╠ambiv╣ ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[171] max←ceil_array ╠numeric╣ ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->ceil_array)
		CHK(6, cleanup, L"[171] max←╠ceil_array╣ numeric ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ceil_array);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[171] max←╠ceil_array numeric╣ ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[171] max←╠ceil_array numeric ambiv ('max_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->max);
	loc->max = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[174] exp←'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[174] exp←'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!pow_vec_ibeam)
		CHK(6, cleanup, L"[174] exp←'exp_vec_ibeam'⌶ numeric ambiv (╠'pow_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(pow_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[174] exp←'exp_vec_ibeam'⌶ numeric ambiv (╠'pow_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[174] exp←'exp_vec_ibeam'⌶ numeric ambiv ╠('pow_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[174] exp←'exp_vec_ibeam'⌶ numeric ╠ambiv╣ ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[174] exp←'exp_vec_ibeam'⌶ ╠numeric╣ ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!exp_vec_ibeam)
		CHK(6, cleanup, L"[174] exp←╠'exp_vec_ibeam'⌶╣ numeric ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(exp_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[174] exp←╠'exp_vec_ibeam'⌶ numeric╣ ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[174] exp←╠'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->exp);
	loc->exp = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[177] log←'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[177] log←'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!log_vec_ibeam)
		CHK(6, cleanup, L"[177] log←'nlg_vec_ibeam'⌶ numeric ambiv (╠'log_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(log_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[177] log←'nlg_vec_ibeam'⌶ numeric ambiv (╠'log_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[177] log←'nlg_vec_ibeam'⌶ numeric ambiv ╠('log_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[177] log←'nlg_vec_ibeam'⌶ numeric ╠ambiv╣ ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[177] log←'nlg_vec_ibeam'⌶ ╠numeric╣ ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!nlg_vec_ibeam)
		CHK(6, cleanup, L"[177] log←╠'nlg_vec_ibeam'⌶╣ numeric ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(nlg_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[177] log←╠'nlg_vec_ibeam'⌶ numeric╣ ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[177] log←╠'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->log);
	loc->log = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8096, ptr8097, 0), cleanup, 
		    L"[180] pitimes←╠{3.14159265358979323846×⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->pitimes);
	loc->pitimes = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8098, ptr8103, 0), cleanup, 
		    L"[181] trig←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->trig);
	loc->trig = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[210] cir←pitimes numeric ambiv (trig numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[210] cir←pitimes numeric ambiv (trig ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->trig)
		CHK(6, cleanup, L"[210] cir←pitimes numeric ambiv (╠trig╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->trig);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[210] cir←pitimes numeric ambiv (╠trig numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[210] cir←pitimes numeric ambiv ╠(trig numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[210] cir←pitimes numeric ╠ambiv╣ (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[210] cir←pitimes ╠numeric╣ ambiv (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->pitimes)
		CHK(6, cleanup, L"[210] cir←╠pitimes╣ numeric ambiv (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->pitimes);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[210] cir←╠pitimes numeric╣ ambiv (trig numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[210] cir←╠pitimes numeric ambiv (trig numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cir);
	loc->cir = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8108, ptr8109, 0), cleanup, 
		    L"[213] binomial←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->binomial);
	loc->binomial = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[217] fac←'factorial_vec_ibeam'⌶ numeric ambiv (binomial numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[217] fac←'factorial_vec_ibeam'⌶ numeric ambiv (binomial ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->binomial)
		CHK(6, cleanup, L"[217] fac←'factorial_vec_ibeam'⌶ numeric ambiv (╠binomial╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->binomial);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[217] fac←'factorial_vec_ibeam'⌶ numeric ambiv (╠binomial numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[217] fac←'factorial_vec_ibeam'⌶ numeric ambiv ╠(binomial numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[217] fac←'factorial_vec_ibeam'⌶ numeric ╠ambiv╣ (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[217] fac←'factorial_vec_ibeam'⌶ ╠numeric╣ ambiv (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!factorial_vec_ibeam)
		CHK(6, cleanup, L"[217] fac←╠'factorial_vec_ibeam'⌶╣ numeric ambiv (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(factorial_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[217] fac←╠'factorial_vec_ibeam'⌶ numeric╣ ambiv (binomial numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[217] fac←╠'factorial_vec_ibeam'⌶ numeric ambiv (binomial numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->fac);
	loc->fac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8110, ptr8111, 1), cleanup, 
		    L"[220] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->is_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->notscl);
	loc->notscl = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8112, ptr8113, 0), cleanup, 
		    L"[221] without←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->without);
	loc->without = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->without)
		CHK(6, cleanup, L"[222] not←notscl ambiv ╠without╣");
	
	*stkhd++ = retain_cell(loc->without);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[222] not←notscl ╠ambiv╣ without");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->notscl)
		CHK(6, cleanup, L"[222] not←╠notscl╣ ambiv without");
	
	*stkhd++ = retain_cell(loc->notscl);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[222] not←╠notscl ambiv without╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->not);
	loc->not = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8114, ptr8115, 1), cleanup, 
		    L"[225] logand←╠{╣");
	
		k->fv[0] = retain_cell(loc->both_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->logand);
	loc->logand = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[229] and←{⎕SIGNAL 2}ambiv (logand numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[229] and←{⎕SIGNAL 2}ambiv (logand ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->logand)
		CHK(6, cleanup, L"[229] and←{⎕SIGNAL 2}ambiv (╠logand╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->logand);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[229] and←{⎕SIGNAL 2}ambiv (╠logand numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[229] and←{⎕SIGNAL 2}ambiv ╠(logand numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[229] and←{⎕SIGNAL 2}╠ambiv╣ (logand numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8116, ptr8117, 0), cleanup, 
		    L"[229] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[229] and←╠{⎕SIGNAL 2}ambiv (logand numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->and);
	loc->and = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8118, ptr8123, 4), cleanup, 
		    L"[232] logor←╠{╣");
	
		k->fv[0] = retain_cell(loc->both_bool);
		k->fv[1] = retain_cell(loc->both_integer);
		k->fv[2] = retain_cell(loc->is_bool);
		k->fv[3] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->logor);
	loc->logor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[239] lor←{⎕SIGNAL 2}ambiv (logor numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[239] lor←{⎕SIGNAL 2}ambiv (logor ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->logor)
		CHK(6, cleanup, L"[239] lor←{⎕SIGNAL 2}ambiv (╠logor╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->logor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[239] lor←{⎕SIGNAL 2}ambiv (╠logor numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[239] lor←{⎕SIGNAL 2}ambiv ╠(logor numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[239] lor←{⎕SIGNAL 2}╠ambiv╣ (logor numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8128, ptr8129, 0), cleanup, 
		    L"[239] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[239] lor←╠{⎕SIGNAL 2}ambiv (logor numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lor);
	loc->lor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8132, ptr8133, 1), cleanup, 
		    L"[242] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->both_bool);
	
		*stkhd++ = k;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}╠ambiv╣{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8130, ptr8131, 0), cleanup, 
		    L"[242] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[242] nan←╠{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->nan);
	loc->nan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8136, ptr8137, 1), cleanup, 
		    L"[245] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->both_bool);
	
		*stkhd++ = k;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}╠ambiv╣{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8134, ptr8135, 0), cleanup, 
		    L"[245] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[245] nor←╠{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->nor);
	loc->nor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8138, ptr8139, 0), cleanup, 
		    L"[248] lessthan←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->lessthan);
	loc->lessthan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[252] lth←{⎕SIGNAL 2} ambiv (lessthan numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[252] lth←{⎕SIGNAL 2} ambiv (lessthan ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->lessthan)
		CHK(6, cleanup, L"[252] lth←{⎕SIGNAL 2} ambiv (╠lessthan╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->lessthan);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[252] lth←{⎕SIGNAL 2} ambiv (╠lessthan numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[252] lth←{⎕SIGNAL 2} ambiv ╠(lessthan numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[252] lth←{⎕SIGNAL 2} ╠ambiv╣ (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8140, ptr8141, 0), cleanup, 
		    L"[252] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[252] lth←╠{⎕SIGNAL 2} ambiv (lessthan numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lth);
	loc->lth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8142, ptr8143, 0), cleanup, 
		    L"[255] lesseql←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->lesseql);
	loc->lesseql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[259] lte←{⎕SIGNAL 2}ambiv (lesseql numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[259] lte←{⎕SIGNAL 2}ambiv (lesseql ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->lesseql)
		CHK(6, cleanup, L"[259] lte←{⎕SIGNAL 2}ambiv (╠lesseql╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->lesseql);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[259] lte←{⎕SIGNAL 2}ambiv (╠lesseql numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[259] lte←{⎕SIGNAL 2}ambiv ╠(lesseql numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[259] lte←{⎕SIGNAL 2}╠ambiv╣ (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8144, ptr8145, 0), cleanup, 
		    L"[259] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[259] lte←╠{⎕SIGNAL 2}ambiv (lesseql numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lte);
	loc->lte = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!eql_vec_ibeam)
		CHK(6, cleanup, L"[262] eql_vec←╠'eql_vec_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(eql_vec_ibeam);
	
	release_cell(loc->eql_vec->value);
	loc->eql_vec->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8146, ptr8147, 3), cleanup, 
		    L"[263] equal←╠{╣");
	
		k->fv[0] = retain_cell(loc->eql_vec);
		k->fv[1] = retain_cell(loc->both_char);
		k->fv[2] = retain_cell(loc->both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->equal);
	loc->equal = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[268] eql←{⎕SIGNAL 2}ambiv (equal ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->equal)
		CHK(6, cleanup, L"[268] eql←{⎕SIGNAL 2}ambiv (╠equal╣ scalar)");
	
	*stkhd++ = retain_cell(loc->equal);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[268] eql←{⎕SIGNAL 2}ambiv ╠(equal scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[268] eql←{⎕SIGNAL 2}╠ambiv╣ (equal scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8148, ptr8149, 0), cleanup, 
		    L"[268] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[268] eql←╠{⎕SIGNAL 2}ambiv (equal scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->eql);
	loc->eql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8150, ptr8151, 0), cleanup, 
		    L"[271] greatereql←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->greatereql);
	loc->greatereql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[275] gte←{⎕SIGNAL 2}ambiv (greatereql numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[275] gte←{⎕SIGNAL 2}ambiv (greatereql ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->greatereql)
		CHK(6, cleanup, L"[275] gte←{⎕SIGNAL 2}ambiv (╠greatereql╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->greatereql);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[275] gte←{⎕SIGNAL 2}ambiv (╠greatereql numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[275] gte←{⎕SIGNAL 2}ambiv ╠(greatereql numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[275] gte←{⎕SIGNAL 2}╠ambiv╣ (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8152, ptr8153, 0), cleanup, 
		    L"[275] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[275] gte←╠{⎕SIGNAL 2}ambiv (greatereql numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->gte);
	loc->gte = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8154, ptr8155, 0), cleanup, 
		    L"[278] greaterthan←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->greaterthan);
	loc->greaterthan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[282] gth←{⎕SIGNAL 2}ambiv (greaterthan numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->numeric)
		CHK(6, cleanup, L"[282] gth←{⎕SIGNAL 2}ambiv (greaterthan ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->numeric);
	
	if (!loc->greaterthan)
		CHK(6, cleanup, L"[282] gth←{⎕SIGNAL 2}ambiv (╠greaterthan╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->greaterthan);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[282] gth←{⎕SIGNAL 2}ambiv (╠greaterthan numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[282] gth←{⎕SIGNAL 2}ambiv ╠(greaterthan numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[282] gth←{⎕SIGNAL 2}╠ambiv╣ (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8156, ptr8157, 0), cleanup, 
		    L"[282] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[282] gth←╠{⎕SIGNAL 2}ambiv (greaterthan numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->gth);
	loc->gth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8158, ptr8159, 0), cleanup, 
		    L"[285] firstocc←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->firstocc);
	loc->firstocc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!neq_vec_ibeam)
		CHK(6, cleanup, L"[286] neq_vec←╠'neq_vec_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(neq_vec_ibeam);
	
	release_cell(loc->neq_vec->value);
	loc->neq_vec->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8160, ptr8161, 3), cleanup, 
		    L"[287] noteq←╠{╣");
	
		k->fv[0] = retain_cell(loc->neq_vec);
		k->fv[1] = retain_cell(loc->both_char);
		k->fv[2] = retain_cell(loc->both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->noteq);
	loc->noteq = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->scalar)
		CHK(6, cleanup, L"[292] neq←firstocc ambiv (noteq ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->scalar);
	
	if (!loc->noteq)
		CHK(6, cleanup, L"[292] neq←firstocc ambiv (╠noteq╣ scalar)");
	
	*stkhd++ = retain_cell(loc->noteq);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[292] neq←firstocc ambiv ╠(noteq scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[292] neq←firstocc ╠ambiv╣ (noteq scalar)");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->firstocc)
		CHK(6, cleanup, L"[292] neq←╠firstocc╣ ambiv (noteq scalar)");
	
	*stkhd++ = retain_cell(loc->firstocc);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[292] neq←╠firstocc ambiv (noteq scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->neq);
	loc->neq = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8162, ptr8163, 0), cleanup, 
		    L"[295] mix←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->mix);
	loc->mix = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8164, ptr8165, 0), cleanup, 
		    L"[296] take←╠{(⊂⍳⍺)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->take);
	loc->take = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->take)
		CHK(6, cleanup, L"[297] tke←mix ambiv ╠take╣");
	
	*stkhd++ = retain_cell(loc->take);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[297] tke←mix ╠ambiv╣ take");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->mix)
		CHK(6, cleanup, L"[297] tke←╠mix╣ ambiv take");
	
	*stkhd++ = retain_cell(loc->mix);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[297] tke←╠mix ambiv take╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->tke);
	loc->tke = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8166, ptr8167, 0), cleanup, 
		    L"[300] split←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->split);
	loc->split = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8168, ptr8169, 0), cleanup, 
		    L"[301] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->drop);
	loc->drop = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->drop)
		CHK(6, cleanup, L"[302] drp←split ambiv ╠drop╣");
	
	*stkhd++ = retain_cell(loc->drop);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[302] drp←split ╠ambiv╣ drop");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->split)
		CHK(6, cleanup, L"[302] drp←╠split╣ ambiv drop");
	
	*stkhd++ = retain_cell(loc->split);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[302] drp←╠split ambiv drop╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->drp);
	loc->drp = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8170, ptr8171, 0), cleanup, 
		    L"[305] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->reverse_last);
	loc->reverse_last = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8172, ptr8173, 0), cleanup, 
		    L"[306] rotate_last←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->rotate_last);
	loc->rotate_last = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rotate_last)
		CHK(6, cleanup, L"[307] rot←reverse_last ambiv ╠rotate_last╣");
	
	*stkhd++ = retain_cell(loc->rotate_last);
	
	if (!loc->ambiv)
		CHK(6, cleanup, L"[307] rot←reverse_last ╠ambiv╣ rotate_last");
	
	*stkhd++ = retain_cell(loc->ambiv);
	
	if (!loc->reverse_last)
		CHK(6, cleanup, L"[307] rot←╠reverse_last╣ ambiv rotate_last");
	
	*stkhd++ = retain_cell(loc->reverse_last);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[307] rot←╠reverse_last ambiv rotate_last╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rot);
	loc->rot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8174, ptr8175, ptr8176, ptr8177, 0), cleanup, 
		    L"[310] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->com);
	loc->com = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8178, ptr8179, ptr8180, ptr8181, 4), cleanup, 
		    L"[313] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	
		k->fv[0] = retain_cell(loc->veach);
		k->fv[1] = retain_cell(loc->chk_scl);
		k->fv[2] = retain_cell(loc->max_shp);
		k->fv[3] = retain_cell(loc->squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->map);
	loc->map = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8182, ptr8185, ptr8188, ptr8191, 0), cleanup, 
		    L"[316] reduce←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->reduce->value);
	loc->reduce->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8194, ptr8197, ptr8200, ptr8203, 1), cleanup, 
		    L"[321] nwreduce←╠{op←⍺⍺¨╣");
	
		k->fv[0] = retain_cell(loc->is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->nwreduce->value);
	loc->nwreduce->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8206, ptr8207, ptr8208, ptr8209, 2), cleanup, 
		    L"[325] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	
		k->fv[0] = retain_cell(loc->nwreduce);
		k->fv[1] = retain_cell(loc->reduce);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->rdf);
	loc->rdf = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8210, ptr8211, 0), cleanup, 
		    L"[330] rpf←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->rpf);
	loc->rpf = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, ptr8212, ptr8213, ptr8214, ptr8215, ptr8216, ptr8217, ptr8218, ptr8219, 0), cleanup, 
		    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->dot);
	loc->dot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, ptr8220, ptr8223, ptr8226, ptr8229, 0), cleanup, 
		    L"[336] oup←╠{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->oup);
	loc->oup = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, ptr8232, ptr8233, ptr8234, ptr8235, ptr8236, ptr8237, ptr8238, ptr8239, 0), cleanup, 
		    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->pow);
	loc->pow = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, ptr8240, ptr8241, ptr8242, ptr8243, ptr8244, ptr8245, ptr8246, ptr8247, 0), cleanup, 
		    L"[342] jot←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->jot);
	loc->jot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	

cleanup:
	release_env(stk, stkhd);
	return err;
}

int
ptr7978(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[13] is_bool←{11≡⎕DR squeeze ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[13] is_bool←{11≡⎕DR ╠squeeze╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[13] is_bool←{11≡⎕DR ╠squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[13] is_bool←{11≡╠⎕DR╣ squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[13] is_bool←{11≡╠⎕DR squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[13] is_bool←{11╠≡╣⎕DR squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[13] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[13] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[13] is_bool←{╠11≡⎕DR squeeze ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[13] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	return err;
}

int
ptr7979(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[13] is_bool←{11≡⎕DR squeeze ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[13] is_bool←{11≡⎕DR ╠squeeze╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[13] is_bool←{11≡⎕DR ╠squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[13] is_bool←{11≡╠⎕DR╣ squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[13] is_bool←{11≡╠⎕DR squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[13] is_bool←{11╠≡╣⎕DR squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[13] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[13] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[13] is_bool←{╠11≡⎕DR squeeze ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[13] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	return err;
}

int
ptr7980(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[14] max_shp←{⍺←╠0╣ ⋄ ⍺('max_shp_ibeam'⌶)⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[14] max_shp←{⍺←╠0╣ ⋄ ⍺('max_shp_ibeam'⌶)⍵}");
	
		*stkhd++ = arr;
	}
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!max_shp_ibeam)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ⍺╠('max_shp_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(max_shp_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ╠⍺╣('max_shp_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[14] max_shp←{⍺←0 ⋄ ╠⍺('max_shp_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[14] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	return err;
}

int
ptr7981(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!max_shp_ibeam)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ⍺╠('max_shp_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(max_shp_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[14] max_shp←{⍺←0 ⋄ ╠⍺╣('max_shp_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[14] max_shp←{⍺←0 ⋄ ╠⍺('max_shp_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[14] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	return err;
}

int
ptr7982(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←╠0╣ ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←╠0╣ ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴╣⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)╠≡╣⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴╠⍺╣)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (╠⍴╣⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)╣≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)≡⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,╣⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢╣,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1╠≡╣≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1≡≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,╣⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢╣,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1╠≡╣≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1≡≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴╣⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢╣⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)╠≡╣≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴╠⍺╣)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴╣⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (╠≢╣⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)≡≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL╣ 5 ⋄ ⎕SIGNAL 4}");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL 5╣ ⋄ ⎕SIGNAL 4}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL╣ 4}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL 4╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[16] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	return err;
}

int
ptr7983(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴╣⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)╠≡╣⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴╠⍺╣)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (╠⍴╣⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)╣≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)≡⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,╣⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢╣,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1╠≡╣≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1≡≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,╣⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢╣,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1╠≡╣≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1≡≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴╣⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢╣⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)╠≡╣≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴╠⍺╣)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴╣⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (╠≢╣⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)≡≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL╣ 5 ⋄ ⎕SIGNAL 4}");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL 5╣ ⋄ ⎕SIGNAL 4}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL╣ 4}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL 4╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[16] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	return err;
}

int
ptr7984(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7985, ptr7986, 0), cleanup, 
		    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7987, ptr7988, 1), cleanup, 
		    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	
		k->fv[0] = retain_cell(lex->has_nat_vals);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[17] chk_valid_shape←╠{╣");
	return err;
}

int
ptr7985(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[18] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[18] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
ptr7986(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[18] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[18] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
ptr7987(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->has_nat_vals->value)
		CHK(6, cleanup, L"[19] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
ptr7988(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->has_nat_vals->value)
		CHK(6, cleanup, L"[19] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
ptr7989(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7990, ptr7991, 0), cleanup, 
		    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr7992, ptr7993, 1), cleanup, 
		    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	
		k->fv[0] = retain_cell(lex->has_nat_vals);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[19] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[17] chk_valid_shape←╠{╣");
	return err;
}

int
ptr7990(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[18] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[18] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
ptr7991(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[18] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[18] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[18] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
ptr7992(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->has_nat_vals->value)
		CHK(6, cleanup, L"[19] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
ptr7993(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[19] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->has_nat_vals->value)
		CHK(6, cleanup, L"[19] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[19] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[19] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[19] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
ptr7994(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_simple;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[22] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_simple->value)
		CHK(6, cleanup, L"[22] both_simple←{╠is_simple╣ ⍺:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] both_simple←{╠is_simple ⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_simple->value)
				CHK(6, cleanup, L"[22] both_simple←{is_simple ⍺:╠is_simple╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_simple->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[22] both_simple←{is_simple ⍺:╠is_simple ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	return err;
}

int
ptr7995(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_simple;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[22] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_simple->value)
		CHK(6, cleanup, L"[22] both_simple←{╠is_simple╣ ⍺:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] both_simple←{╠is_simple ⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_simple->value)
				CHK(6, cleanup, L"[22] both_simple←{is_simple ⍺:╠is_simple╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_simple->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[22] both_simple←{is_simple ⍺:╠is_simple ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	return err;
}

int
ptr7996(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[23] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[23] both_numeric←{╠is_numeric╣ ⍺:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] both_numeric←{╠is_numeric ⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_numeric->value)
				CHK(6, cleanup, L"[23] both_numeric←{is_numeric ⍺:╠is_numeric╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_numeric->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[23] both_numeric←{is_numeric ⍺:╠is_numeric ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	return err;
}

int
ptr7997(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[23] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[23] both_numeric←{╠is_numeric╣ ⍺:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] both_numeric←{╠is_numeric ⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_numeric->value)
				CHK(6, cleanup, L"[23] both_numeric←{is_numeric ⍺:╠is_numeric╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_numeric->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[23] both_numeric←{is_numeric ⍺:╠is_numeric ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	return err;
}

int
ptr7998(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[24] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[24] both_integer←{╠is_integer╣ ⍺:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[24] both_integer←{╠is_integer ⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[24] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_integer->value)
				CHK(6, cleanup, L"[24] both_integer←{is_integer ⍺:╠is_integer╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_integer->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[24] both_integer←{is_integer ⍺:╠is_integer ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[24] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	return err;
}

int
ptr7999(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[24] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[24] both_integer←{╠is_integer╣ ⍺:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[24] both_integer←{╠is_integer ⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[24] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_integer->value)
				CHK(6, cleanup, L"[24] both_integer←{is_integer ⍺:╠is_integer╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_integer->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[24] both_integer←{is_integer ⍺:╠is_integer ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[24] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[24] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	return err;
}

int
ptr8000(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_char;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[25] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_char->value)
		CHK(6, cleanup, L"[25] both_char←{╠is_char╣ ⍺:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_char->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[25] both_char←{╠is_char ⍺╣:is_char ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[25] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[25] both_char←{is_char ⍺:is_char ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_char->value)
				CHK(6, cleanup, L"[25] both_char←{is_char ⍺:╠is_char╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_char->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[25] both_char←{is_char ⍺:╠is_char ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[25] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[25] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[25] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	return err;
}

int
ptr8001(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_char;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[25] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_char->value)
		CHK(6, cleanup, L"[25] both_char←{╠is_char╣ ⍺:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_char->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[25] both_char←{╠is_char ⍺╣:is_char ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[25] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[25] both_char←{is_char ⍺:is_char ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_char->value)
				CHK(6, cleanup, L"[25] both_char←{is_char ⍺:╠is_char╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_char->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[25] both_char←{is_char ⍺:╠is_char ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[25] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[25] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[25] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	return err;
}

int
ptr8002(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[26] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[26] both_bool←{╠is_bool╣ ⍺:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[26] both_bool←{╠is_bool ⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[26] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_bool->value)
				CHK(6, cleanup, L"[26] both_bool←{is_bool ⍺:╠is_bool╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_bool->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[26] both_bool←{is_bool ⍺:╠is_bool ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[26] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	return err;
}

int
ptr8003(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[26] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[26] both_bool←{╠is_bool╣ ⍺:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[26] both_bool←{╠is_bool ⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[26] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->is_bool->value)
				CHK(6, cleanup, L"[26] both_bool←{is_bool ⍺:╠is_bool╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->is_bool->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[26] both_bool←{is_bool ⍺:╠is_bool ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[26] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[26] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	return err;
}

int
ptr8004(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *any;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢╠,╣⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨⍺>≢╠,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>╠≢╣,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨⍺>╠≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺╠>╣≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!alpha)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨╠⍺╣>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨╠⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)╠∨╣⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺╠<╣0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!alpha)
		CHK(6, cleanup, L"[28] assert_inrange←{any(╠⍺╣<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any╠(⍺<0)╣∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any╠(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->any->value)
		CHK(6, cleanup, L"[28] assert_inrange←{╠any╣(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{╠any(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣ ⋄ 0}");
			
				CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣ ⋄ 0}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL╣ 3 ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL 3╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[28] assert_inrange←╠{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}╣");
	return err;
}

int
ptr8005(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *any;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢╠,╣⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨⍺>≢╠,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>╠≢╣,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨⍺>╠≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺╠>╣≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!alpha)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨╠⍺╣>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any(⍺<0)∨╠⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)╠∨╣⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[28] assert_inrange←{any(⍺╠<╣0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!alpha)
		CHK(6, cleanup, L"[28] assert_inrange←{any(╠⍺╣<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any╠(⍺<0)╣∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[28] assert_inrange←{any╠(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->any->value)
		CHK(6, cleanup, L"[28] assert_inrange←{╠any╣(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] assert_inrange←{╠any(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3 ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣ ⋄ 0}");
			
				CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣ ⋄ 0}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL╣ 3 ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL 3╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] assert_inrange←{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[28] assert_inrange←╠{any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3 ⋄ 0}╣");
	return err;
}

int
ptr8006(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[29] numeric←{⍺←╠⊢╣ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8007(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8008(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[29] numeric←{⍺←╠⊢╣ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8009(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8010(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[30] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8011(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8012(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[30] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8013(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8014(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[30] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8015(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8016(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[30] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8017(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct lex_vars {
		struct cell_func_box *same;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->same->value)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[30] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
ptr8018(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *both_simple;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[33] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[33] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[34] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[34] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[34] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[34] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[35] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[35] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[35] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[35] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[35] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[36] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[36] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[36] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[36] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_simple->value)
		CHK(6, cleanup, L"[37] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->both_simple->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[37] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!self)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[38] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[38] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[38] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[38] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] scalar←╠{╣");
	return err;
}

int
ptr8019(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *both_simple;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[33] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[33] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[34] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[34] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[34] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[34] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[35] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[35] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[35] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[35] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[35] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[36] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[36] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[36] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[36] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_simple->value)
		CHK(6, cleanup, L"[37] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->both_simple->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[37] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!self)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[38] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[38] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[38] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[38] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] scalar←╠{╣");
	return err;
}

int
ptr8020(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *both_simple;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[33] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[33] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[34] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[34] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[34] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[34] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[35] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[35] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[35] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[35] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[35] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[36] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[36] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[36] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[36] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_simple->value)
		CHK(6, cleanup, L"[37] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->both_simple->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[37] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!self)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[38] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[38] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[38] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[38] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] scalar←╠{╣");
	return err;
}

int
ptr8021(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *both_simple;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[33] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[33] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[34] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[34] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[34] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[34] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[35] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[35] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[35] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[35] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[35] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[35] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[35] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[35] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[35] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[35] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[36] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[36] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[36] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[36] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[36] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[36] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[36] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[36] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_simple->value)
		CHK(6, cleanup, L"[37] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->both_simple->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[37] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[37] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->s)
				CHK(6, cleanup, L"[37] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[37] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!self)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[38] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[38] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[38] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[38] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[38] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] scalar←╠{╣");
	return err;
}

int
ptr8022(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[45] brk←{⍵⌷╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[45] brk←{⍵╠⌷╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!omega)
		CHK(6, cleanup, L"[45] brk←{╠⍵╣⌷⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[45] brk←{╠⍵⌷⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[45] brk←╠{⍵⌷⍺}╣");
	return err;
}

int
ptr8023(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[45] brk←{⍵⌷╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[45] brk←{⍵╠⌷╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!omega)
		CHK(6, cleanup, L"[45] brk←{╠⍵╣⌷⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[45] brk←{╠⍵⌷⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[45] brk←╠{⍵⌷⍺}╣");
	return err;
}

int
ptr8024(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[48] rgt←{╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[48] rgt←╠{⍵}╣");
	return err;
}

int
ptr8025(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[48] rgt←{╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[48] rgt←╠{⍵}╣");
	return err;
}

int
ptr8026(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[51] lft←{╠⍵╣} ambiv {⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[51] lft←╠{⍵}╣ ambiv {⍺}");
	return err;
}

int
ptr8027(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[51] lft←{╠⍵╣} ambiv {⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[51] lft←╠{⍵}╣ ambiv {⍺}");
	return err;
}

int
ptr8028(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[51] lft←{⍵} ambiv {╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[51] lft←{⍵} ambiv ╠{⍺}╣");
	return err;
}

int
ptr8029(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[51] lft←{⍵} ambiv {╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[51] lft←{⍵} ambiv ╠{⍺}╣");
	return err;
}

int
ptr8030(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->chk_valid_shape->value)
		CHK(6, cleanup, L"[54] reshape←{╠chk_valid_shape╣ ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(lex->chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] reshape←{╠chk_valid_shape ⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[54] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!reshape_ibeam)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ⍺╠('reshape_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(reshape_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺╣('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[54] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺('reshape_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[54] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	return err;
}

int
ptr8031(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->chk_valid_shape->value)
		CHK(6, cleanup, L"[54] reshape←{╠chk_valid_shape╣ ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(lex->chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] reshape←{╠chk_valid_shape ⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[54] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!reshape_ibeam)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ⍺╠('reshape_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(reshape_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[54] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺╣('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[54] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺('reshape_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[54] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	return err;
}

int
ptr8032(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array_box *noax;
		struct cell_array *israv;
		struct cell_array_box *lr;
		struct cell_array_box *rr;
		struct cell_array_box *axis;
		struct cell_array *rk;
		struct cell_array *ar;
		struct cell_array_box *fx;
		struct cell_array *pr;
		struct cell_func *lx;
		struct cell_func *vc;
		struct cell_array_box *c;
		struct cell_array *a;
		struct cell_array *ax;
		struct cell_array *w;
		struct cell_array *wx;
		struct cell_array_box *s;
		struct cell_array *ac;
		struct cell_array *wc;
		struct cell_array_box *z;
		struct cell_array *i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	struct opt_vars {
		struct cell_array_box *axis;
	} *opts;

	opts = (struct opt_vars *)self->opts;

	CHK(mk_array_box(&loc->noax, NULL), cleanup, L"Init mutable variable: loc->noax");
	loc->israv = NULL;
	CHK(mk_array_box(&loc->lr, NULL), cleanup, L"Init mutable variable: loc->lr");
	CHK(mk_array_box(&loc->rr, NULL), cleanup, L"Init mutable variable: loc->rr");
	CHK(mk_array_box(&loc->axis, NULL), cleanup, L"Init mutable variable: loc->axis");
	loc->rk = NULL;
	loc->ar = NULL;
	CHK(mk_array_box(&loc->fx, NULL), cleanup, L"Init mutable variable: loc->fx");
	loc->pr = NULL;
	loc->lx = NULL;
	loc->vc = NULL;
	CHK(mk_array_box(&loc->c, NULL), cleanup, L"Init mutable variable: loc->c");
	loc->a = NULL;
	loc->ax = NULL;
	loc->w = NULL;
	loc->wx = NULL;
	CHK(mk_array_box(&loc->s, NULL), cleanup, L"Init mutable variable: loc->s");
	loc->ac = NULL;
	loc->wc = NULL;
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	loc->i = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[59] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[59] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->noax->value);
	loc->noax->value = retain_cell(stkhd[-1]);
	
	release_cell(loc->israv);
	loc->israv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←╠1╣ ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←╠1╣ ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->israv);
	loc->israv = retain_cell(stkhd[-1]);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴╠⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴╣⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢╣⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lr->value);
	loc->lr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴╠⍵╣ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴╣⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢╣⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rr->value);
	loc->rr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->axis) {
		*stkhd++ = retain_cell(opts->axis);
	} else {
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			CHK(fill_array(arr, dat), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			*stkhd++ = arr;
		}
		
		release_cell(loc->noax->value);
		loc->noax->value = retain_cell(stkhd[-1]);
		
	}
	
	release_cell(loc->axis->value);
	loc->axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->israv)
		CHK(6, cleanup, L"[60] 	╠israv╣:{");
	
	*stkhd++ = retain_cell(loc->israv);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[60] 	╠israv╣:{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[65] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8033, ptr8034, 2), cleanup, 
				    L"[60] 	israv:╠{╣");
			
				k->fv[0] = retain_cell(loc->axis);
				k->fv[1] = retain_cell(loc->noax);
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[60] 	israv:╠{╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[66] 	rk←lr⌈╠rr╣ ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[66] 	rk←lr╠⌈╣rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[66] 	rk←╠lr╣⌈rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[66] 	rk←╠lr⌈rr╣ ⋄ axis⍠←¯1+rk");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rk);
	loc->rk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->axis) {
		*stkhd++ = retain_cell(opts->axis);
	} else {
		if (!loc->rk)
			CHK(6, cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←¯1+╠rk╣");
		
		*stkhd++ = retain_cell(loc->rk);
		
		if (!cdf_prim.add)
			CHK(6, cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←¯1╠+╣rk");
		
		*stkhd++ = retain_cell(cdf_prim.add);
		
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {-1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			CHK(fill_array(arr, dat), cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			*stkhd++ = arr;
		}
		
		{
			struct cell_array *x = stkhd[-1];
			struct cell_func *fn = stkhd[-2];
			struct cell_array *y = stkhd[-3];
			struct cell_array *dst;
		
			CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
			    L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1+rk╣");
		
			release_array(x);
			release_func(fn);
			release_array(y);
		
			stkhd -= 3;
			*stkhd++ = dst;
		}
		
	}
	
	release_cell(loc->axis->value);
	loc->axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[67] 	(0≡lr)∧(0╠≡╣rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[67] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[67] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	(0≡lr)∧╠(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[67] 	(0≡lr)╠∧╣(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[67] 	(0≡╠lr╣)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[67] 	(0╠≡╣lr)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[67] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[67] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	╠(0≡lr)╣∧(0≡rr):(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	╠(0≡lr)∧(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[67] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺),,╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺),╠,╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):(,⍺),╠,⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺)╠,╣,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			if (!alpha)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,╠⍺╣),,⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(╠,╣⍺),,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):╠(,⍺)╣,,⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):╠(,⍺),,⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[68] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[68] 	ar←≢╠⍴╣axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢╠⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←╠≢╣⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←╠≢⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ar);
	loc->ar = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[68] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[68] 	ar←≢⍴axis:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[68] 	ar←≢⍴axis:╠⎕SIGNAL 4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,╣axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢╣,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1╠≢╣≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1≢≢,axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL╣ 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL 5╣ ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[69] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[69] 	~╠is_numeric╣ axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[69] 	~╠is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[69] 	╠~╣is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[69] 	╠~is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[69] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[69] 	~is_numeric axis:╠⎕SIGNAL╣ 11 ⋄ axis≤¯1:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[69] 	~is_numeric axis:╠⎕SIGNAL 11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lte)
		CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis╠≤╣¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lte);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis╣≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis≤¯1╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rk)
		CHK(6, cleanup, L"[70] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rk);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[70] 	axis╠≥╣rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[70] 	╠axis╣≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	╠axis≥rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[70] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[70] 	axis≥rk:╠⎕SIGNAL╣ 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[70] 	axis≥rk:╠⎕SIGNAL 4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr╠-╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr╣-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|╣lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|lr-rr╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1╠<╣|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)╠∧╣1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr╠≢╣0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(╠rr╣≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)╣∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)╠∧╣(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr╠≢╣0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (╠lr╣≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)╣∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)∧(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[71] 	axis≠fx←⌈╠axis╣:⍺{");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[71] 	axis≠fx←╠⌈╣axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[71] 	axis≠fx←╠⌈axis╣:⍺{");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->fx->value);
	loc->fx->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[71] 	axis╠≠╣fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[71] 	╠axis╣≠fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[71] 	╠axis≠fx←⌈axis╣:⍺{");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[71] 	axis≠fx←⌈╠axis╣:⍺{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[74] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8035, ptr8036, 4), cleanup, 
				    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
			
				k->fv[0] = retain_cell(loc->fx);
				k->fv[1] = retain_cell(loc->axis);
				k->fv[2] = retain_cell(loc->rr);
				k->fv[3] = retain_cell(loc->lr);
			
				*stkhd++ = k;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[71] 	axis≠fx←⌈axis:╠⍺╣{");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[71] 	axis≠fx←⌈axis:╠⍺{╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)╠↑╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=╠rr╣)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr╠=╣rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr╣=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)╠-╣lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊╠rr╣)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr╠⌊╣rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(╠lr╣⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)╣-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)-lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠╠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.╠≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!cdf_prim.dot)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨╠.╣≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.dot);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)╠∨╣.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[75] 	(pr↑⍴⍺)╠∨.≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[75] 	(pr↑⍴╠⍺╣)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[75] 	(pr↑╠⍴╣⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[75] 	(pr↑╠⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[75] 	(pr╠↑╣⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[75] 	(╠pr╣↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	╠(pr↑⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	╠(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8037, ptr8038, 1), cleanup, 
		    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		k->fv[0] = retain_cell(loc->fx);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->lx);
	loc->lx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8039, ptr8040, 1), cleanup, 
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	
		k->fv[0] = retain_cell(loc->c);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->vc);
	loc->vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[77] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[77] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)╠-╣1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈╠rr╣)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[77] 	fx←(lr╠⌈╣rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[77] 	fx←(╠lr╣⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←╠(lr⌈rr)╣-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←╠(lr⌈rr)-1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->fx->value);
	loc->fx->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,╠⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←╠,╣⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←╠,⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->a);
	loc->a = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ╠⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!loc->lx)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr ╠lx╣ ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lx);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr╣ lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr lx ⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ax);
	loc->ax = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,╠⍵╣ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,╣⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,⍵╣ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->w);
	loc->w = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!loc->lx)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr ╠lx╣ ⍵");
	
	*stkhd++ = retain_cell(loc->lx);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr╣ lx ⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr lx ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->wx);
	loc->wx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}╠⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8041, ptr8042, 2), cleanup, 
		    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		k->fv[0] = retain_cell(loc->rr);
		k->fv[1] = retain_cell(loc->lr);
	
		*stkhd++ = k;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[78] 	s←⍴╠⍺╣{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴╠⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[78] 	s←╠⍴╣⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	s←╠⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s->value);
	loc->s->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+╠wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->wx);
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax╠+╣wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax╣+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]╠←╣ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[╠fx╣]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->s;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ ╠s[fx]←ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑╠s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx╠↑╣s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx╣↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×╠⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×╣⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c->value);
	loc->c->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->a)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc ╠a╣ ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->a);
	
	if (!loc->vc)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr ╠vc╣ a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->vc);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr╣ vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr vc a╣ ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ac);
	loc->ac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->w)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc ╠w╣ ");
	
	*stkhd++ = retain_cell(loc->w);
	
	if (!loc->vc)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr ╠vc╣ w ");
	
	*stkhd++ = retain_cell(loc->vc);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr╣ vc w ");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr vc w╣ ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->wc);
	loc->wc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[79] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[79] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)╠⍴╣0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷╠s╣)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[79] 	z←(c×fx╠⌷╣s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[79] 	z←(c×╠fx╣⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×╠fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c╠×╣fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->c->value)
		CHK(6, cleanup, L"[79] 	z←(╠c╣×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->c->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←╠(c×fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←╠(c×fx⌷s)⍴0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->a)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←╠a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->a);
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]╠←╣a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷╠ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ax);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)╠÷╣ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!loc->ac)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳╠ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ac);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳╣ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊╠(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊╣(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx╠×╣⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx╣×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->wx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i╠+╣wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->i)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i╣+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i+wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ ╠z[i+wx×⌊(i←⍳ac)÷ax]←a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->w)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←╠w╣");
	
	*stkhd++ = retain_cell(loc->w);
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]╠←╣w");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷╠wx╣]←w");
	
	*stkhd++ = retain_cell(loc->wx);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)╠÷╣wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!loc->wc)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳╠wc╣)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->wc);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳╣wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳wc╣)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊╠(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊╣(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊(i←⍳wc)÷wx╣]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1╠+╣⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax╠×╣1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax╣×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i╠+╣ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->i)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i╣+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i+ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ ╠z[i+ax×1+⌊(i←⍳wc)÷wx]←w╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[80] 	s⍴╠z╣	");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[80] 	s╠⍴╣z	");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[80] 	╠s╣⍴z	");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[80] 	╠s⍴z╣	");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[58] cat←╠{╣");
	return err;
}

int
ptr8033(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *s;
		struct cell_array *pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *axis;
		struct cell_array_box *noax;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->s = NULL;
	loc->pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->noax->value)
		CHK(6, cleanup, L"[61] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[60] 	israv:╠{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[61] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!ravel_ibeam)
				CHK(6, cleanup, L"[61] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[62] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[62] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[62] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[63] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[63] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[63] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[63] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->s)
		CHK(6, cleanup, L"[64] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[64] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[64] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[60] 	israv:╠{╣");
	return err;
}

int
ptr8034(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *s;
		struct cell_array *pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *axis;
		struct cell_array_box *noax;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->s = NULL;
	loc->pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->noax->value)
		CHK(6, cleanup, L"[61] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[61] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!ravel_ibeam)
				CHK(6, cleanup, L"[61] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[62] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[62] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[62] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[63] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[63] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[63] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[63] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->s)
		CHK(6, cleanup, L"[64] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[64] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[64] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[60] 	israv:╠{╣");
	return err;
}

int
ptr8035(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *fx;
		struct cell_array_box *axis;
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[71] 	axis≠fx←⌈axis:╠⍺{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
ptr8036(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *fx;
		struct cell_array_box *axis;
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
ptr8037(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *fx;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[76] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
ptr8038(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *fx;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[76] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
ptr8039(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *c;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->c->value)
				CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
ptr8040(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *c;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->c->value)
				CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
ptr8041(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[78] 	╠s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
ptr8042(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
ptr8043(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array_box *noax;
		struct cell_array *israv;
		struct cell_array_box *lr;
		struct cell_array_box *rr;
		struct cell_array_box *axis;
		struct cell_array *rk;
		struct cell_array *ar;
		struct cell_array_box *fx;
		struct cell_array *pr;
		struct cell_func *lx;
		struct cell_func *vc;
		struct cell_array_box *c;
		struct cell_array *a;
		struct cell_array *ax;
		struct cell_array *w;
		struct cell_array *wx;
		struct cell_array_box *s;
		struct cell_array *ac;
		struct cell_array *wc;
		struct cell_array_box *z;
		struct cell_array *i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	struct opt_vars {
		struct cell_array_box *axis;
	} *opts;

	opts = (struct opt_vars *)self->opts;

	CHK(mk_array_box(&loc->noax, NULL), cleanup, L"Init mutable variable: loc->noax");
	loc->israv = NULL;
	CHK(mk_array_box(&loc->lr, NULL), cleanup, L"Init mutable variable: loc->lr");
	CHK(mk_array_box(&loc->rr, NULL), cleanup, L"Init mutable variable: loc->rr");
	CHK(mk_array_box(&loc->axis, NULL), cleanup, L"Init mutable variable: loc->axis");
	loc->rk = NULL;
	loc->ar = NULL;
	CHK(mk_array_box(&loc->fx, NULL), cleanup, L"Init mutable variable: loc->fx");
	loc->pr = NULL;
	loc->lx = NULL;
	loc->vc = NULL;
	CHK(mk_array_box(&loc->c, NULL), cleanup, L"Init mutable variable: loc->c");
	loc->a = NULL;
	loc->ax = NULL;
	loc->w = NULL;
	loc->wx = NULL;
	CHK(mk_array_box(&loc->s, NULL), cleanup, L"Init mutable variable: loc->s");
	loc->ac = NULL;
	loc->wc = NULL;
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	loc->i = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[59] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[59] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->noax->value);
	loc->noax->value = retain_cell(stkhd[-1]);
	
	release_cell(loc->israv);
	loc->israv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴╠⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴╣⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢╣⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->lr->value);
	loc->lr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴╠⍵╣ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴╣⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢╣⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rr->value);
	loc->rr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->axis) {
		*stkhd++ = retain_cell(opts->axis);
	} else {
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			CHK(fill_array(arr, dat), cleanup, L"[59] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			*stkhd++ = arr;
		}
		
		release_cell(loc->noax->value);
		loc->noax->value = retain_cell(stkhd[-1]);
		
	}
	
	release_cell(loc->axis->value);
	loc->axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->israv)
		CHK(6, cleanup, L"[60] 	╠israv╣:{");
	
	*stkhd++ = retain_cell(loc->israv);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[60] 	╠israv╣:{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[65] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8044, ptr8045, 2), cleanup, 
				    L"[60] 	israv:╠{╣");
			
				k->fv[0] = retain_cell(loc->axis);
				k->fv[1] = retain_cell(loc->noax);
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[60] 	israv:╠{╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[66] 	rk←lr⌈╠rr╣ ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[66] 	rk←lr╠⌈╣rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[66] 	rk←╠lr╣⌈rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[66] 	rk←╠lr⌈rr╣ ⋄ axis⍠←¯1+rk");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rk);
	loc->rk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->axis) {
		*stkhd++ = retain_cell(opts->axis);
	} else {
		if (!loc->rk)
			CHK(6, cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←¯1+╠rk╣");
		
		*stkhd++ = retain_cell(loc->rk);
		
		if (!cdf_prim.add)
			CHK(6, cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←¯1╠+╣rk");
		
		*stkhd++ = retain_cell(cdf_prim.add);
		
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {-1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			CHK(fill_array(arr, dat), cleanup, L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			*stkhd++ = arr;
		}
		
		{
			struct cell_array *x = stkhd[-1];
			struct cell_func *fn = stkhd[-2];
			struct cell_array *y = stkhd[-3];
			struct cell_array *dst;
		
			CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
			    L"[66] 	rk←lr⌈rr ⋄ axis⍠←╠¯1+rk╣");
		
			release_array(x);
			release_func(fn);
			release_array(y);
		
			stkhd -= 3;
			*stkhd++ = dst;
		}
		
	}
	
	release_cell(loc->axis->value);
	loc->axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[67] 	(0≡lr)∧(0╠≡╣rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[67] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[67] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	(0≡lr)∧╠(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[67] 	(0≡lr)╠∧╣(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[67] 	(0≡╠lr╣)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[67] 	(0╠≡╣lr)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[67] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[67] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	╠(0≡lr)╣∧(0≡rr):(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[67] 	╠(0≡lr)∧(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[67] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺),,╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺),╠,╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):(,⍺),╠,⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,⍺)╠,╣,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			if (!alpha)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(,╠⍺╣),,⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[67] 	(0≡lr)∧(0≡rr):(╠,╣⍺),,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):╠(,⍺)╣,,⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[67] 	(0≡lr)∧(0≡rr):╠(,⍺),,⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[68] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[68] 	ar←≢╠⍴╣axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢╠⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←╠≢╣⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←╠≢⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ar);
	loc->ar = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[68] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[68] 	ar←≢⍴axis:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[68] 	ar←≢⍴axis:╠⎕SIGNAL 4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,╣axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢╣,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1╠≢╣≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1≢≢,axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				CHK(fill_array(arr, dat), cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL╣ 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[68] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL 5╣ ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[69] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[69] 	~╠is_numeric╣ axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[69] 	~╠is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[69] 	╠~╣is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[69] 	╠~is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[69] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[69] 	~is_numeric axis:╠⎕SIGNAL╣ 11 ⋄ axis≤¯1:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[69] 	~is_numeric axis:╠⎕SIGNAL 11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lte)
		CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis╠≤╣¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lte);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis╣≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis≤¯1╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[69] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rk)
		CHK(6, cleanup, L"[70] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rk);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[70] 	axis╠≥╣rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[70] 	╠axis╣≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	╠axis≥rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[70] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[70] 	axis≥rk:╠⎕SIGNAL╣ 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[70] 	axis≥rk:╠⎕SIGNAL 4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr╠-╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr╣-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|╣lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|lr-rr╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1╠<╣|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)╠∧╣1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr╠≢╣0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(╠rr╣≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)╣∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)╠∧╣(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr╠≢╣0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (╠lr╣≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)╣∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)∧(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[70] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[71] 	axis≠fx←⌈╠axis╣:⍺{");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[71] 	axis≠fx←╠⌈╣axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[71] 	axis≠fx←╠⌈axis╣:⍺{");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->fx->value);
	loc->fx->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[71] 	axis╠≠╣fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!loc->axis->value)
		CHK(6, cleanup, L"[71] 	╠axis╣≠fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(loc->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[71] 	╠axis≠fx←⌈axis╣:⍺{");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[71] 	axis≠fx←⌈╠axis╣:⍺{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[74] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8046, ptr8047, 4), cleanup, 
				    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
			
				k->fv[0] = retain_cell(loc->fx);
				k->fv[1] = retain_cell(loc->axis);
				k->fv[2] = retain_cell(loc->rr);
				k->fv[3] = retain_cell(loc->lr);
			
				*stkhd++ = k;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[71] 	axis≠fx←⌈axis:╠⍺╣{");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[71] 	axis≠fx←⌈axis:╠⍺{╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)╠↑╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=╠rr╣)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr╠=╣rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr╣=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)╠-╣lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊╠rr╣)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr╠⌊╣rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(╠lr╣⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)╣-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)-lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	(pr↑⍴⍺)∨.≠╠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.╠≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!cdf_prim.dot)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨╠.╣≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.dot);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)╠∨╣.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[75] 	(pr↑⍴⍺)╠∨.≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[75] 	(pr↑⍴╠⍺╣)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[75] 	(pr↑╠⍴╣⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[75] 	(pr↑╠⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[75] 	(pr╠↑╣⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[75] 	(╠pr╣↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	╠(pr↑⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[75] 	╠(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[75] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8048, ptr8049, 1), cleanup, 
		    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		k->fv[0] = retain_cell(loc->fx);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->lx);
	loc->lx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8050, ptr8051, 1), cleanup, 
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	
		k->fv[0] = retain_cell(loc->c);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->vc);
	loc->vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[77] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[77] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)╠-╣1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈╠rr╣)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[77] 	fx←(lr╠⌈╣rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[77] 	fx←(╠lr╣⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←╠(lr⌈rr)╣-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←╠(lr⌈rr)-1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->fx->value);
	loc->fx->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,╠⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←╠,╣⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←╠,⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->a);
	loc->a = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ╠⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!loc->lx)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr ╠lx╣ ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lx);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr╣ lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr lx ⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ax);
	loc->ax = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,╠⍵╣ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,╣⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,⍵╣ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->w);
	loc->w = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!loc->lx)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr ╠lx╣ ⍵");
	
	*stkhd++ = retain_cell(loc->lx);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr╣ lx ⍵");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[77] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr lx ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->wx);
	loc->wx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}╠⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8052, ptr8053, 2), cleanup, 
		    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		k->fv[0] = retain_cell(loc->rr);
		k->fv[1] = retain_cell(loc->lr);
	
		*stkhd++ = k;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[78] 	s←⍴╠⍺╣{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴╠⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[78] 	s←╠⍴╣⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	s←╠⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s->value);
	loc->s->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+╠wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->wx);
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax╠+╣wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax╣+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]╠←╣ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[╠fx╣]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->s;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ ╠s[fx]←ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑╠s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx╠↑╣s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx╣↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×╠⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×╣⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c->value);
	loc->c->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->a)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc ╠a╣ ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->a);
	
	if (!loc->vc)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr ╠vc╣ a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->vc);
	
	if (!loc->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr╣ vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr vc a╣ ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->ac);
	loc->ac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->w)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc ╠w╣ ");
	
	*stkhd++ = retain_cell(loc->w);
	
	if (!loc->vc)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr ╠vc╣ w ");
	
	*stkhd++ = retain_cell(loc->vc);
	
	if (!loc->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr╣ vc w ");
	
	*stkhd++ = retain_cell(loc->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr vc w╣ ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->wc);
	loc->wc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[79] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[79] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)╠⍴╣0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷╠s╣)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[79] 	z←(c×fx╠⌷╣s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!loc->fx->value)
		CHK(6, cleanup, L"[79] 	z←(c×╠fx╣⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×╠fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c╠×╣fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->c->value)
		CHK(6, cleanup, L"[79] 	z←(╠c╣×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->c->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←╠(c×fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←╠(c×fx⌷s)⍴0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->a)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←╠a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->a);
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]╠←╣a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷╠ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ax);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)╠÷╣ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!loc->ac)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳╠ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ac);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳╣ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊╠(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊╣(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx╠×╣⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx╣×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->wx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i╠+╣wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->i)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i╣+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i+wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ ╠z[i+wx×⌊(i←⍳ac)÷ax]←a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->w)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←╠w╣");
	
	*stkhd++ = retain_cell(loc->w);
	
	if (!cdf_prim.set)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]╠←╣w");
	
	*stkhd++ = retain_cell(cdf_prim.set);
	
	if (!loc->wx)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷╠wx╣]←w");
	
	*stkhd++ = retain_cell(loc->wx);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)╠÷╣wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!loc->wc)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳╠wc╣)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->wc);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳╣wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳wc╣)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊╠(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊╣(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊(i←⍳wc)÷wx╣]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1╠+╣⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax╠×╣1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->ax)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax╣×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i╠+╣ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->i)
		CHK(6, cleanup, L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i╣+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i+ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[79] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ ╠z[i+ax×1+⌊(i←⍳wc)÷wx]←w╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[80] 	s⍴╠z╣	");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[80] 	s╠⍴╣z	");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s->value)
		CHK(6, cleanup, L"[80] 	╠s╣⍴z	");
	
	*stkhd++ = retain_cell(loc->s->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[80] 	╠s⍴z╣	");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[58] cat←╠{╣");
	return err;
}

int
ptr8044(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *s;
		struct cell_array *pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *axis;
		struct cell_array_box *noax;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->s = NULL;
	loc->pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->noax->value)
		CHK(6, cleanup, L"[61] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[60] 	israv:╠{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[61] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!ravel_ibeam)
				CHK(6, cleanup, L"[61] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[62] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[62] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[62] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[63] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[63] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[63] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[63] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->s)
		CHK(6, cleanup, L"[64] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[64] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[64] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[60] 	israv:╠{╣");
	return err;
}

int
ptr8045(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *s;
		struct cell_array *pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *axis;
		struct cell_array_box *noax;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->s = NULL;
	loc->pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->noax->value)
		CHK(6, cleanup, L"[61] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[61] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!ravel_ibeam)
				CHK(6, cleanup, L"[61] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[62] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[62] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[62] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[62] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[61] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[62] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[63] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[63] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[63] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[63] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[63] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.max)
		CHK(6, cleanup, L"[64] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->pr);
	loc->pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[64] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[64] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->s)
		CHK(6, cleanup, L"[64] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.tke)
		CHK(6, cleanup, L"[64] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.tke);
	
	if (!loc->pr)
		CHK(6, cleanup, L"[64] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[64] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[60] 	israv:╠{╣");
	return err;
}

int
ptr8046(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *fx;
		struct cell_array_box *axis;
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[71] 	axis≠fx←⌈axis:╠⍺{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
ptr8047(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *fx;
		struct cell_array_box *axis;
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[72] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[72] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[72] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[72] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[72] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[72] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->rr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[73] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[73] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.pow)
		CHK(6, cleanup, L"[73] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.pow);
	
	if (!lex->axis->value)
		CHK(6, cleanup, L"[73] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->axis->value);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[73] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[73] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[73] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[71] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
ptr8048(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *fx;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[76] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
ptr8049(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *fx;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.gte)
		CHK(6, cleanup, L"[76] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.gte);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->fx->value)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
ptr8050(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *c;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->c->value)
				CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
ptr8051(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *c;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!alpha)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->c->value)
				CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[76] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
ptr8052(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[78] 	╠s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
ptr8053(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rr;
		struct cell_array_box *lr;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->rr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->rr->value);
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!lex->lr->value)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[78] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[78] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[78] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
ptr8054(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_simple;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_simple->value)
		CHK(6, cleanup, L"[85] 	╠is_simple╣ ⍵:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(lex->is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	╠is_simple ⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ 1}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8055, ptr8056, 0), cleanup, 
				    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[86] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[86] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[84] depth←╠{╣");
	return err;
}

int
ptr8055(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
ptr8056(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
ptr8057(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_simple;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_simple->value)
		CHK(6, cleanup, L"[85] 	╠is_simple╣ ⍵:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(lex->is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	╠is_simple ⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ 1}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8058, ptr8059, 0), cleanup, 
				    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[86] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[86] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[84] depth←╠{╣");
	return err;
}

int
ptr8058(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
ptr8059(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[85] 	is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[85] 	is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[85] 	is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
ptr8060(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[92] nqv←{⊃⍴╠⍵╣} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[92] nqv←{⊃╠⍴╣⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] nqv←{⊃╠⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[92] nqv←{╠⊃╣⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] nqv←{╠⊃⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[92] nqv←╠{⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	return err;
}

int
ptr8061(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[92] nqv←{⊃⍴╠⍵╣} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[92] nqv←{⊃╠⍴╣⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] nqv←{⊃╠⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[92] nqv←{╠⊃╣⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] nqv←{╠⊃⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[92] nqv←╠{⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	return err;
}

int
ptr8062(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *assert_inrange;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[96] 	0≡≢,╠⍺╣:0⍴⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[96] 	0≡≢╠,╣⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	0≡≢╠,⍺╣:0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[96] 	0≡╠≢╣,⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	0≡╠≢,⍺╣:0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[96] 	0╠≡╣≢,⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	╠0╣≡≢,⍺:0⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[96] 	╠0╣≡≢,⍺:0⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	╠0≡≢,⍺╣:0⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[96] 	0≡≢,╠⍺╣:0⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[96] 	0≡≢,⍺:0⍴╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[96] 	0≡≢,⍺:0╠⍴╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	0≡≢,⍺:╠0╣⍴⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[96] 	0≡≢,⍺:╠0╣⍴⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[96] 	0≡≢,⍺:╠0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[97] 	⍺ assert_inrange ╠⍵╣:");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->assert_inrange->value)
		CHK(6, cleanup, L"[97] 	⍺ ╠assert_inrange╣ ⍵:");
	
	*stkhd++ = retain_cell(lex->assert_inrange->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[97] 	╠⍺╣ assert_inrange ⍵:");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	╠⍺ assert_inrange ⍵╣:");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	⍺ assert_inrange ╠⍵╣:");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[98] 	⍺('index_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!index_ibeam)
		CHK(6, cleanup, L"[98] 	⍺╠('index_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(index_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[98] 	╠⍺╣('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	╠⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] index←╠{╣");
	return err;
}

int
ptr8063(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *assert_inrange;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[96] 	0≡≢,╠⍺╣:0⍴⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[96] 	0≡≢╠,╣⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	0≡≢╠,⍺╣:0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[96] 	0≡╠≢╣,⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	0≡╠≢,⍺╣:0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[96] 	0╠≡╣≢,⍺:0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	╠0╣≡≢,⍺:0⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[96] 	╠0╣≡≢,⍺:0⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	╠0≡≢,⍺╣:0⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[96] 	0≡≢,╠⍺╣:0⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[96] 	0≡≢,⍺:0⍴╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[96] 	0≡≢,⍺:0╠⍴╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	0≡≢,⍺:╠0╣⍴⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[96] 	0≡≢,⍺:╠0╣⍴⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[96] 	0≡≢,⍺:╠0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[97] 	⍺ assert_inrange ╠⍵╣:");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->assert_inrange->value)
		CHK(6, cleanup, L"[97] 	⍺ ╠assert_inrange╣ ⍵:");
	
	*stkhd++ = retain_cell(lex->assert_inrange->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[97] 	╠⍺╣ assert_inrange ⍵:");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	╠⍺ assert_inrange ⍵╣:");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	⍺ assert_inrange ╠⍵╣:");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[98] 	⍺('index_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!index_ibeam)
		CHK(6, cleanup, L"[98] 	⍺╠('index_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(index_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[98] 	╠⍺╣('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	╠⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] index←╠{╣");
	return err;
}

int
ptr8064(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *idx;
		struct cell_array *rnk;
		struct cell_array *csp;
		struct cell_array *csz;
		struct cell_array *i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *index;
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->idx = NULL;
	loc->rnk = NULL;
	loc->csp = NULL;
	loc->csz = NULL;
	loc->i = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[100] sqd←{⍺←╠⊢╣");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[101] 	1≡⍺ ╠1╣:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[101] 	1≡⍺ ╠1╣:⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[101] 	1≡╠⍺╣ 1:⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[101] 	1≡╠⍺ 1╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[101] 	1╠≡╣⍺ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[101] 	╠1╣≡⍺ 1:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[101] 	╠1╣≡⍺ 1:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[101] 	╠1≡⍺ 1╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[101] 	1≡⍺ ╠1╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[101] 	1≡⍺ 1:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[102] 	idx←⊃⍺⊣╠0╣ ⋄ rnk←≢⍴idx");
	
		CHK(fill_array(arr, dat), cleanup, L"[102] 	idx←⊃⍺⊣╠0╣ ⋄ rnk←≢⍴idx");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺╠⊣╣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃⍺╠⊣0╣ ⋄ rnk←≢⍴idx");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[102] 	idx←⊃╠⍺╣⊣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃╠⍺⊣0╣ ⋄ rnk←≢⍴idx");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[102] 	idx←╠⊃╣⍺⊣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←╠⊃⍺⊣0╣ ⋄ rnk←≢⍴idx");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->idx);
	loc->idx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢⍴╠idx╣");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢╠⍴╣idx");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢╠⍴idx╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←╠≢╣⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←╠≢⍴idx╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[103] 	0≡is_integer ╠idx╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[103] 	0≡╠is_integer╣ idx:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[103] 	0≡╠is_integer idx╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[103] 	0╠≡╣is_integer idx:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[103] 	╠0╣≡is_integer idx:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[103] 	╠0╣≡is_integer idx:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[103] 	╠0≡is_integer idx╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[103] 	0≡is_integer ╠idx╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[103] 	0≡is_integer idx:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[103] 	0≡is_integer idx:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[103] 	0≡is_integer idx:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[103] 	0≡is_integer idx:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠rnk╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8065, ptr8066, 0), cleanup, 
		    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠rnk╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[105] 	1≡≢⍴╠⍵╣:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[105] 	1≡≢╠⍴╣⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105] 	1≡≢╠⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[105] 	1≡╠≢╣⍴⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105] 	1≡╠≢⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[105] 	1╠≡╣≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[105] 	╠1╣≡≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[105] 	╠1╣≡≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[105] 	╠1≡≢⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[105] 	1≡≢⍴╠⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴idx index ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->index->value)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴idx ╠index╣ ⍵");
			
			*stkhd++ = retain_cell(lex->index->value);
			
			if (!loc->idx)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴╠idx╣ index ⍵");
			
			*stkhd++ = retain_cell(loc->idx);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:(⍴idx)⍴╠idx index ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)╠⍴╣idx index ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->idx)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴╠idx╣)⍴idx index ⍵");
			
			*stkhd++ = retain_cell(loc->idx);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(╠⍴╣idx)⍴idx index ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:╠(⍴idx)╣⍴idx index ⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:╠(⍴idx)⍴idx index ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1╠↓╣⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1╣↓⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1╣↓⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1↓⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->csp);
	loc->csp = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×╠⌿╣csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←╠×╣⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←╠×⌿╣csp←1↓⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←╠×⌿csp←1↓⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->csz);
	loc->csz = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+╠⍳╣csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+╠⍳csz←×⌿csp←1↓⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.oup)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.oup);
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->csz)
		CHK(6, cleanup, L"[106] 	i←(idx×╠csz╣)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(loc->csz);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[106] 	i←(idx╠×╣csz)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[106] 	i←(╠idx╣×csz)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(loc->idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←╠(idx×csz)╣∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←╠(idx×csz)∘.+⍳csz←×⌿csp←1↓⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴i index ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->index->value)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴i ╠index╣ ⍵");
	
	*stkhd++ = retain_cell(lex->index->value);
	
	if (!loc->i)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴╠i╣ index ⍵");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	((⍴idx),csp)⍴╠i index ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)╠⍴╣i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->csp)
		CHK(6, cleanup, L"[107] 	((⍴idx),╠csp╣)⍴i index ⍵");
	
	*stkhd++ = retain_cell(loc->csp);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[107] 	((⍴idx)╠,╣csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[107] 	((⍴╠idx╣),csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[107] 	((╠⍴╣idx),csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[107] 	(╠(⍴idx)╣,csp)⍴i index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	╠((⍴idx),csp)╣⍴i index ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	╠((⍴idx),csp)⍴i index ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[100] sqd←╠{⍺←⊢╣");
	return err;
}

int
ptr8065(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	return err;
}

int
ptr8066(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	return err;
}

int
ptr8067(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *idx;
		struct cell_array *rnk;
		struct cell_array *csp;
		struct cell_array *csz;
		struct cell_array *i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *index;
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->idx = NULL;
	loc->rnk = NULL;
	loc->csp = NULL;
	loc->csz = NULL;
	loc->i = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[101] 	1≡⍺ ╠1╣:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[101] 	1≡⍺ ╠1╣:⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[101] 	1≡╠⍺╣ 1:⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[101] 	1≡╠⍺ 1╣:⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[101] 	1≡╠⍺ 1╣:⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[101] 	1╠≡╣⍺ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[101] 	╠1╣≡⍺ 1:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[101] 	╠1╣≡⍺ 1:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[101] 	╠1≡⍺ 1╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[101] 	1≡⍺ ╠1╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[101] 	1≡⍺ 1:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[102] 	idx←⊃⍺⊣╠0╣ ⋄ rnk←≢⍴idx");
	
		CHK(fill_array(arr, dat), cleanup, L"[102] 	idx←⊃⍺⊣╠0╣ ⋄ rnk←≢⍴idx");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺╠⊣╣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[102] 	idx←⊃╠⍺╣⊣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[102] 	idx←⊃╠⍺⊣0╣ ⋄ rnk←≢⍴idx");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[102] 	idx←╠⊃╣⍺⊣0 ⋄ rnk←≢⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←╠⊃⍺⊣0╣ ⋄ rnk←≢⍴idx");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->idx);
	loc->idx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢⍴╠idx╣");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢╠⍴╣idx");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←≢╠⍴idx╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←╠≢╣⍴idx");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[102] 	idx←⊃⍺⊣0 ⋄ rnk←╠≢⍴idx╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rnk);
	loc->rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[103] 	0≡is_integer ╠idx╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[103] 	0≡╠is_integer╣ idx:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[103] 	0≡╠is_integer idx╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[103] 	0╠≡╣is_integer idx:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[103] 	╠0╣≡is_integer idx:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[103] 	╠0╣≡is_integer idx:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[103] 	╠0≡is_integer idx╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[103] 	0≡is_integer ╠idx╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[103] 	0≡is_integer idx:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[103] 	0≡is_integer idx:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[103] 	0≡is_integer idx:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[103] 	0≡is_integer idx:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->rnk)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠rnk╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(loc->rnk);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8068, ptr8069, 0), cleanup, 
		    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠rnk╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[105] 	1≡≢⍴╠⍵╣:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[105] 	1≡≢╠⍴╣⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105] 	1≡≢╠⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[105] 	1≡╠≢╣⍴⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105] 	1≡╠≢⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[105] 	1╠≡╣≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[105] 	╠1╣≡≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[105] 	╠1╣≡≢⍴⍵:(⍴idx)⍴idx index ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[105] 	╠1≡≢⍴⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[105] 	1≡≢⍴╠⍵╣:(⍴idx)⍴idx index ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴idx index ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->index->value)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴idx ╠index╣ ⍵");
			
			*stkhd++ = retain_cell(lex->index->value);
			
			if (!loc->idx)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)⍴╠idx╣ index ⍵");
			
			*stkhd++ = retain_cell(loc->idx);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:(⍴idx)⍴╠idx index ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴idx)╠⍴╣idx index ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->idx)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(⍴╠idx╣)⍴idx index ⍵");
			
			*stkhd++ = retain_cell(loc->idx);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[105] 	1≡≢⍴⍵:(╠⍴╣idx)⍴idx index ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:╠(⍴idx)╣⍴idx index ⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105] 	1≡≢⍴⍵:╠(⍴idx)⍴idx index ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓⍴╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1↓╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←1╠↓╣⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1╣↓⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1╣↓⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←×⌿csp←╠1↓⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->csp);
	loc->csp = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←×╠⌿╣csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+⍳csz←╠×╣⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←╠×⌿╣csp←1↓⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+⍳csz←╠×⌿csp←1↓⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->csz);
	loc->csz = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)∘.+╠⍳╣csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106] 	i←(idx×csz)∘.+╠⍳csz←×⌿csp←1↓⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.oup)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.oup);
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106] 	i←(idx×csz)╠∘.+╣⍳csz←×⌿csp←1↓⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->csz)
		CHK(6, cleanup, L"[106] 	i←(idx×╠csz╣)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(loc->csz);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[106] 	i←(idx╠×╣csz)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[106] 	i←(╠idx╣×csz)∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
	*stkhd++ = retain_cell(loc->idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←╠(idx×csz)╣∘.+⍳csz←×⌿csp←1↓⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106] 	i←╠(idx×csz)∘.+⍳csz←×⌿csp←1↓⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->i);
	loc->i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴i index ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->index->value)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴i ╠index╣ ⍵");
	
	*stkhd++ = retain_cell(lex->index->value);
	
	if (!loc->i)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)⍴╠i╣ index ⍵");
	
	*stkhd++ = retain_cell(loc->i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	((⍴idx),csp)⍴╠i index ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[107] 	((⍴idx),csp)╠⍴╣i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->csp)
		CHK(6, cleanup, L"[107] 	((⍴idx),╠csp╣)⍴i index ⍵");
	
	*stkhd++ = retain_cell(loc->csp);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[107] 	((⍴idx)╠,╣csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!loc->idx)
		CHK(6, cleanup, L"[107] 	((⍴╠idx╣),csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(loc->idx);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[107] 	((╠⍴╣idx),csp)⍴i index ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[107] 	(╠(⍴idx)╣,csp)⍴i index ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	╠((⍴idx),csp)╣⍴i index ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107] 	╠((⍴idx),csp)⍴i index ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[100] sqd←╠{⍺←⊢╣");
	return err;
}

int
ptr8068(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	return err;
}

int
ptr8069(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[104] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}rnk:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}rnk:⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[104] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}rnk:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣rnk:⎕SIGNAL 16");
	return err;
}

int
ptr8070(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[112] 	chk_valid_shape ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_valid_shape->value)
		CHK(6, cleanup, L"[112] 	╠chk_valid_shape╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[112] 	╠chk_valid_shape ⍵╣: ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[112] 	chk_valid_shape ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[113] 	0≡≢,╠⍵╣:⊂⍬");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[113] 	0≡≢╠,╣⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[113] 	0≡≢╠,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[113] 	0≡╠≢╣,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[113] 	0≡╠≢,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[113] 	0╠≡╣≢,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[113] 	╠0╣≡≢,⍵:⊂⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[113] 	╠0╣≡≢,⍵:⊂⍬");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[113] 	╠0≡≢,⍵╣:⊂⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[113] 	0≡≢,╠⍵╣:⊂⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[113] 	0≡≢,⍵:⊂╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[113] 	0≡≢,⍵:⊂╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.par)
				CHK(6, cleanup, L"[113] 	0≡≢,⍵:╠⊂╣⍬");
			
			*stkhd++ = retain_cell(cdf_prim.par);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[113] 	0≡≢,⍵:╠⊂⍬╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[114] 	(,0)≡,╠⍵╣:⍬");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[114] 	(,0)≡╠,╣⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] 	(,0)≡╠,⍵╣:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[114] 	(,0)╠≡╣,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] 	(,╠0╣)≡,⍵:⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[114] 	(,╠0╣)≡,⍵:⍬");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[114] 	(╠,╣0)≡,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] 	╠(,0)╣≡,⍵:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[114] 	╠(,0)≡,⍵╣:⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[114] 	(,0)≡,╠⍵╣:⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] 	(,0)≡,⍵:╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[114] 	(,0)≡,⍵:╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[115] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[115] 	1≡≢╠,╣⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[115] 	1≡≢╠,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[115] 	1≡╠≢╣,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[115] 	1≡╠≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[115] 	1╠≡╣≢,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[115] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[115] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[115] 	╠1≡≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[115] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[115] 	1≡≢,⍵:'index_gen_vec'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!index_gen_vec)
				CHK(6, cleanup, L"[115] 	1≡≢,⍵:╠'index_gen_vec'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(index_gen_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[115] 	1≡≢,⍵:╠'index_gen_vec'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[116] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[116] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[116] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[116] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[111] index_gen←╠{╣");
	return err;
}

int
ptr8071(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[112] 	chk_valid_shape ╠⍵╣: ");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_valid_shape->value)
		CHK(6, cleanup, L"[112] 	╠chk_valid_shape╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[112] 	╠chk_valid_shape ⍵╣: ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[112] 	chk_valid_shape ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[113] 	0≡≢,╠⍵╣:⊂⍬");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[113] 	0≡≢╠,╣⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[113] 	0≡≢╠,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[113] 	0≡╠≢╣,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[113] 	0≡╠≢,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[113] 	0╠≡╣≢,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[113] 	╠0╣≡≢,⍵:⊂⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[113] 	╠0╣≡≢,⍵:⊂⍬");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[113] 	╠0≡≢,⍵╣:⊂⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[113] 	0≡≢,╠⍵╣:⊂⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[113] 	0≡≢,⍵:⊂╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[113] 	0≡≢,⍵:⊂╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.par)
				CHK(6, cleanup, L"[113] 	0≡≢,⍵:╠⊂╣⍬");
			
			*stkhd++ = retain_cell(cdf_prim.par);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[113] 	0≡≢,⍵:╠⊂⍬╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[114] 	(,0)≡,╠⍵╣:⍬");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[114] 	(,0)≡╠,╣⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] 	(,0)≡╠,⍵╣:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[114] 	(,0)╠≡╣,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] 	(,╠0╣)≡,⍵:⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[114] 	(,╠0╣)≡,⍵:⍬");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[114] 	(╠,╣0)≡,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] 	╠(,0)╣≡,⍵:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[114] 	╠(,0)≡,⍵╣:⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[114] 	(,0)≡,╠⍵╣:⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] 	(,0)≡,⍵:╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[114] 	(,0)≡,⍵:╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[115] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[115] 	1≡≢╠,╣⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[115] 	1≡≢╠,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[115] 	1≡╠≢╣,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[115] 	1≡╠≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[115] 	1╠≡╣≢,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[115] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[115] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[115] 	╠1≡≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[115] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[115] 	1≡≢,⍵:'index_gen_vec'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!index_gen_vec)
				CHK(6, cleanup, L"[115] 	1≡≢,⍵:╠'index_gen_vec'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(index_gen_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[115] 	1≡≢,⍵:╠'index_gen_vec'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[116] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[116] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[116] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[116] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[111] index_gen←╠{╣");
	return err;
}

int
ptr8072(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[118] index_of←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[118] index_of←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[118] index_of←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[118] index_of←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[118] index_of←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8073(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[118] index_of←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[118] index_of←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[118] index_of←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[118] index_of←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[118] index_of←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8074(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[122] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[122] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8075(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[122] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[122] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[122] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8076(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[126] 	0≡≡╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[126] 	0≡╠≡╣⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	0≡╠≡⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[126] 	0╠≡╣≡⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	╠0╣≡≡⍵:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	╠0╣≡≡⍵:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	╠0≡≡⍵╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	0≡≡╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[126] 	0≡≡⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[127] 	'enclose_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!enclose_ibeam)
		CHK(6, cleanup, L"[127] 	╠'enclose_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(enclose_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	╠'enclose_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[125] enclose←╠{╣");
	return err;
}

int
ptr8077(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[126] 	0≡≡╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[126] 	0≡╠≡╣⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	0≡╠≡⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[126] 	0╠≡╣≡⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	╠0╣≡≡⍵:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	╠0╣≡≡⍵:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	╠0≡≡⍵╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	0≡≡╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[126] 	0≡≡⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[127] 	'enclose_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!enclose_ibeam)
		CHK(6, cleanup, L"[127] 	╠'enclose_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(enclose_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	╠'enclose_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[125] enclose←╠{╣");
	return err;
}

int
ptr8078(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[129] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[129] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[129] par←enclose ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] par←enclose ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8079(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[129] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[129] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[129] par←enclose ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] par←enclose ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8080(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR ╠squeeze╣ ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[132] conjugate←{1289≡⎕DR ╠squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[132] conjugate←{1289≡╠⎕DR╣ squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[132] conjugate←{1289≡╠⎕DR squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[132] conjugate←{1289╠≡╣⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[132] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[132] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[132] conjugate←{╠1289≡⎕DR squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[132] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ╠⍵╣ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!conjugate_vec)
				CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec'⌶╣ ⍵ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(conjugate_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[132] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec'⌶ ⍵╣ ⋄ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[132] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}╣");
	return err;
}

int
ptr8081(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR ╠squeeze╣ ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[132] conjugate←{1289≡⎕DR ╠squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[132] conjugate←{1289≡╠⎕DR╣ squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[132] conjugate←{1289≡╠⎕DR squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[132] conjugate←{1289╠≡╣⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[132] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[132] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[132] conjugate←{╠1289≡⎕DR squeeze ⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[132] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ╠⍵╣ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!conjugate_vec)
				CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec'⌶╣ ⍵ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(conjugate_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[132] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec'⌶ ⍵╣ ⋄ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[132] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[132] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec'⌶ ⍵ ⋄ ⍵}╣");
	return err;
}

int
ptr8082(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[136] sub←{0-╠⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[136] sub←{0╠-╣⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[136] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[136] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[136] sub←{╠0-⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[136] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
ptr8083(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[136] sub←{0-╠⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[136] sub←{0╠-╣⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[136] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[136] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[136] sub←{╠0-⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[136] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
ptr8084(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[140] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[140] 	1289=╠⎕DR╣ ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[140] 	1289=╠⎕DR ⍵╣:⍵÷|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[140] 	1289╠=╣⎕DR ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[140] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[140] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[140] 	╠1289=⎕DR ⍵╣:⍵÷|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[140] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵÷|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵÷╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[140] 	1289=⎕DR ⍵:⍵÷╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.div)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵╠÷╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.div);
			
			if (!omega)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:╠⍵╣÷|⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[140] 	1289=⎕DR ⍵:╠⍵÷|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[141] 	(-⍵<0)+0<╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[141] 	(-⍵<0)+0╠<╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[141] 	(-⍵<0)+╠0╣<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[141] 	(-⍵<0)+╠0╣<⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	(-⍵<0)+╠0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[141] 	(-⍵<0)╠+╣0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[141] 	(-⍵<╠0╣)+0<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[141] 	(-⍵<╠0╣)+0<⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[141] 	(-⍵╠<╣0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!omega)
		CHK(6, cleanup, L"[141] 	(-╠⍵╣<0)+0<⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	(-╠⍵<0)╣+0<⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[141] 	(╠-╣⍵<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[141] 	╠(-⍵<0)╣+0<⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	╠(-⍵<0)+0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[139] sign←╠{╣");
	return err;
}

int
ptr8085(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[140] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[140] 	1289=╠⎕DR╣ ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[140] 	1289=╠⎕DR ⍵╣:⍵÷|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[140] 	1289╠=╣⎕DR ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[140] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[140] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[140] 	╠1289=⎕DR ⍵╣:⍵÷|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[140] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵÷|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵÷╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[140] 	1289=⎕DR ⍵:⍵÷╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.div)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:⍵╠÷╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.div);
			
			if (!omega)
				CHK(6, cleanup, L"[140] 	1289=⎕DR ⍵:╠⍵╣÷|⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[140] 	1289=⎕DR ⍵:╠⍵÷|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[141] 	(-⍵<0)+0<╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[141] 	(-⍵<0)+0╠<╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[141] 	(-⍵<0)+╠0╣<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[141] 	(-⍵<0)+╠0╣<⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	(-⍵<0)+╠0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[141] 	(-⍵<0)╠+╣0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[141] 	(-⍵<╠0╣)+0<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[141] 	(-⍵<╠0╣)+0<⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[141] 	(-⍵╠<╣0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!omega)
		CHK(6, cleanup, L"[141] 	(-╠⍵╣<0)+0<⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	(-╠⍵<0)╣+0<⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[141] 	(╠-╣⍵<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[141] 	╠(-⍵<0)╣+0<⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[141] 	╠(-⍵<0)+0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[139] sign←╠{╣");
	return err;
}

int
ptr8086(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[146] div←{1÷╠⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[146] div←{1╠÷╣⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[146] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[146] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[146] div←{╠1÷⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[146] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
ptr8087(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[146] div←{1÷╠⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[146] div←{1╠÷╣⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[146] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[146] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[146] div←{╠1÷⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[146] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
ptr8088(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[150] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[150] 	0≡╠is_numeric╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[150] 	0≡╠is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[150] 	0╠≡╣is_numeric ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[150] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[150] 	╠0≡is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[150] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[150] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[150] 	0≡is_numeric ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[150] 	0≡is_numeric ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[151] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[151] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[151] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[151] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[151] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[151] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[151] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[151] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[151] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[151] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[151] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[151] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[152] 	'abs_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!abs_ibeam)
		CHK(6, cleanup, L"[152] 	╠'abs_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(abs_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[152] 	╠'abs_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[149] absolute←╠{╣");
	return err;
}

int
ptr8089(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[150] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_numeric->value)
		CHK(6, cleanup, L"[150] 	0≡╠is_numeric╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[150] 	0≡╠is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[150] 	0╠≡╣is_numeric ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[150] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[150] 	╠0≡is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[150] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[150] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[150] 	0≡is_numeric ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[150] 	0≡is_numeric ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[151] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[151] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[151] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[151] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[151] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[151] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[151] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[151] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[151] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[151] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[151] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[151] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[152] 	'abs_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!abs_ibeam)
		CHK(6, cleanup, L"[152] 	╠'abs_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(abs_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[152] 	╠'abs_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[149] absolute←╠{╣");
	return err;
}

int
ptr8090(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+0=╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+0╠=╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺╠+╣0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷╠⍺╣+0=⍺}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊⍵÷╠⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵╠÷╣⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊╠⍵╣÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊╠⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×╠⌊╣⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×╠⌊⍵÷⍺+0=⍺╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺╠×╣⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-╠⍺╣×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-╠⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[154] residue←{⍵╠-╣⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[154] residue←{╠⍵╣-⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{╠⍵-⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[154] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	return err;
}

int
ptr8091(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+0=╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+0╠=╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊⍵÷⍺+╠0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷⍺╠+╣0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵÷╠⍺╣+0=⍺}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊⍵÷╠⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊⍵╠÷╣⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×⌊╠⍵╣÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×⌊╠⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺×╠⌊╣⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[154] residue←{⍵-⍺×╠⌊⍵÷⍺+0=⍺╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[154] residue←{⍵-⍺╠×╣⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[154] residue←{⍵-╠⍺╣×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{⍵-╠⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[154] residue←{⍵╠-╣⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[154] residue←{╠⍵╣-⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[154] residue←{╠⍵-⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[154] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	return err;
}

int
ptr8092(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[159] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[159] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[159] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[159] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[159] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[160] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[160] 	1289=╠⎕DR╣ ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[160] 	1289=╠⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[160] 	1289╠=╣⎕DR ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[160] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[160] 	╠1289=⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[160] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[160] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[160] 	1289=⎕DR ⍵:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[160] 	1289=⎕DR ⍵:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[161] 	'floor_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!floor_vec_ibeam)
		CHK(6, cleanup, L"[161] 	╠'floor_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(floor_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[161] 	╠'floor_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[158] floor_array←╠{╣");
	return err;
}

int
ptr8093(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[159] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[159] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[159] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[159] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[159] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[160] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[160] 	1289=╠⎕DR╣ ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[160] 	1289=╠⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[160] 	1289╠=╣⎕DR ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[160] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[160] 	╠1289=⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[160] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[160] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[160] 	1289=⎕DR ⍵:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[160] 	1289=⎕DR ⍵:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[161] 	'floor_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!floor_vec_ibeam)
		CHK(6, cleanup, L"[161] 	╠'floor_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(floor_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[161] 	╠'floor_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[158] floor_array←╠{╣");
	return err;
}

int
ptr8094(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[167] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[167] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[167] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[167] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[167] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[168] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[168] 	1289=╠⎕DR╣ ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[168] 	1289=╠⎕DR ⍵╣:-⌊-⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[168] 	1289╠=╣⎕DR ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[168] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[168] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[168] 	╠1289=⎕DR ⍵╣:-⌊-⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[168] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-⌊-╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-⌊╠-╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:-⌊╠-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.min)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-╠⌊╣-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.min);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:-╠⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:╠-╣⌊-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:╠-⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[169] 	'ceil_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!ceil_vec_ibeam)
		CHK(6, cleanup, L"[169] 	╠'ceil_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(ceil_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[169] 	╠'ceil_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[166] ceil_array←╠{╣");
	return err;
}

int
ptr8095(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[167] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[167] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[167] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[167] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[167] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[168] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[168] 	1289=╠⎕DR╣ ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[168] 	1289=╠⎕DR ⍵╣:-⌊-⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[168] 	1289╠=╣⎕DR ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[168] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[168] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[168] 	╠1289=⎕DR ⍵╣:-⌊-⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[168] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-⌊-╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-⌊╠-╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:-⌊╠-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.min)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:-╠⌊╣-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.min);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:-╠⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[168] 	1289=⎕DR ⍵:╠-╣⌊-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[168] 	1289=⎕DR ⍵:╠-⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[169] 	'ceil_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!ceil_vec_ibeam)
		CHK(6, cleanup, L"[169] 	╠'ceil_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(ceil_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[169] 	╠'ceil_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[166] ceil_array←╠{╣");
	return err;
}

int
ptr8096(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[180] pitimes←{3.14159265358979323846×╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[180] pitimes←{3.14159265358979323846╠×╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {3.141592653589793};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[180] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[180] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[180] pitimes←{╠3.14159265358979323846×⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[180] pitimes←╠{3.14159265358979323846×⍵}╣");
	return err;
}

int
ptr8097(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[180] pitimes←{3.14159265358979323846×╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[180] pitimes←{3.14159265358979323846╠×╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {3.141592653589793};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[180] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[180] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[180] pitimes←{╠3.14159265358979323846×⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[180] pitimes←╠{3.14159265358979323846×⍵}╣");
	return err;
}

int
ptr8098(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[182] 	0≠≢╠⍺╣:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[182] 	0≠╠≢╣⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[182] 	0≠╠≢⍺╣:⍺ ∇¨⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[182] 	0╠≠╣≢⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[182] 	╠0╣≠≢⍺:⍺ ∇¨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[182] 	╠0╣≠≢⍺:⍺ ∇¨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[182] 	╠0≠≢⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[182] 	0≠≢╠⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ∇¨╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.map)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ∇╠¨╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.map);
			
			if (!self)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ╠∇╣¨⍵");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[182] 	0≠≢⍺:⍺ ╠∇¨╣⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:╠⍺╣ ∇¨⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[182] 	0≠≢⍺:╠⍺ ∇¨⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[183] 	0=╠⍺╣:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[183] 	0╠=╣⍺:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[183] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[183] 	╠0=⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[183] 	0=╠⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:(1-╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[183] 	0=⍺:(1╠-╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:╠(1-⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:╠(1-⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[184] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[184] 	1╠=╣⍺:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[184] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[184] 	╠1=⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[184] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[184] 	1=⍺:'sin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!sin_vec_ibeam)
				CHK(6, cleanup, L"[184] 	1=⍺:╠'sin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(sin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[184] 	1=⍺:╠'sin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[185] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[185] 	¯1╠=╣⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[185] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[185] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[185] 	╠¯1=⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[185] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[185] 	¯1=⍺:'arcsin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arcsin_vec_ibeam)
				CHK(6, cleanup, L"[185] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arcsin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[185] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[186] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[186] 	2╠=╣⍺:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[186] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[186] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[186] 	╠2=⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[186] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[186] 	2=⍺:'cos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cos_vec_ibeam)
				CHK(6, cleanup, L"[186] 	2=⍺:╠'cos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[186] 	2=⍺:╠'cos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[187] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[187] 	¯2╠=╣⍺:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[187] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[187] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[187] 	╠¯2=⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[187] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[187] 	¯2=⍺:'arccos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arccos_vec_ibeam)
				CHK(6, cleanup, L"[187] 	¯2=⍺:╠'arccos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arccos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[187] 	¯2=⍺:╠'arccos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[188] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[188] 	3╠=╣⍺:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[188] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[188] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[188] 	╠3=⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[188] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[188] 	3=⍺:'tan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!tan_vec_ibeam)
				CHK(6, cleanup, L"[188] 	3=⍺:╠'tan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(tan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[188] 	3=⍺:╠'tan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[189] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[189] 	¯3╠=╣⍺:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[189] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[189] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[189] 	╠¯3=⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[189] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[189] 	¯3=⍺:'arctan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arctan_vec_ibeam)
				CHK(6, cleanup, L"[189] 	¯3=⍺:╠'arctan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arctan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[189] 	¯3=⍺:╠'arctan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[190] 	4=╠⍺╣:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[190] 	4╠=╣⍺:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[190] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[190] 	╠4=⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[190] 	4=╠⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:(1+╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[190] 	4=⍺:(1╠+╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:╠(1+⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:╠(1+⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[191] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[191] 	¯4╠=╣⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[191] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[191] 	╠¯4=⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[191] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵╠+╣1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵╣+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.div)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)╠÷╣⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.div);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵╠-╣1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((╠⍵╣-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×(╠(⍵-1)╣÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)╠×╣((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵╠+╣1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(╠⍵╣+1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:╠(⍵+1)╣×((⍵-1)÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:╠(⍵+1)×((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[192] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[192] 	5╠=╣⍺:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[192] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[192] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[192] 	╠5=⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[192] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[192] 	5=⍺:'sinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!sinh_vec_ibeam)
				CHK(6, cleanup, L"[192] 	5=⍺:╠'sinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(sinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	5=⍺:╠'sinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[193] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[193] 	¯5╠=╣⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[193] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[193] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[193] 	╠¯5=⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[193] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[193] 	¯5=⍺:'arcsinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arcsinh_vec_ibeam)
				CHK(6, cleanup, L"[193] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arcsinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[193] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[194] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[194] 	6╠=╣⍺:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[194] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[194] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[194] 	╠6=⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[194] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[194] 	6=⍺:'cosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cosh_vec_ibeam)
				CHK(6, cleanup, L"[194] 	6=⍺:╠'cosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[194] 	6=⍺:╠'cosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[195] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[195] 	¯6╠=╣⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[195] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[195] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[195] 	╠¯6=⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[195] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[195] 	¯6=⍺:'arccosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arccosh_vec_ibeam)
				CHK(6, cleanup, L"[195] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arccosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[195] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[196] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[196] 	7╠=╣⍺:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[196] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[196] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[196] 	╠7=⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[196] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[196] 	7=⍺:'tanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!tanh_vec_ibeam)
				CHK(6, cleanup, L"[196] 	7=⍺:╠'tanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(tanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[196] 	7=⍺:╠'tanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[197] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[197] 	¯7╠=╣⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[197] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[197] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[197] 	╠¯7=⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[197] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[197] 	¯7=⍺:'arctanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arctanh_vec_ibeam)
				CHK(6, cleanup, L"[197] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arctanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[197] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[198] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[198] 	8╠=╣⍺:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[198] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[198] 	╠8=⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[198] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+⍵*2)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+⍵╠*╣2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+╠⍵╣*2)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:(-1+╠⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1╠+╣⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:(-╠1+⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[198] 	8=⍺:(╠-╣1+⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[198] 	8=⍺:╠(-1+⍵*2)╣*0.5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:╠(-1+⍵*2)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[199] 	¯8=╠⍺╣:-8○⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[199] 	¯8╠=╣⍺:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[199] 	╠¯8╣=⍺:-8○⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[199] 	╠¯8╣=⍺:-8○⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[199] 	╠¯8=⍺╣:-8○⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[199] 	¯8=╠⍺╣:-8○⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[199] 	¯8=⍺:-8○╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cir)
				CHK(6, cleanup, L"[199] 	¯8=⍺:-8╠○╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cir);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {8};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[199] 	¯8=⍺:-╠8╣○⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[199] 	¯8=⍺:-╠8╣○⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[199] 	¯8=⍺:-╠8○⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[199] 	¯8=⍺:╠-╣8○⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[199] 	¯8=⍺:╠-8○⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[200] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[200] 	9╠=╣⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	╠9=⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8099, ptr8100, 0), cleanup, 
				    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[201] 	¯9=╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[201] 	¯9╠=╣⍺:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[201] 	╠¯9╣=⍺:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[201] 	╠¯9╣=⍺:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[201] 	╠¯9=⍺╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[201] 	¯9=╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[201] 	¯9=⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[202] 	10=╠⍺╣:|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[202] 	10╠=╣⍺:|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[202] 	╠10╣=⍺:|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[202] 	╠10╣=⍺:|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[202] 	╠10=⍺╣:|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[202] 	10=╠⍺╣:|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[202] 	10=⍺:|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[202] 	10=⍺:╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[202] 	10=⍺:╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[203] 	¯10=╠⍺╣:+⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[203] 	¯10╠=╣⍺:+⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[203] 	╠¯10╣=⍺:+⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[203] 	╠¯10╣=⍺:+⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[203] 	╠¯10=⍺╣:+⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[203] 	¯10=╠⍺╣:+⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[203] 	¯10=⍺:+╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[203] 	¯10=⍺:╠+╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[203] 	¯10=⍺:╠+⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[204] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[204] 	11╠=╣⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	╠11=⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8101, ptr8102, 0), cleanup, 
				    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[205] 	¯11=╠⍺╣:⍵×0J1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[205] 	¯11╠=╣⍺:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[205] 	╠¯11╣=⍺:⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[205] 	╠¯11╣=⍺:⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[205] 	╠¯11=⍺╣:⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[205] 	¯11=╠⍺╣:⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[205] 	¯11=⍺:⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[205] 	¯11=⍺:⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[205] 	¯11=⍺:⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[205] 	¯11=⍺:╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[205] 	¯11=⍺:╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[206] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[206] 	12╠=╣⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[206] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[206] 	╠12=⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[206] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CHAR16;
				unsigned int rnk = 1;
				uint16_t dat[] = {80, 72, 65, 83, 69, 40, 9077, 41, 32, 78, 79, 84, 32, 73, 77, 80, 76, 69, 77, 69, 78, 84, 69, 68};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				arr->shape[0] = 24;
			
				CHK(fill_array(arr, dat), cleanup, L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[207] 	¯12=╠⍺╣:*⍵×0J1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[207] 	¯12╠=╣⍺:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	╠¯12╣=⍺:*⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[207] 	╠¯12╣=⍺:*⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[207] 	╠¯12=⍺╣:*⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[207] 	¯12=╠⍺╣:*⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	¯12=⍺:*⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	¯12=⍺:*⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[207] 	¯12=⍺:*⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[207] 	¯12=⍺:*╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	¯12=⍺:*╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[207] 	¯12=⍺:╠*╣⍵×0J1");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[207] 	¯12=⍺:╠*⍵×0J1╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[208] 	⎕SIGNAL ╠11╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[208] 	⎕SIGNAL ╠11╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[208] 	╠⎕SIGNAL╣ 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[208] 	╠⎕SIGNAL 11╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[181] trig←╠{╣");
	return err;
}

int
ptr8099(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[200] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!realpart_vec_ibeam)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
ptr8100(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[200] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!realpart_vec_ibeam)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
ptr8101(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!imagpart_vec_ibeam)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
ptr8102(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!imagpart_vec_ibeam)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
ptr8103(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[182] 	0≠≢╠⍺╣:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[182] 	0≠╠≢╣⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[182] 	0≠╠≢⍺╣:⍺ ∇¨⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[182] 	0╠≠╣≢⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[182] 	╠0╣≠≢⍺:⍺ ∇¨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[182] 	╠0╣≠≢⍺:⍺ ∇¨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[182] 	╠0≠≢⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[182] 	0≠≢╠⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ∇¨╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.map)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ∇╠¨╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.map);
			
			if (!self)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:⍺ ╠∇╣¨⍵");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[182] 	0≠≢⍺:⍺ ╠∇¨╣⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[182] 	0≠≢⍺:╠⍺╣ ∇¨⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[182] 	0≠≢⍺:╠⍺ ∇¨⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[183] 	0=╠⍺╣:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[183] 	0╠=╣⍺:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[183] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[183] 	╠0=⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[183] 	0=╠⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[183] 	0=⍺:(1-╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:(1-╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[183] 	0=⍺:(1╠-╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[183] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[183] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:╠(1-⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[183] 	0=⍺:╠(1-⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[184] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[184] 	1╠=╣⍺:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[184] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[184] 	╠1=⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[184] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[184] 	1=⍺:'sin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!sin_vec_ibeam)
				CHK(6, cleanup, L"[184] 	1=⍺:╠'sin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(sin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[184] 	1=⍺:╠'sin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[185] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[185] 	¯1╠=╣⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[185] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[185] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[185] 	╠¯1=⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[185] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[185] 	¯1=⍺:'arcsin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arcsin_vec_ibeam)
				CHK(6, cleanup, L"[185] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arcsin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[185] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[186] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[186] 	2╠=╣⍺:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[186] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[186] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[186] 	╠2=⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[186] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[186] 	2=⍺:'cos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cos_vec_ibeam)
				CHK(6, cleanup, L"[186] 	2=⍺:╠'cos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[186] 	2=⍺:╠'cos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[187] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[187] 	¯2╠=╣⍺:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[187] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[187] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[187] 	╠¯2=⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[187] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[187] 	¯2=⍺:'arccos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arccos_vec_ibeam)
				CHK(6, cleanup, L"[187] 	¯2=⍺:╠'arccos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arccos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[187] 	¯2=⍺:╠'arccos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[188] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[188] 	3╠=╣⍺:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[188] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[188] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[188] 	╠3=⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[188] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[188] 	3=⍺:'tan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!tan_vec_ibeam)
				CHK(6, cleanup, L"[188] 	3=⍺:╠'tan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(tan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[188] 	3=⍺:╠'tan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[189] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[189] 	¯3╠=╣⍺:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[189] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[189] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[189] 	╠¯3=⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[189] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[189] 	¯3=⍺:'arctan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arctan_vec_ibeam)
				CHK(6, cleanup, L"[189] 	¯3=⍺:╠'arctan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arctan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[189] 	¯3=⍺:╠'arctan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[190] 	4=╠⍺╣:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[190] 	4╠=╣⍺:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[190] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[190] 	╠4=⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[190] 	4=╠⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[190] 	4=⍺:(1+╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:(1+╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[190] 	4=⍺:(1╠+╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[190] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[190] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:╠(1+⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[190] 	4=⍺:╠(1+⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[191] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[191] 	¯4╠=╣⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[191] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[191] 	╠¯4=⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[191] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵╠+╣1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵╣+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.div)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-1)╠÷╣⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.div);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((⍵╠-╣1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)×((╠⍵╣-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×(╠(⍵-1)╣÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵+1)╠×╣((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[191] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[191] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(⍵╠+╣1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[191] 	¯4=⍺:(╠⍵╣+1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:╠(⍵+1)╣×((⍵-1)÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[191] 	¯4=⍺:╠(⍵+1)×((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[192] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[192] 	5╠=╣⍺:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[192] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[192] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[192] 	╠5=⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[192] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[192] 	5=⍺:'sinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!sinh_vec_ibeam)
				CHK(6, cleanup, L"[192] 	5=⍺:╠'sinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(sinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	5=⍺:╠'sinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[193] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[193] 	¯5╠=╣⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[193] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[193] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[193] 	╠¯5=⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[193] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[193] 	¯5=⍺:'arcsinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arcsinh_vec_ibeam)
				CHK(6, cleanup, L"[193] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arcsinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[193] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[194] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[194] 	6╠=╣⍺:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[194] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[194] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[194] 	╠6=⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[194] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[194] 	6=⍺:'cosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cosh_vec_ibeam)
				CHK(6, cleanup, L"[194] 	6=⍺:╠'cosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[194] 	6=⍺:╠'cosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[195] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[195] 	¯6╠=╣⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[195] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[195] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[195] 	╠¯6=⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[195] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[195] 	¯6=⍺:'arccosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arccosh_vec_ibeam)
				CHK(6, cleanup, L"[195] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arccosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[195] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[196] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[196] 	7╠=╣⍺:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[196] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[196] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[196] 	╠7=⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[196] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[196] 	7=⍺:'tanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!tanh_vec_ibeam)
				CHK(6, cleanup, L"[196] 	7=⍺:╠'tanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(tanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[196] 	7=⍺:╠'tanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[197] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[197] 	¯7╠=╣⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[197] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[197] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[197] 	╠¯7=⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[197] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[197] 	¯7=⍺:'arctanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!arctanh_vec_ibeam)
				CHK(6, cleanup, L"[197] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(arctanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[197] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[198] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[198] 	8╠=╣⍺:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[198] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[198] 	╠8=⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[198] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+⍵*2)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+⍵╠*╣2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			if (!omega)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1+╠⍵╣*2)*0.5");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:(-1+╠⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[198] 	8=⍺:(-1╠+╣⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[198] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[198] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:(-╠1+⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[198] 	8=⍺:(╠-╣1+⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[198] 	8=⍺:╠(-1+⍵*2)╣*0.5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[198] 	8=⍺:╠(-1+⍵*2)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[199] 	¯8=╠⍺╣:-8○⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[199] 	¯8╠=╣⍺:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[199] 	╠¯8╣=⍺:-8○⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[199] 	╠¯8╣=⍺:-8○⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[199] 	╠¯8=⍺╣:-8○⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[199] 	¯8=╠⍺╣:-8○⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[199] 	¯8=⍺:-8○╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cir)
				CHK(6, cleanup, L"[199] 	¯8=⍺:-8╠○╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cir);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {8};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[199] 	¯8=⍺:-╠8╣○⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[199] 	¯8=⍺:-╠8╣○⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[199] 	¯8=⍺:-╠8○⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.sub)
				CHK(6, cleanup, L"[199] 	¯8=⍺:╠-╣8○⍵");
			
			*stkhd++ = retain_cell(cdf_prim.sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[199] 	¯8=⍺:╠-8○⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[200] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[200] 	9╠=╣⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	╠9=⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8104, ptr8105, 0), cleanup, 
				    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[201] 	¯9=╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[201] 	¯9╠=╣⍺:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[201] 	╠¯9╣=⍺:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[201] 	╠¯9╣=⍺:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[201] 	╠¯9=⍺╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[201] 	¯9=╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[201] 	¯9=⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[202] 	10=╠⍺╣:|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[202] 	10╠=╣⍺:|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[202] 	╠10╣=⍺:|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[202] 	╠10╣=⍺:|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[202] 	╠10=⍺╣:|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[202] 	10=╠⍺╣:|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[202] 	10=⍺:|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[202] 	10=⍺:╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[202] 	10=⍺:╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[203] 	¯10=╠⍺╣:+⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[203] 	¯10╠=╣⍺:+⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[203] 	╠¯10╣=⍺:+⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[203] 	╠¯10╣=⍺:+⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[203] 	╠¯10=⍺╣:+⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[203] 	¯10=╠⍺╣:+⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[203] 	¯10=⍺:+╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[203] 	¯10=⍺:╠+╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[203] 	¯10=⍺:╠+⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[204] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[204] 	11╠=╣⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	╠11=⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8106, ptr8107, 0), cleanup, 
				    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[205] 	¯11=╠⍺╣:⍵×0J1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[205] 	¯11╠=╣⍺:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[205] 	╠¯11╣=⍺:⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[205] 	╠¯11╣=⍺:⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[205] 	╠¯11=⍺╣:⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[205] 	¯11=╠⍺╣:⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[205] 	¯11=⍺:⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[205] 	¯11=⍺:⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[205] 	¯11=⍺:⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[205] 	¯11=⍺:╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[205] 	¯11=⍺:╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[206] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[206] 	12╠=╣⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[206] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[206] 	╠12=⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[206] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[206] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CHAR16;
				unsigned int rnk = 1;
				uint16_t dat[] = {80, 72, 65, 83, 69, 40, 9077, 41, 32, 78, 79, 84, 32, 73, 77, 80, 76, 69, 77, 69, 78, 84, 69, 68};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				arr->shape[0] = 24;
			
				CHK(fill_array(arr, dat), cleanup, L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[206] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[207] 	¯12=╠⍺╣:*⍵×0J1");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[207] 	¯12╠=╣⍺:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	╠¯12╣=⍺:*⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[207] 	╠¯12╣=⍺:*⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[207] 	╠¯12=⍺╣:*⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[207] 	¯12=╠⍺╣:*⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	¯12=⍺:*⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	¯12=⍺:*⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[207] 	¯12=⍺:*⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[207] 	¯12=⍺:*╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	¯12=⍺:*╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.exp)
				CHK(6, cleanup, L"[207] 	¯12=⍺:╠*╣⍵×0J1");
			
			*stkhd++ = retain_cell(cdf_prim.exp);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[207] 	¯12=⍺:╠*⍵×0J1╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[208] 	⎕SIGNAL ╠11╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[208] 	⎕SIGNAL ╠11╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[208] 	╠⎕SIGNAL╣ 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[208] 	╠⎕SIGNAL 11╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[181] trig←╠{╣");
	return err;
}

int
ptr8104(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[200] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!realpart_vec_ibeam)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
ptr8105(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[200] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[200] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[200] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!realpart_vec_ibeam)
				CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[200] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[200] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[200] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
ptr8106(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!imagpart_vec_ibeam)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
ptr8107(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!imagpart_vec_ibeam)
				CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[204] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
ptr8108(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊╣⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺╠≠╣⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!alpha)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺╣≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)╠∧╣⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵╠<╣0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(╠⍵╣<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)╣∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)╠∧╣(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊╠⍵╣)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=╠⌊╣⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=╠⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵╠=╣⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(╠⍵╣=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,╠(⍵=⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,╠(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[214] 	∨⌿╠,╣(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿╠,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[214] 	∨╠⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[214] 	╠∨╣⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[214] 	╠∨⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	╠∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!⍵-╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!⍵╠-╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!╠⍵╣-⍺");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	(!⍵)÷(!⍺)×!╠⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×╠!╣⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	(!⍵)÷(!⍺)×╠!⍵-⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)╠×╣!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!╠⍺╣)×!⍵-⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(╠!╣⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	(!⍵)÷╠(!⍺)╣×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	(!⍵)÷╠(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[215] 	(!⍵)╠÷╣(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[215] 	(!╠⍵╣)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(╠!╣⍵)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	╠(!⍵)╣÷(!⍺)×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	╠(!⍵)÷(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[213] binomial←╠{╣");
	return err;
}

int
ptr8109(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!alpha)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊╣⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺╠≠╣⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!alpha)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺╣≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)╠∧╣⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵╠<╣0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(╠⍵╣<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)╣∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)╠∧╣(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊╠⍵╣)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.min)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵=╠⌊╣⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿,(⍵=╠⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[214] 	∨⌿,(⍵╠=╣⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	if (!omega)
		CHK(6, cleanup, L"[214] 	∨⌿,(╠⍵╣=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,╠(⍵=⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	∨⌿,╠(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[214] 	∨⌿╠,╣(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	∨⌿╠,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[214] 	∨╠⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[214] 	╠∨╣⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[214] 	╠∨⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[214] 	╠∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[214] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!⍵-╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!⍵╠-╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×!╠⍵╣-⍺");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	(!⍵)÷(!⍺)×!╠⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)×╠!╣⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	(!⍵)÷(!⍺)×╠!⍵-⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!⍺)╠×╣!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(!╠⍺╣)×!⍵-⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(!⍵)÷(╠!╣⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	(!⍵)÷╠(!⍺)╣×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	(!⍵)÷╠(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[215] 	(!⍵)╠÷╣(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[215] 	(!╠⍵╣)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.fac)
		CHK(6, cleanup, L"[215] 	(╠!╣⍵)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[215] 	╠(!⍵)╣÷(!⍺)×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	╠(!⍵)÷(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[213] binomial←╠{╣");
	return err;
}

int
ptr8110(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *is_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[220] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[220] notscl←{╠is_bool╣ ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[220] notscl←{╠is_bool ⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[220] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!not_vec_ibeam)
				CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(not_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[220] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[220] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8111(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *is_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[220] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[220] notscl←{╠is_bool╣ ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[220] notscl←{╠is_bool ⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[220] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!not_vec_ibeam)
				CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(not_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[220] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[220] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[220] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8112(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[221] without←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[221] without←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[221] without←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[221] without←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[221] without←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8113(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[221] without←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[221] without←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[221] without←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[221] without←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[221] without←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8114(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->z = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[226] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[226] 	⍺ ╠both_bool╣ ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[226] 	╠⍺╣ both_bool ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[226] 	╠⍺ both_bool ⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[226] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:⍺('and_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!and_vec_ibeam)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:⍺╠('and_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(and_vec_ibeam);
			
			if (!alpha)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:╠⍺╣('and_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[226] 	⍺ both_bool ⍵:╠⍺('and_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←⍺∨╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←⍺╠∨╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	if (!alpha)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←╠⍺╣∨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷z+0=z←╠⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z);
	loc->z = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0╠=╣z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[227] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[227] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷z+╠0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z╠+╣0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->z)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷╠z╣+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(loc->z);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷╠z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[227] 	⍺×⍵╠÷╣z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[227] 	⍺×╠⍵╣÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×╠⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[227] 	⍺╠×╣⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[227] 	╠⍺╣×⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	╠⍺×⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[225] logand←╠{╣");
	return err;
}

int
ptr8115(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	loc->z = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[226] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[226] 	⍺ ╠both_bool╣ ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[226] 	╠⍺╣ both_bool ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[226] 	╠⍺ both_bool ⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[226] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:⍺('and_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!and_vec_ibeam)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:⍺╠('and_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(and_vec_ibeam);
			
			if (!alpha)
				CHK(6, cleanup, L"[226] 	⍺ both_bool ⍵:╠⍺╣('and_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[226] 	⍺ both_bool ⍵:╠⍺('and_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←⍺∨╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←⍺╠∨╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	if (!alpha)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0=z←╠⍺╣∨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷z+0=z←╠⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z);
	loc->z = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z+0╠=╣z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[227] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[227] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷z+╠0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷z╠+╣0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->z)
		CHK(6, cleanup, L"[227] 	⍺×⍵÷╠z╣+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(loc->z);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×⍵÷╠z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.div)
		CHK(6, cleanup, L"[227] 	⍺×⍵╠÷╣z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.div);
	
	if (!omega)
		CHK(6, cleanup, L"[227] 	⍺×╠⍵╣÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	⍺×╠⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[227] 	⍺╠×╣⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[227] 	╠⍺╣×⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	╠⍺×⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[225] logand←╠{╣");
	return err;
}

int
ptr8116(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[229] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[229] and←{╠⎕SIGNAL╣ 2}ambiv (logand numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[229] and←{╠⎕SIGNAL 2╣}ambiv (logand numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[229] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	return err;
}

int
ptr8117(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[229] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[229] and←{╠⎕SIGNAL╣ 2}ambiv (logand numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[229] and←{╠⎕SIGNAL 2╣}ambiv (logand numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[229] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	return err;
}

int
ptr8118(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *both_bool;
		struct cell_func_box *both_integer;
		struct cell_func_box *is_bool;
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[233] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[233] 	⍺ ╠both_bool╣ ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[233] 	╠⍺╣ both_bool ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[233] 	╠⍺ both_bool ⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[233] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:⍺('lor_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lor_vec_ibeam)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:⍺╠('lor_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(lor_vec_ibeam);
			
			if (!alpha)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:╠⍺╣('lor_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[233] 	⍺ both_bool ⍵:╠⍺('lor_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧╠is_integer╣ ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[234] 	(is_bool ⍺)∧╠is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)╠∧╣is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!alpha)
		CHK(6, cleanup, L"[234] 	(is_bool ╠⍺╣)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[234] 	(╠is_bool╣ ⍺)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[234] 	╠(is_bool ⍺)╣∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[234] 	╠(is_bool ⍺)∧is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[234] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)╠×╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!alpha)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~╠⍺╣)×|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(╠~╣⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)╣×|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺╠+╣(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!alpha)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:╠⍺╣+(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:╠⍺+(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧╠is_integer╣ ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[235] 	(is_bool ⍵)∧╠is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)╠∧╣is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!omega)
		CHK(6, cleanup, L"[235] 	(is_bool ╠⍵╣)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[235] 	(╠is_bool╣ ⍵)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[235] 	╠(is_bool ⍵)╣∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[235] 	╠(is_bool ⍵)∧is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[235] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×|╠⍺╣");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|╣⍺");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|⍺╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)╠×╣|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~╠⍵╣)×|⍺");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(╠~╣⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)╣×|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵╠+╣(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:╠⍵╣+(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:╠⍵+(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_integer->value)
		CHK(6, cleanup, L"[236] 	⍺ ╠both_integer╣ ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(lex->both_integer->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	╠⍺╣ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	╠⍺ both_integer ⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8119, ptr8120, 0), cleanup, 
				    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
			
				*stkhd++ = k;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|╠⍺╣){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(╠|╣⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:╠(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8121, ptr8122, 0), cleanup, 
		    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	
		*stkhd++ = k;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|╠⍺╣){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(╠|╣⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	╠(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[232] logor←╠{╣");
	return err;
}

int
ptr8119(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8120(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8121(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[237] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[237] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8122(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[237] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8123(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *both_bool;
		struct cell_func_box *both_integer;
		struct cell_func_box *is_bool;
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[233] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[233] 	⍺ ╠both_bool╣ ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[233] 	╠⍺╣ both_bool ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[233] 	╠⍺ both_bool ⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[233] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:⍺('lor_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lor_vec_ibeam)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:⍺╠('lor_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(lor_vec_ibeam);
			
			if (!alpha)
				CHK(6, cleanup, L"[233] 	⍺ both_bool ⍵:╠⍺╣('lor_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[233] 	⍺ both_bool ⍵:╠⍺('lor_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧╠is_integer╣ ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[234] 	(is_bool ⍺)∧╠is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[234] 	(is_bool ⍺)╠∧╣is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!alpha)
		CHK(6, cleanup, L"[234] 	(is_bool ╠⍺╣)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[234] 	(╠is_bool╣ ⍺)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[234] 	╠(is_bool ⍺)╣∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[234] 	╠(is_bool ⍺)∧is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[234] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)╠×╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!alpha)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~╠⍺╣)×|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+(╠~╣⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)╣×|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:⍺╠+╣(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!alpha)
				CHK(6, cleanup, L"[234] 	(is_bool ⍺)∧is_integer ⍵:╠⍺╣+(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[234] 	(is_bool ⍺)∧is_integer ⍵:╠⍺+(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧╠is_integer╣ ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[235] 	(is_bool ⍵)∧╠is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.and)
		CHK(6, cleanup, L"[235] 	(is_bool ⍵)╠∧╣is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.and);
	
	if (!omega)
		CHK(6, cleanup, L"[235] 	(is_bool ╠⍵╣)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->is_bool->value)
		CHK(6, cleanup, L"[235] 	(╠is_bool╣ ⍵)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[235] 	╠(is_bool ⍵)╣∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[235] 	╠(is_bool ⍵)∧is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[235] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×|╠⍺╣");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|╣⍺");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|⍺╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.mul)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)╠×╣|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.mul);
			
			if (!omega)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~╠⍵╣)×|⍺");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+(╠~╣⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)╣×|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.add)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:⍵╠+╣(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.add);
			
			if (!omega)
				CHK(6, cleanup, L"[235] 	(is_bool ⍵)∧is_integer ⍺:╠⍵╣+(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[235] 	(is_bool ⍵)∧is_integer ⍺:╠⍵+(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_integer->value)
		CHK(6, cleanup, L"[236] 	⍺ ╠both_integer╣ ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(lex->both_integer->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	╠⍺╣ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	╠⍺ both_integer ⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, ptr8124, ptr8125, 0), cleanup, 
				    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
			
				*stkhd++ = k;
			}
			
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|╠⍺╣){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			if (!cdf_prim.res)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(╠|╣⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[236] 	⍺ both_integer ⍵:╠(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8126, ptr8127, 0), cleanup, 
		    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	
		*stkhd++ = k;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|╠⍺╣){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(╠|╣⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	╠(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[232] logor←╠{╣");
	return err;
}

int
ptr8124(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8125(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.neq)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.neq);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[236] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[236] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8126(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[237] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[237] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8127(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.gth)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.gth);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->m);
	loc->m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.lor)
		CHK(6, cleanup, L"[237] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alpha)
				CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!self)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(self);
	
	if (!omega)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!loc->m)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->m);
	
	if (!cdf_prim.not)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.mul)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.mul);
	
	if (!alpha)
		CHK(6, cleanup, L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[237] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
ptr8128(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[239] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[239] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[239] lor←{╠⎕SIGNAL╣ 2}ambiv (logor numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] lor←{╠⎕SIGNAL 2╣}ambiv (logor numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[239] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	return err;
}

int
ptr8129(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[239] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[239] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[239] lor←{╠⎕SIGNAL╣ 2}ambiv (logor numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] lor←{╠⎕SIGNAL 2╣}ambiv (logor numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[239] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	return err;
}

int
ptr8130(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[242] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[242] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[242] nan←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[242] nan←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[242] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
ptr8131(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[242] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[242] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[242] nan←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[242] nan←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[242] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
ptr8132(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[242] nan←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.and)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∧╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.and);
			
			if (!alpha)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[242] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8133(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[242] nan←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.and)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∧╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.and);
			
			if (!alpha)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[242] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[242] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8134(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[245] nor←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] nor←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
ptr8135(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[245] nor←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] nor←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
ptr8136(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[245] nor←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.lor)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∨╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.lor);
			
			if (!alpha)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8137(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *both_bool;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_bool->value)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->both_bool->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[245] nor←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.lor)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∨╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.lor);
			
			if (!alpha)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.not)
				CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
ptr8138(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[249] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[249] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[249] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[249] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[249] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[249] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[249] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[250] 	⍺('lth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lth_vec_ibeam)
		CHK(6, cleanup, L"[250] 	⍺╠('lth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(lth_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[250] 	╠⍺╣('lth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[250] 	╠⍺('lth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[248] lessthan←╠{╣");
	return err;
}

int
ptr8139(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[249] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[249] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[249] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[249] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[249] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[249] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[249] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[249] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[250] 	⍺('lth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lth_vec_ibeam)
		CHK(6, cleanup, L"[250] 	⍺╠('lth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(lth_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[250] 	╠⍺╣('lth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[250] 	╠⍺('lth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[248] lessthan←╠{╣");
	return err;
}

int
ptr8140(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[252] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[252] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[252] lth←{╠⎕SIGNAL╣ 2} ambiv (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[252] lth←{╠⎕SIGNAL 2╣} ambiv (lessthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[252] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	return err;
}

int
ptr8141(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[252] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[252] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[252] lth←{╠⎕SIGNAL╣ 2} ambiv (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[252] lth←{╠⎕SIGNAL 2╣} ambiv (lessthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[252] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	return err;
}

int
ptr8142(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[256] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[256] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[256] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[256] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[256] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[256] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[256] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[257] 	⍺('lte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lte_vec_ibeam)
		CHK(6, cleanup, L"[257] 	⍺╠('lte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(lte_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[257] 	╠⍺╣('lte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[257] 	╠⍺('lte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[255] lesseql←╠{╣");
	return err;
}

int
ptr8143(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[256] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[256] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[256] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[256] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[256] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[256] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[256] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[256] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[257] 	⍺('lte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lte_vec_ibeam)
		CHK(6, cleanup, L"[257] 	⍺╠('lte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(lte_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[257] 	╠⍺╣('lte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[257] 	╠⍺('lte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[255] lesseql←╠{╣");
	return err;
}

int
ptr8144(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[259] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[259] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[259] lte←{╠⎕SIGNAL╣ 2}ambiv (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] lte←{╠⎕SIGNAL 2╣}ambiv (lesseql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[259] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	return err;
}

int
ptr8145(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[259] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[259] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[259] lte←{╠⎕SIGNAL╣ 2}ambiv (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] lte←{╠⎕SIGNAL 2╣}ambiv (lesseql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[259] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	return err;
}

int
ptr8146(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *eql_vec;
		struct cell_func_box *both_char;
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[264] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[264] 	⍺ ╠both_numeric╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[264] 	╠⍺╣ both_numeric ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[264] 	╠⍺ both_numeric ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[264] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->eql_vec->value)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->eql_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[264] 	⍺ both_numeric ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[265] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_char->value)
		CHK(6, cleanup, L"[265] 	⍺ ╠both_char╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_char->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[265] 	╠⍺╣ both_char ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[265] 	╠⍺ both_char ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[265] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->eql_vec->value)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->eql_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[265] 	⍺ both_char ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[263] equal←╠{╣");
	return err;
}

int
ptr8147(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *eql_vec;
		struct cell_func_box *both_char;
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[264] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[264] 	⍺ ╠both_numeric╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[264] 	╠⍺╣ both_numeric ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[264] 	╠⍺ both_numeric ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[264] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->eql_vec->value)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->eql_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[264] 	⍺ both_numeric ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[264] 	⍺ both_numeric ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[265] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_char->value)
		CHK(6, cleanup, L"[265] 	⍺ ╠both_char╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_char->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[265] 	╠⍺╣ both_char ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[265] 	╠⍺ both_char ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[265] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->eql_vec->value)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->eql_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[265] 	⍺ both_char ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[265] 	⍺ both_char ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[263] equal←╠{╣");
	return err;
}

int
ptr8148(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[268] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[268] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[268] eql←{╠⎕SIGNAL╣ 2}ambiv (equal scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[268] eql←{╠⎕SIGNAL 2╣}ambiv (equal scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[268] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	return err;
}

int
ptr8149(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[268] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[268] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[268] eql←{╠⎕SIGNAL╣ 2}ambiv (equal scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[268] eql←{╠⎕SIGNAL 2╣}ambiv (equal scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[268] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	return err;
}

int
ptr8150(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[272] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[272] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[272] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[272] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[272] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[272] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[272] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[273] 	⍺('gte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!gte_vec_ibeam)
		CHK(6, cleanup, L"[273] 	⍺╠('gte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(gte_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[273] 	╠⍺╣('gte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	╠⍺('gte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[271] greatereql←╠{╣");
	return err;
}

int
ptr8151(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[272] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[272] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[272] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[272] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[272] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[272] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[272] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[272] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[273] 	⍺('gte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!gte_vec_ibeam)
		CHK(6, cleanup, L"[273] 	⍺╠('gte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(gte_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[273] 	╠⍺╣('gte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	╠⍺('gte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[271] greatereql←╠{╣");
	return err;
}

int
ptr8152(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[275] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[275] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[275] gte←{╠⎕SIGNAL╣ 2}ambiv (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[275] gte←{╠⎕SIGNAL 2╣}ambiv (greatereql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[275] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	return err;
}

int
ptr8153(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[275] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[275] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[275] gte←{╠⎕SIGNAL╣ 2}ambiv (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[275] gte←{╠⎕SIGNAL 2╣}ambiv (greatereql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[275] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	return err;
}

int
ptr8154(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[279] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[279] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[279] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[279] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[279] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[279] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[279] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[280] 	⍺('gth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!gth_vec_ibeam)
		CHK(6, cleanup, L"[280] 	⍺╠('gth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(gth_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[280] 	╠⍺╣('gth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	╠⍺('gth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[278] greaterthan←╠{╣");
	return err;
}

int
ptr8155(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[279] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[279] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[279] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[279] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[279] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[279] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[279] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.q_dr)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[279] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[280] 	⍺('gth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!gth_vec_ibeam)
		CHK(6, cleanup, L"[280] 	⍺╠('gth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(gth_vec_ibeam);
	
	if (!alpha)
		CHK(6, cleanup, L"[280] 	╠⍺╣('gth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	╠⍺('gth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[278] greaterthan←╠{╣");
	return err;
}

int
ptr8156(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[282] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[282] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[282] gth←{╠⎕SIGNAL╣ 2}ambiv (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[282] gth←{╠⎕SIGNAL 2╣}ambiv (greaterthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[282] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	return err;
}

int
ptr8157(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[282] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[282] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[282] gth←{╠⎕SIGNAL╣ 2}ambiv (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[282] gth←{╠⎕SIGNAL 2╣}ambiv (greaterthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[282] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	return err;
}

int
ptr8158(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[285] firstocc←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[285] firstocc←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[285] firstocc←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[285] firstocc←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[285] firstocc←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8159(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[285] firstocc←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[285] firstocc←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[285] firstocc←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[285] firstocc←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[285] firstocc←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8160(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *neq_vec;
		struct cell_func_box *both_char;
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[288] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[288] 	⍺ ╠both_numeric╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[288] 	╠⍺╣ both_numeric ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[288] 	╠⍺ both_numeric ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[288] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->neq_vec->value)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->neq_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[288] 	⍺ both_numeric ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[289] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_char->value)
		CHK(6, cleanup, L"[289] 	⍺ ╠both_char╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_char->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[289] 	╠⍺╣ both_char ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[289] 	╠⍺ both_char ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[289] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->neq_vec->value)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->neq_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[289] 	⍺ both_char ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[290] 	╠1╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[290] 	╠1╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[287] noteq←╠{╣");
	return err;
}

int
ptr8161(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *neq_vec;
		struct cell_func_box *both_char;
		struct cell_func_box *both_numeric;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[288] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_numeric->value)
		CHK(6, cleanup, L"[288] 	⍺ ╠both_numeric╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_numeric->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[288] 	╠⍺╣ both_numeric ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[288] 	╠⍺ both_numeric ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[288] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->neq_vec->value)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->neq_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[288] 	⍺ both_numeric ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[289] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->both_char->value)
		CHK(6, cleanup, L"[289] 	⍺ ╠both_char╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->both_char->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[289] 	╠⍺╣ both_char ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[289] 	╠⍺ both_char ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[289] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->neq_vec->value)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->neq_vec->value);
			
			if (!alpha)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[289] 	⍺ both_char ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[290] 	╠1╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[290] 	╠1╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[287] noteq←╠{╣");
	return err;
}

int
ptr8162(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[295] mix←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[295] mix←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[295] mix←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[295] mix←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[295] mix←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8163(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[295] mix←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[295] mix←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[295] mix←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[295] mix←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[295] mix←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8164(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[296] take←{(⊂⍳⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[296] take←{(⊂⍳⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[296] take←{(⊂⍳╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[296] take←{(⊂╠⍳╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] take←{(⊂╠⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[296] take←{(╠⊂╣⍳⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] take←{╠(⊂⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] take←{╠(⊂⍳⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[296] take←╠{(⊂⍳⍺)⌷⍵}╣");
	return err;
}

int
ptr8165(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[296] take←{(⊂⍳⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[296] take←{(⊂⍳⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[296] take←{(⊂⍳╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[296] take←{(⊂╠⍳╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] take←{(⊂╠⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[296] take←{(╠⊂╣⍳⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] take←{╠(⊂⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] take←{╠(⊂⍳⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[296] take←╠{(⊂⍳⍺)⌷⍵}╣");
	return err;
}

int
ptr8166(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[300] split←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[300] split←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[300] split←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[300] split←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[300] split←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8167(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[300] split←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[300] split←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[300] split←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[300] split←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[300] split←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8168(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)╠-╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢╠⍵╣)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(╠≢╣⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+⍳╠(≢⍵)╣-⍺)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+⍳╠(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+╠⍳╣(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+╠⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺╠+╣⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[301] drop←{(⊂╠⍺╣+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{(⊂╠⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[301] drop←{(╠⊂╣⍺+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[301] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	return err;
}

int
ptr8169(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)-╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢⍵)╠-╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(≢╠⍵╣)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+⍳(╠≢╣⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+⍳╠(≢⍵)╣-⍺)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+⍳╠(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺+╠⍳╣(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{(⊂⍺+╠⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[301] drop←{(⊂⍺╠+╣⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[301] drop←{(⊂╠⍺╣+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{(⊂╠⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[301] drop←{(╠⊂╣⍺+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[301] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[301] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[301] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	return err;
}

int
ptr8170(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢╠⍵╣)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳╠≢╣⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-1+⍳╠≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+╠⍳╣≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-1+╠⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1╠+╣⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[305] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[305] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-╠1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)╠-╣1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢╠⍵╣)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(╠≢╣⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂╠(≢⍵)╣-1+⍳≢⍵)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂╠(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[305] reverse_last←{(╠⊂╣(≢⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[305] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	return err;
}

int
ptr8171(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳≢╠⍵╣)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+⍳╠≢╣⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-1+⍳╠≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1+╠⍳╣≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-1+╠⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)-1╠+╣⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[305] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[305] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂(≢⍵)-╠1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢⍵)╠-╣1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omega)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(≢╠⍵╣)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[305] reverse_last←{(⊂(╠≢╣⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂╠(≢⍵)╣-1+⍳≢⍵)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{(⊂╠(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[305] reverse_last←{(╠⊂╣(≢⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[305] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[305] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[305] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	return err;
}

int
ptr8172(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[306] rotate_last←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[306] rotate_last←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[306] rotate_last←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[306] rotate_last←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[306] rotate_last←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8173(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[306] rotate_last←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[306] rotate_last←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[306] rotate_last←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[306] rotate_last←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[306] rotate_last←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8174(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[310] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alphaalpha)
				CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←╠⍵╣ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[310] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
ptr8175(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[310] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alphaalpha)
				CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[310] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
ptr8176(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[310] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alphaalpha)
				CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←╠⍵╣ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[310] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
ptr8177(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eql)
		CHK(6, cleanup, L"[310] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[310] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[310] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!alphaalpha)
				CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!omega)
		CHK(6, cleanup, L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[310] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[310] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
ptr8178(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[313] map←{⍺←╠⊢╣ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[313] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
ptr8179(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺╣ veach ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺╣ veach ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[313] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
ptr8180(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[313] map←{⍺←╠⊢╣ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[313] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
ptr8181(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *veach;
		struct cell_func_box *chk_scl;
		struct cell_func_box *max_shp;
		struct cell_func_box *squeeze;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	loc->s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->chk_scl->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->chk_scl->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->max_shp->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->max_shp->value);
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->veach->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->veach->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->squeeze->value)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!loc->s)
		CHK(6, cleanup, L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[313] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
ptr8182(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
		struct cell_array *c;
		struct cell_array *zs;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->s = NULL;
	loc->c = NULL;
	loc->zs = NULL;
	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[317] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[317] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!self)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c);
	loc->c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->zs);
	loc->zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!identity_ibeam)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(identity_ibeam);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8183, ptr8184, 2), cleanup, 
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->op);
		k->fv[1] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[319] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[319] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[319] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[316] reduce←╠{╣");
	return err;
}

int
ptr8183(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8184(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8185(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
		struct cell_array *c;
		struct cell_array *zs;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->s = NULL;
	loc->c = NULL;
	loc->zs = NULL;
	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[317] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[317] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!self)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c);
	loc->c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->zs);
	loc->zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!identity_ibeam)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(identity_ibeam);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8186, ptr8187, 2), cleanup, 
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->op);
		k->fv[1] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[319] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[319] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[319] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[316] reduce←╠{╣");
	return err;
}

int
ptr8186(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8187(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8188(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
		struct cell_array *c;
		struct cell_array *zs;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->s = NULL;
	loc->c = NULL;
	loc->zs = NULL;
	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[317] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[317] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!self)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c);
	loc->c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->zs);
	loc->zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!identity_ibeam)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(identity_ibeam);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8189, ptr8190, 2), cleanup, 
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->op);
		k->fv[1] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[319] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[319] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[319] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[316] reduce←╠{╣");
	return err;
}

int
ptr8189(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8190(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8191(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array *s;
		struct cell_array *c;
		struct cell_array *zs;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->s = NULL;
	loc->c = NULL;
	loc->zs = NULL;
	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[317] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[317] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[317] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.cat)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!self)
				CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[317] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->s);
	loc->s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->c);
	loc->c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->s)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->s);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[317] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->zs);
	loc->zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!identity_ibeam)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(identity_ibeam);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->c)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->c);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(omega);
			
			if (!cdf_prim.rho)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.rho);
			
			if (!loc->zs)
				CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[318] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8192, ptr8193, 2), cleanup, 
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->op);
		k->fv[1] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->c)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[319] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[319] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[319] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[316] reduce←╠{╣");
	return err;
}

int
ptr8192(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8193(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!alpha)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[319] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[319] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[319] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
ptr8194(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
		struct cell_array_box *rc;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->rc, NULL), cleanup, L"Init mutable variable: loc->rc");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.map)
		CHK(6, cleanup, L"[321] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[321] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[321] nwreduce←{op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[322] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[322] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rc->value);
	loc->rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array_box *bx = loc->rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8195, ptr8196, 3), cleanup, 
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->rc);
		k->fv[1] = retain_cell(loc->op);
		k->fv[2] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[323] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[323] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[321] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
ptr8195(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8196(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8197(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
		struct cell_array_box *rc;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->rc, NULL), cleanup, L"Init mutable variable: loc->rc");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.map)
		CHK(6, cleanup, L"[321] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[321] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[321] nwreduce←{op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[322] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[322] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rc->value);
	loc->rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array_box *bx = loc->rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8198, ptr8199, 3), cleanup, 
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->rc);
		k->fv[1] = retain_cell(loc->op);
		k->fv[2] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[323] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[323] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[321] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
ptr8198(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8199(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8200(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
		struct cell_array_box *rc;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->rc, NULL), cleanup, L"Init mutable variable: loc->rc");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.map)
		CHK(6, cleanup, L"[321] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[321] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[321] nwreduce←{op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[322] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[322] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rc->value);
	loc->rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array_box *bx = loc->rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8201, ptr8202, 3), cleanup, 
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->rc);
		k->fv[1] = retain_cell(loc->op);
		k->fv[2] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[323] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[323] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[321] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
ptr8201(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8202(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8203(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
		struct cell_array_box *rc;
		struct cell_array_box *z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *is_integer;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	CHK(mk_array_box(&loc->rc, NULL), cleanup, L"Init mutable variable: loc->rc");
	CHK(mk_array_box(&loc->z, NULL), cleanup, L"Init mutable variable: loc->z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.map)
		CHK(6, cleanup, L"[321] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[321] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[321] nwreduce←{op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!lex->is_integer->value)
		CHK(6, cleanup, L"[322] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[322] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->rc->value);
	loc->rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.res)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!loc->rc->value)
		CHK(6, cleanup, L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->rc->value);
	
	{
		struct cell_array_box *bx = loc->rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[322] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.drp)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->z->value);
	loc->z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8204, ptr8205, 3), cleanup, 
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->rc);
		k->fv[1] = retain_cell(loc->op);
		k->fv[2] = retain_cell(loc->z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[323] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rot)
		CHK(6, cleanup, L"[323] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[323] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!loc->z->value)
		CHK(6, cleanup, L"[323] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[321] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
ptr8204(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8205(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *rc;
		struct cell_func_box *op;
		struct cell_array_box *z;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!omega)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.sqd)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.sqd);
	
	if (!lex->rc->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->rc->value);
	
	if (!cdf_prim.iot)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.add)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.add);
	
	if (!alpha)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	if (!lex->z->value)
		CHK(6, cleanup, L"[323] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->z->value);
	
	{
		struct cell_array_box *bx = lex->z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[323] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[323] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
ptr8206(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_moper_box *nwreduce;
		struct cell_moper_box *reduce;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[325] rdf←{⍺←╠⊢╣ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[326] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->reduce->value)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->reduce->value);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->nwreduce->value)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->nwreduce->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
ptr8207(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_moper_box *nwreduce;
		struct cell_moper_box *reduce;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[326] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->reduce->value)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->reduce->value);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->nwreduce->value)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->nwreduce->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
ptr8208(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_moper_box *nwreduce;
		struct cell_moper_box *reduce;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[325] rdf←{⍺←╠⊢╣ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[326] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->reduce->value)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->reduce->value);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->nwreduce->value)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->nwreduce->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
ptr8209(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct lex_vars {
		struct cell_moper_box *nwreduce;
		struct cell_moper_box *reduce;
	} *lex;

	lex = (struct lex_vars *)deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.lth)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.lft)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.lft);
	
	if (!alpha)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.nqv)
		CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.q_signal)
				CHK(6, cleanup, L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[325] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[326] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[326] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[326] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[326] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(omega);
			
			if (!lex->reduce->value)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->reduce->value);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[326] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[326] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(omega);
	
	if (!lex->nwreduce->value)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->nwreduce->value);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[326] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
ptr8210(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[330] rpf←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[330] rpf←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[330] rpf←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[330] rpf←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[330] rpf←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8211(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[330] rpf←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[330] rpf←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[330] rpf←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[330] rpf←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[330] rpf←╠{⎕SIGNAL 16}╣");
	return err;
}

int
ptr8212(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rpf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rpf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8213(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rpf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rpf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8214(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8215(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8216(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rpf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rpf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8217(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rpf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rpf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8218(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8219(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[333] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[333] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[333] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[333] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[333] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[333] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[333] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
ptr8220(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array_box *op;
	} loc_frm, *loc;

	loc = &loc_frm;

	CHK(mk_array_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	err = 0;
	stkhd = &stk[0];

	if (!alphaalpha)
		CHK(6, cleanup, L"[336] oup←{op←╠⍺⍺╣ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}╠¨╣⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8221, ptr8222, 1), cleanup, 
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	
		k->fv[0] = retain_cell(loc->op);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}¨╣⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺╣{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,╠⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,╣⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃╣,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)╠⍴╣⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴╠⍵╣)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴╣⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴╠⍺╣),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((╠⍴╣⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←╠{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}╣");
	return err;
}

int
ptr8221(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8222(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8223(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_array *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_array_box *op;
	} loc_frm, *loc;

	loc = &loc_frm;

	CHK(mk_array_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	err = 0;
	stkhd = &stk[0];

	if (!alphaalpha)
		CHK(6, cleanup, L"[336] oup←{op←╠⍺⍺╣ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}╠¨╣⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8224, ptr8225, 1), cleanup, 
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	
		k->fv[0] = retain_cell(loc->op);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}¨╣⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺╣{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,╠⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,╣⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃╣,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)╠⍴╣⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴╠⍵╣)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴╣⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴╠⍺╣),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((╠⍴╣⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←╠{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}╣");
	return err;
}

int
ptr8224(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_array_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8225(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8226(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
	} loc_frm, *loc;

	loc = &loc_frm;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	err = 0;
	stkhd = &stk[0];

	if (!alphaalpha)
		CHK(6, cleanup, L"[336] oup←{op←╠⍺⍺╣ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}╠¨╣⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8227, ptr8228, 1), cleanup, 
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	
		k->fv[0] = retain_cell(loc->op);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}¨╣⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺╣{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,╠⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,╣⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃╣,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)╠⍴╣⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴╠⍵╣)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴╣⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴╠⍺╣),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((╠⍴╣⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←╠{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}╣");
	return err;
}

int
ptr8227(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_fam, op->fptr_fad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8228(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_fam, op->fptr_fad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8229(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *deldel;
	struct cell_func *alphaalpha;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];

	struct {
		struct cell_func_box *op;
	} loc_frm, *loc;

	loc = &loc_frm;

	CHK(mk_func_box(&loc->op, NULL), cleanup, L"Init mutable variable: loc->op");
	err = 0;
	stkhd = &stk[0];

	if (!alphaalpha)
		CHK(6, cleanup, L"[336] oup←{op←╠⍺⍺╣ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	release_cell(loc->op->value);
	loc->op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.par)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨╠⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}╠¨╣⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, ptr8230, ptr8231, 1), cleanup, 
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	
		k->fv[0] = retain_cell(loc->op);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}¨╣⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺╣{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿╠⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,╠⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,╣⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿╣⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃╠,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃╣,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴╠⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)╠⍴╣⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴╠⍵╣)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴╣⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),╠⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴╠⍺╣),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	if (!cdf_prim.rho)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((╠⍴╣⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)╣⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ╠((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←╠{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨⍵}¨⊂⍵}╣");
	return err;
}

int
ptr8230(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;

	alpha = NULL;

	struct lex_vars {
		struct cell_func_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_fam, op->fptr_fad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8231(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *op;
	} *lex;

	lex = (struct lex_vars *)self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!omega)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op¨╠⍵╣}¨⊂⍵}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!cdf_prim.map)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘op╠¨╣⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.map);
	
	if (!lex->op->value)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺∘╠op╣¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(lex->op->value);
	
	if (!cdf_prim.jot)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿⍺╠∘╣op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.jot);
	
	if (!alpha)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺╣∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_fam, op->fptr_fad, x, y), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op╣¨⍵}¨⊂⍵}");
	
		release_array(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨╣⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,⌿╠⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rdf)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃,╠⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rdf);
	
	if (!cdf_prim.cat)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,╣⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cat);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿╣⍺∘op¨⍵}¨⊂⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{⊃╠,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.dis)
		CHK(6, cleanup, L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃╣,⌿⍺∘op¨⍵}¨⊂⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺{╠⊃,⌿⍺∘op¨⍵╣}¨⊂⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[336] oup←{op←⍺⍺ ⋄ ((⍴⍺),⍴⍵)⍴⊃,⌿⍺╠{⊃,⌿⍺∘op¨⍵}╣¨⊂⍵}");
	return err;
}

int
ptr8232(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[339] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8233(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[339] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8234(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[339] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8235(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[339] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8236(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8237(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8238(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	release_cell(alpha);
	alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8239(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[339] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!omega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(omega);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.rgt)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.sub)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!omegaomega)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!deldel)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(deldel);
	
	if (!alphaalpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!alpha)
		CHK(6, cleanup, L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[339] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[339] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
ptr8240(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 4;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
				dat[3] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omegaomega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!omega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8241(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 2, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 4;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
				dat[3] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omegaomega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!omega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8242(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omegaomega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!omega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8243(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_array *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omegaomega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!omega)
				CHK(6, cleanup, L"[347] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8244(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8245(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_array *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8246(struct cell_array **z,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *alpha;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	alpha = NULL;
	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

int
ptr8247(struct cell_array **z,
    struct cell_array *alpha,
    struct cell_array *omega,
    struct cell_func *self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *deldel;
	struct cell_func *alphaalpha;
	struct cell_func *omegaomega;

	deldel = self->fv[0];
	alphaalpha = self->fv[1];
	omegaomega = self->fv[2];

	struct {
		struct cell_array *type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->type);
	loc->type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[344] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[344] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[344] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[344] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			if (!alpha)
				CHK(6, cleanup, L"[344] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[344] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[345] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[345] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[345] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[345] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[345] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[345] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[346] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[346] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!omega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(omega);
			
			if (!omegaomega)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!alphaalpha)
				CHK(6, cleanup, L"[346] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.eqv)
		CHK(6, cleanup, L"[347] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.eqv);
	
	if (!loc->type)
		CHK(6, cleanup, L"[347] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[347] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[347] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[348] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.q_signal)
		CHK(6, cleanup, L"[348] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[348] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[342] jot←╠{╣");
	return err;
}

EXPORT int
q_signal(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->q_signal;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
q_signal_dwa(void *z, void *l, void *r)
{
	return call_dwa(q_signal, z, l, r, L"q_signal");
}

EXPORT int
q_dr(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->q_dr;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
q_dr_dwa(void *z, void *l, void *r)
{
	return call_dwa(q_dr, z, l, r, L"q_dr");
}

EXPORT int
squeeze(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->squeeze->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
squeeze_dwa(void *z, void *l, void *r)
{
	return call_dwa(squeeze, z, l, r, L"squeeze");
}

EXPORT int
is_simple(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->is_simple->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
is_simple_dwa(void *z, void *l, void *r)
{
	return call_dwa(is_simple, z, l, r, L"is_simple");
}

EXPORT int
is_numeric(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->is_numeric->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
is_numeric_dwa(void *z, void *l, void *r)
{
	return call_dwa(is_numeric, z, l, r, L"is_numeric");
}

EXPORT int
is_char(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->is_char->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
is_char_dwa(void *z, void *l, void *r)
{
	return call_dwa(is_char, z, l, r, L"is_char");
}

EXPORT int
is_integer(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->is_integer->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
is_integer_dwa(void *z, void *l, void *r)
{
	return call_dwa(is_integer, z, l, r, L"is_integer");
}

EXPORT int
is_bool(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->is_bool->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
is_bool_dwa(void *z, void *l, void *r)
{
	return call_dwa(is_bool, z, l, r, L"is_bool");
}

EXPORT int
max_shp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->max_shp->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
max_shp_dwa(void *z, void *l, void *r)
{
	return call_dwa(max_shp, z, l, r, L"max_shp");
}

EXPORT int
has_nat_vals(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->has_nat_vals->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
has_nat_vals_dwa(void *z, void *l, void *r)
{
	return call_dwa(has_nat_vals, z, l, r, L"has_nat_vals");
}

EXPORT int
chk_scl(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->chk_scl->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
chk_scl_dwa(void *z, void *l, void *r)
{
	return call_dwa(chk_scl, z, l, r, L"chk_scl");
}

EXPORT int
chk_valid_shape(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->chk_valid_shape->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
chk_valid_shape_dwa(void *z, void *l, void *r)
{
	return call_dwa(chk_valid_shape, z, l, r, L"chk_valid_shape");
}

EXPORT int
both_simple(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->both_simple->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
both_simple_dwa(void *z, void *l, void *r)
{
	return call_dwa(both_simple, z, l, r, L"both_simple");
}

EXPORT int
both_numeric(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->both_numeric->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
both_numeric_dwa(void *z, void *l, void *r)
{
	return call_dwa(both_numeric, z, l, r, L"both_numeric");
}

EXPORT int
both_integer(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->both_integer->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
both_integer_dwa(void *z, void *l, void *r)
{
	return call_dwa(both_integer, z, l, r, L"both_integer");
}

EXPORT int
both_char(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->both_char->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
both_char_dwa(void *z, void *l, void *r)
{
	return call_dwa(both_char, z, l, r, L"both_char");
}

EXPORT int
both_bool(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->both_bool->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
both_bool_dwa(void *z, void *l, void *r)
{
	return call_dwa(both_bool, z, l, r, L"both_bool");
}

EXPORT int
any(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->any->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
any_dwa(void *z, void *l, void *r)
{
	return call_dwa(any, z, l, r, L"any");
}

EXPORT int
assert_inrange(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->assert_inrange->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
assert_inrange_dwa(void *z, void *l, void *r)
{
	return call_dwa(assert_inrange, z, l, r, L"assert_inrange");
}

EXPORT int
set(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->set;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
set_dwa(void *z, void *l, void *r)
{
	return call_dwa(set, z, l, r, L"set");
}

EXPORT int
brk(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->brk;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
brk_dwa(void *z, void *l, void *r)
{
	return call_dwa(brk, z, l, r, L"brk");
}

EXPORT int
rgt(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->rgt;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
rgt_dwa(void *z, void *l, void *r)
{
	return call_dwa(rgt, z, l, r, L"rgt");
}

EXPORT int
lft(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lft;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lft_dwa(void *z, void *l, void *r)
{
	return call_dwa(lft, z, l, r, L"lft");
}

EXPORT int
reshape(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->reshape;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
reshape_dwa(void *z, void *l, void *r)
{
	return call_dwa(reshape, z, l, r, L"reshape");
}

EXPORT int
rho(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->rho;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
rho_dwa(void *z, void *l, void *r)
{
	return call_dwa(rho, z, l, r, L"rho");
}

EXPORT int
cat(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->cat;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
cat_dwa(void *z, void *l, void *r)
{
	return call_dwa(cat, z, l, r, L"cat");
}

EXPORT int
depth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->depth;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
depth_dwa(void *z, void *l, void *r)
{
	return call_dwa(depth, z, l, r, L"depth");
}

EXPORT int
same(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->same->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
same_dwa(void *z, void *l, void *r)
{
	return call_dwa(same, z, l, r, L"same");
}

EXPORT int
eqv(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->eqv;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
eqv_dwa(void *z, void *l, void *r)
{
	return call_dwa(eqv, z, l, r, L"eqv");
}

EXPORT int
nqv(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->nqv;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
nqv_dwa(void *z, void *l, void *r)
{
	return call_dwa(nqv, z, l, r, L"nqv");
}

EXPORT int
index(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->index->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
index_dwa(void *z, void *l, void *r)
{
	return call_dwa(index, z, l, r, L"index");
}

EXPORT int
sqd(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->sqd;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
sqd_dwa(void *z, void *l, void *r)
{
	return call_dwa(sqd, z, l, r, L"sqd");
}

EXPORT int
index_gen(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->index_gen;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
index_gen_dwa(void *z, void *l, void *r)
{
	return call_dwa(index_gen, z, l, r, L"index_gen");
}

EXPORT int
index_of(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->index_of;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
index_of_dwa(void *z, void *l, void *r)
{
	return call_dwa(index_of, z, l, r, L"index_of");
}

EXPORT int
iot(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->iot;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
iot_dwa(void *z, void *l, void *r)
{
	return call_dwa(iot, z, l, r, L"iot");
}

EXPORT int
dis(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->dis;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
dis_dwa(void *z, void *l, void *r)
{
	return call_dwa(dis, z, l, r, L"dis");
}

EXPORT int
enclose(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->enclose;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
enclose_dwa(void *z, void *l, void *r)
{
	return call_dwa(enclose, z, l, r, L"enclose");
}

EXPORT int
par(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->par;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
par_dwa(void *z, void *l, void *r)
{
	return call_dwa(par, z, l, r, L"par");
}

EXPORT int
conjugate(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->conjugate;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
conjugate_dwa(void *z, void *l, void *r)
{
	return call_dwa(conjugate, z, l, r, L"conjugate");
}

EXPORT int
add(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->add;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
add_dwa(void *z, void *l, void *r)
{
	return call_dwa(add, z, l, r, L"add");
}

EXPORT int
sub(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->sub;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
sub_dwa(void *z, void *l, void *r)
{
	return call_dwa(sub, z, l, r, L"sub");
}

EXPORT int
sign(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->sign;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
sign_dwa(void *z, void *l, void *r)
{
	return call_dwa(sign, z, l, r, L"sign");
}

EXPORT int
mul(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->mul;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
mul_dwa(void *z, void *l, void *r)
{
	return call_dwa(mul, z, l, r, L"mul");
}

EXPORT int
div(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->div;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
div_dwa(void *z, void *l, void *r)
{
	return call_dwa(div, z, l, r, L"div");
}

EXPORT int
absolute(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->absolute;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
absolute_dwa(void *z, void *l, void *r)
{
	return call_dwa(absolute, z, l, r, L"absolute");
}

EXPORT int
residue(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->residue;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
residue_dwa(void *z, void *l, void *r)
{
	return call_dwa(residue, z, l, r, L"residue");
}

EXPORT int
res(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->res;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
res_dwa(void *z, void *l, void *r)
{
	return call_dwa(res, z, l, r, L"res");
}

EXPORT int
floor_array(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->floor_array;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
floor_array_dwa(void *z, void *l, void *r)
{
	return call_dwa(floor_array, z, l, r, L"floor_array");
}

EXPORT int
min(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->min;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
min_dwa(void *z, void *l, void *r)
{
	return call_dwa(min, z, l, r, L"min");
}

EXPORT int
ceil_array(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->ceil_array;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
ceil_array_dwa(void *z, void *l, void *r)
{
	return call_dwa(ceil_array, z, l, r, L"ceil_array");
}

EXPORT int
max(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->max;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
max_dwa(void *z, void *l, void *r)
{
	return call_dwa(max, z, l, r, L"max");
}

EXPORT int
exp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->exp;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
exp_dwa(void *z, void *l, void *r)
{
	return call_dwa(exp, z, l, r, L"exp");
}

EXPORT int
log(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->log;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
log_dwa(void *z, void *l, void *r)
{
	return call_dwa(log, z, l, r, L"log");
}

EXPORT int
pitimes(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->pitimes;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
pitimes_dwa(void *z, void *l, void *r)
{
	return call_dwa(pitimes, z, l, r, L"pitimes");
}

EXPORT int
trig(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->trig;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
trig_dwa(void *z, void *l, void *r)
{
	return call_dwa(trig, z, l, r, L"trig");
}

EXPORT int
cir(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->cir;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
cir_dwa(void *z, void *l, void *r)
{
	return call_dwa(cir, z, l, r, L"cir");
}

EXPORT int
binomial(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->binomial;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
binomial_dwa(void *z, void *l, void *r)
{
	return call_dwa(binomial, z, l, r, L"binomial");
}

EXPORT int
fac(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->fac;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
fac_dwa(void *z, void *l, void *r)
{
	return call_dwa(fac, z, l, r, L"fac");
}

EXPORT int
notscl(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->notscl;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
notscl_dwa(void *z, void *l, void *r)
{
	return call_dwa(notscl, z, l, r, L"notscl");
}

EXPORT int
without(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->without;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
without_dwa(void *z, void *l, void *r)
{
	return call_dwa(without, z, l, r, L"without");
}

EXPORT int
not(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->not;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
not_dwa(void *z, void *l, void *r)
{
	return call_dwa(not, z, l, r, L"not");
}

EXPORT int
logand(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->logand;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
logand_dwa(void *z, void *l, void *r)
{
	return call_dwa(logand, z, l, r, L"logand");
}

EXPORT int
and(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->and;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
and_dwa(void *z, void *l, void *r)
{
	return call_dwa(and, z, l, r, L"and");
}

EXPORT int
logor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->logor;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
logor_dwa(void *z, void *l, void *r)
{
	return call_dwa(logor, z, l, r, L"logor");
}

EXPORT int
lor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lor;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lor_dwa(void *z, void *l, void *r)
{
	return call_dwa(lor, z, l, r, L"lor");
}

EXPORT int
nan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->nan;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
nan_dwa(void *z, void *l, void *r)
{
	return call_dwa(nan, z, l, r, L"nan");
}

EXPORT int
nor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->nor;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
nor_dwa(void *z, void *l, void *r)
{
	return call_dwa(nor, z, l, r, L"nor");
}

EXPORT int
lessthan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lessthan;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lessthan_dwa(void *z, void *l, void *r)
{
	return call_dwa(lessthan, z, l, r, L"lessthan");
}

EXPORT int
lth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lth;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lth_dwa(void *z, void *l, void *r)
{
	return call_dwa(lth, z, l, r, L"lth");
}

EXPORT int
lesseql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lesseql;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lesseql_dwa(void *z, void *l, void *r)
{
	return call_dwa(lesseql, z, l, r, L"lesseql");
}

EXPORT int
lte(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->lte;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
lte_dwa(void *z, void *l, void *r)
{
	return call_dwa(lte, z, l, r, L"lte");
}

EXPORT int
eql_vec(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->eql_vec->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
eql_vec_dwa(void *z, void *l, void *r)
{
	return call_dwa(eql_vec, z, l, r, L"eql_vec");
}

EXPORT int
equal(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->equal;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
equal_dwa(void *z, void *l, void *r)
{
	return call_dwa(equal, z, l, r, L"equal");
}

EXPORT int
eql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->eql;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
eql_dwa(void *z, void *l, void *r)
{
	return call_dwa(eql, z, l, r, L"eql");
}

EXPORT int
greatereql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->greatereql;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
greatereql_dwa(void *z, void *l, void *r)
{
	return call_dwa(greatereql, z, l, r, L"greatereql");
}

EXPORT int
gte(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->gte;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
gte_dwa(void *z, void *l, void *r)
{
	return call_dwa(gte, z, l, r, L"gte");
}

EXPORT int
greaterthan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->greaterthan;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
greaterthan_dwa(void *z, void *l, void *r)
{
	return call_dwa(greaterthan, z, l, r, L"greaterthan");
}

EXPORT int
gth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->gth;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
gth_dwa(void *z, void *l, void *r)
{
	return call_dwa(gth, z, l, r, L"gth");
}

EXPORT int
firstocc(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->firstocc;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
firstocc_dwa(void *z, void *l, void *r)
{
	return call_dwa(firstocc, z, l, r, L"firstocc");
}

EXPORT int
neq_vec(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->neq_vec->value;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
neq_vec_dwa(void *z, void *l, void *r)
{
	return call_dwa(neq_vec, z, l, r, L"neq_vec");
}

EXPORT int
noteq(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->noteq;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
noteq_dwa(void *z, void *l, void *r)
{
	return call_dwa(noteq, z, l, r, L"noteq");
}

EXPORT int
neq(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->neq;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
neq_dwa(void *z, void *l, void *r)
{
	return call_dwa(neq, z, l, r, L"neq");
}

EXPORT int
mix(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->mix;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
mix_dwa(void *z, void *l, void *r)
{
	return call_dwa(mix, z, l, r, L"mix");
}

EXPORT int
take(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->take;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
take_dwa(void *z, void *l, void *r)
{
	return call_dwa(take, z, l, r, L"take");
}

EXPORT int
tke(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->tke;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
tke_dwa(void *z, void *l, void *r)
{
	return call_dwa(tke, z, l, r, L"tke");
}

EXPORT int
split(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->split;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
split_dwa(void *z, void *l, void *r)
{
	return call_dwa(split, z, l, r, L"split");
}

EXPORT int
drop(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->drop;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
drop_dwa(void *z, void *l, void *r)
{
	return call_dwa(drop, z, l, r, L"drop");
}

EXPORT int
drp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->drp;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
drp_dwa(void *z, void *l, void *r)
{
	return call_dwa(drp, z, l, r, L"drp");
}

EXPORT int
reverse_last(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->reverse_last;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
reverse_last_dwa(void *z, void *l, void *r)
{
	return call_dwa(reverse_last, z, l, r, L"reverse_last");
}

EXPORT int
rotate_last(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->rotate_last;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
rotate_last_dwa(void *z, void *l, void *r)
{
	return call_dwa(rotate_last, z, l, r, L"rotate_last");
}

EXPORT int
rot(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->rot;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
rot_dwa(void *z, void *l, void *r)
{
	return call_dwa(rot, z, l, r, L"rot");
}

EXPORT int
rpf(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;

	cdf_prim_init();

	loc = &cdf_prim;
	self = loc->rpf;

	if (l == NULL)
		return self->fptr_mon(z, r, self);
	else
		return self->fptr_dya(z, l, r, self);
}

EXPORT int
rpf_dwa(void *z, void *l, void *r)
{
	return call_dwa(rpf, z, l, r, L"rpf");
}

