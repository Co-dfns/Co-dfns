#include "codfns.h"
#include "prim.h"

EXPORT int
DyalogGetInterpreterFunctions(void *p)
{
    return set_dwafns(p);
}

extern struct cell_func *cdf_q_signal_ibeam;
extern struct cell_func *cdf_q_dr_ibeam;
extern struct cell_array *cdf_span_array;
extern struct cell_func *cdf_squeeze_ibeam;
extern struct cell_func *cdf_is_simple_ibeam;
extern struct cell_func *cdf_is_numeric_ibeam;
extern struct cell_func *cdf_is_char_ibeam;
extern struct cell_func *cdf_is_integer_ibeam;
extern struct cell_func *cdf_is_span_ibeam;
extern struct cell_func *cdf_max_shp_ibeam;
extern struct cell_func *cdf_max_shp_ibeam;
extern struct cell_func *cdf_has_nat_vals_ibeam;
extern struct cell_func *cdf_any_ibeam;
extern struct cell_moper *cdf_veach_ibeam;
extern struct cell_func *cdf_set_ibeam;
extern struct cell_func *cdf_reshape_ibeam;
extern struct cell_func *cdf_reshape_ibeam;
extern struct cell_func *cdf_shape_ibeam;
extern struct cell_func *cdf_ravel_ibeam;
extern struct cell_func *cdf_ravel_ibeam;
extern struct cell_func *cdf_ravel_ibeam;
extern struct cell_func *cdf_ravel_ibeam;
extern struct cell_func *cdf_same_ibeam;
extern struct cell_func *cdf_nqv_ibeam;
extern struct cell_func *cdf_index_ibeam;
extern struct cell_func *cdf_index_ibeam;
extern struct cell_func *cdf_index_gen_vec;
extern struct cell_func *cdf_index_gen_vec;
extern struct cell_func *cdf_disclose_ibeam;
extern struct cell_func *cdf_enclose_ibeam;
extern struct cell_func *cdf_enclose_ibeam;
extern struct cell_func *cdf_conjugate_vec_ibeam;
extern struct cell_func *cdf_conjugate_vec_ibeam;
extern struct cell_func *cdf_add_vec_ibeam;
extern struct cell_func *cdf_sub_vec_ibeam;
extern struct cell_func *cdf_mul_vec_ibeam;
extern struct cell_func *cdf_div_vec_ibeam;
extern struct cell_func *cdf_abs_vec_ibeam;
extern struct cell_func *cdf_abs_vec_ibeam;
extern struct cell_func *cdf_floor_vec_ibeam;
extern struct cell_func *cdf_floor_vec_ibeam;
extern struct cell_func *cdf_min_vec_ibeam;
extern struct cell_func *cdf_ceil_vec_ibeam;
extern struct cell_func *cdf_ceil_vec_ibeam;
extern struct cell_func *cdf_max_vec_ibeam;
extern struct cell_func *cdf_pow_vec_ibeam;
extern struct cell_func *cdf_exp_vec_ibeam;
extern struct cell_func *cdf_log_vec_ibeam;
extern struct cell_func *cdf_nlg_vec_ibeam;
extern struct cell_func *cdf_sin_vec_ibeam;
extern struct cell_func *cdf_arcsin_vec_ibeam;
extern struct cell_func *cdf_cos_vec_ibeam;
extern struct cell_func *cdf_arccos_vec_ibeam;
extern struct cell_func *cdf_tan_vec_ibeam;
extern struct cell_func *cdf_arctan_vec_ibeam;
extern struct cell_func *cdf_sinh_vec_ibeam;
extern struct cell_func *cdf_arcsinh_vec_ibeam;
extern struct cell_func *cdf_cosh_vec_ibeam;
extern struct cell_func *cdf_arccosh_vec_ibeam;
extern struct cell_func *cdf_tanh_vec_ibeam;
extern struct cell_func *cdf_arctanh_vec_ibeam;
extern struct cell_func *cdf_realpart_vec_ibeam;
extern struct cell_func *cdf_realpart_vec_ibeam;
extern struct cell_func *cdf_imagpart_vec_ibeam;
extern struct cell_func *cdf_imagpart_vec_ibeam;
extern struct cell_func *cdf_sin_vec_ibeam;
extern struct cell_func *cdf_arcsin_vec_ibeam;
extern struct cell_func *cdf_cos_vec_ibeam;
extern struct cell_func *cdf_arccos_vec_ibeam;
extern struct cell_func *cdf_tan_vec_ibeam;
extern struct cell_func *cdf_arctan_vec_ibeam;
extern struct cell_func *cdf_sinh_vec_ibeam;
extern struct cell_func *cdf_arcsinh_vec_ibeam;
extern struct cell_func *cdf_cosh_vec_ibeam;
extern struct cell_func *cdf_arccosh_vec_ibeam;
extern struct cell_func *cdf_tanh_vec_ibeam;
extern struct cell_func *cdf_arctanh_vec_ibeam;
extern struct cell_func *cdf_realpart_vec_ibeam;
extern struct cell_func *cdf_realpart_vec_ibeam;
extern struct cell_func *cdf_imagpart_vec_ibeam;
extern struct cell_func *cdf_imagpart_vec_ibeam;
extern struct cell_func *cdf_factorial_vec_ibeam;
extern struct cell_func *cdf_not_vec_ibeam;
extern struct cell_func *cdf_not_vec_ibeam;
extern struct cell_func *cdf_and_vec_ibeam;
extern struct cell_func *cdf_and_vec_ibeam;
extern struct cell_func *cdf_lor_vec_ibeam;
extern struct cell_func *cdf_lor_vec_ibeam;
extern struct cell_func *cdf_lth_vec_ibeam;
extern struct cell_func *cdf_lth_vec_ibeam;
extern struct cell_func *cdf_lte_vec_ibeam;
extern struct cell_func *cdf_lte_vec_ibeam;
extern struct cell_func *cdf_eql_vec_ibeam;
extern struct cell_func *cdf_gte_vec_ibeam;
extern struct cell_func *cdf_gte_vec_ibeam;
extern struct cell_func *cdf_gth_vec_ibeam;
extern struct cell_func *cdf_gth_vec_ibeam;
extern struct cell_func *cdf_neq_vec_ibeam;
extern struct cell_moper *cdf_identity_ibeam;
extern struct cell_moper *cdf_identity_ibeam;
extern struct cell_moper *cdf_identity_ibeam;
extern struct cell_moper *cdf_identity_ibeam;

int cdf_ptr8798(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8799(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8800(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8801(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8802(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8803(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8804(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8805(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8806(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8807(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8808(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8809(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8810(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8811(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8812(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8813(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8814(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8815(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8816(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8817(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8818(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8819(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8820(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8821(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8822(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8823(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8824(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8825(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8826(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8827(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8828(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8829(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8830(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8831(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8832(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8833(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8834(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8835(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8836(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8837(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8838(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8839(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8840(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8841(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8842(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8843(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8844(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8845(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8846(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8847(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8848(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8849(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8850(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8851(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8852(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8853(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8854(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8855(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8856(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8857(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8858(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8859(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8860(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8861(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8862(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8863(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8864(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8865(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8866(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8867(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8868(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8869(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8870(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8871(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8872(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8873(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8874(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8875(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8876(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8877(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8878(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8879(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8880(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8881(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8882(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8883(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8884(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8885(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8886(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8887(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8888(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8889(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8890(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8891(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8892(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8893(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8894(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8895(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8896(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8897(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8898(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8899(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8900(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8901(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8902(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8903(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8904(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8905(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8906(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8907(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8908(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8909(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8910(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8911(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8912(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8913(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8914(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8915(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8916(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8917(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8918(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8919(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8920(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8921(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8922(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8923(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8924(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8925(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8926(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8927(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8928(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8929(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8930(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8931(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8932(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8933(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8934(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8935(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8936(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8937(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8938(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8939(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8940(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8941(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8942(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8943(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8944(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8945(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8946(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8947(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8948(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8949(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8950(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8951(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8952(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8953(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8954(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8955(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8956(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8957(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8958(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8959(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8960(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8961(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8962(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8963(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8964(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8965(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8966(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8967(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8968(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8969(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8970(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8971(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8972(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8973(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8974(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8975(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8976(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8977(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8978(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8979(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8980(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8981(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8982(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8983(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8984(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8985(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8986(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8987(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8988(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8989(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8990(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8991(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8992(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8993(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8994(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8995(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8996(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8997(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8998(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr8999(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9000(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9001(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9002(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9003(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9004(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9005(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9006(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9007(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9008(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9009(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9010(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9011(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9012(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9013(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9014(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9015(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9016(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9017(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9018(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9019(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9020(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9021(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9022(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9023(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9024(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9025(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9026(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9027(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9028(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9029(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9030(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9031(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9032(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9033(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9034(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9035(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9036(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9037(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9038(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9039(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9040(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9041(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9042(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9043(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9044(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9045(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9046(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9047(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9048(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9049(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9050(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9051(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9052(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9053(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9054(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9055(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9056(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9057(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9058(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9059(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9060(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9061(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9062(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9063(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9064(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9065(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9066(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9067(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9068(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9069(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9070(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9071(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9072(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9073(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9074(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9075(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9076(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9077(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9078(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9079(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9080(struct cell_array **,
    struct cell_array *,
    struct cell_func *);
int cdf_ptr9081(struct cell_array **,
    struct cell_array *,
    struct cell_array *,
    struct cell_func *);

int cdf_prim_flag = 0;

wchar_t *cdf_prim_names[] = {L"q_signal", L"q_dr", L"spn", L"squeeze", L"is_simple", L"is_numeric", L"is_char", L"is_integer", L"is_bool", L"is_span", L"max_shp", L"has_nat_vals", L"chk_scl", L"chk_valid_shape", L"both_simple", L"both_numeric", L"both_integer", L"both_char", L"both_bool", L"any", L"numeric", L"ambiv", L"same", L"veach", L"scalar", L"idx_shp_check", L"idx_rng_check", L"set", L"brk", L"rgt", L"lft", L"reshape", L"rho", L"cat", L"depth", L"eqv", L"nqv", L"sqd_vec", L"sqd", L"index_gen", L"index_of", L"iot", L"dis", L"enclose", L"par", L"conjugate", L"add", L"sub", L"sign", L"mul", L"div", L"absolute", L"residue", L"res", L"floor_array", L"min", L"ceil_array", L"max", L"exp", L"log", L"pitimes", L"trig", L"cir", L"binomial", L"fac", L"notscl", L"without", L"not", L"logand", L"and", L"logor", L"lor", L"nan", L"nor", L"lessthan", L"lth", L"lesseql", L"lte", L"eql_vec", L"equal", L"eql", L"greatereql", L"gte", L"greaterthan", L"gth", L"firstocc", L"neq_vec", L"noteq", L"neq", L"mix", L"take", L"tke", L"split", L"drop", L"drp", L"reverse_last", L"rotate_last", L"rot", L"com", L"map", L"reduce", L"nwreduce", L"rdf", L"rpf", L"scf", L"dot", L"oup", L"pow", L"jot"};

EXPORT int
cdf_prim_init(void)
{
	struct cdf_prim_loc *loc;
	void *stk[128];
	void **stkhd;
	int err;

	if (cdf_prim_flag)
		return 0;

	err = 0;
	cdf_prim_flag = 1;
	stkhd = &stk[0];
	loc = &cdf_prim;
	loc->__count = 109;
	loc->__names = cdf_prim_names;

	if (debug_info)
		release_array(debug_info);

	debug_info = NULL;

	CHKFN(cdf_prim_init(), cleanup);

	loc->cdf_q_signal = NULL;
	loc->cdf_q_dr = NULL;
	loc->cdf_spn = NULL;
	CHK(mk_func_box(&loc->cdf_squeeze, NULL), cleanup, L"Init mutable variable: loc->cdf_squeeze");
	CHK(mk_func_box(&loc->cdf_is_simple, NULL), cleanup, L"Init mutable variable: loc->cdf_is_simple");
	CHK(mk_func_box(&loc->cdf_is_numeric, NULL), cleanup, L"Init mutable variable: loc->cdf_is_numeric");
	CHK(mk_func_box(&loc->cdf_is_char, NULL), cleanup, L"Init mutable variable: loc->cdf_is_char");
	CHK(mk_func_box(&loc->cdf_is_integer, NULL), cleanup, L"Init mutable variable: loc->cdf_is_integer");
	CHK(mk_func_box(&loc->cdf_is_bool, NULL), cleanup, L"Init mutable variable: loc->cdf_is_bool");
	CHK(mk_func_box(&loc->cdf_is_span, NULL), cleanup, L"Init mutable variable: loc->cdf_is_span");
	CHK(mk_func_box(&loc->cdf_max_shp, NULL), cleanup, L"Init mutable variable: loc->cdf_max_shp");
	CHK(mk_func_box(&loc->cdf_has_nat_vals, NULL), cleanup, L"Init mutable variable: loc->cdf_has_nat_vals");
	CHK(mk_func_box(&loc->cdf_chk_scl, NULL), cleanup, L"Init mutable variable: loc->cdf_chk_scl");
	CHK(mk_func_box(&loc->cdf_chk_valid_shape, NULL), cleanup, L"Init mutable variable: loc->cdf_chk_valid_shape");
	CHK(mk_func_box(&loc->cdf_both_simple, NULL), cleanup, L"Init mutable variable: loc->cdf_both_simple");
	CHK(mk_func_box(&loc->cdf_both_numeric, NULL), cleanup, L"Init mutable variable: loc->cdf_both_numeric");
	CHK(mk_func_box(&loc->cdf_both_integer, NULL), cleanup, L"Init mutable variable: loc->cdf_both_integer");
	CHK(mk_func_box(&loc->cdf_both_char, NULL), cleanup, L"Init mutable variable: loc->cdf_both_char");
	CHK(mk_func_box(&loc->cdf_both_bool, NULL), cleanup, L"Init mutable variable: loc->cdf_both_bool");
	CHK(mk_func_box(&loc->cdf_any, NULL), cleanup, L"Init mutable variable: loc->cdf_any");
	loc->cdf_numeric = NULL;
	loc->cdf_ambiv = NULL;
	CHK(mk_func_box(&loc->cdf_same, NULL), cleanup, L"Init mutable variable: loc->cdf_same");
	CHK(mk_moper_box(&loc->cdf_veach, NULL), cleanup, L"Init mutable variable: loc->cdf_veach");
	loc->cdf_scalar = NULL;
	loc->cdf_idx_shp_check = NULL;
	loc->cdf_idx_rng_check = NULL;
	loc->cdf_set = NULL;
	loc->cdf_brk = NULL;
	loc->cdf_rgt = NULL;
	loc->cdf_lft = NULL;
	loc->cdf_reshape = NULL;
	loc->cdf_rho = NULL;
	loc->cdf_cat = NULL;
	loc->cdf_depth = NULL;
	loc->cdf_eqv = NULL;
	loc->cdf_nqv = NULL;
	CHK(mk_func_box(&loc->cdf_sqd_vec, NULL), cleanup, L"Init mutable variable: loc->cdf_sqd_vec");
	loc->cdf_sqd = NULL;
	loc->cdf_index_gen = NULL;
	loc->cdf_index_of = NULL;
	loc->cdf_iot = NULL;
	loc->cdf_dis = NULL;
	loc->cdf_enclose = NULL;
	loc->cdf_par = NULL;
	loc->cdf_conjugate = NULL;
	loc->cdf_add = NULL;
	loc->cdf_sub = NULL;
	loc->cdf_sign = NULL;
	loc->cdf_mul = NULL;
	loc->cdf_div = NULL;
	loc->cdf_absolute = NULL;
	loc->cdf_residue = NULL;
	loc->cdf_res = NULL;
	loc->cdf_floor_array = NULL;
	loc->cdf_min = NULL;
	loc->cdf_ceil_array = NULL;
	loc->cdf_max = NULL;
	loc->cdf_exp = NULL;
	loc->cdf_log = NULL;
	loc->cdf_pitimes = NULL;
	loc->cdf_trig = NULL;
	loc->cdf_cir = NULL;
	loc->cdf_binomial = NULL;
	loc->cdf_fac = NULL;
	loc->cdf_notscl = NULL;
	loc->cdf_without = NULL;
	loc->cdf_not = NULL;
	loc->cdf_logand = NULL;
	loc->cdf_and = NULL;
	loc->cdf_logor = NULL;
	loc->cdf_lor = NULL;
	loc->cdf_nan = NULL;
	loc->cdf_nor = NULL;
	loc->cdf_lessthan = NULL;
	loc->cdf_lth = NULL;
	loc->cdf_lesseql = NULL;
	loc->cdf_lte = NULL;
	CHK(mk_func_box(&loc->cdf_eql_vec, NULL), cleanup, L"Init mutable variable: loc->cdf_eql_vec");
	loc->cdf_equal = NULL;
	loc->cdf_eql = NULL;
	loc->cdf_greatereql = NULL;
	loc->cdf_gte = NULL;
	loc->cdf_greaterthan = NULL;
	loc->cdf_gth = NULL;
	loc->cdf_firstocc = NULL;
	CHK(mk_func_box(&loc->cdf_neq_vec, NULL), cleanup, L"Init mutable variable: loc->cdf_neq_vec");
	loc->cdf_noteq = NULL;
	loc->cdf_neq = NULL;
	loc->cdf_mix = NULL;
	loc->cdf_take = NULL;
	loc->cdf_tke = NULL;
	loc->cdf_split = NULL;
	loc->cdf_drop = NULL;
	loc->cdf_drp = NULL;
	loc->cdf_reverse_last = NULL;
	loc->cdf_rotate_last = NULL;
	loc->cdf_rot = NULL;
	loc->cdf_com = NULL;
	loc->cdf_map = NULL;
	CHK(mk_moper_box(&loc->cdf_reduce, NULL), cleanup, L"Init mutable variable: loc->cdf_reduce");
	CHK(mk_moper_box(&loc->cdf_nwreduce, NULL), cleanup, L"Init mutable variable: loc->cdf_nwreduce");
	loc->cdf_rdf = NULL;
	loc->cdf_rpf = NULL;
	loc->cdf_scf = NULL;
	loc->cdf_dot = NULL;
	loc->cdf_oup = NULL;
	loc->cdf_pow = NULL;
	loc->cdf_jot = NULL;

	if (!cdf_q_signal_ibeam)
		CHK(6, cleanup, L"[4] q_signal←╠'q_signal_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_q_signal_ibeam);
	
	release_cell(loc->cdf_q_signal);
	loc->cdf_q_signal = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_q_dr_ibeam)
		CHK(6, cleanup, L"[5] q_dr←╠'q_dr_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_q_dr_ibeam);
	
	release_cell(loc->cdf_q_dr);
	loc->cdf_q_dr = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_span_array)
		CHK(6, cleanup, L"[8] spn←╠'span_array'⌶⌶⌶⌶╣");
	
	*stkhd++ = retain_cell(cdf_span_array);
	
	release_cell(loc->cdf_spn);
	loc->cdf_spn = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_squeeze_ibeam)
		CHK(6, cleanup, L"[11] squeeze←╠'squeeze_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_squeeze_ibeam);
	
	release_cell(loc->cdf_squeeze->value);
	loc->cdf_squeeze->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_is_simple_ibeam)
		CHK(6, cleanup, L"[12] is_simple←╠'is_simple_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_is_simple_ibeam);
	
	release_cell(loc->cdf_is_simple->value);
	loc->cdf_is_simple->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_is_numeric_ibeam)
		CHK(6, cleanup, L"[13] is_numeric←╠'is_numeric_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_is_numeric_ibeam);
	
	release_cell(loc->cdf_is_numeric->value);
	loc->cdf_is_numeric->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_is_char_ibeam)
		CHK(6, cleanup, L"[14] is_char←╠'is_char_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_is_char_ibeam);
	
	release_cell(loc->cdf_is_char->value);
	loc->cdf_is_char->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_is_integer_ibeam)
		CHK(6, cleanup, L"[15] is_integer←╠'is_integer_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_is_integer_ibeam);
	
	release_cell(loc->cdf_is_integer->value);
	loc->cdf_is_integer->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8798, cdf_ptr8799, 1), cleanup, 
		    L"[16] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_is_bool->value);
	loc->cdf_is_bool->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_is_span_ibeam)
		CHK(6, cleanup, L"[17] is_span←╠'is_span_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_is_span_ibeam);
	
	release_cell(loc->cdf_is_span->value);
	loc->cdf_is_span->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8800, cdf_ptr8801, 0), cleanup, 
		    L"[18] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_max_shp->value);
	loc->cdf_max_shp->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_has_nat_vals_ibeam)
		CHK(6, cleanup, L"[19] has_nat_vals←╠'has_nat_vals_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_has_nat_vals_ibeam);
	
	release_cell(loc->cdf_has_nat_vals->value);
	loc->cdf_has_nat_vals->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8802, cdf_ptr8803, 0), cleanup, 
		    L"[20] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_chk_scl->value);
	loc->cdf_chk_scl->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8804, cdf_ptr8809, 1), cleanup, 
		    L"[21] chk_valid_shape←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_has_nat_vals);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_chk_valid_shape->value);
	loc->cdf_chk_valid_shape->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8814, cdf_ptr8815, 1), cleanup, 
		    L"[26] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_simple);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_both_simple->value);
	loc->cdf_both_simple->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8816, cdf_ptr8817, 1), cleanup, 
		    L"[27] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_both_numeric->value);
	loc->cdf_both_numeric->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8818, cdf_ptr8819, 1), cleanup, 
		    L"[28] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_both_integer->value);
	loc->cdf_both_integer->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8820, cdf_ptr8821, 1), cleanup, 
		    L"[29] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_char);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_both_char->value);
	loc->cdf_both_char->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8822, cdf_ptr8823, 1), cleanup, 
		    L"[30] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_both_bool->value);
	loc->cdf_both_bool->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_any_ibeam)
		CHK(6, cleanup, L"[31] any←╠'any_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_any_ibeam);
	
	release_cell(loc->cdf_any->value);
	loc->cdf_any->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr8824, cdf_ptr8825, cdf_ptr8826, cdf_ptr8827, 1), cleanup, 
		    L"[32] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->cdf_both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_numeric);
	loc->cdf_numeric = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, cdf_ptr8828, cdf_ptr8829, cdf_ptr8830, cdf_ptr8831, cdf_ptr8832, cdf_ptr8833, cdf_ptr8834, cdf_ptr8835, 1), cleanup, 
		    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_same);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_ambiv);
	loc->cdf_ambiv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_veach_ibeam)
		CHK(6, cleanup, L"[34] veach←╠'veach_ibeam'⌶⌶╣");
	
	*stkhd++ = retain_cell(cdf_veach_ibeam);
	
	release_cell(loc->cdf_veach->value);
	loc->cdf_veach->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr8836, cdf_ptr8837, cdf_ptr8838, cdf_ptr8839, 5), cleanup, 
		    L"[35] scalar←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_veach);
		k->fv[1] = retain_cell(loc->cdf_both_simple);
		k->fv[2] = retain_cell(loc->cdf_chk_scl);
		k->fv[3] = retain_cell(loc->cdf_max_shp);
		k->fv[4] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_scalar);
	loc->cdf_scalar = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8840, cdf_ptr8841, 0), cleanup, 
		    L"[45] idx_shp_check←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_idx_shp_check);
	loc->cdf_idx_shp_check = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8842, cdf_ptr8843, 3), cleanup, 
		    L"[52] idx_rng_check←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_any);
		k->fv[1] = retain_cell(loc->cdf_is_integer);
		k->fv[2] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_idx_rng_check);
	loc->cdf_idx_rng_check = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_set_ibeam)
		CHK(6, cleanup, L"[58] set←╠'set_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_set_ibeam);
	
	release_cell(loc->cdf_set);
	loc->cdf_set = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8844, cdf_ptr8845, 0), cleanup, 
		    L"[61] brk←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_brk);
	loc->cdf_brk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8846, cdf_ptr8847, 0), cleanup, 
		    L"[67] rgt←╠{⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_rgt);
	loc->cdf_rgt = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8850, cdf_ptr8851, 0), cleanup, 
		    L"[70] lft←{⍵} ambiv ╠{⍺}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[70] lft←{⍵} ╠ambiv╣ {⍺}");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8848, cdf_ptr8849, 0), cleanup, 
		    L"[70] lft←╠{⍵}╣ ambiv {⍺}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[70] lft←╠{⍵} ambiv {⍺}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lft);
	loc->cdf_lft = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8852, cdf_ptr8853, 1), cleanup, 
		    L"[73] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_chk_valid_shape);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_reshape);
	loc->cdf_reshape = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_reshape)
		CHK(6, cleanup, L"[74] rho←('shape_ibeam'⌶) ambiv ╠reshape╣");
	
	*stkhd++ = retain_cell(loc->cdf_reshape);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[74] rho←('shape_ibeam'⌶) ╠ambiv╣ reshape");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!cdf_shape_ibeam)
		CHK(6, cleanup, L"[74] rho←╠('shape_ibeam'⌶)╣ ambiv reshape");
	
	*stkhd++ = retain_cell(cdf_shape_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[74] rho←╠('shape_ibeam'⌶) ambiv reshape╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rho);
	loc->cdf_rho = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8854, cdf_ptr8865, 1), cleanup, 
		    L"[77] cat←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_cat);
	loc->cdf_cat = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8876, cdf_ptr8879, 1), cleanup, 
		    L"[103] depth←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_simple);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_depth);
	loc->cdf_depth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_same_ibeam)
		CHK(6, cleanup, L"[110] same←╠'same_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_same_ibeam);
	
	release_cell(loc->cdf_same->value);
	loc->cdf_same->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_same->value)
		CHK(6, cleanup, L"[111] eqv←depth ambiv ╠same╣");
	
	*stkhd++ = retain_cell(loc->cdf_same->value);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[111] eqv←depth ╠ambiv╣ same");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_depth)
		CHK(6, cleanup, L"[111] eqv←╠depth╣ ambiv same");
	
	*stkhd++ = retain_cell(loc->cdf_depth);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[111] eqv←╠depth ambiv same╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_eqv);
	loc->cdf_eqv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_nqv_ibeam)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ╠('nqv_ibeam'⌶)╣");
	
	*stkhd++ = retain_cell(cdf_nqv_ibeam);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵} ╠ambiv╣ ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8882, cdf_ptr8883, 0), cleanup, 
		    L"[114] nqv←╠{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[114] nqv←╠{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_nqv);
	loc->cdf_nqv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8884, cdf_ptr8885, 4), cleanup, 
		    L"[117] sqd_vec←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_any);
		k->fv[1] = retain_cell(loc->cdf_is_span);
		k->fv[2] = retain_cell(loc->cdf_is_integer);
		k->fv[3] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_sqd_vec->value);
	loc->cdf_sqd_vec->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8886, cdf_ptr8893, 2), cleanup, 
		    L"[124] sqd←╠{⍺←⊢╣");
	
		k->fv[0] = retain_cell(loc->cdf_sqd_vec);
		k->fv[1] = retain_cell(loc->cdf_is_span);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_sqd);
	loc->cdf_sqd = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8900, cdf_ptr8901, 1), cleanup, 
		    L"[135] index_gen←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_chk_valid_shape);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_index_gen);
	loc->cdf_index_gen = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8902, cdf_ptr8903, 0), cleanup, 
		    L"[142] index_of←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_index_of);
	loc->cdf_index_of = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_index_of)
		CHK(6, cleanup, L"[143] iot←index_gen ambiv ╠index_of╣");
	
	*stkhd++ = retain_cell(loc->cdf_index_of);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[143] iot←index_gen ╠ambiv╣ index_of");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_index_gen)
		CHK(6, cleanup, L"[143] iot←╠index_gen╣ ambiv index_of");
	
	*stkhd++ = retain_cell(loc->cdf_index_gen);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[143] iot←╠index_gen ambiv index_of╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_iot);
	loc->cdf_iot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8904, cdf_ptr8905, 0), cleanup, 
		    L"[146] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[146] dis←('disclose_ibeam'⌶) ╠ambiv╣ {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!cdf_disclose_ibeam)
		CHK(6, cleanup, L"[146] dis←╠('disclose_ibeam'⌶)╣ ambiv {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(cdf_disclose_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[146] dis←╠('disclose_ibeam'⌶) ambiv {⎕SIGNAL 16}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_dis);
	loc->cdf_dis = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8906, cdf_ptr8907, 0), cleanup, 
		    L"[149] enclose←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_enclose);
	loc->cdf_enclose = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8908, cdf_ptr8909, 0), cleanup, 
		    L"[153] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[153] par←enclose ╠ambiv╣ {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_enclose)
		CHK(6, cleanup, L"[153] par←╠enclose╣ ambiv {⎕SIGNAL 16}");
	
	*stkhd++ = retain_cell(loc->cdf_enclose);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[153] par←╠enclose ambiv {⎕SIGNAL 16}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_par);
	loc->cdf_par = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8910, cdf_ptr8911, 1), cleanup, 
		    L"[156] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_conjugate);
	loc->cdf_conjugate = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[157] add←conjugate ambiv ('add_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[157] add←conjugate ambiv ('add_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_add_vec_ibeam)
		CHK(6, cleanup, L"[157] add←conjugate ambiv (╠'add_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_add_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[157] add←conjugate ambiv (╠'add_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[157] add←conjugate ambiv ╠('add_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[157] add←conjugate ╠ambiv╣ ('add_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_conjugate)
		CHK(6, cleanup, L"[157] add←╠conjugate╣ ambiv ('add_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_conjugate);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[157] add←╠conjugate ambiv ('add_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_add);
	loc->cdf_add = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[160] sub←{0-⍵} ambiv ('sub_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[160] sub←{0-⍵} ambiv ('sub_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_sub_vec_ibeam)
		CHK(6, cleanup, L"[160] sub←{0-⍵} ambiv (╠'sub_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_sub_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[160] sub←{0-⍵} ambiv (╠'sub_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[160] sub←{0-⍵} ambiv ╠('sub_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[160] sub←{0-⍵} ╠ambiv╣ ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8912, cdf_ptr8913, 0), cleanup, 
		    L"[160] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[160] sub←╠{0-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_sub);
	loc->cdf_sub = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8914, cdf_ptr8915, 0), cleanup, 
		    L"[163] sign←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_sign);
	loc->cdf_sign = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[167] mul←sign ambiv ('mul_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[167] mul←sign ambiv ('mul_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_mul_vec_ibeam)
		CHK(6, cleanup, L"[167] mul←sign ambiv (╠'mul_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_mul_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[167] mul←sign ambiv (╠'mul_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[167] mul←sign ambiv ╠('mul_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[167] mul←sign ╠ambiv╣ ('mul_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_sign)
		CHK(6, cleanup, L"[167] mul←╠sign╣ ambiv ('mul_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_sign);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[167] mul←╠sign ambiv ('mul_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_mul);
	loc->cdf_mul = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[170] div←{1÷⍵} ambiv ('div_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[170] div←{1÷⍵} ambiv ('div_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_div_vec_ibeam)
		CHK(6, cleanup, L"[170] div←{1÷⍵} ambiv (╠'div_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_div_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[170] div←{1÷⍵} ambiv (╠'div_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[170] div←{1÷⍵} ambiv ╠('div_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[170] div←{1÷⍵} ╠ambiv╣ ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8916, cdf_ptr8917, 0), cleanup, 
		    L"[170] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[170] div←╠{1÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_div);
	loc->cdf_div = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8918, cdf_ptr8919, 1), cleanup, 
		    L"[173] absolute←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_absolute);
	loc->cdf_absolute = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8920, cdf_ptr8921, 0), cleanup, 
		    L"[178] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_residue);
	loc->cdf_residue = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_residue)
		CHK(6, cleanup, L"[179] res←absolute numeric ambiv ╠residue╣");
	
	*stkhd++ = retain_cell(loc->cdf_residue);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[179] res←absolute numeric ╠ambiv╣ residue");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[179] res←absolute ╠numeric╣ ambiv residue");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_absolute)
		CHK(6, cleanup, L"[179] res←╠absolute╣ numeric ambiv residue");
	
	*stkhd++ = retain_cell(loc->cdf_absolute);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[179] res←╠absolute numeric╣ ambiv residue");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[179] res←╠absolute numeric ambiv residue╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_res);
	loc->cdf_res = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8922, cdf_ptr8923, 1), cleanup, 
		    L"[182] floor_array←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_floor_array);
	loc->cdf_floor_array = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[187] min←floor_array numeric ambiv ('min_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[187] min←floor_array numeric ambiv ('min_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_min_vec_ibeam)
		CHK(6, cleanup, L"[187] min←floor_array numeric ambiv (╠'min_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_min_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[187] min←floor_array numeric ambiv (╠'min_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[187] min←floor_array numeric ambiv ╠('min_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[187] min←floor_array numeric ╠ambiv╣ ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[187] min←floor_array ╠numeric╣ ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_floor_array)
		CHK(6, cleanup, L"[187] min←╠floor_array╣ numeric ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_floor_array);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[187] min←╠floor_array numeric╣ ambiv ('min_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[187] min←╠floor_array numeric ambiv ('min_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_min);
	loc->cdf_min = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8924, cdf_ptr8925, 1), cleanup, 
		    L"[190] ceil_array←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_ceil_array);
	loc->cdf_ceil_array = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[195] max←ceil_array numeric ambiv ('max_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[195] max←ceil_array numeric ambiv ('max_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_max_vec_ibeam)
		CHK(6, cleanup, L"[195] max←ceil_array numeric ambiv (╠'max_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_max_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[195] max←ceil_array numeric ambiv (╠'max_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[195] max←ceil_array numeric ambiv ╠('max_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[195] max←ceil_array numeric ╠ambiv╣ ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[195] max←ceil_array ╠numeric╣ ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_ceil_array)
		CHK(6, cleanup, L"[195] max←╠ceil_array╣ numeric ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ceil_array);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[195] max←╠ceil_array numeric╣ ambiv ('max_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[195] max←╠ceil_array numeric ambiv ('max_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_max);
	loc->cdf_max = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[198] exp←'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[198] exp←'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_pow_vec_ibeam)
		CHK(6, cleanup, L"[198] exp←'exp_vec_ibeam'⌶ numeric ambiv (╠'pow_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_pow_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[198] exp←'exp_vec_ibeam'⌶ numeric ambiv (╠'pow_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[198] exp←'exp_vec_ibeam'⌶ numeric ambiv ╠('pow_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[198] exp←'exp_vec_ibeam'⌶ numeric ╠ambiv╣ ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[198] exp←'exp_vec_ibeam'⌶ ╠numeric╣ ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_exp_vec_ibeam)
		CHK(6, cleanup, L"[198] exp←╠'exp_vec_ibeam'⌶╣ numeric ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_exp_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[198] exp←╠'exp_vec_ibeam'⌶ numeric╣ ambiv ('pow_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[198] exp←╠'exp_vec_ibeam'⌶ numeric ambiv ('pow_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_exp);
	loc->cdf_exp = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[201] log←'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[201] log←'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_log_vec_ibeam)
		CHK(6, cleanup, L"[201] log←'nlg_vec_ibeam'⌶ numeric ambiv (╠'log_vec_ibeam'⌶╣ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_log_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[201] log←'nlg_vec_ibeam'⌶ numeric ambiv (╠'log_vec_ibeam'⌶ numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[201] log←'nlg_vec_ibeam'⌶ numeric ambiv ╠('log_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[201] log←'nlg_vec_ibeam'⌶ numeric ╠ambiv╣ ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[201] log←'nlg_vec_ibeam'⌶ ╠numeric╣ ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_nlg_vec_ibeam)
		CHK(6, cleanup, L"[201] log←╠'nlg_vec_ibeam'⌶╣ numeric ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_nlg_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[201] log←╠'nlg_vec_ibeam'⌶ numeric╣ ambiv ('log_vec_ibeam'⌶ numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[201] log←╠'nlg_vec_ibeam'⌶ numeric ambiv ('log_vec_ibeam'⌶ numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_log);
	loc->cdf_log = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8926, cdf_ptr8927, 0), cleanup, 
		    L"[204] pitimes←╠{3.14159265358979323846×⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_pitimes);
	loc->cdf_pitimes = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8928, cdf_ptr8933, 0), cleanup, 
		    L"[205] trig←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_trig);
	loc->cdf_trig = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[234] cir←pitimes numeric ambiv (trig numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[234] cir←pitimes numeric ambiv (trig ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_trig)
		CHK(6, cleanup, L"[234] cir←pitimes numeric ambiv (╠trig╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_trig);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[234] cir←pitimes numeric ambiv (╠trig numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[234] cir←pitimes numeric ambiv ╠(trig numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[234] cir←pitimes numeric ╠ambiv╣ (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[234] cir←pitimes ╠numeric╣ ambiv (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_pitimes)
		CHK(6, cleanup, L"[234] cir←╠pitimes╣ numeric ambiv (trig numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_pitimes);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[234] cir←╠pitimes numeric╣ ambiv (trig numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[234] cir←╠pitimes numeric ambiv (trig numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_cir);
	loc->cdf_cir = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8938, cdf_ptr8939, 0), cleanup, 
		    L"[237] binomial←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_binomial);
	loc->cdf_binomial = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[241] fac←'factorial_vec_ibeam'⌶ numeric ambiv (binomial numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[241] fac←'factorial_vec_ibeam'⌶ numeric ambiv (binomial ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_binomial)
		CHK(6, cleanup, L"[241] fac←'factorial_vec_ibeam'⌶ numeric ambiv (╠binomial╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_binomial);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[241] fac←'factorial_vec_ibeam'⌶ numeric ambiv (╠binomial numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[241] fac←'factorial_vec_ibeam'⌶ numeric ambiv ╠(binomial numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[241] fac←'factorial_vec_ibeam'⌶ numeric ╠ambiv╣ (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[241] fac←'factorial_vec_ibeam'⌶ ╠numeric╣ ambiv (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!cdf_factorial_vec_ibeam)
		CHK(6, cleanup, L"[241] fac←╠'factorial_vec_ibeam'⌶╣ numeric ambiv (binomial numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_factorial_vec_ibeam);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[241] fac←╠'factorial_vec_ibeam'⌶ numeric╣ ambiv (binomial numeric scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[241] fac←╠'factorial_vec_ibeam'⌶ numeric ambiv (binomial numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_fac);
	loc->cdf_fac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8940, cdf_ptr8941, 1), cleanup, 
		    L"[244] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_notscl);
	loc->cdf_notscl = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8942, cdf_ptr8943, 0), cleanup, 
		    L"[245] without←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_without);
	loc->cdf_without = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_without)
		CHK(6, cleanup, L"[246] not←notscl ambiv ╠without╣");
	
	*stkhd++ = retain_cell(loc->cdf_without);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[246] not←notscl ╠ambiv╣ without");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_notscl)
		CHK(6, cleanup, L"[246] not←╠notscl╣ ambiv without");
	
	*stkhd++ = retain_cell(loc->cdf_notscl);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[246] not←╠notscl ambiv without╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_not);
	loc->cdf_not = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8944, cdf_ptr8945, 1), cleanup, 
		    L"[249] logand←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_both_bool);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_logand);
	loc->cdf_logand = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[253] and←{⎕SIGNAL 2}ambiv (logand numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[253] and←{⎕SIGNAL 2}ambiv (logand ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_logand)
		CHK(6, cleanup, L"[253] and←{⎕SIGNAL 2}ambiv (╠logand╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_logand);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[253] and←{⎕SIGNAL 2}ambiv (╠logand numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[253] and←{⎕SIGNAL 2}ambiv ╠(logand numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[253] and←{⎕SIGNAL 2}╠ambiv╣ (logand numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8946, cdf_ptr8947, 0), cleanup, 
		    L"[253] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[253] and←╠{⎕SIGNAL 2}ambiv (logand numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_and);
	loc->cdf_and = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8948, cdf_ptr8953, 4), cleanup, 
		    L"[256] logor←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_both_bool);
		k->fv[1] = retain_cell(loc->cdf_both_integer);
		k->fv[2] = retain_cell(loc->cdf_is_bool);
		k->fv[3] = retain_cell(loc->cdf_is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_logor);
	loc->cdf_logor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[263] lor←{⎕SIGNAL 2}ambiv (logor numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[263] lor←{⎕SIGNAL 2}ambiv (logor ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_logor)
		CHK(6, cleanup, L"[263] lor←{⎕SIGNAL 2}ambiv (╠logor╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_logor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[263] lor←{⎕SIGNAL 2}ambiv (╠logor numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[263] lor←{⎕SIGNAL 2}ambiv ╠(logor numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[263] lor←{⎕SIGNAL 2}╠ambiv╣ (logor numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8958, cdf_ptr8959, 0), cleanup, 
		    L"[263] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[263] lor←╠{⎕SIGNAL 2}ambiv (logor numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lor);
	loc->cdf_lor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8962, cdf_ptr8963, 1), cleanup, 
		    L"[266] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->cdf_both_bool);
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}╠ambiv╣{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8960, cdf_ptr8961, 0), cleanup, 
		    L"[266] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[266] nan←╠{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_nan);
	loc->cdf_nan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8966, cdf_ptr8967, 1), cleanup, 
		    L"[269] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	
		k->fv[0] = retain_cell(loc->cdf_both_bool);
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}╠ambiv╣{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8964, cdf_ptr8965, 0), cleanup, 
		    L"[269] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[269] nor←╠{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_nor);
	loc->cdf_nor = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8968, cdf_ptr8969, 0), cleanup, 
		    L"[272] lessthan←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_lessthan);
	loc->cdf_lessthan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[276] lth←{⎕SIGNAL 2} ambiv (lessthan numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[276] lth←{⎕SIGNAL 2} ambiv (lessthan ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_lessthan)
		CHK(6, cleanup, L"[276] lth←{⎕SIGNAL 2} ambiv (╠lessthan╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_lessthan);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[276] lth←{⎕SIGNAL 2} ambiv (╠lessthan numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[276] lth←{⎕SIGNAL 2} ambiv ╠(lessthan numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[276] lth←{⎕SIGNAL 2} ╠ambiv╣ (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8970, cdf_ptr8971, 0), cleanup, 
		    L"[276] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[276] lth←╠{⎕SIGNAL 2} ambiv (lessthan numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lth);
	loc->cdf_lth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8972, cdf_ptr8973, 0), cleanup, 
		    L"[279] lesseql←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_lesseql);
	loc->cdf_lesseql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[283] lte←{⎕SIGNAL 2}ambiv (lesseql numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[283] lte←{⎕SIGNAL 2}ambiv (lesseql ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_lesseql)
		CHK(6, cleanup, L"[283] lte←{⎕SIGNAL 2}ambiv (╠lesseql╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_lesseql);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[283] lte←{⎕SIGNAL 2}ambiv (╠lesseql numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[283] lte←{⎕SIGNAL 2}ambiv ╠(lesseql numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[283] lte←{⎕SIGNAL 2}╠ambiv╣ (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8974, cdf_ptr8975, 0), cleanup, 
		    L"[283] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[283] lte←╠{⎕SIGNAL 2}ambiv (lesseql numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lte);
	loc->cdf_lte = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_eql_vec_ibeam)
		CHK(6, cleanup, L"[286] eql_vec←╠'eql_vec_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_eql_vec_ibeam);
	
	release_cell(loc->cdf_eql_vec->value);
	loc->cdf_eql_vec->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8976, cdf_ptr8977, 3), cleanup, 
		    L"[287] equal←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_eql_vec);
		k->fv[1] = retain_cell(loc->cdf_both_char);
		k->fv[2] = retain_cell(loc->cdf_both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_equal);
	loc->cdf_equal = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[292] eql←{⎕SIGNAL 2}ambiv (equal ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_equal)
		CHK(6, cleanup, L"[292] eql←{⎕SIGNAL 2}ambiv (╠equal╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_equal);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[292] eql←{⎕SIGNAL 2}ambiv ╠(equal scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[292] eql←{⎕SIGNAL 2}╠ambiv╣ (equal scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8978, cdf_ptr8979, 0), cleanup, 
		    L"[292] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[292] eql←╠{⎕SIGNAL 2}ambiv (equal scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_eql);
	loc->cdf_eql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8980, cdf_ptr8981, 0), cleanup, 
		    L"[295] greatereql←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_greatereql);
	loc->cdf_greatereql = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[299] gte←{⎕SIGNAL 2}ambiv (greatereql numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[299] gte←{⎕SIGNAL 2}ambiv (greatereql ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_greatereql)
		CHK(6, cleanup, L"[299] gte←{⎕SIGNAL 2}ambiv (╠greatereql╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_greatereql);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[299] gte←{⎕SIGNAL 2}ambiv (╠greatereql numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[299] gte←{⎕SIGNAL 2}ambiv ╠(greatereql numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[299] gte←{⎕SIGNAL 2}╠ambiv╣ (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8982, cdf_ptr8983, 0), cleanup, 
		    L"[299] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[299] gte←╠{⎕SIGNAL 2}ambiv (greatereql numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_gte);
	loc->cdf_gte = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8984, cdf_ptr8985, 0), cleanup, 
		    L"[302] greaterthan←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_greaterthan);
	loc->cdf_greaterthan = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[306] gth←{⎕SIGNAL 2}ambiv (greaterthan numeric ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_numeric)
		CHK(6, cleanup, L"[306] gth←{⎕SIGNAL 2}ambiv (greaterthan ╠numeric╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_numeric);
	
	if (!loc->cdf_greaterthan)
		CHK(6, cleanup, L"[306] gth←{⎕SIGNAL 2}ambiv (╠greaterthan╣ numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_greaterthan);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[306] gth←{⎕SIGNAL 2}ambiv (╠greaterthan numeric╣ scalar)");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[306] gth←{⎕SIGNAL 2}ambiv ╠(greaterthan numeric scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[306] gth←{⎕SIGNAL 2}╠ambiv╣ (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8986, cdf_ptr8987, 0), cleanup, 
		    L"[306] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[306] gth←╠{⎕SIGNAL 2}ambiv (greaterthan numeric scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_gth);
	loc->cdf_gth = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8988, cdf_ptr8989, 0), cleanup, 
		    L"[309] firstocc←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_firstocc);
	loc->cdf_firstocc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_neq_vec_ibeam)
		CHK(6, cleanup, L"[310] neq_vec←╠'neq_vec_ibeam'⌶╣");
	
	*stkhd++ = retain_cell(cdf_neq_vec_ibeam);
	
	release_cell(loc->cdf_neq_vec->value);
	loc->cdf_neq_vec->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8990, cdf_ptr8991, 3), cleanup, 
		    L"[311] noteq←╠{╣");
	
		k->fv[0] = retain_cell(loc->cdf_neq_vec);
		k->fv[1] = retain_cell(loc->cdf_both_char);
		k->fv[2] = retain_cell(loc->cdf_both_numeric);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_noteq);
	loc->cdf_noteq = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_scalar)
		CHK(6, cleanup, L"[316] neq←firstocc ambiv (noteq ╠scalar╣)");
	
	*stkhd++ = retain_cell(loc->cdf_scalar);
	
	if (!loc->cdf_noteq)
		CHK(6, cleanup, L"[316] neq←firstocc ambiv (╠noteq╣ scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_noteq);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[316] neq←firstocc ambiv ╠(noteq scalar)╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[316] neq←firstocc ╠ambiv╣ (noteq scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_firstocc)
		CHK(6, cleanup, L"[316] neq←╠firstocc╣ ambiv (noteq scalar)");
	
	*stkhd++ = retain_cell(loc->cdf_firstocc);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[316] neq←╠firstocc ambiv (noteq scalar)╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_neq);
	loc->cdf_neq = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8992, cdf_ptr8993, 0), cleanup, 
		    L"[319] mix←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_mix);
	loc->cdf_mix = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8994, cdf_ptr8995, 0), cleanup, 
		    L"[320] take←╠{(⊂⍳⍺)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_take);
	loc->cdf_take = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_take)
		CHK(6, cleanup, L"[321] tke←mix ambiv ╠take╣");
	
	*stkhd++ = retain_cell(loc->cdf_take);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[321] tke←mix ╠ambiv╣ take");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_mix)
		CHK(6, cleanup, L"[321] tke←╠mix╣ ambiv take");
	
	*stkhd++ = retain_cell(loc->cdf_mix);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[321] tke←╠mix ambiv take╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_tke);
	loc->cdf_tke = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8996, cdf_ptr8997, 0), cleanup, 
		    L"[324] split←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_split);
	loc->cdf_split = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8998, cdf_ptr8999, 0), cleanup, 
		    L"[325] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_drop);
	loc->cdf_drop = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_drop)
		CHK(6, cleanup, L"[326] drp←split ambiv ╠drop╣");
	
	*stkhd++ = retain_cell(loc->cdf_drop);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[326] drp←split ╠ambiv╣ drop");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_split)
		CHK(6, cleanup, L"[326] drp←╠split╣ ambiv drop");
	
	*stkhd++ = retain_cell(loc->cdf_split);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[326] drp←╠split ambiv drop╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_drp);
	loc->cdf_drp = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9000, cdf_ptr9001, 0), cleanup, 
		    L"[329] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_reverse_last);
	loc->cdf_reverse_last = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9002, cdf_ptr9003, 0), cleanup, 
		    L"[330] rotate_last←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_rotate_last);
	loc->cdf_rotate_last = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rotate_last)
		CHK(6, cleanup, L"[331] rot←reverse_last ambiv ╠rotate_last╣");
	
	*stkhd++ = retain_cell(loc->cdf_rotate_last);
	
	if (!loc->cdf_ambiv)
		CHK(6, cleanup, L"[331] rot←reverse_last ╠ambiv╣ rotate_last");
	
	*stkhd++ = retain_cell(loc->cdf_ambiv);
	
	if (!loc->cdf_reverse_last)
		CHK(6, cleanup, L"[331] rot←╠reverse_last╣ ambiv rotate_last");
	
	*stkhd++ = retain_cell(loc->cdf_reverse_last);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[331] rot←╠reverse_last ambiv rotate_last╣");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rot);
	loc->cdf_rot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9004, cdf_ptr9005, cdf_ptr9006, cdf_ptr9007, 0), cleanup, 
		    L"[334] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_com);
	loc->cdf_com = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9008, cdf_ptr9009, cdf_ptr9010, cdf_ptr9011, 4), cleanup, 
		    L"[337] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_veach);
		k->fv[1] = retain_cell(loc->cdf_chk_scl);
		k->fv[2] = retain_cell(loc->cdf_max_shp);
		k->fv[3] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_map);
	loc->cdf_map = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9012, cdf_ptr9015, cdf_ptr9018, cdf_ptr9021, 0), cleanup, 
		    L"[340] reduce←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_reduce->value);
	loc->cdf_reduce->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9024, cdf_ptr9027, cdf_ptr9030, cdf_ptr9033, 1), cleanup, 
		    L"[345] nwreduce←╠{op←⍺⍺¨╣");
	
		k->fv[0] = retain_cell(loc->cdf_is_integer);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_nwreduce->value);
	loc->cdf_nwreduce->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9036, cdf_ptr9037, cdf_ptr9038, cdf_ptr9039, 2), cleanup, 
		    L"[349] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	
		k->fv[0] = retain_cell(loc->cdf_nwreduce);
		k->fv[1] = retain_cell(loc->cdf_reduce);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_rdf);
	loc->cdf_rdf = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9040, cdf_ptr9041, 0), cleanup, 
		    L"[354] rpf←╠{⎕SIGNAL 16}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_rpf);
	loc->cdf_rpf = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9042, cdf_ptr9045, cdf_ptr9048, cdf_ptr9051, 1), cleanup, 
		    L"[357] scf←╠{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_squeeze);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_scf);
	loc->cdf_scf = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, cdf_ptr9054, cdf_ptr9055, cdf_ptr9056, cdf_ptr9057, cdf_ptr9058, cdf_ptr9059, cdf_ptr9060, cdf_ptr9061, 0), cleanup, 
		    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_dot);
	loc->cdf_dot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_moper *k;
	
		CHK(mk_moper(&k, cdf_ptr9062, cdf_ptr9063, cdf_ptr9064, cdf_ptr9065, 0), cleanup, 
		    L"[363] oup←╠{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_oup);
	loc->cdf_oup = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, cdf_ptr9066, cdf_ptr9067, cdf_ptr9068, cdf_ptr9069, cdf_ptr9070, cdf_ptr9071, cdf_ptr9072, cdf_ptr9073, 0), cleanup, 
		    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_pow);
	loc->cdf_pow = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_doper *k;
	
		CHK(mk_doper(&k, cdf_ptr9074, cdf_ptr9075, cdf_ptr9076, cdf_ptr9077, cdf_ptr9078, cdf_ptr9079, cdf_ptr9080, cdf_ptr9081, 0), cleanup, 
		    L"[369] jot←╠{╣");
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_jot);
	loc->cdf_jot = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	

cleanup:
	release_env(stk, stkhd);
	return err;
}

int
cdf_ptr8798(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[16] is_bool←{11≡⎕DR squeeze ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[16] is_bool←{11≡⎕DR ╠squeeze╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] is_bool←{11≡⎕DR ╠squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[16] is_bool←{11≡╠⎕DR╣ squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] is_bool←{11≡╠⎕DR squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[16] is_bool←{11╠≡╣⎕DR squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] is_bool←{╠11≡⎕DR squeeze ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[16] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	return err;
}

int
cdf_ptr8799(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[16] is_bool←{11≡⎕DR squeeze ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[16] is_bool←{11≡⎕DR ╠squeeze╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] is_bool←{11≡⎕DR ╠squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[16] is_bool←{11≡╠⎕DR╣ squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[16] is_bool←{11≡╠⎕DR squeeze ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[16] is_bool←{11╠≡╣⎕DR squeeze ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[16] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[16] is_bool←{╠11╣≡⎕DR squeeze ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[16] is_bool←{╠11≡⎕DR squeeze ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[16] is_bool←╠{11≡⎕DR squeeze ⍵}╣");
	return err;
}

int
cdf_ptr8800(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[18] max_shp←{⍺←╠0╣ ⋄ ⍺('max_shp_ibeam'⌶)⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[18] max_shp←{⍺←╠0╣ ⋄ ⍺('max_shp_ibeam'⌶)⍵}");
	
		*stkhd++ = arr;
	}
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_max_shp_ibeam)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ⍺╠('max_shp_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(cdf_max_shp_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ╠⍺╣('max_shp_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] max_shp←{⍺←0 ⋄ ╠⍺('max_shp_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	return err;
}

int
cdf_ptr8801(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_max_shp_ibeam)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ⍺╠('max_shp_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(cdf_max_shp_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[18] max_shp←{⍺←0 ⋄ ╠⍺╣('max_shp_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[18] max_shp←{⍺←0 ⋄ ╠⍺('max_shp_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[18] max_shp←╠{⍺←0 ⋄ ⍺('max_shp_ibeam'⌶)⍵}╣");
	return err;
}

int
cdf_ptr8802(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←╠0╣ ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←╠0╣ ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴╣⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)╠≡╣⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴╠⍺╣)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (╠⍴╣⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)╣≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)≡⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,╣⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢╣,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1╠≡╣≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1≡≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,╣⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢╣,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1╠≡╣≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1≡≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴╣⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢╣⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)╠≡╣≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴╠⍺╣)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴╣⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (╠≢╣⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)≡≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL╣ 5 ⋄ ⎕SIGNAL 4}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL 5╣ ⋄ ⎕SIGNAL 4}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL╣ 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL 4╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[20] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	return err;
}

int
cdf_ptr8803(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴╣⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡╠⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)╠≡╣⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴╠⍺╣)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (╠⍴╣⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)╣≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ ╠(⍴⍺)≡⍴⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴╠⍵╣:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:╠0╣ ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,╣⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢╠,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢╣,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡╠≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1╠≡╣≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1╣≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ ╠1≡≢,⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,╠⍺╣:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:╠0╣ ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,╣⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢╠,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢╣,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡╠≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1╠≡╣≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1╣≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ ╠1≡≢,⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,╠⍵╣:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:╠0╣ ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴╣⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢╠⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢╣⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡╠≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)╠≡╣≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴╠⍺╣)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴╣⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢╠⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (╠≢╣⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)╣≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ ╠(≢⍴⍺)≡≢⍴⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴╠⍵╣:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL ╠5╣ ⋄ ⎕SIGNAL 4}");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL╣ 5 ⋄ ⎕SIGNAL 4}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:╠⎕SIGNAL 5╣ ⋄ ⎕SIGNAL 4}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL ╠4╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL╣ 4}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[20] chk_scl←{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ╠⎕SIGNAL 4╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[20] chk_scl←╠{⍺←0 ⋄ (⍴⍺)≡⍴⍵:0 ⋄ 1≡≢,⍺:0 ⋄ 1≡≢,⍵:0 ⋄ (≢⍴⍺)≡≢⍴⍵:⎕SIGNAL 5 ⋄ ⎕SIGNAL 4}╣");
	return err;
}

int
cdf_ptr8804(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8805, cdf_ptr8806, 0), cleanup, 
		    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8807, cdf_ptr8808, 1), cleanup, 
		    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	
		k->fv[0] = retain_cell(lex->cdf_has_nat_vals);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[24] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[24] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[21] chk_valid_shape←╠{╣");
	return err;
}

int
cdf_ptr8805(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[22] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[22] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rnk);
	loc->cdf_rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8806(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[22] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[22] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rnk);
	loc->cdf_rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8807(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_has_nat_vals->value)
		CHK(6, cleanup, L"[23] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
cdf_ptr8808(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_has_nat_vals->value)
		CHK(6, cleanup, L"[23] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
cdf_ptr8809(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8810, cdf_ptr8811, 0), cleanup, 
		    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8812, cdf_ptr8813, 1), cleanup, 
		    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	
		k->fv[0] = retain_cell(lex->cdf_has_nat_vals);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[23] 	{has_nat_vals ⍵:0 ⋄ 1}⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[24] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[24] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[21] chk_valid_shape←╠{╣");
	return err;
}

int
cdf_ptr8810(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[22] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[22] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rnk);
	loc->cdf_rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8811(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_rnk;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_rnk = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴╠⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[22] 	{rnk←≢╠⍴╣⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←≢╠⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[22] 	{rnk←╠≢╣⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[22] 	{rnk←╠≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rnk);
	loc->cdf_rnk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡╠rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0╠≡╣rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0╣≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ ╠0≡rnk╣:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{╠rnk←≢⍴⍵╣ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rnk)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡╠rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rnk);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1╠≡╣rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1╣≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ ╠1≡rnk╣:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:╠0╣ ⋄ 1≡rnk:0 ⋄ 1}⍵:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:╠0╣ ⋄ 1}⍵:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[22] 	{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ ╠1╣}⍵:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[22] 	╠{rnk←≢⍴⍵ ⋄ 0≡rnk:0 ⋄ 1≡rnk:0 ⋄ 1}╣⍵:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8812(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_has_nat_vals->value)
		CHK(6, cleanup, L"[23] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
cdf_ptr8813(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_has_nat_vals;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[23] 	{has_nat_vals ╠⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_has_nat_vals->value)
		CHK(6, cleanup, L"[23] 	{╠has_nat_vals╣ ⍵:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_has_nat_vals->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[23] 	{╠has_nat_vals ⍵╣:0 ⋄ 1}⍵:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:╠0╣ ⋄ 1}⍵:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[23] 	{has_nat_vals ⍵:0 ⋄ ╠1╣}⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[23] 	╠{has_nat_vals ⍵:0 ⋄ 1}╣⍵:⎕SIGNAL 11");
	return err;
}

int
cdf_ptr8814(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_simple;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[26] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_simple->value)
		CHK(6, cleanup, L"[26] both_simple←{╠is_simple╣ ⍺:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[26] both_simple←{╠is_simple ⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[26] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_simple->value)
				CHK(6, cleanup, L"[26] both_simple←{is_simple ⍺:╠is_simple╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_simple->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[26] both_simple←{is_simple ⍺:╠is_simple ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[26] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8815(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_simple;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[26] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_simple->value)
		CHK(6, cleanup, L"[26] both_simple←{╠is_simple╣ ⍺:is_simple ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[26] both_simple←{╠is_simple ⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[26] both_simple←{is_simple ╠⍺╣:is_simple ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_simple->value)
				CHK(6, cleanup, L"[26] both_simple←{is_simple ⍺:╠is_simple╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_simple->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[26] both_simple←{is_simple ⍺:╠is_simple ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[26] both_simple←{is_simple ⍺:is_simple ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[26] both_simple←╠{is_simple ⍺:is_simple ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8816(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[27] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[27] both_numeric←{╠is_numeric╣ ⍺:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[27] both_numeric←{╠is_numeric ⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[27] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_numeric->value)
				CHK(6, cleanup, L"[27] both_numeric←{is_numeric ⍺:╠is_numeric╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[27] both_numeric←{is_numeric ⍺:╠is_numeric ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[27] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8817(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[27] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[27] both_numeric←{╠is_numeric╣ ⍺:is_numeric ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[27] both_numeric←{╠is_numeric ⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[27] both_numeric←{is_numeric ╠⍺╣:is_numeric ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_numeric->value)
				CHK(6, cleanup, L"[27] both_numeric←{is_numeric ⍺:╠is_numeric╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[27] both_numeric←{is_numeric ⍺:╠is_numeric ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[27] both_numeric←{is_numeric ⍺:is_numeric ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[27] both_numeric←╠{is_numeric ⍺:is_numeric ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8818(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[28] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[28] both_integer←{╠is_integer╣ ⍺:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] both_integer←{╠is_integer ⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[28] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_integer->value)
				CHK(6, cleanup, L"[28] both_integer←{is_integer ⍺:╠is_integer╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_integer->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[28] both_integer←{is_integer ⍺:╠is_integer ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[28] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8819(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[28] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[28] both_integer←{╠is_integer╣ ⍺:is_integer ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[28] both_integer←{╠is_integer ⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[28] both_integer←{is_integer ╠⍺╣:is_integer ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_integer->value)
				CHK(6, cleanup, L"[28] both_integer←{is_integer ⍺:╠is_integer╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_integer->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[28] both_integer←{is_integer ⍺:╠is_integer ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[28] both_integer←{is_integer ⍺:is_integer ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[28] both_integer←╠{is_integer ⍺:is_integer ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8820(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_char;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[29] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_char->value)
		CHK(6, cleanup, L"[29] both_char←{╠is_char╣ ⍺:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_char->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] both_char←{╠is_char ⍺╣:is_char ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[29] both_char←{is_char ⍺:is_char ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_char->value)
				CHK(6, cleanup, L"[29] both_char←{is_char ⍺:╠is_char╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_char->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[29] both_char←{is_char ⍺:╠is_char ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8821(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_char;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[29] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_char->value)
		CHK(6, cleanup, L"[29] both_char←{╠is_char╣ ⍺:is_char ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_char->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[29] both_char←{╠is_char ⍺╣:is_char ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[29] both_char←{is_char ╠⍺╣:is_char ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[29] both_char←{is_char ⍺:is_char ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_char->value)
				CHK(6, cleanup, L"[29] both_char←{is_char ⍺:╠is_char╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_char->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[29] both_char←{is_char ⍺:╠is_char ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[29] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[29] both_char←{is_char ⍺:is_char ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[29] both_char←╠{is_char ⍺:is_char ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8822(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[30] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[30] both_bool←{╠is_bool╣ ⍺:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] both_bool←{╠is_bool ⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_bool->value)
				CHK(6, cleanup, L"[30] both_bool←{is_bool ⍺:╠is_bool╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_bool->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] both_bool←{is_bool ⍺:╠is_bool ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8823(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[30] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[30] both_bool←{╠is_bool╣ ⍺:is_bool ⍵ ⋄ 0}");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[30] both_bool←{╠is_bool ⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[30] both_bool←{is_bool ╠⍺╣:is_bool ⍵ ⋄ 0}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ╠⍵╣ ⋄ 0}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_is_bool->value)
				CHK(6, cleanup, L"[30] both_bool←{is_bool ⍺:╠is_bool╣ ⍵ ⋄ 0}");
			
			*stkhd++ = retain_cell(lex->cdf_is_bool->value);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[30] both_bool←{is_bool ⍺:╠is_bool ⍵╣ ⋄ 0}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[30] both_bool←{is_bool ⍺:is_bool ⍵ ⋄ ╠0╣}");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[30] both_bool←╠{is_bool ⍺:is_bool ⍵ ⋄ 0}╣");
	return err;
}

int
cdf_ptr8824(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[32] numeric←{⍺←╠⊢╣ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8825(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8826(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[32] numeric←{⍺←╠⊢╣ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8827(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)╠both_numeric╣ ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣╠0╣)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺╠⊣╣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (╠⍺╣⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)╣both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ ╠(⍺⊣0)both_numeric ⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ╠⍵╣:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ╠⍺⍺╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺╣ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:╠⍺ ⍺⍺ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[32] numeric←{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[32] numeric←╠{⍺←⊢ ⋄ (⍺⊣0)both_numeric ⍵:⍺ ⍺⍺ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8828(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[33] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8829(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8830(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[33] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8831(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8832(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[33] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8833(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8834(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[33] ambiv←{⍺←╠⊢╣ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8835(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct lex_vars {
		struct cell_func_box *cdf_same;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺╣ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ╠⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!lex->cdf_same->value)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 ╠same╣ ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_same->value);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[33] ambiv←{⍺←⊢ ⋄ ╠1╣ same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ ╠1 same ⍺ 1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ ╠1╣:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ╠⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺╣ ⍵ ⋄ ⍺ ⍵⍵ ⍵}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:╠⍺⍺ ⍵╣ ⋄ ⍺ ⍵⍵ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ╠⍵⍵╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺╣ ⍵⍵ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[33] ambiv←{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ╠⍺ ⍵⍵ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[33] ambiv←╠{⍺←⊢ ⋄ 1 same ⍺ 1:⍺⍺ ⍵ ⋄ ⍺ ⍵⍵ ⍵}╣");
	return err;
}

int
cdf_ptr8836(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_both_simple;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[36] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[36] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[37] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[37] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[37] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[38] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[38] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[38] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[38] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[39] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[39] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[39] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[39] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[39] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_simple->value)
		CHK(6, cleanup, L"[40] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_simple->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[40] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[40] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[41] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[41] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[41] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[41] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[41] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[35] scalar←╠{╣");
	return err;
}

int
cdf_ptr8837(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_both_simple;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[36] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[36] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[37] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[37] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[37] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[38] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[38] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[38] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[38] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[39] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[39] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[39] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[39] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[39] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_simple->value)
		CHK(6, cleanup, L"[40] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_simple->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[40] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[40] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[41] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[41] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[41] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[41] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[41] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[35] scalar←╠{╣");
	return err;
}

int
cdf_ptr8838(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_both_simple;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[36] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[36] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[37] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[37] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[37] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[38] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[38] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[38] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[38] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[39] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[39] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[39] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[39] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[39] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_simple->value)
		CHK(6, cleanup, L"[40] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_simple->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[40] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[40] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[41] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[41] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[41] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[41] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[41] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[35] scalar←╠{╣");
	return err;
}

int
cdf_ptr8839(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_both_simple;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[36] 	⍺ ╠chk_scl╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[36] 	╠⍺╣ chk_scl ⍵: ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[36] 	╠⍺ chk_scl ⍵╣: ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[36] 	⍺ chk_scl ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[37] 	s←⍺ max_shp ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[37] 	s←⍺ ╠max_shp╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[37] 	s←╠⍺╣ max_shp ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[37] 	s←╠⍺ max_shp ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[38] 	0≡≢╠,╣⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡≢╠,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[38] 	0≡╠≢╣,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[38] 	0≡╠≢,⍺╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[38] 	0╠≡╣≢,⍺:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[38] 	╠0╣≡≢,⍺:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[38] 	╠0≡≢,⍺╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[38] 	0≡≢,╠⍺╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[38] 	0≡≢,⍺:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[38] 	0≡≢,⍺:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[38] 	0≡≢,⍺:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[39] 	0≡≢╠,╣⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡≢╠,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[39] 	0≡╠≢╣,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[39] 	0≡╠≢,⍵╣:s⍴0");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[39] 	0╠≡╣≢,⍵:s⍴0");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		CHK(fill_array(arr, dat), cleanup, L"[39] 	╠0╣≡≢,⍵:s⍴0");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[39] 	╠0≡≢,⍵╣:s⍴0");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[39] 	0≡≢,╠⍵╣:s⍴0");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[39] 	0≡≢,⍵:s⍴╠0╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:s╠⍴╣0");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[39] 	0≡≢,⍵:╠s╣⍴0");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[39] 	0≡≢,⍵:╠s⍴0╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_simple->value)
		CHK(6, cleanup, L"[40] 	⍺ ╠both_simple╣ ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_simple->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[40] 	╠⍺╣ both_simple ⍵:s⍴⍺ ⍺⍺ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[40] 	╠⍺ both_simple ⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[40] 	⍺ both_simple ╠⍵╣:s⍴⍺ ⍺⍺ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ⍺⍺ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴⍺ ╠⍺⍺╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s⍴╠⍺╣ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:s⍴╠⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:s╠⍴╣⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_s)
				CHK(6, cleanup, L"[40] 	⍺ both_simple ⍵:╠s╣⍴⍺ ⍺⍺ ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_s);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[40] 	⍺ both_simple ⍵:╠s⍴⍺ ⍺⍺ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ veach ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ∇ ╠veach╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ⍺ ╠∇╣ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[41] 	s⍴squeeze ⍺ ╠∇ veach╣ ⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[41] 	s⍴squeeze ╠⍺╣ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	s⍴squeeze ╠⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[41] 	s⍴╠squeeze╣ ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[41] 	s⍴╠squeeze ⍺ ∇ veach ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[41] 	s╠⍴╣squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[41] 	╠s╣⍴squeeze ⍺ ∇ veach ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[41] 	╠s⍴squeeze ⍺ ∇ veach ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[35] scalar←╠{╣");
	return err;
}

int
cdf_ptr8840(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_idx;
		struct cell_array *cdf_ic;
		struct cell_array *cdf_vc;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_idx = NULL;
	loc->cdf_ic = NULL;
	loc->cdf_vc = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[46] 	326≢⎕DR ╠⍺╣:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[46] 	326≢╠⎕DR╣ ⍺:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[46] 	326≢╠⎕DR ⍺╣:⎕SIGNAL 99");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[46] 	326╠≢╣⎕DR ⍺:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {326};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[46] 	╠326╣≢⎕DR ⍺:⎕SIGNAL 99");
	
		CHK(fill_array(arr, dat), cleanup, L"[46] 	╠326╣≢⎕DR ⍺:⎕SIGNAL 99");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[46] 	╠326≢⎕DR ⍺╣:⎕SIGNAL 99");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[46] 	326≢⎕DR ╠⍺╣:⎕SIGNAL 99");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {99};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[46] 	326≢⎕DR ⍺:⎕SIGNAL ╠99╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[46] 	326≢⎕DR ⍺:⎕SIGNAL ╠99╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[46] 	326≢⎕DR ⍺:╠⎕SIGNAL╣ 99");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[46] 	326≢⎕DR ⍺:╠⎕SIGNAL 99╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[47] 	1≢≢,╠⍺╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[47] 	1≢≢╠,╣⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[47] 	1≢≢╠,⍺╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[47] 	1≢╠≢╣,⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[47] 	1≢╠≢,⍺╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[47] 	1╠≢╣≢,⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[47] 	╠1╣≢≢,⍺:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[47] 	╠1╣≢≢,⍺:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[47] 	╠1≢≢,⍺╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[47] 	1≢≢,╠⍺╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[47] 	1≢≢,⍺:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[47] 	1≢≢,⍺:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[47] 	1≢≢,⍺:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[47] 	1≢≢,⍺:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[48] 	idx←⊃╠⍺╣ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[48] 	idx←╠⊃╣⍺ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←╠⊃⍺╣ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_idx);
	loc->cdf_idx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,╠idx╣ ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢╠,╣idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢╠,idx╣ ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←╠≢╣,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←╠≢,idx╣ ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ic);
	loc->cdf_ic = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢,╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢╠,╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢╠,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←╠≢╣,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←╠≢,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_vc);
	loc->cdf_vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧ic≢╠vc╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧ic╠≢╣vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_ic)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧╠ic╣≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_ic);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	(1≢vc)∧╠ic≢vc╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[49] 	(1≢vc)╠∧╣ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[49] 	(1≢╠vc╣)∧ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[49] 	(1╠≢╣vc)∧ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[49] 	(╠1╣≢vc)∧ic≢vc:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[49] 	(╠1╣≢vc)∧ic≢vc:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	╠(1≢vc)╣∧ic≢vc:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	╠(1≢vc)∧ic≢vc╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[49] 	(1≢vc)∧ic≢╠vc╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[49] 	(1≢vc)∧ic≢vc:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[49] 	(1≢vc)∧ic≢vc:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[49] 	(1≢vc)∧ic≢vc:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[49] 	(1≢vc)∧ic≢vc:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[50] 	╠idx╣");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[45] idx_shp_check←╠{╣");
	return err;
}

int
cdf_ptr8841(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_idx;
		struct cell_array *cdf_ic;
		struct cell_array *cdf_vc;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_idx = NULL;
	loc->cdf_ic = NULL;
	loc->cdf_vc = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[46] 	326≢⎕DR ╠⍺╣:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[46] 	326≢╠⎕DR╣ ⍺:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[46] 	326≢╠⎕DR ⍺╣:⎕SIGNAL 99");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[46] 	326╠≢╣⎕DR ⍺:⎕SIGNAL 99");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {326};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[46] 	╠326╣≢⎕DR ⍺:⎕SIGNAL 99");
	
		CHK(fill_array(arr, dat), cleanup, L"[46] 	╠326╣≢⎕DR ⍺:⎕SIGNAL 99");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[46] 	╠326≢⎕DR ⍺╣:⎕SIGNAL 99");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[46] 	326≢⎕DR ╠⍺╣:⎕SIGNAL 99");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {99};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[46] 	326≢⎕DR ⍺:⎕SIGNAL ╠99╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[46] 	326≢⎕DR ⍺:⎕SIGNAL ╠99╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[46] 	326≢⎕DR ⍺:╠⎕SIGNAL╣ 99");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[46] 	326≢⎕DR ⍺:╠⎕SIGNAL 99╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[47] 	1≢≢,╠⍺╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[47] 	1≢≢╠,╣⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[47] 	1≢≢╠,⍺╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[47] 	1≢╠≢╣,⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[47] 	1≢╠≢,⍺╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[47] 	1╠≢╣≢,⍺:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[47] 	╠1╣≢≢,⍺:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[47] 	╠1╣≢≢,⍺:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[47] 	╠1≢≢,⍺╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[47] 	1≢≢,╠⍺╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[47] 	1≢≢,⍺:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[47] 	1≢≢,⍺:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[47] 	1≢≢,⍺:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[47] 	1≢≢,⍺:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[48] 	idx←⊃╠⍺╣ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[48] 	idx←╠⊃╣⍺ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←╠⊃⍺╣ ⋄ ic←≢,idx ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_idx);
	loc->cdf_idx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,╠idx╣ ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢╠,╣idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢╠,idx╣ ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←╠≢╣,idx ⋄ vc←≢,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←╠≢,idx╣ ⋄ vc←≢,⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ic);
	loc->cdf_ic = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢,╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢╠,╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←≢╠,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←╠≢╣,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[48] 	idx←⊃⍺ ⋄ ic←≢,idx ⋄ vc←╠≢,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_vc);
	loc->cdf_vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧ic≢╠vc╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧ic╠≢╣vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_ic)
		CHK(6, cleanup, L"[49] 	(1≢vc)∧╠ic╣≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_ic);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	(1≢vc)∧╠ic≢vc╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[49] 	(1≢vc)╠∧╣ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[49] 	(1≢╠vc╣)∧ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[49] 	(1╠≢╣vc)∧ic≢vc:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[49] 	(╠1╣≢vc)∧ic≢vc:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[49] 	(╠1╣≢vc)∧ic≢vc:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	╠(1≢vc)╣∧ic≢vc:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[49] 	╠(1≢vc)∧ic≢vc╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[49] 	(1≢vc)∧ic≢╠vc╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[49] 	(1≢vc)∧ic≢vc:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[49] 	(1≢vc)∧ic≢vc:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[49] 	(1≢vc)∧ic≢vc:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[49] 	(1≢vc)∧ic≢vc:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[50] 	╠idx╣");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[45] idx_shp_check←╠{╣");
	return err;
}

int
cdf_ptr8842(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_idx;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_any;
		struct cell_func_box *cdf_is_integer;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_idx = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[53] 	0≡≢,╠⍺╣:⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[53] 	0≡≢╠,╣⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[53] 	0≡≢╠,⍺╣:⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[53] 	0≡╠≢╣,⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[53] 	0≡╠≢,⍺╣:⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[53] 	0╠≡╣≢,⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[53] 	╠0╣≡≢,⍺:⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[53] 	╠0╣≡≢,⍺:⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[53] 	╠0≡≢,⍺╣:⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[53] 	0≡≢,╠⍺╣:⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[53] 	0≡≢,⍺:╠⍺╣");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←╠squeeze╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡is_integer⊢idx←╠squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_idx);
	loc->cdf_idx = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[54] 	0≡is_integer╠⊢╣idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡is_integer╠⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[54] 	0≡╠is_integer╣⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡╠is_integer⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[54] 	0╠≡╣is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[54] 	╠0╣≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[54] 	╠0╣≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[54] 	╠0≡is_integer⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[54] 	0≡is_integer⊢idx←squeeze ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[54] 	0≡is_integer⊢idx←squeeze ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢,╠⍵╣:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢╠,╣⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨idx>≢╠,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>╠≢╣,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨idx>╠≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx╠>╣≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨╠idx╣>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨╠idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[55] 	any(idx<0)╠∨╣idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[55] 	any(idx<╠0╣)∨idx>≢,⍵:⎕SIGNAL 3");
	
		CHK(fill_array(arr, dat), cleanup, L"[55] 	any(idx<╠0╣)∨idx>≢,⍵:⎕SIGNAL 3");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[55] 	any(idx╠<╣0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[55] 	any(╠idx╣<0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any╠(idx<0)╣∨idx>≢,⍵:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any╠(idx<0)∨idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_any->value)
		CHK(6, cleanup, L"[55] 	╠any╣(idx<0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(lex->cdf_any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	╠any(idx<0)∨idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[55] 	any(idx<0)∨idx>≢,╠⍵╣:⎕SIGNAL 3");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:⎕SIGNAL ╠3╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:⎕SIGNAL ╠3╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:╠⎕SIGNAL╣ 3");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[55] 	any(idx<0)∨idx>≢,⍵:╠⎕SIGNAL 3╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[56] 	╠idx╣");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[52] idx_rng_check←╠{╣");
	return err;
}

int
cdf_ptr8843(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_idx;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_any;
		struct cell_func_box *cdf_is_integer;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_idx = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[53] 	0≡≢,╠⍺╣:⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[53] 	0≡≢╠,╣⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[53] 	0≡≢╠,⍺╣:⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[53] 	0≡╠≢╣,⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[53] 	0≡╠≢,⍺╣:⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[53] 	0╠≡╣≢,⍺:⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[53] 	╠0╣≡≢,⍺:⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[53] 	╠0╣≡≢,⍺:⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[53] 	╠0≡≢,⍺╣:⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[53] 	0≡≢,╠⍺╣:⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[53] 	0≡≢,⍺:╠⍺╣");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←╠squeeze╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡is_integer⊢idx←╠squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_idx);
	loc->cdf_idx = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[54] 	0≡is_integer╠⊢╣idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡is_integer╠⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[54] 	0≡╠is_integer╣⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[54] 	0≡╠is_integer⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[54] 	0╠≡╣is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[54] 	╠0╣≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[54] 	╠0╣≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[54] 	╠0≡is_integer⊢idx←squeeze ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[54] 	0≡is_integer⊢idx←squeeze ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[54] 	0≡is_integer⊢idx←squeeze ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[54] 	0≡is_integer⊢idx←squeeze ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢,╠⍵╣:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢╠,╣⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨idx>≢╠,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>╠≢╣,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨idx>╠≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨idx╠>╣≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[55] 	any(idx<0)∨╠idx╣>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any(idx<0)∨╠idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[55] 	any(idx<0)╠∨╣idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[55] 	any(idx<╠0╣)∨idx>≢,⍵:⎕SIGNAL 3");
	
		CHK(fill_array(arr, dat), cleanup, L"[55] 	any(idx<╠0╣)∨idx>≢,⍵:⎕SIGNAL 3");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[55] 	any(idx╠<╣0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[55] 	any(╠idx╣<0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any╠(idx<0)╣∨idx>≢,⍵:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[55] 	any╠(idx<0)∨idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_any->value)
		CHK(6, cleanup, L"[55] 	╠any╣(idx<0)∨idx>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(lex->cdf_any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[55] 	╠any(idx<0)∨idx>≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[55] 	any(idx<0)∨idx>≢,╠⍵╣:⎕SIGNAL 3");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:⎕SIGNAL ╠3╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:⎕SIGNAL ╠3╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[55] 	any(idx<0)∨idx>≢,⍵:╠⎕SIGNAL╣ 3");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[55] 	any(idx<0)∨idx>≢,⍵:╠⎕SIGNAL 3╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_idx)
		CHK(6, cleanup, L"[56] 	╠idx╣");
	
	*stkhd++ = retain_cell(loc->cdf_idx);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[52] idx_rng_check←╠{╣");
	return err;
}

int
cdf_ptr8844(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢,╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢╠,╣⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢⍴⍺)≢≢╠,⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢╠≢╣,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢⍴⍺)≢╠≢,⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)╠≢╣≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[62] 	(≢⍴╠⍺╣)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[62] 	(≢╠⍴╣⍺)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢╠⍴⍺)╣≢≢,⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(╠≢╣⍴⍺)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	╠(≢⍴⍺)╣≢≢,⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 	╠(≢⍴⍺)≢≢,⍵╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 	(≢⍴⍺)≢≢,╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 	(≢⍴⍺)≢≢,⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[63] 	⍵⌷╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[63] 	⍵╠⌷╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[63] 	╠⍵╣⌷⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 	╠⍵⌷⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[61] brk←╠{╣");
	return err;
}

int
cdf_ptr8845(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢,╠⍵╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢╠,╣⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢⍴⍺)≢≢╠,⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢╠≢╣,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢⍴⍺)≢╠≢,⍵╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(≢⍴⍺)╠≢╣≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[62] 	(≢⍴╠⍺╣)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[62] 	(≢╠⍴╣⍺)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	(≢╠⍴⍺)╣≢≢,⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[62] 	(╠≢╣⍴⍺)≢≢,⍵:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[62] 	╠(≢⍴⍺)╣≢≢,⍵:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[62] 	╠(≢⍴⍺)≢≢,⍵╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[62] 	(≢⍴⍺)≢≢,╠⍵╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[62] 	(≢⍴⍺)≢≢,⍵:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[62] 	(≢⍴⍺)≢≢,⍵:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[63] 	⍵⌷╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[63] 	⍵╠⌷╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[63] 	╠⍵╣⌷⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[63] 	╠⍵⌷⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[61] brk←╠{╣");
	return err;
}

int
cdf_ptr8846(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[67] rgt←{╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[67] rgt←╠{⍵}╣");
	return err;
}

int
cdf_ptr8847(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[67] rgt←{╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[67] rgt←╠{⍵}╣");
	return err;
}

int
cdf_ptr8848(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[70] lft←{╠⍵╣} ambiv {⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[70] lft←╠{⍵}╣ ambiv {⍺}");
	return err;
}

int
cdf_ptr8849(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[70] lft←{╠⍵╣} ambiv {⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[70] lft←╠{⍵}╣ ambiv {⍺}");
	return err;
}

int
cdf_ptr8850(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[70] lft←{⍵} ambiv {╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[70] lft←{⍵} ambiv ╠{⍺}╣");
	return err;
}

int
cdf_ptr8851(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[70] lft←{⍵} ambiv {╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[70] lft←{⍵} ambiv ╠{⍺}╣");
	return err;
}

int
cdf_ptr8852(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_chk_valid_shape->value)
		CHK(6, cleanup, L"[73] reshape←{╠chk_valid_shape╣ ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] reshape←{╠chk_valid_shape ⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[73] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_reshape_ibeam)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ⍺╠('reshape_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(cdf_reshape_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺╣('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺('reshape_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[73] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	return err;
}

int
cdf_ptr8853(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_chk_valid_shape->value)
		CHK(6, cleanup, L"[73] reshape←{╠chk_valid_shape╣ ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[73] reshape←{╠chk_valid_shape ⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[73] reshape←{chk_valid_shape ╠⍺╣: ⋄ ⍺('reshape_ibeam'⌶)⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_reshape_ibeam)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ⍺╠('reshape_ibeam'⌶)╣⍵}");
	
	*stkhd++ = retain_cell(cdf_reshape_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[73] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺╣('reshape_ibeam'⌶)⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[73] reshape←{chk_valid_shape ⍺: ⋄ ╠⍺('reshape_ibeam'⌶)⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[73] reshape←╠{chk_valid_shape ⍺: ⋄ ⍺('reshape_ibeam'⌶)⍵}╣");
	return err;
}

int
cdf_ptr8854(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array_box *cdf_noax;
		struct cell_array *cdf_israv;
		struct cell_array_box *cdf_lr;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_axis;
		struct cell_array *cdf_rk;
		struct cell_array *cdf_ar;
		struct cell_array_box *cdf_fx;
		struct cell_array *cdf_pr;
		struct cell_func *cdf_lx;
		struct cell_func *cdf_vc;
		struct cell_array_box *cdf_c;
		struct cell_array *cdf_a;
		struct cell_array *cdf_ax;
		struct cell_array *cdf_w;
		struct cell_array *cdf_wx;
		struct cell_array_box *cdf_s;
		struct cell_array *cdf_ac;
		struct cell_array *cdf_wc;
		struct cell_array_box *cdf_z;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	struct opt_vars {
		struct cell_array_box *cdf_axis;
	} *opts;

	opts = (struct opt_vars *)cdf_self->opts;

	CHK(mk_array_box(&loc->cdf_noax, NULL), cleanup, L"Init mutable variable: loc->cdf_noax");
	loc->cdf_israv = NULL;
	CHK(mk_array_box(&loc->cdf_lr, NULL), cleanup, L"Init mutable variable: loc->cdf_lr");
	CHK(mk_array_box(&loc->cdf_rr, NULL), cleanup, L"Init mutable variable: loc->cdf_rr");
	CHK(mk_array_box(&loc->cdf_axis, NULL), cleanup, L"Init mutable variable: loc->cdf_axis");
	loc->cdf_rk = NULL;
	loc->cdf_ar = NULL;
	CHK(mk_array_box(&loc->cdf_fx, NULL), cleanup, L"Init mutable variable: loc->cdf_fx");
	loc->cdf_pr = NULL;
	loc->cdf_lx = NULL;
	loc->cdf_vc = NULL;
	CHK(mk_array_box(&loc->cdf_c, NULL), cleanup, L"Init mutable variable: loc->cdf_c");
	loc->cdf_a = NULL;
	loc->cdf_ax = NULL;
	loc->cdf_w = NULL;
	loc->cdf_wx = NULL;
	CHK(mk_array_box(&loc->cdf_s, NULL), cleanup, L"Init mutable variable: loc->cdf_s");
	loc->cdf_ac = NULL;
	loc->cdf_wc = NULL;
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[78] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[78] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_noax->value);
	loc->cdf_noax->value = retain_cell(stkhd[-1]);
	
	release_cell(loc->cdf_israv);
	loc->cdf_israv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←╠1╣ ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←╠1╣ ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_israv);
	loc->cdf_israv = retain_cell(stkhd[-1]);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴╠⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴╣⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢╣⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lr->value);
	loc->cdf_lr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴╠⍵╣ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴╣⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢╣⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rr->value);
	loc->cdf_rr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->cdf_axis) {
		*stkhd++ = retain_cell(opts->cdf_axis);
	} else {
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			CHK(fill_array(arr, dat), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			*stkhd++ = arr;
		}
		
		release_cell(loc->cdf_noax->value);
		loc->cdf_noax->value = retain_cell(stkhd[-1]);
		
	}
	
	release_cell(loc->cdf_axis->value);
	loc->cdf_axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_israv)
		CHK(6, cleanup, L"[79] 	╠israv╣:{");
	
	*stkhd++ = retain_cell(loc->cdf_israv);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[79] 	╠israv╣:{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[84] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8855, cdf_ptr8856, 2), cleanup, 
				    L"[79] 	israv:╠{╣");
			
				k->fv[0] = retain_cell(loc->cdf_axis);
				k->fv[1] = retain_cell(loc->cdf_noax);
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[79] 	israv:╠{╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[85] 	rk←lr⌈╠rr╣ ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[85] 	rk←lr╠⌈╣rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[85] 	rk←╠lr╣⌈rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	rk←╠lr⌈rr╣ ⋄ axis⍠←¯1+rk");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rk);
	loc->cdf_rk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->cdf_axis) {
		*stkhd++ = retain_cell(opts->cdf_axis);
	} else {
		if (!loc->cdf_rk)
			CHK(6, cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←¯1+╠rk╣");
		
		*stkhd++ = retain_cell(loc->cdf_rk);
		
		if (!cdf_prim.cdf_add)
			CHK(6, cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←¯1╠+╣rk");
		
		*stkhd++ = retain_cell(cdf_prim.cdf_add);
		
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {-1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			CHK(fill_array(arr, dat), cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			*stkhd++ = arr;
		}
		
		{
			struct cell_array *x = stkhd[-1];
			struct cell_func *fn = stkhd[-2];
			struct cell_array *y = stkhd[-3];
			struct cell_array *dst;
		
			CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
			    L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1+rk╣");
		
			release_array(x);
			release_func(fn);
			release_array(y);
		
			stkhd -= 3;
			*stkhd++ = dst;
		}
		
	}
	
	release_cell(loc->cdf_axis->value);
	loc->cdf_axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[86] 	(0≡lr)∧(0╠≡╣rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	(0≡lr)∧╠(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[86] 	(0≡lr)╠∧╣(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[86] 	(0≡╠lr╣)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[86] 	(0╠≡╣lr)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	╠(0≡lr)╣∧(0≡rr):(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	╠(0≡lr)∧(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[86] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺),,╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺),╠,╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):(,⍺),╠,⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺)╠,╣,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,╠⍺╣),,⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(╠,╣⍺),,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):╠(,⍺)╣,,⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):╠(,⍺),,⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[87] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[87] 	ar←≢╠⍴╣axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢╠⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←╠≢╣⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←╠≢⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ar);
	loc->cdf_ar = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[87] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[87] 	ar←≢⍴axis:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[87] 	ar←≢⍴axis:╠⎕SIGNAL 4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,╣axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢╣,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1╠≢╣≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1≢≢,axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL╣ 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL 5╣ ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[88] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[88] 	~╠is_numeric╣ axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[88] 	~╠is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[88] 	╠~╣is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[88] 	╠~is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[88] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[88] 	~is_numeric axis:╠⎕SIGNAL╣ 11 ⋄ axis≤¯1:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[88] 	~is_numeric axis:╠⎕SIGNAL 11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lte)
		CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis╠≤╣¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lte);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis╣≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis≤¯1╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rk)
		CHK(6, cleanup, L"[89] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rk);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[89] 	axis╠≥╣rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[89] 	╠axis╣≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	╠axis≥rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[89] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[89] 	axis≥rk:╠⎕SIGNAL╣ 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[89] 	axis≥rk:╠⎕SIGNAL 4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr╠-╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr╣-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|╣lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|lr-rr╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1╠<╣|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)╠∧╣1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr╠≢╣0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(╠rr╣≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)╣∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)╠∧╣(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr╠≢╣0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (╠lr╣≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)╣∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)∧(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[90] 	axis≠fx←⌈╠axis╣:⍺{");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[90] 	axis≠fx←╠⌈╣axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[90] 	axis≠fx←╠⌈axis╣:⍺{");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_fx->value);
	loc->cdf_fx->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[90] 	axis╠≠╣fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[90] 	╠axis╣≠fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[90] 	╠axis≠fx←⌈axis╣:⍺{");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[90] 	axis≠fx←⌈╠axis╣:⍺{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[93] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8857, cdf_ptr8858, 4), cleanup, 
				    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
			
				k->fv[0] = retain_cell(loc->cdf_fx);
				k->fv[1] = retain_cell(loc->cdf_axis);
				k->fv[2] = retain_cell(loc->cdf_rr);
				k->fv[3] = retain_cell(loc->cdf_lr);
			
				*stkhd++ = k;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[90] 	axis≠fx←⌈axis:╠⍺╣{");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[90] 	axis≠fx←⌈axis:╠⍺{╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)╠↑╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=╠rr╣)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr╠=╣rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr╣=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)╠-╣lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊╠rr╣)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr╠⌊╣rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(╠lr╣⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)╣-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)-lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠╠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.╠≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_prim.cdf_dot)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨╠.╣≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dot);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)╠∨╣.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[94] 	(pr↑⍴⍺)╠∨.≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[94] 	(pr↑⍴╠⍺╣)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[94] 	(pr↑╠⍴╣⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[94] 	(pr↑╠⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[94] 	(pr╠↑╣⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[94] 	(╠pr╣↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	╠(pr↑⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	╠(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8859, cdf_ptr8860, 1), cleanup, 
		    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fx);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_lx);
	loc->cdf_lx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8861, cdf_ptr8862, 1), cleanup, 
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_c);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_vc);
	loc->cdf_vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[96] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)╠-╣1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈╠rr╣)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[96] 	fx←(lr╠⌈╣rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[96] 	fx←(╠lr╣⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←╠(lr⌈rr)╣-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←╠(lr⌈rr)-1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_fx->value);
	loc->cdf_fx->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,╠⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←╠,╣⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←╠,⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_a);
	loc->cdf_a = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ╠⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!loc->cdf_lx)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr ╠lx╣ ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lx);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr╣ lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr lx ⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ax);
	loc->cdf_ax = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,╠⍵╣ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,╣⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,⍵╣ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_w);
	loc->cdf_w = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!loc->cdf_lx)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr ╠lx╣ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lx);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr╣ lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr lx ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_wx);
	loc->cdf_wx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}╠⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8863, cdf_ptr8864, 2), cleanup, 
		    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		k->fv[0] = retain_cell(loc->cdf_rr);
		k->fv[1] = retain_cell(loc->cdf_lr);
	
		*stkhd++ = k;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[97] 	s←⍴╠⍺╣{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴╠⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[97] 	s←╠⍴╣⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[97] 	s←╠⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s->value);
	loc->cdf_s->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+╠wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax╠+╣wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax╣+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]╠←╣ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[╠fx╣]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_s;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ ╠s[fx]←ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑╠s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx╠↑╣s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx╣↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×╠⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×╣⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c->value);
	loc->cdf_c->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_a)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc ╠a╣ ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_a);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr ╠vc╣ a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr╣ vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr vc a╣ ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ac);
	loc->cdf_ac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_w)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc ╠w╣ ");
	
	*stkhd++ = retain_cell(loc->cdf_w);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr ╠vc╣ w ");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr╣ vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr vc w╣ ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_wc);
	loc->cdf_wc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[98] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[98] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)╠⍴╣0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷╠s╣)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[98] 	z←(c×fx╠⌷╣s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[98] 	z←(c×╠fx╣⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×╠fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c╠×╣fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_c->value)
		CHK(6, cleanup, L"[98] 	z←(╠c╣×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_c->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←╠(c×fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←╠(c×fx⌷s)⍴0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_a)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←╠a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_a);
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]╠←╣a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷╠ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)╠÷╣ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_ac)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳╠ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ac);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳╣ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊╠(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊╣(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx╠×╣⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx╣×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i╠+╣wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i╣+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i+wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ ╠z[i+wx×⌊(i←⍳ac)÷ax]←a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_w)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←╠w╣");
	
	*stkhd++ = retain_cell(loc->cdf_w);
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]╠←╣w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷╠wx╣]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)╠÷╣wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_wc)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳╠wc╣)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wc);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳╣wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳wc╣)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊╠(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊╣(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊(i←⍳wc)÷wx╣]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1╠+╣⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax╠×╣1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax╣×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i╠+╣ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i╣+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i+ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ ╠z[i+ax×1+⌊(i←⍳wc)÷wx]←w╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[99] 	s⍴╠z╣	");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[99] 	s╠⍴╣z	");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[99] 	╠s╣⍴z	");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[99] 	╠s⍴z╣	");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[77] cat←╠{╣");
	return err;
}

int
cdf_ptr8855(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_noax;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_s = NULL;
	loc->cdf_pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_noax->value)
		CHK(6, cleanup, L"[80] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[79] 	israv:╠{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[80] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_ravel_ibeam)
				CHK(6, cleanup, L"[80] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[81] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[81] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[81] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[81] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[82] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[82] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[82] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[82] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[82] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[83] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[83] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[83] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[79] 	israv:╠{╣");
	return err;
}

int
cdf_ptr8856(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_noax;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_s = NULL;
	loc->cdf_pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_noax->value)
		CHK(6, cleanup, L"[80] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[80] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_ravel_ibeam)
				CHK(6, cleanup, L"[80] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[81] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[81] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[81] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[81] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[82] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[82] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[82] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[82] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[82] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[83] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[83] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[83] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[79] 	israv:╠{╣");
	return err;
}

int
cdf_ptr8857(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[90] 	axis≠fx←⌈axis:╠⍺{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
cdf_ptr8858(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
cdf_ptr8859(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[95] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
cdf_ptr8860(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[95] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
cdf_ptr8861(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_c;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->cdf_c->value)
				CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->cdf_c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
cdf_ptr8862(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_c;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->cdf_c->value)
				CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->cdf_c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
cdf_ptr8863(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	╠s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
cdf_ptr8864(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
cdf_ptr8865(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array_box *cdf_noax;
		struct cell_array *cdf_israv;
		struct cell_array_box *cdf_lr;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_axis;
		struct cell_array *cdf_rk;
		struct cell_array *cdf_ar;
		struct cell_array_box *cdf_fx;
		struct cell_array *cdf_pr;
		struct cell_func *cdf_lx;
		struct cell_func *cdf_vc;
		struct cell_array_box *cdf_c;
		struct cell_array *cdf_a;
		struct cell_array *cdf_ax;
		struct cell_array *cdf_w;
		struct cell_array *cdf_wx;
		struct cell_array_box *cdf_s;
		struct cell_array *cdf_ac;
		struct cell_array *cdf_wc;
		struct cell_array_box *cdf_z;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	struct opt_vars {
		struct cell_array_box *cdf_axis;
	} *opts;

	opts = (struct opt_vars *)cdf_self->opts;

	CHK(mk_array_box(&loc->cdf_noax, NULL), cleanup, L"Init mutable variable: loc->cdf_noax");
	loc->cdf_israv = NULL;
	CHK(mk_array_box(&loc->cdf_lr, NULL), cleanup, L"Init mutable variable: loc->cdf_lr");
	CHK(mk_array_box(&loc->cdf_rr, NULL), cleanup, L"Init mutable variable: loc->cdf_rr");
	CHK(mk_array_box(&loc->cdf_axis, NULL), cleanup, L"Init mutable variable: loc->cdf_axis");
	loc->cdf_rk = NULL;
	loc->cdf_ar = NULL;
	CHK(mk_array_box(&loc->cdf_fx, NULL), cleanup, L"Init mutable variable: loc->cdf_fx");
	loc->cdf_pr = NULL;
	loc->cdf_lx = NULL;
	loc->cdf_vc = NULL;
	CHK(mk_array_box(&loc->cdf_c, NULL), cleanup, L"Init mutable variable: loc->cdf_c");
	loc->cdf_a = NULL;
	loc->cdf_ax = NULL;
	loc->cdf_w = NULL;
	loc->cdf_wx = NULL;
	CHK(mk_array_box(&loc->cdf_s, NULL), cleanup, L"Init mutable variable: loc->cdf_s");
	loc->cdf_ac = NULL;
	loc->cdf_wc = NULL;
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[78] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		CHK(fill_array(arr, dat), cleanup, L"[78] 	israv←noax←╠0╣ ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_noax->value);
	loc->cdf_noax->value = retain_cell(stkhd[-1]);
	
	release_cell(loc->cdf_israv);
	loc->cdf_israv = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴╠⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴╣⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢╠⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢╣⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←╠≢⍴⍺╣ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_lr->value);
	loc->cdf_lr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴╠⍵╣ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴╣⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢╠⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢╣⍴⍵ ⋄ axis⍠←noax←1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←╠≢⍴⍵╣ ⋄ axis⍠←noax←1");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rr->value);
	loc->cdf_rr->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->cdf_axis) {
		*stkhd++ = retain_cell(opts->cdf_axis);
	} else {
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			CHK(fill_array(arr, dat), cleanup, L"[78] 	israv←noax←0 ⋄ ⍺←israv←1 ⋄ lr←≢⍴⍺ ⋄ rr←≢⍴⍵ ⋄ axis⍠←noax←╠1╣");
		
			*stkhd++ = arr;
		}
		
		release_cell(loc->cdf_noax->value);
		loc->cdf_noax->value = retain_cell(stkhd[-1]);
		
	}
	
	release_cell(loc->cdf_axis->value);
	loc->cdf_axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_israv)
		CHK(6, cleanup, L"[79] 	╠israv╣:{");
	
	*stkhd++ = retain_cell(loc->cdf_israv);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[79] 	╠israv╣:{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[84] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8866, cdf_ptr8867, 2), cleanup, 
				    L"[79] 	israv:╠{╣");
			
				k->fv[0] = retain_cell(loc->cdf_axis);
				k->fv[1] = retain_cell(loc->cdf_noax);
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[79] 	israv:╠{╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[85] 	rk←lr⌈╠rr╣ ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[85] 	rk←lr╠⌈╣rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[85] 	rk←╠lr╣⌈rr ⋄ axis⍠←¯1+rk");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[85] 	rk←╠lr⌈rr╣ ⋄ axis⍠←¯1+rk");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rk);
	loc->cdf_rk = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (opts && opts->cdf_axis) {
		*stkhd++ = retain_cell(opts->cdf_axis);
	} else {
		if (!loc->cdf_rk)
			CHK(6, cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←¯1+╠rk╣");
		
		*stkhd++ = retain_cell(loc->cdf_rk);
		
		if (!cdf_prim.cdf_add)
			CHK(6, cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←¯1╠+╣rk");
		
		*stkhd++ = retain_cell(cdf_prim.cdf_add);
		
		{
			struct cell_array *arr;
		
			enum array_type typ = ARR_SINT;
			unsigned int rnk = 0;
			int16_t dat[] = {-1};
		
			CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			CHK(fill_array(arr, dat), cleanup, L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1╣+rk");
		
			*stkhd++ = arr;
		}
		
		{
			struct cell_array *x = stkhd[-1];
			struct cell_func *fn = stkhd[-2];
			struct cell_array *y = stkhd[-3];
			struct cell_array *dst;
		
			CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
			    L"[85] 	rk←lr⌈rr ⋄ axis⍠←╠¯1+rk╣");
		
			release_array(x);
			release_func(fn);
			release_array(y);
		
			stkhd -= 3;
			*stkhd++ = dst;
		}
		
	}
	
	release_cell(loc->cdf_axis->value);
	loc->cdf_axis->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[86] 	(0≡lr)∧(0╠≡╣rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	(0≡lr)∧(╠0╣≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	(0≡lr)∧╠(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[86] 	(0≡lr)╠∧╣(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[86] 	(0≡╠lr╣)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[86] 	(0╠≡╣lr)∧(0≡rr):(,⍺),,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[86] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[86] 	(╠0╣≡lr)∧(0≡rr):(,⍺),,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	╠(0≡lr)╣∧(0≡rr):(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[86] 	╠(0≡lr)∧(0≡rr)╣:(,⍺),,⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[86] 	(0≡lr)∧(0≡╠rr╣):(,⍺),,⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺),,╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺),╠,╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):(,⍺),╠,⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,⍺)╠,╣,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(,╠⍺╣),,⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[86] 	(0≡lr)∧(0≡rr):(╠,╣⍺),,⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):╠(,⍺)╣,,⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[86] 	(0≡lr)∧(0≡rr):╠(,⍺),,⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[87] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[87] 	ar←≢╠⍴╣axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢╠⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←╠≢╣⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←╠≢⍴axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ar);
	loc->cdf_ar = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[87] 	ar←≢⍴╠axis╣:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL ╠4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[87] 	ar←≢⍴axis:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[87] 	ar←≢⍴axis:╠⎕SIGNAL 4╣ ⋄ 1≢≢,axis:⎕SIGNAL 5 ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,╣axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢╠,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢╣,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢╠≢,axis╣:⎕SIGNAL 5 ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1╠≢╣≢,axis:⎕SIGNAL 5 ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1╣≢≢,axis:⎕SIGNAL 5 ");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ ╠1≢≢,axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,╠axis╣:⎕SIGNAL 5 ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				CHK(fill_array(arr, dat), cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:⎕SIGNAL ╠5╣ ");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL╣ 5 ");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[87] 	ar←≢⍴axis:⎕SIGNAL 4 ⋄ 1≢≢,axis:╠⎕SIGNAL 5╣ ");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[88] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[88] 	~╠is_numeric╣ axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[88] 	~╠is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[88] 	╠~╣is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[88] 	╠~is_numeric axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[88] 	~is_numeric ╠axis╣:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL ╠11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[88] 	~is_numeric axis:╠⎕SIGNAL╣ 11 ⋄ axis≤¯1:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[88] 	~is_numeric axis:╠⎕SIGNAL 11╣ ⋄ axis≤¯1:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lte)
		CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis╠≤╣¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lte);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis╣≤¯1:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ ╠axis≤¯1╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤╠¯1╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[88] 	~is_numeric axis:⎕SIGNAL 11 ⋄ axis≤¯1:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rk)
		CHK(6, cleanup, L"[89] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rk);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[89] 	axis╠≥╣rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[89] 	╠axis╣≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	╠axis≥rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[89] 	axis≥╠rk╣:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL ╠4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[89] 	axis≥rk:╠⎕SIGNAL╣ 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[89] 	axis≥rk:╠⎕SIGNAL 4╣ ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr╠-╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr╣-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|╠lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|╣lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<╠|lr-rr╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1╠<╣|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1╣<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧╠1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)╠∧╣1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢╠0╣)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr╠≢╣0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(╠rr╣≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)╣∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧╠(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)╠∧╣(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢╠0╣)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr╠≢╣0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (╠lr╣≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)╣∧(rr≢0)∧1<|lr-rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ ╠(lr≢0)∧(rr≢0)∧1<|lr-rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-╠rr╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[89] 	axis≥rk:⎕SIGNAL 4 ⋄ (lr≢0)∧(rr≢0)∧1<|lr-rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[90] 	axis≠fx←⌈╠axis╣:⍺{");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[90] 	axis≠fx←╠⌈╣axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[90] 	axis≠fx←╠⌈axis╣:⍺{");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_fx->value);
	loc->cdf_fx->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[90] 	axis╠≠╣fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!loc->cdf_axis->value)
		CHK(6, cleanup, L"[90] 	╠axis╣≠fx←⌈axis:⍺{");
	
	*stkhd++ = retain_cell(loc->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[90] 	╠axis≠fx←⌈axis╣:⍺{");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[90] 	axis≠fx←⌈╠axis╣:⍺{");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[93] 	}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8868, cdf_ptr8869, 4), cleanup, 
				    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
			
				k->fv[0] = retain_cell(loc->cdf_fx);
				k->fv[1] = retain_cell(loc->cdf_axis);
				k->fv[2] = retain_cell(loc->cdf_rr);
				k->fv[3] = retain_cell(loc->cdf_lr);
			
				*stkhd++ = k;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[90] 	axis≠fx←⌈axis:╠⍺╣{");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[90] 	axis≠fx←⌈axis:╠⍺{╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)╠↑╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=╠rr╣)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr╠=╣rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr╣=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-╠lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)╠-╣lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊╠rr╣)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr╠⌊╣rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(╠lr╣⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)╣-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠(pr←╠(lr⌊rr)-lr=rr╣)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	(pr↑⍴⍺)∨.≠╠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.╠≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_prim.cdf_dot)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨╠.╣≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dot);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)╠∨╣.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_func *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_ffm, op->fptr_ffd, x, y), cleanup,
		    L"[94] 	(pr↑⍴⍺)╠∨.≠╣(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(x);
		release_doper(op);
		release_func(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[94] 	(pr↑⍴╠⍺╣)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[94] 	(pr↑╠⍴╣⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[94] 	(pr↑╠⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[94] 	(pr╠↑╣⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[94] 	(╠pr╣↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	╠(pr↑⍴⍺)╣∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[94] 	╠(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[94] 	(pr↑⍴⍺)∨.≠(pr←(lr⌊rr)-lr=rr)↑⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8870, cdf_ptr8871, 1), cleanup, 
		    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fx);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_lx);
	loc->cdf_lx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8872, cdf_ptr8873, 1), cleanup, 
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	
		k->fv[0] = retain_cell(loc->cdf_c);
	
		*stkhd++ = k;
	}
	
	release_cell(loc->cdf_vc);
	loc->cdf_vc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[96] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[96] 	fx←(lr⌈rr)-╠1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)╠-╣1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈╠rr╣)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[96] 	fx←(lr╠⌈╣rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[96] 	fx←(╠lr╣⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←╠(lr⌈rr)╣-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←╠(lr⌈rr)-1╣ ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_fx->value);
	loc->cdf_fx->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,╠⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←╠,╣⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←╠,⍺╣ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_a);
	loc->cdf_a = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ╠⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!loc->cdf_lx)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr ╠lx╣ ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lx);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr╣ lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←╠lr lx ⍺╣ ⋄ w←,⍵ ⋄ wx←rr lx ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ax);
	loc->cdf_ax = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,╠⍵╣ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,╣⍵ ⋄ wx←rr lx ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←╠,⍵╣ ⋄ wx←rr lx ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_w);
	loc->cdf_w = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr lx ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!loc->cdf_lx)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←rr ╠lx╣ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_lx);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr╣ lx ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[96] 	fx←(lr⌈rr)-1 ⋄ a←,⍺ ⋄ ax←lr lx ⍺ ⋄ w←,⍵ ⋄ wx←╠rr lx ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_wx);
	loc->cdf_wx = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}╠⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8874, cdf_ptr8875, 2), cleanup, 
		    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		k->fv[0] = retain_cell(loc->cdf_rr);
		k->fv[1] = retain_cell(loc->cdf_lr);
	
		*stkhd++ = k;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[97] 	s←⍴╠⍺╣{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴╠⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[97] 	s←╠⍴╣⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[97] 	s←╠⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s->value);
	loc->cdf_s->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+╠wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax╠+╣wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax╣+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←╠ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]╠←╣ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[╠fx╣]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s╠[fx]╣←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_s;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ ╠s[fx]←ax+wx╣ ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑╠s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx╠↑╣s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx╣↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿╠fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×╠⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×╣⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿╣fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←╠×⌿fx↑s╣ ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c->value);
	loc->cdf_c->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_a)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc ╠a╣ ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_a);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr ╠vc╣ a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!loc->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr╣ vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←╠lr vc a╣ ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ac);
	loc->cdf_ac = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_w)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc ╠w╣ ");
	
	*stkhd++ = retain_cell(loc->cdf_w);
	
	if (!loc->cdf_vc)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr ╠vc╣ w ");
	
	*stkhd++ = retain_cell(loc->cdf_vc);
	
	if (!loc->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr╣ vc w ");
	
	*stkhd++ = retain_cell(loc->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←╠rr vc w╣ ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_wc);
	loc->cdf_wc = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[98] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[98] 	z←(c×fx⌷s)⍴╠0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)╠⍴╣0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷╠s╣)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[98] 	z←(c×fx╠⌷╣s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!loc->cdf_fx->value)
		CHK(6, cleanup, L"[98] 	z←(c×╠fx╣⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×╠fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c╠×╣fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_c->value)
		CHK(6, cleanup, L"[98] 	z←(╠c╣×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_c->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←╠(c×fx⌷s)╣⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←╠(c×fx⌷s)⍴0╣ ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_a)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←╠a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_a);
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]╠←╣a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷╠ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)╠÷╣ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_ac)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳╠ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ac);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳╣ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←╠⍳ac╣)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊╠(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊╣(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×╠⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx╠×╣⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx╣×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+╠wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i╠+╣wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i╣+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[╠i+wx×⌊(i←⍳ac)÷ax╣]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z╠[i+wx×⌊(i←⍳ac)÷ax]╣←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ ╠z[i+wx×⌊(i←⍳ac)÷ax]←a╣ ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←w");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_w)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]←╠w╣");
	
	*stkhd++ = retain_cell(loc->cdf_w);
	
	if (!cdf_prim.cdf_set)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷wx]╠←╣w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_set);
	
	if (!loc->cdf_wx)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)÷╠wx╣]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wx);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳wc)╠÷╣wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_wc)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←⍳╠wc╣)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_wc);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳╣wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊(i←╠⍳wc╣)÷wx]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+⌊╠(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊╣(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1+╠⌊(i←⍳wc)÷wx╣]←w");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×1╠+╣⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		CHK(fill_array(arr, dat), cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1╣+⌊(i←⍳wc)÷wx]←w");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax×╠1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+ax╠×╣1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_ax)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax╣×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_ax);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i+╠ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[i╠+╣ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i╣+ax×1+⌊(i←⍳wc)÷wx]←w");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z[╠i+ax×1+⌊(i←⍳wc)÷wx╣]←w");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ z╠[i+ax×1+⌊(i←⍳wc)÷wx]╣←w");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array_box *bx = loc->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[98] 	z←(c×fx⌷s)⍴0 ⋄ z[i+wx×⌊(i←⍳ac)÷ax]←a ⋄ ╠z[i+ax×1+⌊(i←⍳wc)÷wx]←w╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[99] 	s⍴╠z╣	");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[99] 	s╠⍴╣z	");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s->value)
		CHK(6, cleanup, L"[99] 	╠s╣⍴z	");
	
	*stkhd++ = retain_cell(loc->cdf_s->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[99] 	╠s⍴z╣	");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[77] cat←╠{╣");
	return err;
}

int
cdf_ptr8866(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_noax;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_s = NULL;
	loc->cdf_pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_noax->value)
		CHK(6, cleanup, L"[80] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[79] 	israv:╠{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[80] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_ravel_ibeam)
				CHK(6, cleanup, L"[80] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[81] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[81] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[81] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[81] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[82] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[82] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[82] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[82] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[82] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[83] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[83] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[83] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[79] 	israv:╠{╣");
	return err;
}

int
cdf_ptr8867(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_pr;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_noax;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_s = NULL;
	loc->cdf_pr = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_noax->value)
		CHK(6, cleanup, L"[80] 		╠noax╣:'ravel_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_noax->value);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[80] 		noax:'ravel_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_ravel_ibeam)
				CHK(6, cleanup, L"[80] 		noax:╠'ravel_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_ravel_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[81] 		1≢≢,╠axis╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[81] 		1≢≢╠,╣axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢≢╠,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1≢╠≢╣,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[81] 		1≢╠≢,axis╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[81] 		1╠≢╣≢,axis:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[81] 		╠1╣≢≢,axis:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[81] 		╠1≢≢,axis╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[80] 		noax:╠'ravel_ibeam'⌶ ⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[81] 		1≢≢,axis:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[81] 		1≢≢,axis:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		axis=⌈╠axis╣:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[82] 		axis=╠⌈╣axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[82] 		axis=╠⌈axis╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[82] 		axis╠=╣⌈axis:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[82] 		╠axis╣=⌈axis:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[82] 		╠axis=⌈axis╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[81] 		1≢≢,axis:╠⎕SIGNAL 16╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[82] 		axis=⌈axis:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←⍴╠⍵╣)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴╣⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←⌈axis)↓s←╠⍴⍵╣)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈axis)╠↓╣s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←⌈╠axis╣)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[83] 		((pr↑s),1,(pr←╠⌈╣axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,(pr←╠⌈axis╣)↓s←⍴⍵)⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pr);
	loc->cdf_pr = retain_cell(stkhd[-1]);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),1,╠(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s),1╠,╣(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[83] 		((pr↑s),╠1╣,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		((pr↑s),╠1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[83] 		((pr↑s)╠,╣1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[83] 		((pr↑╠s╣),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[83] 		((pr╠↑╣s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_pr)
		CHK(6, cleanup, L"[83] 		((╠pr╣↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_pr);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		(╠(pr↑s)╣,1,(pr←⌈axis)↓s←⍴⍵)⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)╣⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[83] 		╠((pr↑s),1,(pr←⌈axis)↓s←⍴⍵)⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[79] 	israv:╠{╣");
	return err;
}

int
cdf_ptr8868(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[90] 	axis≠fx←⌈axis:╠⍺{╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
cdf_ptr8869(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
		struct cell_array_box *cdf_axis;
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠╠rr╣:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr╠≠╣rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧╠lr╣≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧(rr≢0)∧╠lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)╠∧╣lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢╠0╣)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr╠≢╣0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[91] 		(lr≢0)∧(╠rr╣≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)╣∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		(lr≢0)∧╠(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[91] 		(lr≢0)╠∧╣(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢╠0╣)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[91] 		(lr╠≢╣0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[91] 		(╠lr╣≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)╣∧(rr≢0)∧lr≠rr:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[91] 		╠(lr≢0)∧(rr≢0)∧lr≠rr╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[91] 		(lr≢0)∧(rr≢0)∧lr≠rr:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)╠⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢╠0╣)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr╠≢╣0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(╠rr╣≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣╠(rr≢0)╣⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]╠⍣╣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx],[╠axis╣]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,╣[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis╣]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)╣⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[fx]╠,[axis]⍣(rr≢0)⊢⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺),[╠fx╣],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,╣[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)⊢⍺)╠,[fx╣],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)⊢╠⍺╣),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr≢0)╠⊢╣⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣(lr≢0)╠⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[92] 		(,[axis]⍣(lr≢╠0╣)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(lr╠≢╣0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[92] 		(,[axis]⍣(╠lr╣≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		(,[axis]⍣╠(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_pow)
		CHK(6, cleanup, L"[92] 		(,[axis]╠⍣╣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_pow);
	
	if (!lex->cdf_axis->value)
		CHK(6, cleanup, L"[92] 		(,[╠axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(lex->cdf_axis->value);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[92] 		(╠,╣[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *aa = stkhd[-1];
		struct cell_array *axis = stkhd[-2];
		struct cell_func *dst;
	
		CHK(derive_func_opts(&dst, aa, 1), cleanup,
		    L"[92] 		(╠,[axis╣]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		dst->opts[0] = axis;
	
		release_func(aa);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[92] 		(╠,[axis]⍣(lr≢0)╣⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺)╣,[fx],[axis]⍣(rr≢0)⊢⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[92] 		╠(,[axis]⍣(lr≢0)⊢⍺),[fx],[axis]⍣(rr≢0)⊢⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[90] 	axis≠fx←⌈axis:⍺╠{╣");
	return err;
}

int
cdf_ptr8870(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[95] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
cdf_ptr8871(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fx;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_gte)
		CHK(6, cleanup, L"[95] 	lx←{fx╠≥╣⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gte);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{╠fx╣≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{╠fx≥⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥╠⍺╣:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:╠1╣ ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴╠⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴╣⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷╠⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx╠⌷╣⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_fx->value)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx╣⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fx->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ ╠fx⌷⍴⍵╣} ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←╠{fx≥⍺:1 ⋄ fx⌷⍴⍵}╣ ⋄ vc←{⍺≡0:c ⋄ ≢⍵}");
	return err;
}

int
cdf_ptr8872(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_c;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->cdf_c->value)
				CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->cdf_c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
cdf_ptr8873(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_c;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺╠≡╣0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺╣≡0:c ⋄ ≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{╠⍺≡0╣:c ⋄ ≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡╠0╣:c ⋄ ≢⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!lex->cdf_c->value)
				CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:╠c╣ ⋄ ≢⍵}");
			
			*stkhd++ = retain_cell(lex->cdf_c->value);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←{⍺≡0:c ⋄ ╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[95] 	lx←{fx≥⍺:1 ⋄ fx⌷⍴⍵} ⋄ vc←╠{⍺≡0:c ⋄ ≢⍵}╣");
	return err;
}

int
cdf_ptr8874(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	╠s←⍴⍺{lr>rr:⍺ ⋄ ⍵}⍵╣ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
cdf_ptr8875(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rr;
		struct cell_array_box *cdf_lr;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_rr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>╠rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_rr->value);
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr╠>╣rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!lex->cdf_lr->value)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{╠lr╣>rr:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(lex->cdf_lr->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[97] 	s←⍴⍺{╠lr>rr╣:⍺ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:╠⍺╣ ⋄ ⍵}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[97] 	s←⍴⍺{lr>rr:⍺ ⋄ ╠⍵╣}⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[97] 	s←⍴⍺╠{lr>rr:⍺ ⋄ ⍵}╣⍵ ⋄ s[fx]←ax+wx ⋄ c←×⌿fx↑s ⋄ ac←lr vc a ⋄ wc←rr vc w ");
	return err;
}

int
cdf_ptr8876(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_d;
		struct cell_array *cdf_pd;
		struct cell_array *cdf_mx;
		struct cell_array *cdf_nd;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_simple;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_d = NULL;
	loc->cdf_pd = NULL;
	loc->cdf_mx = NULL;
	loc->cdf_nd = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_simple->value)
		CHK(6, cleanup, L"[104]    ╠is_simple╣ ⍵:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    ╠is_simple ⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ 1}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8877, cdf_ptr8878, 0), cleanup, 
				    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[105]    0≡≢,╠⍵╣:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[105]    0≡≢╠,╣⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105]    0≡≢╠,⍵╣:(×d)+d←∇⊃⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[105]    0≡╠≢╣,⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105]    0≡╠≢,⍵╣:(×d)+d←∇⊃⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[105]    0╠≡╣≢,⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[105]    ╠0╣≡≢,⍵:(×d)+d←∇⊃⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[105]    ╠0╣≡≢,⍵:(×d)+d←∇⊃⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[105]    ╠0≡≢,⍵╣:(×d)+d←∇⊃⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[105]    0≡≢,╠⍵╣:(×d)+d←∇⊃⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←∇⊃╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_dis)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←∇╠⊃╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_dis);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:(×d)+d←∇╠⊃⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←╠∇╣⊃⍵");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:(×d)+d←╠∇⊃⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			release_cell(loc->cdf_d);
			loc->cdf_d = retain_cell(stkhd[-1]);
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)╠+╣d←∇⊃⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!loc->cdf_d)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×╠d╣)+d←∇⊃⍵");
			
			*stkhd++ = retain_cell(loc->cdf_d);
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(╠×╣d)+d←∇⊃⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:╠(×d)╣+d←∇⊃⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:╠(×d)+d←∇⊃⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨,╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨╠,╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨╠,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇╠¨╣,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇╣¨,⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇¨╣,⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_d);
	loc->cdf_d = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←╠|╣d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←╠|d←∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pd);
	loc->cdf_pd = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈╠⌿╣pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[106]    nd←1+mx←╠⌈╣⌿pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106]    nd←1+mx←╠⌈⌿╣pd←|d←∇¨,⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←╠⌈⌿pd←|d←∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_mx);
	loc->cdf_mx = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[106]    nd←1╠+╣mx←⌈⌿pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[106]    nd←╠1╣+mx←⌈⌿pd←|d←∇¨,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[106]    nd←╠1╣+mx←⌈⌿pd←|d←∇¨,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106]    nd←╠1+mx←⌈⌿pd←|d←∇¨,⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_nd);
	loc->cdf_nd = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_mx)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠╠mx╣:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_mx);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd╠≠╣mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!loc->cdf_pd)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨╠pd╣≠mx:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_pd);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿(d<0)∨╠pd≠mx╣:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)╠∨╣pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[107]    ∨⌿(d<╠0╣)∨pd≠mx:-nd");
	
		CHK(fill_array(arr, dat), cleanup, L"[107]    ∨⌿(d<╠0╣)∨pd≠mx:-nd");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[107]    ∨⌿(d╠<╣0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_d)
		CHK(6, cleanup, L"[107]    ∨⌿(╠d╣<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_d);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿╠(d<0)╣∨pd≠mx:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿╠(d<0)∨pd≠mx╣:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[107]    ∨╠⌿╣(d<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[107]    ╠∨╣⌿(d<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[107]    ╠∨⌿╣(d<0)∨pd≠mx:-nd");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[107]    ╠∨⌿(d<0)∨pd≠mx╣:-nd");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[107]    ∨⌿(d<0)∨pd≠╠mx╣:-nd");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!loc->cdf_nd)
				CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠mx:-╠nd╣");
			
			*stkhd++ = retain_cell(loc->cdf_nd);
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠mx:╠-╣nd");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[107]    ∨⌿(d<0)∨pd≠mx:╠-nd╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_nd)
		CHK(6, cleanup, L"[108]    ╠nd╣");
	
	*stkhd++ = retain_cell(loc->cdf_nd);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[103] depth←╠{╣");
	return err;
}

int
cdf_ptr8877(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
cdf_ptr8878(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
cdf_ptr8879(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_d;
		struct cell_array *cdf_pd;
		struct cell_array *cdf_mx;
		struct cell_array *cdf_nd;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_simple;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_d = NULL;
	loc->cdf_pd = NULL;
	loc->cdf_mx = NULL;
	loc->cdf_nd = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_simple->value)
		CHK(6, cleanup, L"[104]    ╠is_simple╣ ⍵:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_simple->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    ╠is_simple ⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ╠⍵╣:{0≡≢⍴⍵:0 ⋄ 1}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ 1}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8880, cdf_ptr8881, 0), cleanup, 
				    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[105]    0≡≢,╠⍵╣:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[105]    0≡≢╠,╣⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105]    0≡≢╠,⍵╣:(×d)+d←∇⊃⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[105]    0≡╠≢╣,⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[105]    0≡╠≢,⍵╣:(×d)+d←∇⊃⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[105]    0╠≡╣≢,⍵:(×d)+d←∇⊃⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[105]    ╠0╣≡≢,⍵:(×d)+d←∇⊃⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[105]    ╠0╣≡≢,⍵:(×d)+d←∇⊃⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[105]    ╠0≡≢,⍵╣:(×d)+d←∇⊃⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[105]    0≡≢,╠⍵╣:(×d)+d←∇⊃⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←∇⊃╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_dis)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←∇╠⊃╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_dis);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:(×d)+d←∇╠⊃⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)+d←╠∇╣⊃⍵");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:(×d)+d←╠∇⊃⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			release_cell(loc->cdf_d);
			loc->cdf_d = retain_cell(stkhd[-1]);
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×d)╠+╣d←∇⊃⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!loc->cdf_d)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(×╠d╣)+d←∇⊃⍵");
			
			*stkhd++ = retain_cell(loc->cdf_d);
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[105]    0≡≢,⍵:(╠×╣d)+d←∇⊃⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:╠(×d)╣+d←∇⊃⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[105]    0≡≢,⍵:╠(×d)+d←∇⊃⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨,╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨╠,╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←∇¨╠,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←∇╠¨╣,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_self)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇╣¨,⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇¨╣,⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←|d←╠∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_d);
	loc->cdf_d = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈⌿pd←╠|╣d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←⌈⌿pd←╠|d←∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_pd);
	loc->cdf_pd = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[106]    nd←1+mx←⌈╠⌿╣pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_max)
		CHK(6, cleanup, L"[106]    nd←1+mx←╠⌈╣⌿pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_max);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[106]    nd←1+mx←╠⌈⌿╣pd←|d←∇¨,⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[106]    nd←1+mx←╠⌈⌿pd←|d←∇¨,⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_mx);
	loc->cdf_mx = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[106]    nd←1╠+╣mx←⌈⌿pd←|d←∇¨,⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[106]    nd←╠1╣+mx←⌈⌿pd←|d←∇¨,⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[106]    nd←╠1╣+mx←⌈⌿pd←|d←∇¨,⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[106]    nd←╠1+mx←⌈⌿pd←|d←∇¨,⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_nd);
	loc->cdf_nd = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_mx)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠╠mx╣:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_mx);
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd╠≠╣mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!loc->cdf_pd)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨╠pd╣≠mx:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_pd);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿(d<0)∨╠pd≠mx╣:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[107]    ∨⌿(d<0)╠∨╣pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[107]    ∨⌿(d<╠0╣)∨pd≠mx:-nd");
	
		CHK(fill_array(arr, dat), cleanup, L"[107]    ∨⌿(d<╠0╣)∨pd≠mx:-nd");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[107]    ∨⌿(d╠<╣0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_d)
		CHK(6, cleanup, L"[107]    ∨⌿(╠d╣<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(loc->cdf_d);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿╠(d<0)╣∨pd≠mx:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[107]    ∨⌿╠(d<0)∨pd≠mx╣:-nd");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[107]    ∨╠⌿╣(d<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[107]    ╠∨╣⌿(d<0)∨pd≠mx:-nd");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[107]    ╠∨⌿╣(d<0)∨pd≠mx:-nd");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[107]    ╠∨⌿(d<0)∨pd≠mx╣:-nd");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[107]    ∨⌿(d<0)∨pd≠╠mx╣:-nd");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!loc->cdf_nd)
				CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠mx:-╠nd╣");
			
			*stkhd++ = retain_cell(loc->cdf_nd);
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[107]    ∨⌿(d<0)∨pd≠mx:╠-╣nd");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[107]    ∨⌿(d<0)∨pd≠mx:╠-nd╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_nd)
		CHK(6, cleanup, L"[108]    ╠nd╣");
	
	*stkhd++ = retain_cell(loc->cdf_nd);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[103] depth←╠{╣");
	return err;
}

int
cdf_ptr8880(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
cdf_ptr8881(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢⍴╠⍵╣:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡≢╠⍴╣⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡≢╠⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0≡╠≢╣⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{0≡╠≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[104]    is_simple ⍵:{0╠≡╣≢⍴⍵:0 ⋄ 1}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{╠0╣≡≢⍴⍵:0 ⋄ 1}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[104]    is_simple ⍵:{╠0≡≢⍴⍵╣:0 ⋄ 1}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:╠0╣ ⋄ 1}⍵");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[104]    is_simple ⍵:{0≡≢⍴⍵:0 ⋄ ╠1╣}⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[104]    is_simple ⍵:╠{0≡≢⍴⍵:0 ⋄ 1}╣⍵");
	return err;
}

int
cdf_ptr8882(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴╠⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[114] nqv←{⍬≡╠⍴╣⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡╠⍴⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[114] nqv←{⍬╠≡╣⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] nqv←{╠⍬╣≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[114] nqv←{╠⍬╣≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[114] nqv←{╠⍬≡⍴⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[114] ╠nqv←{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] nqv←{⍬≡⍴⍵:╠1╣ ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
			
				CHK(fill_array(arr, dat), cleanup, L"[114] nqv←{⍬≡⍴⍵:╠1╣ ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃⍴╠⍵╣} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃╠⍴╣⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃╠⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ╠⊃╣⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ╠⊃⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[114] nqv←╠{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	return err;
}

int
cdf_ptr8883(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴╠⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[114] nqv←{⍬≡╠⍴╣⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡╠⍴⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[114] nqv←{⍬╠≡╣⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] nqv←{╠⍬╣≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[114] nqv←{╠⍬╣≡⍴⍵:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[114] nqv←{╠⍬≡⍴⍵╣:1 ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ╠⊃⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[114] nqv←{⍬≡⍴⍵:╠1╣ ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
			
				CHK(fill_array(arr, dat), cleanup, L"[114] nqv←{⍬≡⍴⍵:╠1╣ ⋄ ⊃⍴⍵} ambiv ('nqv_ibeam'⌶)");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃⍴╠⍵╣} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃╠⍴╣⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ⊃╠⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ╠⊃╣⍴⍵} ambiv ('nqv_ibeam'⌶)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[114] nqv←{⍬≡⍴⍵:1 ⋄ ╠⊃⍴⍵╣} ambiv ('nqv_ibeam'⌶)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[114] nqv←╠{⍬≡⍴⍵:1 ⋄ ⊃⍴⍵}╣ ambiv ('nqv_ibeam'⌶)");
	return err;
}

int
cdf_ptr8884(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_any;
		struct cell_func_box *cdf_is_span;
		struct cell_func_box *cdf_is_integer;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[118] 	is_span ╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[118] 	╠is_span╣ ⍺:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[118] 	╠is_span ⍺╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[118] 	is_span ╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[118] 	is_span ⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[119] 	0≡is_integer squeeze ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[119] 	0≡is_integer ╠squeeze╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[119] 	0≡is_integer ╠squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[119] 	0≡╠is_integer╣ squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[119] 	0≡╠is_integer squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[119] 	0╠≡╣is_integer squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[119] 	╠0╣≡is_integer squeeze ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[119] 	╠0╣≡is_integer squeeze ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[119] 	╠0≡is_integer squeeze ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[119] 	0≡is_integer squeeze ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[119] 	0≡is_integer squeeze ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[119] 	0≡is_integer squeeze ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[119] 	0≡is_integer squeeze ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[119] 	0≡is_integer squeeze ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[120] 	0≡≢,╠⍺╣:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[120] 	0≡≢╠,╣⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[120] 	0≡≢╠,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[120] 	0≡╠≢╣,⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[120] 	0≡╠≢,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[120] 	0╠≡╣≢,⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[120] 	╠0╣≡≢,⍺:(⍴⍺)⍴0⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[120] 	╠0╣≡≢,⍺:(⍴⍺)⍴0⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[120] 	╠0≡≢,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[120] 	0≡≢,╠⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴0⍴╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴0╠⍴╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0╣⍴⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0╣⍴⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)╠⍴╣0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴╠⍺╣)⍴0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(╠⍴╣⍺)⍴0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:╠(⍴⍺)╣⍴0⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:╠(⍴⍺)⍴0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢╠,╣⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨⍺>≢╠,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>╠≢╣,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨⍺>╠≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺╠>╣≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨╠⍺╣>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨╠⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[121] 	any(⍺<0)╠∨╣⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[121] 	any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3");
	
		CHK(fill_array(arr, dat), cleanup, L"[121] 	any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[121] 	any(⍺╠<╣0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[121] 	any(╠⍺╣<0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any╠(⍺<0)╣∨⍺>≢,⍵:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any╠(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_any->value)
		CHK(6, cleanup, L"[121] 	╠any╣(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(lex->cdf_any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	╠any(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[121] 	any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL╣ 3");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[121] 	any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL 3╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴⍺('index_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_index_ibeam)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴⍺╠('index_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_index_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴╠⍺╣('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[122] 	(⍴⍺)⍴╠⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[122] 	(⍴⍺)╠⍴╣⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[122] 	(⍴╠⍺╣)⍴⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[122] 	(╠⍴╣⍺)⍴⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[122] 	╠(⍴⍺)╣⍴⍺('index_ibeam'⌶)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[122] 	╠(⍴⍺)⍴⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[117] sqd_vec←╠{╣");
	return err;
}

int
cdf_ptr8885(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_any;
		struct cell_func_box *cdf_is_span;
		struct cell_func_box *cdf_is_integer;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[118] 	is_span ╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[118] 	╠is_span╣ ⍺:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[118] 	╠is_span ⍺╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[118] 	is_span ╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[118] 	is_span ⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[119] 	0≡is_integer squeeze ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[119] 	0≡is_integer ╠squeeze╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[119] 	0≡is_integer ╠squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[119] 	0≡╠is_integer╣ squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[119] 	0≡╠is_integer squeeze ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[119] 	0╠≡╣is_integer squeeze ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[119] 	╠0╣≡is_integer squeeze ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[119] 	╠0╣≡is_integer squeeze ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[119] 	╠0≡is_integer squeeze ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[119] 	0≡is_integer squeeze ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[119] 	0≡is_integer squeeze ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[119] 	0≡is_integer squeeze ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[119] 	0≡is_integer squeeze ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[119] 	0≡is_integer squeeze ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[120] 	0≡≢,╠⍺╣:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[120] 	0≡≢╠,╣⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[120] 	0≡≢╠,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[120] 	0≡╠≢╣,⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[120] 	0≡╠≢,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[120] 	0╠≡╣≢,⍺:(⍴⍺)⍴0⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[120] 	╠0╣≡≢,⍺:(⍴⍺)⍴0⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[120] 	╠0╣≡≢,⍺:(⍴⍺)⍴0⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[120] 	╠0≡≢,⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[120] 	0≡≢,╠⍺╣:(⍴⍺)⍴0⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴0⍴╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴0╠⍴╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0╣⍴⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0╣⍴⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:(⍴⍺)⍴╠0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴⍺)╠⍴╣0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(⍴╠⍺╣)⍴0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[120] 	0≡≢,⍺:(╠⍴╣⍺)⍴0⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:╠(⍴⍺)╣⍴0⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[120] 	0≡≢,⍺:╠(⍴⍺)⍴0⍴⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢╠,╣⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨⍺>≢╠,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>╠≢╣,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨⍺>╠≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺╠>╣≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[121] 	any(⍺<0)∨╠⍺╣>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any(⍺<0)∨╠⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[121] 	any(⍺<0)╠∨╣⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[121] 	any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3");
	
		CHK(fill_array(arr, dat), cleanup, L"[121] 	any(⍺<╠0╣)∨⍺>≢,⍵:⎕SIGNAL 3");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[121] 	any(⍺╠<╣0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[121] 	any(╠⍺╣<0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any╠(⍺<0)╣∨⍺>≢,⍵:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[121] 	any╠(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_any->value)
		CHK(6, cleanup, L"[121] 	╠any╣(⍺<0)∨⍺>≢,⍵:⎕SIGNAL 3");
	
	*stkhd++ = retain_cell(lex->cdf_any->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[121] 	╠any(⍺<0)∨⍺>≢,⍵╣:⎕SIGNAL 3");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[121] 	any(⍺<0)∨⍺>≢,╠⍵╣:⎕SIGNAL 3");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {3};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:⎕SIGNAL ╠3╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[121] 	any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL╣ 3");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[121] 	any(⍺<0)∨⍺>≢,⍵:╠⎕SIGNAL 3╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴⍺('index_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_index_ibeam)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴⍺╠('index_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_index_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[122] 	(⍴⍺)⍴╠⍺╣('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[122] 	(⍴⍺)⍴╠⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[122] 	(⍴⍺)╠⍴╣⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[122] 	(⍴╠⍺╣)⍴⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[122] 	(╠⍴╣⍺)⍴⍺('index_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[122] 	╠(⍴⍺)╣⍴⍺('index_ibeam'⌶)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[122] 	╠(⍴⍺)⍴⍺('index_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[117] sqd_vec←╠{╣");
	return err;
}

int
cdf_ptr8886(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_i;
		struct cell_array *cdf_c;
		struct cell_array_box *cdf_ws;
		struct cell_array *cdf_o;
		struct cell_array *cdf_j;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_sqd_vec;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_i = NULL;
	loc->cdf_c = NULL;
	CHK(mk_array_box(&loc->cdf_ws, NULL), cleanup, L"Init mutable variable: loc->cdf_ws");
	loc->cdf_o = NULL;
	loc->cdf_j = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[124] sqd←{⍺←╠⊢╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[125] 	1≡⍺ ╠1╣:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[125] 	1≡⍺ ╠1╣:⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[125] 	1≡╠⍺╣ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[125] 	1≡╠⍺ 1╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[125] 	1╠≡╣⍺ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[125] 	╠1╣≡⍺ 1:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[125] 	╠1╣≡⍺ 1:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[125] 	╠1≡⍺ 1╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[125] 	1≡⍺ ╠1╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[125] 	1≡⍺ 1:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺╠⊣╣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺╠⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺╣⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢╠⍴╣i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢╠⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠≢╣⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠≢⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8887, cdf_ptr8888, 0), cleanup, 
		    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴╠⍵╣:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢╠⍴╣⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢╠⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡╠≢╣⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡╠≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1╠≡╣≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1╣≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1╣≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8889, cdf_ptr8890, 0), cleanup, 
		    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[127] 	(1≡c←≢╠i╣){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[127] 	(1≡c←╠≢╣i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←╠≢i╣){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[127] 	(1╠≡╣c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(╠1╣≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(╠1╣≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	╠(1≡c←≢i)╣{⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	╠(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴╠⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_sqd_vec->value)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)╠sqd_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_sqd_vec->value);
			
			if (!loc->cdf_i)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃╠i╣)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_i);
			
			if (!cdf_prim.cdf_dis)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(╠⊃╣i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_dis);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:╠(⊃i)╣sqd_vec ⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:╠(⊃i)sqd_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[128] 	c>≢⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[128] 	c>≢╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[128] 	c>≢╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[128] 	c>╠≢╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[128] 	c>╠≢⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[128] 	c╠>╣≢⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[128] 	╠c╣>≢⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[128] 	╠c>≢⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[128] 	c>≢⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[128] 	c>≢⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[128] 	c>≢⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[128] 	c>≢⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[128] 	c>≢⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ws->value);
	loc->cdf_ws->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳╠≢╣ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳╠≢ws←⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳╣≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳≢ws←⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c╠↑╣⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╠c╣↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╠c↑⍳≢ws←⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╠¨╣c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8891, cdf_ptr8892, 2), cleanup, 
		    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	
		k->fv[0] = retain_cell(loc->cdf_ws);
		k->fv[1] = retain_cell(lex->cdf_is_span);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╣c↑⍳≢ws←⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[129] 	i←╠i╣{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[129] 	i←╠i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,╠⌽╣⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,╠⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1╠,╣⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1╣,⌽⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1╣,⌽⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_scf)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×╠⍀╣1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_scf);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×╣⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×⍀╣1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←╠⌽╣×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←╠⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_o);
	loc->cdf_o = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c╠⌷╣o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳╠c╣⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳╠c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴╠⍳╣c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴╠⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)╠⍴╣⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴╠⍵╣)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓╠⍴╣⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓╠⍴⍵)╣⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c╠↓╣⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(╠c╣↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂╠(c↓⍴⍵)╣⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂╠(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),╠⊂╣(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),╠⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o)╠,╣⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_o)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓╠o╣),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_o);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1╠↓╣o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑╠1╣↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑╠1╣↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑╠1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c╠↑╣1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×╠c╣↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×╠c↑1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i╠×╣c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(╠i╣×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿╠(i×c↑1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿╠(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+╠⌿╣(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_oup)
		CHK(6, cleanup, L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_oup);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃╠∘.+⌿╣(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃╠∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[130] 	j←╠⊃╣∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←╠⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_j);
	loc->cdf_j = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[131] 	j sqd_vec ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_sqd_vec->value)
		CHK(6, cleanup, L"[131] 	j ╠sqd_vec╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_sqd_vec->value);
	
	if (!loc->cdf_j)
		CHK(6, cleanup, L"[131] 	╠j╣ sqd_vec ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_j);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[131] 	╠j sqd_vec ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[124] sqd←╠{⍺←⊢╣");
	return err;
}

int
cdf_ptr8887(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺╣⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8888(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8889(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){╠⍺╣:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	(1≡c←╠≢╣i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:╠⍵╣ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	return err;
}

int
cdf_ptr8890(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){╠⍺╣:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	╠(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:╠⍵╣ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	return err;
}

int
cdf_ptr8891(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_ws;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ╠⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[129] 	i←i{╠is_span╣ ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳╣≢ws←⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[╠⍵╣] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				arr->shape[0] = 1;
				CHK(alloc_array(arr), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_brk)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws╠[╣⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_brk);
			
			if (!lex->cdf_ws->value)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳╠ws╣[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(lex->cdf_ws->value);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳╠ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_iot)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:╠⍳╣ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_iot);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:╠⍳ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ╠⍺╣}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	return err;
}

int
cdf_ptr8892(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_ws;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ╠⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[129] 	i←i{╠is_span╣ ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[╠⍵╣] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				arr->shape[0] = 1;
				CHK(alloc_array(arr), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_brk)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws╠[╣⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_brk);
			
			if (!lex->cdf_ws->value)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳╠ws╣[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(lex->cdf_ws->value);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳╠ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_iot)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:╠⍳╣ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_iot);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:╠⍳ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ╠⍺╣}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	return err;
}

int
cdf_ptr8893(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_i;
		struct cell_array *cdf_c;
		struct cell_array_box *cdf_ws;
		struct cell_array *cdf_o;
		struct cell_array *cdf_j;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_sqd_vec;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_i = NULL;
	loc->cdf_c = NULL;
	CHK(mk_array_box(&loc->cdf_ws, NULL), cleanup, L"Init mutable variable: loc->cdf_ws");
	loc->cdf_o = NULL;
	loc->cdf_j = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[125] 	1≡⍺ ╠1╣:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[125] 	1≡⍺ ╠1╣:⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[125] 	1≡╠⍺╣ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[125] 	1≡╠⍺ 1╣:⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[125] 	1≡╠⍺ 1╣:⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[125] 	1╠≡╣⍺ 1:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[125] 	╠1╣≡⍺ 1:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[125] 	╠1╣≡⍺ 1:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[125] 	╠1≡⍺ 1╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[125] 	1≡⍺ ╠1╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[125] 	1≡⍺ 1:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺╠⊣╣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺╣⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺⊣0╣:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢╠⍴╣i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢╠⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠≢╣⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╠≢⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8894, cdf_ptr8895, 0), cleanup, 
		    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣╠0╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL ╠4╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL ╠4╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:╠⎕SIGNAL╣ 4");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:╠⎕SIGNAL 4╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴╠⍵╣:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢╠⍴╣⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢╠⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡╠≢╣⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡╠≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1╠≡╣≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1╣≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1╣≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}╠1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8896, cdf_ptr8897, 0), cleanup, 
		    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = k;
	}
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[127] 	(1≡c←≢╠i╣){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[127] 	(1≡c←╠≢╣i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[127] 	(1≡c←╠≢i╣){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[127] 	(1╠≡╣c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(╠1╣≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(╠1╣≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	╠(1≡c←≢i)╣{⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[127] 	╠(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴╠⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_sqd_vec->value)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)╠sqd_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_sqd_vec->value);
			
			if (!loc->cdf_i)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃╠i╣)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(loc->cdf_i);
			
			if (!cdf_prim.cdf_dis)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(╠⊃╣i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_dis);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:╠(⊃i)╣sqd_vec ⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:╠(⊃i)sqd_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[128] 	c>≢⍴╠⍵╣:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[128] 	c>≢╠⍴╣⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[128] 	c>≢╠⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[128] 	c>╠≢╣⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[128] 	c>╠≢⍴⍵╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[128] 	c╠>╣≢⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[128] 	╠c╣>≢⍴⍵:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[128] 	╠c>≢⍴⍵╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[128] 	c>≢⍴╠⍵╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[128] 	c>≢⍴⍵:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[128] 	c>≢⍴⍵:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[128] 	c>≢⍴⍵:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[128] 	c>≢⍴⍵:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_ws->value);
	loc->cdf_ws->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳╠≢╣ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳╠≢ws←⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳╣≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳≢ws←⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c╠↑╣⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╠c╣↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╠c↑⍳≢ws←⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╠¨╣c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8898, cdf_ptr8899, 2), cleanup, 
		    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	
		k->fv[0] = retain_cell(loc->cdf_ws);
		k->fv[1] = retain_cell(lex->cdf_is_span);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨╣c↑⍳≢ws←⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[129] 	i←╠i╣{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[129] 	i←╠i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽╠⍴╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽╠⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,╠⌽╣⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,╠⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1╠,╣⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1╣,⌽⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1╣,⌽⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀╠1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_scf)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×╠⍀╣1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_scf);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×╣⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×⍀╣1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽╠×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←╠⌽╣×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←╠⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_o);
	loc->cdf_o = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c╠⌷╣o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳╠c╣⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳╠c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴╠⍳╣c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴╠⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)╠⍴╣⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴╠⍵╣)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓╠⍴╣⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c↓╠⍴⍵)╣⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(c╠↓╣⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂(╠c╣↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂╠(c↓⍴⍵)╣⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),⊂╠(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),╠⊂╣(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑1↓o),╠⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓o)╠,╣⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!loc->cdf_o)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1↓╠o╣),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_o);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑1╠↓╣o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑╠1╣↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[130] 	j←⊃∘.+⌿(i×c↑╠1╣↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×c↑╠1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_tke)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×c╠↑╣1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_tke);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i×╠c╣↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿(i×╠c↑1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(i╠×╣c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+⌿(╠i╣×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿╠(i×c↑1↓o)╣,⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[130] 	j←⊃∘.+⌿╠(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[130] 	j←⊃∘.+╠⌿╣(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_oup)
		CHK(6, cleanup, L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_oup);
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃╠∘.+╣⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[130] 	j←⊃╠∘.+⌿╣(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←⊃╠∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[130] 	j←╠⊃╣∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[130] 	j←╠⊃∘.+⌿(i×c↑1↓o),⊂(c↓⍴⍵)⍴⍳c⌷o←⌽×⍀1,⌽⍴⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_j);
	loc->cdf_j = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[131] 	j sqd_vec ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_sqd_vec->value)
		CHK(6, cleanup, L"[131] 	j ╠sqd_vec╣ ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_sqd_vec->value);
	
	if (!loc->cdf_j)
		CHK(6, cleanup, L"[131] 	╠j╣ sqd_vec ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_j);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[131] 	╠j sqd_vec ⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[124] sqd←╠{⍺←⊢╣");
	return err;
}

int
cdf_ptr8894(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←╠⍺⊣0╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0╣:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8895(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡╠⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0╠≡╣⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{╠0╣≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{╠0≡⍵╣:0 ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:╠0╣ ⋄ 1≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡╠⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[126] 	{0≡⍵:0 ⋄ 1╠≡╣⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ ╠1╣≡⍵:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[126] 	{0≡⍵:0 ⋄ ╠1≡⍵╣:0 ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:╠0╣ ⋄ 1}≢⍴i←⍺⊣0:⎕SIGNAL 4");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		CHK(fill_array(arr, dat), cleanup, L"[126] 	{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ ╠1╣}≢⍴i←⍺⊣0:⎕SIGNAL 4");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[126] 	╠{0≡⍵:0 ⋄ 1≡⍵:0 ⋄ 1}╣≢⍴i←⍺⊣0:⎕SIGNAL 4");
	return err;
}

int
cdf_ptr8896(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){╠⍺╣:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	(1≡c←╠≢╣i){⍺:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:╠⍵╣ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	return err;
}

int
cdf_ptr8897(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[127] 	(1≡c←≢i){╠⍺╣:⍵ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[127] 	╠(1≡c←≢i){⍺:⍵ ⋄ 0}1≡≢⍴⍵╣:(⊃i)sqd_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[127] 	(1≡c←≢i){⍺:╠⍵╣ ⋄ 0}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[127] 	(1≡c←≢i){⍺:⍵ ⋄ ╠0╣}1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[127] 	(1≡c←≢i)╠{⍺:⍵ ⋄ 0}╣1≡≢⍴⍵:(⊃i)sqd_vec ⍵");
	return err;
}

int
cdf_ptr8898(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_ws;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ╠⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[129] 	i←i{╠is_span╣ ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑╠⍳╣≢ws←⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[╠⍵╣] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				arr->shape[0] = 1;
				CHK(alloc_array(arr), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_brk)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws╠[╣⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_brk);
			
			if (!lex->cdf_ws->value)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳╠ws╣[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(lex->cdf_ws->value);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳╠ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_iot)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:╠⍳╣ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_iot);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:╠⍳ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ╠⍺╣}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	return err;
}

int
cdf_ptr8899(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_ws;
		struct cell_func_box *cdf_is_span;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ╠⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_span->value)
		CHK(6, cleanup, L"[129] 	i←i{╠is_span╣ ⍺:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_span->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[129] 	i←i{╠is_span ⍺╣:⍳ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[╠⍵╣] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				arr->shape[0] = 1;
				CHK(alloc_array(arr), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳ws╠[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_brk)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws╠[╣⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_brk);
			
			if (!lex->cdf_ws->value)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳╠ws╣[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(lex->cdf_ws->value);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:⍳╠ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_iot)
				CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:╠⍳╣ws[⍵] ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_iot);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[129] 	i←i{is_span ⍺:╠⍳ws[⍵]╣ ⋄ ⍺}¨c↑⍳≢ws←⍴⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[129] 	i←i{is_span ⍺:⍳ws[⍵] ⋄ ╠⍺╣}¨c↑⍳≢ws←⍴⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[129] 	i←i╠{is_span ⍺:⍳ws[⍵] ⋄ ⍺}╣¨c↑⍳≢ws←⍴⍵");
	return err;
}

int
cdf_ptr8900(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[136] 	chk_valid_shape ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_valid_shape->value)
		CHK(6, cleanup, L"[136] 	╠chk_valid_shape╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[136] 	╠chk_valid_shape ⍵╣: ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[136] 	chk_valid_shape ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[137] 	0≡≢,╠⍵╣:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[137] 	0≡≢╠,╣⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[137] 	0≡≢╠,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[137] 	0≡╠≢╣,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[137] 	0≡╠≢,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[137] 	0╠≡╣≢,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[137] 	╠0╣≡≢,⍵:⊂⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[137] 	╠0╣≡≢,⍵:⊂⍬");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[137] 	╠0≡≢,⍵╣:⊂⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[137] 	0≡≢,╠⍵╣:⊂⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[137] 	0≡≢,⍵:⊂╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[137] 	0≡≢,⍵:⊂╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_par)
				CHK(6, cleanup, L"[137] 	0≡≢,⍵:╠⊂╣⍬");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_par);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[137] 	0≡≢,⍵:╠⊂⍬╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[138] 	(,0)≡,╠⍵╣:⍬");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[138] 	(,0)≡╠,╣⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[138] 	(,0)≡╠,⍵╣:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[138] 	(,0)╠≡╣,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[138] 	(,╠0╣)≡,⍵:⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[138] 	(,╠0╣)≡,⍵:⍬");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[138] 	(╠,╣0)≡,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[138] 	╠(,0)╣≡,⍵:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[138] 	╠(,0)≡,⍵╣:⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[138] 	(,0)≡,╠⍵╣:⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[138] 	(,0)≡,⍵:╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[138] 	(,0)≡,⍵:╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[139] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[139] 	1≡≢╠,╣⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[139] 	1≡≢╠,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[139] 	1≡╠≢╣,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[139] 	1≡╠≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[139] 	1╠≡╣≢,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[139] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[139] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[139] 	╠1≡≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[139] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[139] 	1≡≢,⍵:'index_gen_vec'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_index_gen_vec)
				CHK(6, cleanup, L"[139] 	1≡≢,⍵:╠'index_gen_vec'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_index_gen_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[139] 	1≡≢,⍵:╠'index_gen_vec'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[140] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[140] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[140] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[140] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[135] index_gen←╠{╣");
	return err;
}

int
cdf_ptr8901(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_chk_valid_shape;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[136] 	chk_valid_shape ╠⍵╣: ");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_valid_shape->value)
		CHK(6, cleanup, L"[136] 	╠chk_valid_shape╣ ⍵: ");
	
	*stkhd++ = retain_cell(lex->cdf_chk_valid_shape->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[136] 	╠chk_valid_shape ⍵╣: ");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[136] 	chk_valid_shape ╠⍵╣: ");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[137] 	0≡≢,╠⍵╣:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[137] 	0≡≢╠,╣⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[137] 	0≡≢╠,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[137] 	0≡╠≢╣,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[137] 	0≡╠≢,⍵╣:⊂⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[137] 	0╠≡╣≢,⍵:⊂⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[137] 	╠0╣≡≢,⍵:⊂⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[137] 	╠0╣≡≢,⍵:⊂⍬");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[137] 	╠0≡≢,⍵╣:⊂⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[137] 	0≡≢,╠⍵╣:⊂⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[137] 	0≡≢,⍵:⊂╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[137] 	0≡≢,⍵:⊂╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_par)
				CHK(6, cleanup, L"[137] 	0≡≢,⍵:╠⊂╣⍬");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_par);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[137] 	0≡≢,⍵:╠⊂⍬╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[138] 	(,0)≡,╠⍵╣:⍬");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[138] 	(,0)≡╠,╣⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[138] 	(,0)≡╠,⍵╣:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[138] 	(,0)╠≡╣,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[138] 	(,╠0╣)≡,⍵:⍬");
	
		CHK(fill_array(arr, dat), cleanup, L"[138] 	(,╠0╣)≡,⍵:⍬");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[138] 	(╠,╣0)≡,⍵:⍬");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[138] 	╠(,0)╣≡,⍵:⍬");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[138] 	╠(,0)≡,⍵╣:⍬");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[138] 	(,0)≡,╠⍵╣:⍬");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[138] 	(,0)≡,⍵:╠⍬╣");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[138] 	(,0)≡,⍵:╠⍬╣");
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[139] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[139] 	1≡≢╠,╣⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[139] 	1≡≢╠,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[139] 	1≡╠≢╣,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[139] 	1≡╠≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[139] 	1╠≡╣≢,⍵:'index_gen_vec'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[139] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[139] 	╠1╣≡≢,⍵:'index_gen_vec'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[139] 	╠1≡≢,⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[139] 	1≡≢,╠⍵╣:'index_gen_vec'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[139] 	1≡≢,⍵:'index_gen_vec'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_index_gen_vec)
				CHK(6, cleanup, L"[139] 	1≡≢,⍵:╠'index_gen_vec'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_index_gen_vec);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[139] 	1≡≢,⍵:╠'index_gen_vec'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[140] 	⎕SIGNAL ╠16╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[140] 	⎕SIGNAL ╠16╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[140] 	╠⎕SIGNAL╣ 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[140] 	╠⎕SIGNAL 16╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[135] index_gen←╠{╣");
	return err;
}

int
cdf_ptr8902(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[142] index_of←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[142] index_of←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[142] index_of←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[142] index_of←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[142] index_of←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8903(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[142] index_of←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[142] index_of←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[142] index_of←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[142] index_of←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[142] index_of←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8904(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[146] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[146] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8905(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[146] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[146] dis←('disclose_ibeam'⌶) ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[146] dis←('disclose_ibeam'⌶) ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8906(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[150] 	0≡≡╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[150] 	0≡╠≡╣⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[150] 	0≡╠≡⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[150] 	0╠≡╣≡⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	╠0╣≡≡⍵:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[150] 	╠0╣≡≡⍵:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[150] 	╠0≡≡⍵╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[150] 	0≡≡╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[150] 	0≡≡⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[151] 	'enclose_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_enclose_ibeam)
		CHK(6, cleanup, L"[151] 	╠'enclose_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_enclose_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[151] 	╠'enclose_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[149] enclose←╠{╣");
	return err;
}

int
cdf_ptr8907(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[150] 	0≡≡╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[150] 	0≡╠≡╣⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[150] 	0≡╠≡⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[150] 	0╠≡╣≡⍵:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[150] 	╠0╣≡≡⍵:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[150] 	╠0╣≡≡⍵:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[150] 	╠0≡≡⍵╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[150] 	0≡≡╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[150] 	0≡≡⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[151] 	'enclose_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_enclose_ibeam)
		CHK(6, cleanup, L"[151] 	╠'enclose_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_enclose_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[151] 	╠'enclose_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[149] enclose←╠{╣");
	return err;
}

int
cdf_ptr8908(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[153] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[153] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[153] par←enclose ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[153] par←enclose ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[153] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8909(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[153] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[153] par←enclose ambiv {⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[153] par←enclose ambiv {╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[153] par←enclose ambiv {╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[153] par←enclose ambiv ╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8910(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR ╠squeeze╣ ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[156] conjugate←{1289≡⎕DR ╠squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[156] conjugate←{1289≡╠⎕DR╣ squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[156] conjugate←{1289≡╠⎕DR squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[156] conjugate←{1289╠≡╣⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[156] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[156] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[156] conjugate←{╠1289≡⎕DR squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[156] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_conjugate_vec_ibeam)
				CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(cdf_conjugate_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[156] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[156] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣");
	return err;
}

int
cdf_ptr8911(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR ╠squeeze╣ ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[156] conjugate←{1289≡⎕DR ╠squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[156] conjugate←{1289≡╠⎕DR╣ squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[156] conjugate←{1289≡╠⎕DR squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[156] conjugate←{1289╠≡╣⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[156] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[156] conjugate←{╠1289╣≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[156] conjugate←{╠1289≡⎕DR squeeze ⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[156] conjugate←{1289≡⎕DR squeeze ╠⍵╣:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_conjugate_vec_ibeam)
				CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}");
			
			*stkhd++ = retain_cell(cdf_conjugate_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[156] conjugate←{1289≡⎕DR squeeze ⍵:╠'conjugate_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[156] conjugate←{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[156] conjugate←╠{1289≡⎕DR squeeze ⍵:'conjugate_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣");
	return err;
}

int
cdf_ptr8912(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[160] sub←{0-╠⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[160] sub←{0╠-╣⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[160] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[160] sub←{╠0-⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[160] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
cdf_ptr8913(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[160] sub←{0-╠⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[160] sub←{0╠-╣⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[160] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[160] sub←{╠0╣-⍵} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[160] sub←{╠0-⍵╣} ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[160] sub←╠{0-⍵}╣ ambiv ('sub_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
cdf_ptr8914(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[164] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[164] 	1289=╠⎕DR╣ ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[164] 	1289=╠⎕DR ⍵╣:⍵÷|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[164] 	1289╠=╣⎕DR ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[164] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[164] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[164] 	╠1289=⎕DR ⍵╣:⍵÷|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[164] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵÷|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵÷╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[164] 	1289=⎕DR ⍵:⍵÷╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_div)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵╠÷╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_div);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:╠⍵╣÷|⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[164] 	1289=⎕DR ⍵:╠⍵÷|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[165] 	(-⍵<0)+0<╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[165] 	(-⍵<0)+0╠<╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[165] 	(-⍵<0)+╠0╣<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[165] 	(-⍵<0)+╠0╣<⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	(-⍵<0)+╠0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[165] 	(-⍵<0)╠+╣0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[165] 	(-⍵<╠0╣)+0<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[165] 	(-⍵<╠0╣)+0<⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[165] 	(-⍵╠<╣0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[165] 	(-╠⍵╣<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	(-╠⍵<0)╣+0<⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[165] 	(╠-╣⍵<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[165] 	╠(-⍵<0)╣+0<⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	╠(-⍵<0)+0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[163] sign←╠{╣");
	return err;
}

int
cdf_ptr8915(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[164] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[164] 	1289=╠⎕DR╣ ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[164] 	1289=╠⎕DR ⍵╣:⍵÷|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[164] 	1289╠=╣⎕DR ⍵:⍵÷|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[164] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[164] 	╠1289╣=⎕DR ⍵:⍵÷|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[164] 	╠1289=⎕DR ⍵╣:⍵÷|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[164] 	1289=⎕DR ╠⍵╣:⍵÷|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵÷|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵÷╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[164] 	1289=⎕DR ⍵:⍵÷╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_div)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:⍵╠÷╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_div);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[164] 	1289=⎕DR ⍵:╠⍵╣÷|⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[164] 	1289=⎕DR ⍵:╠⍵÷|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[165] 	(-⍵<0)+0<╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[165] 	(-⍵<0)+0╠<╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[165] 	(-⍵<0)+╠0╣<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[165] 	(-⍵<0)+╠0╣<⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	(-⍵<0)+╠0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[165] 	(-⍵<0)╠+╣0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[165] 	(-⍵<╠0╣)+0<⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[165] 	(-⍵<╠0╣)+0<⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[165] 	(-⍵╠<╣0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[165] 	(-╠⍵╣<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	(-╠⍵<0)╣+0<⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[165] 	(╠-╣⍵<0)+0<⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[165] 	╠(-⍵<0)╣+0<⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[165] 	╠(-⍵<0)+0<⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[163] sign←╠{╣");
	return err;
}

int
cdf_ptr8916(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[170] div←{1÷╠⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[170] div←{1╠÷╣⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[170] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[170] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[170] div←{╠1÷⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[170] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
cdf_ptr8917(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[170] div←{1÷╠⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[170] div←{1╠÷╣⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[170] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[170] div←{╠1╣÷⍵} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[170] div←{╠1÷⍵╣} ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[170] div←╠{1÷⍵}╣ ambiv ('div_vec_ibeam'⌶ numeric scalar)");
	return err;
}

int
cdf_ptr8918(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[174] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[174] 	0≡╠is_numeric╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[174] 	0≡╠is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[174] 	0╠≡╣is_numeric ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[174] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[174] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[174] 	╠0≡is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[174] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[174] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[174] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[174] 	0≡is_numeric ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[174] 	0≡is_numeric ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[175] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[175] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[175] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[175] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[175] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[175] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[175] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[175] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[175] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[175] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[175] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[175] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[176] 	'abs_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_abs_vec_ibeam)
		CHK(6, cleanup, L"[176] 	╠'abs_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_abs_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[176] 	╠'abs_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[173] absolute←╠{╣");
	return err;
}

int
cdf_ptr8919(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[174] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_numeric->value)
		CHK(6, cleanup, L"[174] 	0≡╠is_numeric╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(lex->cdf_is_numeric->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[174] 	0≡╠is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[174] 	0╠≡╣is_numeric ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[174] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[174] 	╠0╣≡is_numeric ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[174] 	╠0≡is_numeric ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[174] 	0≡is_numeric ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[174] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[174] 	0≡is_numeric ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[174] 	0≡is_numeric ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[174] 	0≡is_numeric ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[175] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[175] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[175] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[175] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[175] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[175] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[175] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[175] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[175] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[175] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[175] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[175] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[176] 	'abs_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_abs_vec_ibeam)
		CHK(6, cleanup, L"[176] 	╠'abs_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_abs_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[176] 	╠'abs_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[173] absolute←╠{╣");
	return err;
}

int
cdf_ptr8920(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+0=╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+0╠=╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺╠+╣0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷╠⍺╣+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊⍵÷╠⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵╠÷╣⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊╠⍵╣÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊╠⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×╠⌊╣⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×╠⌊⍵÷⍺+0=⍺╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺╠×╣⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-╠⍺╣×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-╠⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[178] residue←{⍵╠-╣⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[178] residue←{╠⍵╣-⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{╠⍵-⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[178] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	return err;
}

int
cdf_ptr8921(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+0=╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+0╠=╣⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0╣=⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊⍵÷⍺+╠0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷⍺╠+╣0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵÷╠⍺╣+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊⍵÷╠⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊⍵╠÷╣⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×⌊╠⍵╣÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×⌊╠⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺×╠⌊╣⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[178] residue←{⍵-⍺×╠⌊⍵÷⍺+0=⍺╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[178] residue←{⍵-⍺╠×╣⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[178] residue←{⍵-╠⍺╣×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{⍵-╠⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[178] residue←{⍵╠-╣⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[178] residue←{╠⍵╣-⍺×⌊⍵÷⍺+0=⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[178] residue←{╠⍵-⍺×⌊⍵÷⍺+0=⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[178] residue←╠{⍵-⍺×⌊⍵÷⍺+0=⍺}╣");
	return err;
}

int
cdf_ptr8922(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[183] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[183] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[183] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[183] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[183] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[184] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[184] 	1289=╠⎕DR╣ ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[184] 	1289=╠⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[184] 	1289╠=╣⎕DR ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[184] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[184] 	╠1289=⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[184] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[184] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[184] 	1289=⎕DR ⍵:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[184] 	1289=⎕DR ⍵:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[185] 	'floor_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_floor_vec_ibeam)
		CHK(6, cleanup, L"[185] 	╠'floor_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_floor_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[185] 	╠'floor_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[182] floor_array←╠{╣");
	return err;
}

int
cdf_ptr8923(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[183] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[183] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[183] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[183] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[183] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[184] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[184] 	1289=╠⎕DR╣ ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[184] 	1289=╠⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[184] 	1289╠=╣⎕DR ⍵:⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[184] 	╠1289╣=⎕DR ⍵:⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[184] 	╠1289=⎕DR ⍵╣:⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[184] 	1289=⎕DR ╠⍵╣:⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[184] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[184] 	1289=⎕DR ⍵:⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[184] 	1289=⎕DR ⍵:╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[184] 	1289=⎕DR ⍵:╠⎕SIGNAL 16╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[185] 	'floor_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_floor_vec_ibeam)
		CHK(6, cleanup, L"[185] 	╠'floor_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_floor_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[185] 	╠'floor_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[182] floor_array←╠{╣");
	return err;
}

int
cdf_ptr8924(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[191] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[191] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[191] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[191] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[191] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[192] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[192] 	1289=╠⎕DR╣ ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[192] 	1289=╠⎕DR ⍵╣:-⌊-⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[192] 	1289╠=╣⎕DR ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[192] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[192] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[192] 	╠1289=⎕DR ⍵╣:-⌊-⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[192] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-⌊-╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-⌊╠-╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:-⌊╠-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_min)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-╠⌊╣-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_min);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:-╠⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:╠-╣⌊-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:╠-⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[193] 	'ceil_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_ceil_vec_ibeam)
		CHK(6, cleanup, L"[193] 	╠'ceil_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_ceil_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[193] 	╠'ceil_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[190] ceil_array←╠{╣");
	return err;
}

int
cdf_ptr8925(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[191] 	is_integer ╠⍵╣:⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[191] 	╠is_integer╣ ⍵:⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[191] 	╠is_integer ⍵╣:⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[191] 	is_integer ╠⍵╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[191] 	is_integer ⍵:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[192] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[192] 	1289=╠⎕DR╣ ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[192] 	1289=╠⎕DR ⍵╣:-⌊-⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[192] 	1289╠=╣⎕DR ⍵:-⌊-⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[192] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[192] 	╠1289╣=⎕DR ⍵:-⌊-⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[192] 	╠1289=⎕DR ⍵╣:-⌊-⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[192] 	1289=⎕DR ╠⍵╣:-⌊-⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-⌊-╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-⌊╠-╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:-⌊╠-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_min)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:-╠⌊╣-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_min);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:-╠⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[192] 	1289=⎕DR ⍵:╠-╣⌊-⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[192] 	1289=⎕DR ⍵:╠-⌊-⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[193] 	'ceil_vec_ibeam'⌶ ╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_ceil_vec_ibeam)
		CHK(6, cleanup, L"[193] 	╠'ceil_vec_ibeam'⌶╣ ⍵");
	
	*stkhd++ = retain_cell(cdf_ceil_vec_ibeam);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[193] 	╠'ceil_vec_ibeam'⌶ ⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[190] ceil_array←╠{╣");
	return err;
}

int
cdf_ptr8926(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[204] pitimes←{3.14159265358979323846×╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[204] pitimes←{3.14159265358979323846╠×╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {3.141592653589793};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] pitimes←{╠3.14159265358979323846×⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] pitimes←╠{3.14159265358979323846×⍵}╣");
	return err;
}

int
cdf_ptr8927(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[204] pitimes←{3.14159265358979323846×╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[204] pitimes←{3.14159265358979323846╠×╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {3.141592653589793};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[204] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[204] pitimes←{╠3.14159265358979323846╣×⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[204] pitimes←{╠3.14159265358979323846×⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[204] pitimes←╠{3.14159265358979323846×⍵}╣");
	return err;
}

int
cdf_ptr8928(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[206] 	0≠≡╠⍺╣:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[206] 	0≠╠≡╣⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[206] 	0≠╠≡⍺╣:⍺ ∇¨⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[206] 	0╠≠╣≡⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	╠0╣≠≡⍺:⍺ ∇¨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[206] 	╠0╣≠≡⍺:⍺ ∇¨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[206] 	╠0≠≡⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[206] 	0≠≡╠⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ∇¨╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_map)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ∇╠¨╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_map);
			
			if (!cdf_self)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ╠∇╣¨⍵");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[206] 	0≠≡⍺:⍺ ╠∇¨╣⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:╠⍺╣ ∇¨⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[206] 	0≠≡⍺:╠⍺ ∇¨⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[207] 	0=╠⍺╣:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[207] 	0╠=╣⍺:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[207] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[207] 	╠0=⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[207] 	0=╠⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:(1-╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[207] 	0=⍺:(1╠-╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:╠(1-⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:╠(1-⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[208] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[208] 	1╠=╣⍺:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[208] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[208] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[208] 	╠1=⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[208] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[208] 	1=⍺:'sin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_sin_vec_ibeam)
				CHK(6, cleanup, L"[208] 	1=⍺:╠'sin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_sin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[208] 	1=⍺:╠'sin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[209] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[209] 	¯1╠=╣⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[209] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[209] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[209] 	╠¯1=⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[209] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[209] 	¯1=⍺:'arcsin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arcsin_vec_ibeam)
				CHK(6, cleanup, L"[209] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arcsin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[209] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[210] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[210] 	2╠=╣⍺:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[210] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[210] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[210] 	╠2=⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[210] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[210] 	2=⍺:'cos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_cos_vec_ibeam)
				CHK(6, cleanup, L"[210] 	2=⍺:╠'cos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_cos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[210] 	2=⍺:╠'cos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[211] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[211] 	¯2╠=╣⍺:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[211] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[211] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[211] 	╠¯2=⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[211] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[211] 	¯2=⍺:'arccos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arccos_vec_ibeam)
				CHK(6, cleanup, L"[211] 	¯2=⍺:╠'arccos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arccos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[211] 	¯2=⍺:╠'arccos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[212] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[212] 	3╠=╣⍺:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[212] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[212] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[212] 	╠3=⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[212] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[212] 	3=⍺:'tan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_tan_vec_ibeam)
				CHK(6, cleanup, L"[212] 	3=⍺:╠'tan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_tan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[212] 	3=⍺:╠'tan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[213] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[213] 	¯3╠=╣⍺:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[213] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[213] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[213] 	╠¯3=⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[213] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[213] 	¯3=⍺:'arctan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arctan_vec_ibeam)
				CHK(6, cleanup, L"[213] 	¯3=⍺:╠'arctan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arctan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[213] 	¯3=⍺:╠'arctan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[214] 	4=╠⍺╣:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[214] 	4╠=╣⍺:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[214] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	╠4=⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[214] 	4=╠⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:(1+╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[214] 	4=⍺:(1╠+╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:╠(1+⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:╠(1+⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[215] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[215] 	¯4╠=╣⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[215] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	╠¯4=⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[215] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵╠+╣1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵╣+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_div)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)╠÷╣⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_div);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵╠-╣1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((╠⍵╣-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×(╠(⍵-1)╣÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)╠×╣((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵╠+╣1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(╠⍵╣+1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:╠(⍵+1)╣×((⍵-1)÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:╠(⍵+1)×((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[216] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[216] 	5╠=╣⍺:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[216] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[216] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[216] 	╠5=⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[216] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[216] 	5=⍺:'sinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_sinh_vec_ibeam)
				CHK(6, cleanup, L"[216] 	5=⍺:╠'sinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_sinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[216] 	5=⍺:╠'sinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[217] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[217] 	¯5╠=╣⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[217] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[217] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[217] 	╠¯5=⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[217] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[217] 	¯5=⍺:'arcsinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arcsinh_vec_ibeam)
				CHK(6, cleanup, L"[217] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arcsinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[217] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[218] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[218] 	6╠=╣⍺:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[218] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[218] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[218] 	╠6=⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[218] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[218] 	6=⍺:'cosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_cosh_vec_ibeam)
				CHK(6, cleanup, L"[218] 	6=⍺:╠'cosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_cosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[218] 	6=⍺:╠'cosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[219] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[219] 	¯6╠=╣⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[219] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[219] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[219] 	╠¯6=⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[219] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[219] 	¯6=⍺:'arccosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arccosh_vec_ibeam)
				CHK(6, cleanup, L"[219] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arccosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[219] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[220] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[220] 	7╠=╣⍺:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[220] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[220] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[220] 	╠7=⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[220] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[220] 	7=⍺:'tanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_tanh_vec_ibeam)
				CHK(6, cleanup, L"[220] 	7=⍺:╠'tanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_tanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[220] 	7=⍺:╠'tanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[221] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[221] 	¯7╠=╣⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[221] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[221] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[221] 	╠¯7=⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[221] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[221] 	¯7=⍺:'arctanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arctanh_vec_ibeam)
				CHK(6, cleanup, L"[221] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arctanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[221] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[222] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[222] 	8╠=╣⍺:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[222] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[222] 	╠8=⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[222] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+⍵*2)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+⍵╠*╣2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+╠⍵╣*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:(-1+╠⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1╠+╣⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:(-╠1+⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[222] 	8=⍺:(╠-╣1+⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[222] 	8=⍺:╠(-1+⍵*2)╣*0.5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:╠(-1+⍵*2)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[223] 	¯8=╠⍺╣:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[223] 	¯8╠=╣⍺:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[223] 	╠¯8╣=⍺:-8○⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[223] 	╠¯8╣=⍺:-8○⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[223] 	╠¯8=⍺╣:-8○⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[223] 	¯8=╠⍺╣:-8○⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[223] 	¯8=⍺:-8○╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cir)
				CHK(6, cleanup, L"[223] 	¯8=⍺:-8╠○╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cir);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {8};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[223] 	¯8=⍺:-╠8╣○⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[223] 	¯8=⍺:-╠8╣○⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[223] 	¯8=⍺:-╠8○⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[223] 	¯8=⍺:╠-╣8○⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[223] 	¯8=⍺:╠-8○⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[224] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[224] 	9╠=╣⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	╠9=⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8929, cdf_ptr8930, 0), cleanup, 
				    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[225] 	¯9=╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[225] 	¯9╠=╣⍺:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[225] 	╠¯9╣=⍺:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[225] 	╠¯9╣=⍺:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[225] 	╠¯9=⍺╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[225] 	¯9=╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[225] 	¯9=⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[226] 	10=╠⍺╣:|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[226] 	10╠=╣⍺:|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[226] 	╠10╣=⍺:|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[226] 	╠10╣=⍺:|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[226] 	╠10=⍺╣:|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[226] 	10=╠⍺╣:|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[226] 	10=⍺:|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[226] 	10=⍺:╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[226] 	10=⍺:╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[227] 	¯10=╠⍺╣:+⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[227] 	¯10╠=╣⍺:+⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[227] 	╠¯10╣=⍺:+⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[227] 	╠¯10╣=⍺:+⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	╠¯10=⍺╣:+⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[227] 	¯10=╠⍺╣:+⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[227] 	¯10=⍺:+╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[227] 	¯10=⍺:╠+╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[227] 	¯10=⍺:╠+⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[228] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[228] 	11╠=╣⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	╠11=⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8931, cdf_ptr8932, 0), cleanup, 
				    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[229] 	¯11=╠⍺╣:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[229] 	¯11╠=╣⍺:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] 	╠¯11╣=⍺:⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[229] 	╠¯11╣=⍺:⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[229] 	╠¯11=⍺╣:⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[229] 	¯11=╠⍺╣:⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] 	¯11=⍺:⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[229] 	¯11=⍺:⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[229] 	¯11=⍺:⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[229] 	¯11=⍺:╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[229] 	¯11=⍺:╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[230] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[230] 	12╠=╣⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[230] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[230] 	╠12=⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[230] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CHAR16;
				unsigned int rnk = 1;
				uint16_t dat[] = {80, 72, 65, 83, 69, 40, 9077, 41, 32, 78, 79, 84, 32, 73, 77, 80, 76, 69, 77, 69, 78, 84, 69, 68};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				arr->shape[0] = 24;
			
				CHK(fill_array(arr, dat), cleanup, L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[231] 	¯12=╠⍺╣:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[231] 	¯12╠=╣⍺:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[231] 	╠¯12╣=⍺:*⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[231] 	╠¯12╣=⍺:*⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[231] 	╠¯12=⍺╣:*⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[231] 	¯12=╠⍺╣:*⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[231] 	¯12=⍺:*⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[231] 	¯12=⍺:*⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[231] 	¯12=⍺:*⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[231] 	¯12=⍺:*╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[231] 	¯12=⍺:*╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[231] 	¯12=⍺:╠*╣⍵×0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[231] 	¯12=⍺:╠*⍵×0J1╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[232] 	⎕SIGNAL ╠11╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[232] 	⎕SIGNAL ╠11╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[232] 	╠⎕SIGNAL╣ 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[232] 	╠⎕SIGNAL 11╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[205] trig←╠{╣");
	return err;
}

int
cdf_ptr8929(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[224] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_realpart_vec_ibeam)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
cdf_ptr8930(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[224] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_realpart_vec_ibeam)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
cdf_ptr8931(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_imagpart_vec_ibeam)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
cdf_ptr8932(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_imagpart_vec_ibeam)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
cdf_ptr8933(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[206] 	0≠≡╠⍺╣:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[206] 	0≠╠≡╣⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[206] 	0≠╠≡⍺╣:⍺ ∇¨⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[206] 	0╠≠╣≡⍺:⍺ ∇¨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[206] 	╠0╣≠≡⍺:⍺ ∇¨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[206] 	╠0╣≠≡⍺:⍺ ∇¨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[206] 	╠0≠≡⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[206] 	0≠≡╠⍺╣:⍺ ∇¨⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ∇¨╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_map)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ∇╠¨╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_map);
			
			if (!cdf_self)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:⍺ ╠∇╣¨⍵");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[206] 	0≠≡⍺:⍺ ╠∇¨╣⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[206] 	0≠≡⍺:╠⍺╣ ∇¨⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[206] 	0≠≡⍺:╠⍺ ∇¨⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[207] 	0=╠⍺╣:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[207] 	0╠=╣⍺:(1-⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[207] 	╠0╣=⍺:(1-⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[207] 	╠0=⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[207] 	0=╠⍺╣:(1-⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(1-⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(1-⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[207] 	0=⍺:(1-╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:(1-╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[207] 	0=⍺:(1╠-╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[207] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[207] 	0=⍺:(╠1╣-⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:╠(1-⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[207] 	0=⍺:╠(1-⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[208] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[208] 	1╠=╣⍺:'sin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[208] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[208] 	╠1╣=⍺:'sin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[208] 	╠1=⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[208] 	1=╠⍺╣:'sin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[208] 	1=⍺:'sin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_sin_vec_ibeam)
				CHK(6, cleanup, L"[208] 	1=⍺:╠'sin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_sin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[208] 	1=⍺:╠'sin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[209] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[209] 	¯1╠=╣⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[209] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[209] 	╠¯1╣=⍺:'arcsin_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[209] 	╠¯1=⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[209] 	¯1=╠⍺╣:'arcsin_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[209] 	¯1=⍺:'arcsin_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arcsin_vec_ibeam)
				CHK(6, cleanup, L"[209] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arcsin_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[209] 	¯1=⍺:╠'arcsin_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[210] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[210] 	2╠=╣⍺:'cos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[210] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[210] 	╠2╣=⍺:'cos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[210] 	╠2=⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[210] 	2=╠⍺╣:'cos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[210] 	2=⍺:'cos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_cos_vec_ibeam)
				CHK(6, cleanup, L"[210] 	2=⍺:╠'cos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_cos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[210] 	2=⍺:╠'cos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[211] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[211] 	¯2╠=╣⍺:'arccos_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[211] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[211] 	╠¯2╣=⍺:'arccos_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[211] 	╠¯2=⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[211] 	¯2=╠⍺╣:'arccos_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[211] 	¯2=⍺:'arccos_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arccos_vec_ibeam)
				CHK(6, cleanup, L"[211] 	¯2=⍺:╠'arccos_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arccos_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[211] 	¯2=⍺:╠'arccos_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[212] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[212] 	3╠=╣⍺:'tan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[212] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[212] 	╠3╣=⍺:'tan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[212] 	╠3=⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[212] 	3=╠⍺╣:'tan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[212] 	3=⍺:'tan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_tan_vec_ibeam)
				CHK(6, cleanup, L"[212] 	3=⍺:╠'tan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_tan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[212] 	3=⍺:╠'tan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[213] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[213] 	¯3╠=╣⍺:'arctan_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[213] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[213] 	╠¯3╣=⍺:'arctan_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[213] 	╠¯3=⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[213] 	¯3=╠⍺╣:'arctan_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[213] 	¯3=⍺:'arctan_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arctan_vec_ibeam)
				CHK(6, cleanup, L"[213] 	¯3=⍺:╠'arctan_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arctan_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[213] 	¯3=⍺:╠'arctan_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[214] 	4=╠⍺╣:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[214] 	4╠=╣⍺:(1+⍵*2)*.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[214] 	╠4╣=⍺:(1+⍵*2)*.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[214] 	╠4=⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[214] 	4=╠⍺╣:(1+⍵*2)*.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(1+⍵*2)*╠.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+⍵*2)╠*╣.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(1+⍵*╠2╣)*.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+⍵╠*╣2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[214] 	4=⍺:(1+╠⍵╣*2)*.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:(1+╠⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[214] 	4=⍺:(1╠+╣⍵*2)*.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[214] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[214] 	4=⍺:(╠1╣+⍵*2)*.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:╠(1+⍵*2)╣*.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[214] 	4=⍺:╠(1+⍵*2)*.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[215] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[215] 	¯4╠=╣⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-4};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[215] 	╠¯4╣=⍺:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[215] 	╠¯4=⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[215] 	¯4=╠⍺╣:(⍵+1)×((⍵-1)÷⍵+1)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+1)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵+╠1╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷⍵╠+╣1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵╣+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)÷╠⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_div)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-1)╠÷╣⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_div);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵-╠1╣)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((⍵╠-╣1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)×((╠⍵╣-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×(╠(⍵-1)╣÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:(⍵+1)×╠((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵+1)╠×╣((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[215] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[215] 	¯4=⍺:(⍵+╠1╣)×((⍵-1)÷⍵+1)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(⍵╠+╣1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[215] 	¯4=⍺:(╠⍵╣+1)×((⍵-1)÷⍵+1)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:╠(⍵+1)╣×((⍵-1)÷⍵+1)*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[215] 	¯4=⍺:╠(⍵+1)×((⍵-1)÷⍵+1)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[216] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[216] 	5╠=╣⍺:'sinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[216] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[216] 	╠5╣=⍺:'sinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[216] 	╠5=⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[216] 	5=╠⍺╣:'sinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[216] 	5=⍺:'sinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_sinh_vec_ibeam)
				CHK(6, cleanup, L"[216] 	5=⍺:╠'sinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_sinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[216] 	5=⍺:╠'sinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[217] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[217] 	¯5╠=╣⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-5};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[217] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[217] 	╠¯5╣=⍺:'arcsinh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[217] 	╠¯5=⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[217] 	¯5=╠⍺╣:'arcsinh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[217] 	¯5=⍺:'arcsinh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arcsinh_vec_ibeam)
				CHK(6, cleanup, L"[217] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arcsinh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[217] 	¯5=⍺:╠'arcsinh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[218] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[218] 	6╠=╣⍺:'cosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[218] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[218] 	╠6╣=⍺:'cosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[218] 	╠6=⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[218] 	6=╠⍺╣:'cosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[218] 	6=⍺:'cosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_cosh_vec_ibeam)
				CHK(6, cleanup, L"[218] 	6=⍺:╠'cosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_cosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[218] 	6=⍺:╠'cosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[219] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[219] 	¯6╠=╣⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-6};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[219] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[219] 	╠¯6╣=⍺:'arccosh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[219] 	╠¯6=⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[219] 	¯6=╠⍺╣:'arccosh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[219] 	¯6=⍺:'arccosh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arccosh_vec_ibeam)
				CHK(6, cleanup, L"[219] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arccosh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[219] 	¯6=⍺:╠'arccosh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[220] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[220] 	7╠=╣⍺:'tanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[220] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[220] 	╠7╣=⍺:'tanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[220] 	╠7=⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[220] 	7=╠⍺╣:'tanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[220] 	7=⍺:'tanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_tanh_vec_ibeam)
				CHK(6, cleanup, L"[220] 	7=⍺:╠'tanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_tanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[220] 	7=⍺:╠'tanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[221] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[221] 	¯7╠=╣⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-7};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[221] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[221] 	╠¯7╣=⍺:'arctanh_vec_ibeam'⌶ ⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[221] 	╠¯7=⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[221] 	¯7=╠⍺╣:'arctanh_vec_ibeam'⌶ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[221] 	¯7=⍺:'arctanh_vec_ibeam'⌶ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_arctanh_vec_ibeam)
				CHK(6, cleanup, L"[221] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_arctanh_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[221] 	¯7=⍺:╠'arctanh_vec_ibeam'⌶ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[222] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[222] 	8╠=╣⍺:(-1+⍵*2)*0.5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		CHK(fill_array(arr, dat), cleanup, L"[222] 	╠8╣=⍺:(-1+⍵*2)*0.5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[222] 	╠8=⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[222] 	8=╠⍺╣:(-1+⍵*2)*0.5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_DBL;
				unsigned int rnk = 0;
				double dat[] = {0.5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-1+⍵*2)*╠0.5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+⍵*2)╠*╣0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {2};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-1+⍵*╠2╣)*0.5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+⍵╠*╣2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1+╠⍵╣*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:(-1+╠⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[222] 	8=⍺:(-1╠+╣⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {1};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[222] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				CHK(fill_array(arr, dat), cleanup, L"[222] 	8=⍺:(-╠1╣+⍵*2)*0.5");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:(-╠1+⍵*2)╣*0.5");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[222] 	8=⍺:(╠-╣1+⍵*2)*0.5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[222] 	8=⍺:╠(-1+⍵*2)╣*0.5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[222] 	8=⍺:╠(-1+⍵*2)*0.5╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[223] 	¯8=╠⍺╣:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[223] 	¯8╠=╣⍺:-8○⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-8};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[223] 	╠¯8╣=⍺:-8○⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[223] 	╠¯8╣=⍺:-8○⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[223] 	╠¯8=⍺╣:-8○⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[223] 	¯8=╠⍺╣:-8○⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[223] 	¯8=⍺:-8○╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cir)
				CHK(6, cleanup, L"[223] 	¯8=⍺:-8╠○╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cir);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {8};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[223] 	¯8=⍺:-╠8╣○⍵");
			
				CHK(fill_array(arr, dat), cleanup, L"[223] 	¯8=⍺:-╠8╣○⍵");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[223] 	¯8=⍺:-╠8○⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_sub)
				CHK(6, cleanup, L"[223] 	¯8=⍺:╠-╣8○⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_sub);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[223] 	¯8=⍺:╠-8○⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[224] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[224] 	9╠=╣⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	╠9╣=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	╠9=⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=╠⍺╣:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8934, cdf_ptr8935, 0), cleanup, 
				    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[225] 	¯9=╠⍺╣:⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[225] 	¯9╠=╣⍺:⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-9};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[225] 	╠¯9╣=⍺:⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[225] 	╠¯9╣=⍺:⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[225] 	╠¯9=⍺╣:⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[225] 	¯9=╠⍺╣:⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[225] 	¯9=⍺:╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[226] 	10=╠⍺╣:|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[226] 	10╠=╣⍺:|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[226] 	╠10╣=⍺:|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[226] 	╠10╣=⍺:|⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[226] 	╠10=⍺╣:|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[226] 	10=╠⍺╣:|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[226] 	10=⍺:|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[226] 	10=⍺:╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[226] 	10=⍺:╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[227] 	¯10=╠⍺╣:+⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[227] 	¯10╠=╣⍺:+⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-10};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[227] 	╠¯10╣=⍺:+⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[227] 	╠¯10╣=⍺:+⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[227] 	╠¯10=⍺╣:+⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[227] 	¯10=╠⍺╣:+⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[227] 	¯10=⍺:+╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[227] 	¯10=⍺:╠+╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[227] 	¯10=⍺:╠+⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[228] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[228] 	11╠=╣⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	╠11╣=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	╠11=⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=╠⍺╣:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8936, cdf_ptr8937, 0), cleanup, 
				    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
			
				*stkhd++ = k;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[229] 	¯11=╠⍺╣:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[229] 	¯11╠=╣⍺:⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] 	╠¯11╣=⍺:⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[229] 	╠¯11╣=⍺:⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[229] 	╠¯11=⍺╣:⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[229] 	¯11=╠⍺╣:⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[229] 	¯11=⍺:⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[229] 	¯11=⍺:⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[229] 	¯11=⍺:⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[229] 	¯11=⍺:╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[229] 	¯11=⍺:╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[230] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[230] 	12╠=╣⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		CHK(fill_array(arr, dat), cleanup, L"[230] 	╠12╣=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[230] 	╠12=⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[230] 	12=╠⍺╣:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {16};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL ╠16╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[230] 	12=⍺:'PHASE(⍵) NOT IMPLEMENTED'╠⎕SIGNAL╣ 16");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CHAR16;
				unsigned int rnk = 1;
				uint16_t dat[] = {80, 72, 65, 83, 69, 40, 9077, 41, 32, 78, 79, 84, 32, 73, 77, 80, 76, 69, 77, 69, 78, 84, 69, 68};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				arr->shape[0] = 24;
			
				CHK(fill_array(arr, dat), cleanup, L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'╣⎕SIGNAL 16");
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[230] 	12=⍺:╠'PHASE(⍵) NOT IMPLEMENTED'⎕SIGNAL 16╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[231] 	¯12=╠⍺╣:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[231] 	¯12╠=╣⍺:*⍵×0J1");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[231] 	╠¯12╣=⍺:*⍵×0J1");
	
		CHK(fill_array(arr, dat), cleanup, L"[231] 	╠¯12╣=⍺:*⍵×0J1");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[231] 	╠¯12=⍺╣:*⍵×0J1");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[231] 	¯12=╠⍺╣:*⍵×0J1");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_CMPX;
				unsigned int rnk = 0;
				struct apl_cmpx dat[] = {{0, 1}};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[231] 	¯12=⍺:*⍵×╠0J1╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[231] 	¯12=⍺:*⍵×╠0J1╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[231] 	¯12=⍺:*⍵╠×╣0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[231] 	¯12=⍺:*╠⍵╣×0J1");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[231] 	¯12=⍺:*╠⍵×0J1╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_exp)
				CHK(6, cleanup, L"[231] 	¯12=⍺:╠*╣⍵×0J1");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_exp);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[231] 	¯12=⍺:╠*⍵×0J1╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[232] 	⎕SIGNAL ╠11╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[232] 	⎕SIGNAL ╠11╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[232] 	╠⎕SIGNAL╣ 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[232] 	╠⎕SIGNAL 11╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[205] trig←╠{╣");
	return err;
}

int
cdf_ptr8934(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[224] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_realpart_vec_ibeam)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
cdf_ptr8935(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ╠⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡╠⎕DR╣ ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[224] 	9=⍺:{1289≡╠⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289╠≡╣⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[224] 	9=⍺:{╠1289╣≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[224] 	9=⍺:{╠1289≡⎕DR ⍵╣:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ╠⍵╣ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_realpart_vec_ibeam)
				CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶╣ ⍵ ⋄ ⍵}⍵");
			
			*stkhd++ = retain_cell(cdf_realpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[224] 	9=⍺:{1289≡⎕DR ⍵:╠'realpart_vec_ibeam'⌶ ⍵╣ ⋄ ⍵}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[224] 	9=⍺:{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ╠⍵╣}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[224] 	9=⍺:╠{1289≡⎕DR ⍵:'realpart_vec_ibeam'⌶ ⍵ ⋄ ⍵}╣⍵");
	return err;
}

int
cdf_ptr8936(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵╣");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_imagpart_vec_ibeam)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
cdf_ptr8937(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ╠⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡╠⎕DR╣ ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡╠⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289╠≡╣⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{╠1289╣≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{╠1289≡⎕DR ⍵╣:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ╠⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_imagpart_vec_ibeam)
				CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶╣ ⍵ ⋄ (⍴⍵)⍴0}⍵");
			
			*stkhd++ = retain_cell(cdf_imagpart_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[228] 	11=⍺:{1289≡⎕DR ⍵:╠'imagpart_vec_ibeam'⌶ ⍵╣ ⋄ (⍴⍵)⍴0}⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴╠0╣}⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)╠⍴╣0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴╠⍵╣)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (╠⍴╣⍵)⍴0}⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)╣⍴0}⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[228] 	11=⍺:{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ ╠(⍴⍵)⍴0╣}⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[228] 	11=⍺:╠{1289≡⎕DR ⍵:'imagpart_vec_ibeam'⌶ ⍵ ⋄ (⍴⍵)⍴0}╣⍵");
	return err;
}

int
cdf_ptr8938(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊╣⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺╠≠╣⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺╣≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)╠∧╣⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵╠<╣0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(╠⍵╣<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)╣∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)╠∧╣(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊╠⍵╣)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=╠⌊╣⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=╠⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵╠=╣⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(╠⍵╣=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,╠(⍵=⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,╠(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[238] 	∨⌿╠,╣(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿╠,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[238] 	∨╠⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[238] 	╠∨╣⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[238] 	╠∨⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	╠∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!⍵-╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!⍵╠-╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!╠⍵╣-⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	(!⍵)÷(!⍺)×!╠⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×╠!╣⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	(!⍵)÷(!⍺)×╠!⍵-⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)╠×╣!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!╠⍺╣)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(╠!╣⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	(!⍵)÷╠(!⍺)╣×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	(!⍵)÷╠(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[239] 	(!⍵)╠÷╣(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[239] 	(!╠⍵╣)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(╠!╣⍵)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	╠(!⍵)╣÷(!⍺)×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	╠(!⍵)÷(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] binomial←╠{╣");
	return err;
}

int
cdf_ptr8939(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_alpha)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊╣⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠╠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺╠≠╣⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺╣≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧╠⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)╠∧╣⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<╠0╣)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵╠<╣0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(╠⍵╣<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)╣∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=⌊⍵)∧╠(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)╠∧╣(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊╠⍵╣)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵=╠⌊╣⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿,(⍵=╠⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[238] 	∨⌿,(⍵╠=╣⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[238] 	∨⌿,(╠⍵╣=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,╠(⍵=⌊⍵)╣∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[238] 	∨⌿,╠(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[238] 	∨⌿╠,╣(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	∨⌿╠,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[238] 	∨╠⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[238] 	╠∨╣⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[238] 	╠∨⌿╣,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL 11");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[238] 	╠∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[238] 	∨⌿,(⍵=⌊⍵)∧(⍵<0)∧⍺≠⌊⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!⍵-╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!⍵╠-╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×!╠⍵╣-⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	(!⍵)÷(!⍺)×!╠⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)×╠!╣⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	(!⍵)÷(!⍺)×╠!⍵-⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!⍺)╠×╣!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(!╠⍺╣)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(!⍵)÷(╠!╣⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	(!⍵)÷╠(!⍺)╣×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	(!⍵)÷╠(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[239] 	(!⍵)╠÷╣(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[239] 	(!╠⍵╣)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_fac)
		CHK(6, cleanup, L"[239] 	(╠!╣⍵)÷(!⍺)×!⍵-⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_fac);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[239] 	╠(!⍵)╣÷(!⍺)×!⍵-⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[239] 	╠(!⍵)÷(!⍺)×!⍵-⍺╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[237] binomial←╠{╣");
	return err;
}

int
cdf_ptr8940(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_is_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[244] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[244] notscl←{╠is_bool╣ ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[244] notscl←{╠is_bool ⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[244] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_not_vec_ibeam)
				CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_not_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[244] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[244] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8941(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_is_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[244] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[244] notscl←{╠is_bool╣ ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[244] notscl←{╠is_bool ⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[244] notscl←{is_bool ╠⍵╣:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_not_vec_ibeam)
				CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶╣ ⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_not_vec_ibeam);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[244] notscl←{is_bool ⍵:╠'not_vec_ibeam'⌶ ⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[244] notscl←{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[244] notscl←╠{is_bool ⍵:'not_vec_ibeam'⌶ ⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8942(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] without←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] without←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[245] without←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] without←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] without←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8943(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[245] without←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[245] without←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[245] without←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[245] without←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[245] without←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8944(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_z = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[250] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[250] 	⍺ ╠both_bool╣ ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[250] 	╠⍺╣ both_bool ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[250] 	╠⍺ both_bool ⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[250] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:⍺('and_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_and_vec_ibeam)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:⍺╠('and_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(cdf_and_vec_ibeam);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:╠⍺╣('and_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[250] 	⍺ both_bool ⍵:╠⍺('and_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←⍺∨╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←⍺╠∨╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←╠⍺╣∨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷z+0=z←╠⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z);
	loc->cdf_z = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0╠=╣z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[251] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[251] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷z+╠0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z╠+╣0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_z)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷╠z╣+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷╠z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[251] 	⍺×⍵╠÷╣z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[251] 	⍺×╠⍵╣÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×╠⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[251] 	⍺╠×╣⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[251] 	╠⍺╣×⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	╠⍺×⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[249] logand←╠{╣");
	return err;
}

int
cdf_ptr8945(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	loc->cdf_z = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[250] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[250] 	⍺ ╠both_bool╣ ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[250] 	╠⍺╣ both_bool ⍵:⍺('and_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[250] 	╠⍺ both_bool ⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[250] 	⍺ both_bool ╠⍵╣:⍺('and_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:⍺('and_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_and_vec_ibeam)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:⍺╠('and_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(cdf_and_vec_ibeam);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[250] 	⍺ both_bool ⍵:╠⍺╣('and_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[250] 	⍺ both_bool ⍵:╠⍺('and_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←⍺∨╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←⍺╠∨╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0=z←╠⍺╣∨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷z+0=z←╠⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z);
	loc->cdf_z = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z+0╠=╣z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[251] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[251] 	⍺×⍵÷z+╠0╣=z←⍺∨⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷z+╠0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷z╠+╣0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_z)
		CHK(6, cleanup, L"[251] 	⍺×⍵÷╠z╣+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×⍵÷╠z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[251] 	⍺×⍵╠÷╣z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[251] 	⍺×╠⍵╣÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	⍺×╠⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[251] 	⍺╠×╣⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[251] 	╠⍺╣×⍵÷z+0=z←⍺∨⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[251] 	╠⍺×⍵÷z+0=z←⍺∨⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[249] logand←╠{╣");
	return err;
}

int
cdf_ptr8946(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[253] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[253] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[253] and←{╠⎕SIGNAL╣ 2}ambiv (logand numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[253] and←{╠⎕SIGNAL 2╣}ambiv (logand numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[253] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	return err;
}

int
cdf_ptr8947(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[253] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[253] and←{⎕SIGNAL ╠2╣}ambiv (logand numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[253] and←{╠⎕SIGNAL╣ 2}ambiv (logand numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[253] and←{╠⎕SIGNAL 2╣}ambiv (logand numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[253] and←╠{⎕SIGNAL 2}╣ambiv (logand numeric scalar)");
	return err;
}

int
cdf_ptr8948(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
		struct cell_func_box *cdf_both_integer;
		struct cell_func_box *cdf_is_bool;
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[257] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[257] 	⍺ ╠both_bool╣ ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[257] 	╠⍺╣ both_bool ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[257] 	╠⍺ both_bool ⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[257] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:⍺('lor_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_lor_vec_ibeam)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:⍺╠('lor_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(cdf_lor_vec_ibeam);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:╠⍺╣('lor_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[257] 	⍺ both_bool ⍵:╠⍺('lor_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧╠is_integer╣ ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[258] 	(is_bool ⍺)∧╠is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)╠∧╣is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[258] 	(is_bool ╠⍺╣)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[258] 	(╠is_bool╣ ⍺)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[258] 	╠(is_bool ⍺)╣∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[258] 	╠(is_bool ⍺)∧is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[258] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)╠×╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~╠⍺╣)×|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(╠~╣⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)╣×|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺╠+╣(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:╠⍺╣+(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:╠⍺+(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧╠is_integer╣ ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] 	(is_bool ⍵)∧╠is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)╠∧╣is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[259] 	(is_bool ╠⍵╣)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[259] 	(╠is_bool╣ ⍵)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] 	╠(is_bool ⍵)╣∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[259] 	╠(is_bool ⍵)∧is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[259] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×|╠⍺╣");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|╣⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|⍺╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)╠×╣|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~╠⍵╣)×|⍺");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(╠~╣⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)╣×|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵╠+╣(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:╠⍵╣+(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:╠⍵+(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_integer->value)
		CHK(6, cleanup, L"[260] 	⍺ ╠both_integer╣ ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_integer->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	╠⍺╣ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	╠⍺ both_integer ⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8949, cdf_ptr8950, 0), cleanup, 
				    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
			
				*stkhd++ = k;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|╠⍺╣){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(╠|╣⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:╠(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8951, cdf_ptr8952, 0), cleanup, 
		    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	
		*stkhd++ = k;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|╠⍺╣){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(╠|╣⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	╠(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[256] logor←╠{╣");
	return err;
}

int
cdf_ptr8949(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8950(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8951(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[261] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[261] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8952(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[261] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8953(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
		struct cell_func_box *cdf_both_integer;
		struct cell_func_box *cdf_is_bool;
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[257] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[257] 	⍺ ╠both_bool╣ ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[257] 	╠⍺╣ both_bool ⍵:⍺('lor_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[257] 	╠⍺ both_bool ⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[257] 	⍺ both_bool ╠⍵╣:⍺('lor_vec_ibeam'⌶)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:⍺('lor_vec_ibeam'⌶)╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_lor_vec_ibeam)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:⍺╠('lor_vec_ibeam'⌶)╣⍵");
			
			*stkhd++ = retain_cell(cdf_lor_vec_ibeam);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[257] 	⍺ both_bool ⍵:╠⍺╣('lor_vec_ibeam'⌶)⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[257] 	⍺ both_bool ⍵:╠⍺('lor_vec_ibeam'⌶)⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧╠is_integer╣ ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[258] 	(is_bool ⍺)∧╠is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[258] 	(is_bool ⍺)╠∧╣is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[258] 	(is_bool ╠⍺╣)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[258] 	(╠is_bool╣ ⍺)∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[258] 	╠(is_bool ⍺)╣∧is_integer ⍵:⍺+(~⍺)×|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[258] 	╠(is_bool ⍺)∧is_integer ⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[258] 	(is_bool ⍺)∧is_integer ╠⍵╣:⍺+(~⍺)×|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)×╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~⍺)╠×╣|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(~╠⍺╣)×|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+(╠~╣⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)╣×|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺+╠(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:⍺╠+╣(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[258] 	(is_bool ⍺)∧is_integer ⍵:╠⍺╣+(~⍺)×|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[258] 	(is_bool ⍺)∧is_integer ⍵:╠⍺+(~⍺)×|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧╠is_integer╣ ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] 	(is_bool ⍵)∧╠is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_and)
		CHK(6, cleanup, L"[259] 	(is_bool ⍵)╠∧╣is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_and);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[259] 	(is_bool ╠⍵╣)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_is_bool->value)
		CHK(6, cleanup, L"[259] 	(╠is_bool╣ ⍵)∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_bool->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[259] 	╠(is_bool ⍵)╣∧is_integer ⍺:⍵+(~⍵)×|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[259] 	╠(is_bool ⍵)∧is_integer ⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[259] 	(is_bool ⍵)∧is_integer ╠⍺╣:⍵+(~⍵)×|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×|╠⍺╣");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|╣⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)×╠|⍺╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_mul)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~⍵)╠×╣|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_mul);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(~╠⍵╣)×|⍺");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+(╠~╣⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)╣×|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵+╠(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_add)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:⍵╠+╣(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_add);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[259] 	(is_bool ⍵)∧is_integer ⍺:╠⍵╣+(~⍵)×|⍺");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[259] 	(is_bool ⍵)∧is_integer ⍺:╠⍵+(~⍵)×|⍺╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_integer->value)
		CHK(6, cleanup, L"[260] 	⍺ ╠both_integer╣ ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_integer->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	╠⍺╣ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	╠⍺ both_integer ⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ╠⍵╣:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *k;
			
				CHK(mk_func(&k, cdf_ptr8954, cdf_ptr8955, 0), cleanup, 
				    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
			
				*stkhd++ = k;
			}
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|╠⍺╣){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			if (!cdf_prim.cdf_res)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(╠|╣⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_res);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[260] 	⍺ both_integer ⍵:╠(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╠|⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr8956, cdf_ptr8957, 0), cleanup, 
		    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	
		*stkhd++ = k;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|╠⍺╣){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(╠|╣⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	╠(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[256] logor←╠{╣");
	return err;
}

int
cdf_ptr8954(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ⍵:╠(|⍺)╣{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8955(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠╠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_neq)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵╠≠╣0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_neq);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵╣≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←╠⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨╠⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨╣⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿╣m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~╠∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){╠~╣∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){╠~∨⌿m←⍵≠0╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[260] 	⍺ both_integer ⍵:(|⍺){~∨⌿m←⍵≠0:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[260] 	⍺ both_integer ⍵:(|⍺)╠{~∨⌿m←⍵≠0:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8956(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[261] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[261] 	╠(|⍺)╣{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8957(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct {
		struct cell_array *cdf_m;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_m = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_DBL;
		unsigned int rnk = 0;
		double dat[] = {1E-12};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>╠1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_gth)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵╠>╣1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_gth);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←╠⍵╣>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←╠⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_m);
	loc->cdf_m = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨╠⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_prim.cdf_lor)
		CHK(6, cleanup, L"[261] 	(|⍺){~╠∨╣⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lor);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿╣m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~╠∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){╠~╣∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){╠~∨⌿m←⍵>1e¯12╣:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alpha)
				CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:╠⍺╣ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|╠⍺╣}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵╠|╣⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵╣|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×╠⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m╠×╣⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m╣×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+╠m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)╠+╣m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~╠m╣)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~╣m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×╠~m)╣+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵╠×╣~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(╠⍵╣×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)╣+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇╠(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_self)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)╠∇╣(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_self);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×╠⍵╣)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m╠×╣⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m╣×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+╠m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)╠+╣m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!loc->cdf_m)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~╠m╣)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(loc->cdf_m);
	
	if (!cdf_prim.cdf_not)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~╣m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_not);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×╠~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺╠×╣~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ((╠⍺╣×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ (╠(⍺×~m)╣+m×⍵)∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)╣∇(⍵×~m)+m×⍵|⍺}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[261] 	(|⍺){~∨⌿m←⍵>1e¯12:⍺ ⋄ ╠((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺╣}|⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[261] 	(|⍺)╠{~∨⌿m←⍵>1e¯12:⍺ ⋄ ((⍺×~m)+m×⍵)∇(⍵×~m)+m×⍵|⍺}╣|⍵");
	return err;
}

int
cdf_ptr8958(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[263] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[263] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[263] lor←{╠⎕SIGNAL╣ 2}ambiv (logor numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[263] lor←{╠⎕SIGNAL 2╣}ambiv (logor numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[263] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	return err;
}

int
cdf_ptr8959(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[263] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[263] lor←{⎕SIGNAL ╠2╣}ambiv (logor numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[263] lor←{╠⎕SIGNAL╣ 2}ambiv (logor numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[263] lor←{╠⎕SIGNAL 2╣}ambiv (logor numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[263] lor←╠{⎕SIGNAL 2}╣ambiv (logor numeric scalar)");
	return err;
}

int
cdf_ptr8960(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[266] nan←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[266] nan←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[266] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
cdf_ptr8961(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] nan←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[266] nan←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[266] nan←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[266] nan←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
cdf_ptr8962(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[266] nan←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_and)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∧╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_and);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[266] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8963(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[266] nan←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∧⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_and)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∧╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_and);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∧⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∧⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[266] nan←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[266] nan←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∧⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8964(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[269] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[269] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[269] nor←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[269] nor←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[269] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
cdf_ptr8965(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[269] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		CHK(fill_array(arr, dat), cleanup, L"[269] nor←{⎕SIGNAL ╠2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[269] nor←{╠⎕SIGNAL╣ 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[269] nor←{╠⎕SIGNAL 2╣}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[269] nor←╠{⎕SIGNAL 2}╣ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	return err;
}

int
cdf_ptr8966(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[269] nor←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_lor)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∨╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_lor);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[269] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8967(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_both_bool;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_bool->value)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ ╠both_bool╣ ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(lex->cdf_both_bool->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{╠⍺╣ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[269] nor←{⎕SIGNAL 2}ambiv{╠⍺ both_bool ⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ╠⍵╣:~⍺∨⍵ ⋄ ⎕SIGNAL 11}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨╠⍵╣ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_lor)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺╠∨╣⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_lor);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺╣∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~╠⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_not)
				CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~╣⍺∨⍵ ⋄ ⎕SIGNAL 11}");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_not);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:╠~⍺∨⍵╣ ⋄ ⎕SIGNAL 11}");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {11};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL ╠11╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL╣ 11}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[269] nor←{⎕SIGNAL 2}ambiv{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ╠⎕SIGNAL 11╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[269] nor←{⎕SIGNAL 2}ambiv╠{⍺ both_bool ⍵:~⍺∨⍵ ⋄ ⎕SIGNAL 11}╣");
	return err;
}

int
cdf_ptr8968(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[273] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[273] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[273] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[273] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[273] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[273] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[274] 	⍺('lth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_lth_vec_ibeam)
		CHK(6, cleanup, L"[274] 	⍺╠('lth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_lth_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[274] 	╠⍺╣('lth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[274] 	╠⍺('lth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[272] lessthan←╠{╣");
	return err;
}

int
cdf_ptr8969(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[273] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[273] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[273] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[273] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[273] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[273] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[273] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[274] 	⍺('lth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_lth_vec_ibeam)
		CHK(6, cleanup, L"[274] 	⍺╠('lth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_lth_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[274] 	╠⍺╣('lth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[274] 	╠⍺('lth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[272] lessthan←╠{╣");
	return err;
}

int
cdf_ptr8970(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[276] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[276] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[276] lth←{╠⎕SIGNAL╣ 2} ambiv (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[276] lth←{╠⎕SIGNAL 2╣} ambiv (lessthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[276] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	return err;
}

int
cdf_ptr8971(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[276] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[276] lth←{⎕SIGNAL ╠2╣} ambiv (lessthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[276] lth←{╠⎕SIGNAL╣ 2} ambiv (lessthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[276] lth←{╠⎕SIGNAL 2╣} ambiv (lessthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[276] lth←╠{⎕SIGNAL 2}╣ ambiv (lessthan numeric scalar)");
	return err;
}

int
cdf_ptr8972(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[280] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[280] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[280] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[280] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[280] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[280] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[281] 	⍺('lte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_lte_vec_ibeam)
		CHK(6, cleanup, L"[281] 	⍺╠('lte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_lte_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[281] 	╠⍺╣('lte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[281] 	╠⍺('lte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[279] lesseql←╠{╣");
	return err;
}

int
cdf_ptr8973(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[280] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[280] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[280] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[280] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[280] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[280] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[280] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[281] 	⍺('lte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_lte_vec_ibeam)
		CHK(6, cleanup, L"[281] 	⍺╠('lte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_lte_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[281] 	╠⍺╣('lte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[281] 	╠⍺('lte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[279] lesseql←╠{╣");
	return err;
}

int
cdf_ptr8974(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[283] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[283] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[283] lte←{╠⎕SIGNAL╣ 2}ambiv (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[283] lte←{╠⎕SIGNAL 2╣}ambiv (lesseql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[283] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	return err;
}

int
cdf_ptr8975(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[283] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[283] lte←{⎕SIGNAL ╠2╣}ambiv (lesseql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[283] lte←{╠⎕SIGNAL╣ 2}ambiv (lesseql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[283] lte←{╠⎕SIGNAL 2╣}ambiv (lesseql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[283] lte←╠{⎕SIGNAL 2}╣ambiv (lesseql numeric scalar)");
	return err;
}

int
cdf_ptr8976(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_eql_vec;
		struct cell_func_box *cdf_both_char;
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[288] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[288] 	⍺ ╠both_numeric╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[288] 	╠⍺╣ both_numeric ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[288] 	╠⍺ both_numeric ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[288] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_eql_vec->value)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_eql_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[288] 	⍺ both_numeric ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[289] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_char->value)
		CHK(6, cleanup, L"[289] 	⍺ ╠both_char╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_char->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[289] 	╠⍺╣ both_char ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[289] 	╠⍺ both_char ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[289] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_eql_vec->value)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_eql_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[289] 	⍺ both_char ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[290] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[290] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[287] equal←╠{╣");
	return err;
}

int
cdf_ptr8977(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_eql_vec;
		struct cell_func_box *cdf_both_char;
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[288] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[288] 	⍺ ╠both_numeric╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[288] 	╠⍺╣ both_numeric ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[288] 	╠⍺ both_numeric ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[288] 	⍺ both_numeric ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_eql_vec->value)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_eql_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[288] 	⍺ both_numeric ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[288] 	⍺ both_numeric ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[289] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_char->value)
		CHK(6, cleanup, L"[289] 	⍺ ╠both_char╣ ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_char->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[289] 	╠⍺╣ both_char ⍵:⍺ eql_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[289] 	╠⍺ both_char ⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[289] 	⍺ both_char ╠⍵╣:⍺ eql_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ eql_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_eql_vec->value)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:⍺ ╠eql_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_eql_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[289] 	⍺ both_char ⍵:╠⍺╣ eql_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[289] 	⍺ both_char ⍵:╠⍺ eql_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[290] 	╠0╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[290] 	╠0╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[287] equal←╠{╣");
	return err;
}

int
cdf_ptr8978(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[292] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[292] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[292] eql←{╠⎕SIGNAL╣ 2}ambiv (equal scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[292] eql←{╠⎕SIGNAL 2╣}ambiv (equal scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[292] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	return err;
}

int
cdf_ptr8979(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[292] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[292] eql←{⎕SIGNAL ╠2╣}ambiv (equal scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[292] eql←{╠⎕SIGNAL╣ 2}ambiv (equal scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[292] eql←{╠⎕SIGNAL 2╣}ambiv (equal scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[292] eql←╠{⎕SIGNAL 2}╣ambiv (equal scalar)");
	return err;
}

int
cdf_ptr8980(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[296] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[296] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[296] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[296] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[296] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[297] 	⍺('gte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_gte_vec_ibeam)
		CHK(6, cleanup, L"[297] 	⍺╠('gte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_gte_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[297] 	╠⍺╣('gte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[297] 	╠⍺('gte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[295] greatereql←╠{╣");
	return err;
}

int
cdf_ptr8981(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[296] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[296] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[296] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[296] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[296] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[296] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[297] 	⍺('gte_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_gte_vec_ibeam)
		CHK(6, cleanup, L"[297] 	⍺╠('gte_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_gte_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[297] 	╠⍺╣('gte_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[297] 	╠⍺('gte_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[295] greatereql←╠{╣");
	return err;
}

int
cdf_ptr8982(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[299] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[299] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[299] gte←{╠⎕SIGNAL╣ 2}ambiv (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[299] gte←{╠⎕SIGNAL 2╣}ambiv (greatereql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[299] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	return err;
}

int
cdf_ptr8983(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[299] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[299] gte←{⎕SIGNAL ╠2╣}ambiv (greatereql numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[299] gte←{╠⎕SIGNAL╣ 2}ambiv (greatereql numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[299] gte←{╠⎕SIGNAL 2╣}ambiv (greatereql numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[299] gte←╠{⎕SIGNAL 2}╣ambiv (greatereql numeric scalar)");
	return err;
}

int
cdf_ptr8984(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[303] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[303] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[303] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[303] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[303] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[303] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[303] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[304] 	⍺('gth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_gth_vec_ibeam)
		CHK(6, cleanup, L"[304] 	⍺╠('gth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_gth_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[304] 	╠⍺╣('gth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[304] 	╠⍺('gth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[302] greaterthan←╠{╣");
	return err;
}

int
cdf_ptr8985(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[303] 	1289≡╠⎕DR╣ ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[303] 	1289≡╠⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[303] 	1289╠≡╣⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[303] 	╠1289╣≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[303] 	╠1289≡⎕DR ⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[303] 	1289≡⎕DR ╠⍵╣:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL ╠11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:╠⎕SIGNAL╣ 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[303] 	1289≡⎕DR ⍵:╠⎕SIGNAL 11╣ ⋄ 1289≡⎕DR ⍺:⎕SIGNAL 11");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_q_dr)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR╣ ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_dr);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡╠⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289╠≡╣⎕DR ⍺:⎕SIGNAL 11");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1289};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289╣≡⎕DR ⍺:⎕SIGNAL 11");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ ╠1289≡⎕DR ⍺╣:⎕SIGNAL 11");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ╠⍺╣:⎕SIGNAL 11");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:⎕SIGNAL ╠11╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL╣ 11");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[303] 	1289≡⎕DR ⍵:⎕SIGNAL 11 ⋄ 1289≡⎕DR ⍺:╠⎕SIGNAL 11╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[304] 	⍺('gth_vec_ibeam'⌶)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_gth_vec_ibeam)
		CHK(6, cleanup, L"[304] 	⍺╠('gth_vec_ibeam'⌶)╣⍵");
	
	*stkhd++ = retain_cell(cdf_gth_vec_ibeam);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[304] 	╠⍺╣('gth_vec_ibeam'⌶)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[304] 	╠⍺('gth_vec_ibeam'⌶)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[302] greaterthan←╠{╣");
	return err;
}

int
cdf_ptr8986(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[306] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[306] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[306] gth←{╠⎕SIGNAL╣ 2}ambiv (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[306] gth←{╠⎕SIGNAL 2╣}ambiv (greaterthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[306] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	return err;
}

int
cdf_ptr8987(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[306] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		CHK(fill_array(arr, dat), cleanup, L"[306] gth←{⎕SIGNAL ╠2╣}ambiv (greaterthan numeric scalar)");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[306] gth←{╠⎕SIGNAL╣ 2}ambiv (greaterthan numeric scalar)");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[306] gth←{╠⎕SIGNAL 2╣}ambiv (greaterthan numeric scalar)");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[306] gth←╠{⎕SIGNAL 2}╣ambiv (greaterthan numeric scalar)");
	return err;
}

int
cdf_ptr8988(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[309] firstocc←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[309] firstocc←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[309] firstocc←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[309] firstocc←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[309] firstocc←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8989(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[309] firstocc←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[309] firstocc←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[309] firstocc←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[309] firstocc←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[309] firstocc←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8990(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_neq_vec;
		struct cell_func_box *cdf_both_char;
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[312] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[312] 	⍺ ╠both_numeric╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[312] 	╠⍺╣ both_numeric ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[312] 	╠⍺ both_numeric ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[312] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_neq_vec->value)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_neq_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[312] 	⍺ both_numeric ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[313] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_char->value)
		CHK(6, cleanup, L"[313] 	⍺ ╠both_char╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_char->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[313] 	╠⍺╣ both_char ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] 	╠⍺ both_char ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_neq_vec->value)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_neq_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[313] 	⍺ both_char ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[314] 	╠1╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[314] 	╠1╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[311] noteq←╠{╣");
	return err;
}

int
cdf_ptr8991(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_neq_vec;
		struct cell_func_box *cdf_both_char;
		struct cell_func_box *cdf_both_numeric;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[312] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_numeric->value)
		CHK(6, cleanup, L"[312] 	⍺ ╠both_numeric╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_numeric->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[312] 	╠⍺╣ both_numeric ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[312] 	╠⍺ both_numeric ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[312] 	⍺ both_numeric ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_neq_vec->value)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_neq_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[312] 	⍺ both_numeric ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[312] 	⍺ both_numeric ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[313] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_both_char->value)
		CHK(6, cleanup, L"[313] 	⍺ ╠both_char╣ ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(lex->cdf_both_char->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[313] 	╠⍺╣ both_char ⍵:⍺ neq_vec ⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[313] 	╠⍺ both_char ⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[313] 	⍺ both_char ╠⍵╣:⍺ neq_vec ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:⍺ neq_vec ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_neq_vec->value)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:⍺ ╠neq_vec╣ ⍵");
			
			*stkhd++ = retain_cell(lex->cdf_neq_vec->value);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[313] 	⍺ both_char ⍵:╠⍺╣ neq_vec ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[313] 	⍺ both_char ⍵:╠⍺ neq_vec ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[314] 	╠1╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[314] 	╠1╣");
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[311] noteq←╠{╣");
	return err;
}

int
cdf_ptr8992(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] mix←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] mix←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[319] mix←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] mix←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] mix←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8993(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[319] mix←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[319] mix←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[319] mix←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[319] mix←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[319] mix←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8994(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[320] take←{(⊂⍳⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[320] take←{(⊂⍳⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[320] take←{(⊂⍳╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[320] take←{(⊂╠⍳╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[320] take←{(⊂╠⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[320] take←{(╠⊂╣⍳⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[320] take←{╠(⊂⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[320] take←{╠(⊂⍳⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[320] take←╠{(⊂⍳⍺)⌷⍵}╣");
	return err;
}

int
cdf_ptr8995(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[320] take←{(⊂⍳⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[320] take←{(⊂⍳⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[320] take←{(⊂⍳╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[320] take←{(⊂╠⍳╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[320] take←{(⊂╠⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[320] take←{(╠⊂╣⍳⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[320] take←{╠(⊂⍳⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[320] take←{╠(⊂⍳⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[320] take←╠{(⊂⍳⍺)⌷⍵}╣");
	return err;
}

int
cdf_ptr8996(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[324] split←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[324] split←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[324] split←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[324] split←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[324] split←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8997(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[324] split←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[324] split←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[324] split←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[324] split←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[324] split←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr8998(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)╠-╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢╠⍵╣)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(╠≢╣⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+⍳╠(≢⍵)╣-⍺)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+⍳╠(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+╠⍳╣(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+╠⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺╠+╣⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[325] drop←{(⊂╠⍺╣+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{(⊂╠⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[325] drop←{(╠⊂╣⍺+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	return err;
}

int
cdf_ptr8999(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-⍺)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-⍺)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)-╠⍺╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢⍵)╠-╣⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(≢╠⍵╣)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+⍳(╠≢╣⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+⍳╠(≢⍵)╣-⍺)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+⍳╠(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺+╠⍳╣(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{(⊂⍺+╠⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[325] drop←{(⊂⍺╠+╣⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[325] drop←{(⊂╠⍺╣+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{(⊂╠⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[325] drop←{(╠⊂╣⍺+⍳(≢⍵)-⍺)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[325] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[325] drop←{╠(⊂⍺+⍳(≢⍵)-⍺)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[325] drop←╠{(⊂⍺+⍳(≢⍵)-⍺)⌷⍵}╣");
	return err;
}

int
cdf_ptr9000(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢╠⍵╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳╠≢╣⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-1+⍳╠≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+╠⍳╣≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-1+╠⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1╠+╣⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[329] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[329] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-╠1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)╠-╣1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢╠⍵╣)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(╠≢╣⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂╠(≢⍵)╣-1+⍳≢⍵)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂╠(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[329] reverse_last←{(╠⊂╣(≢⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[329] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	return err;
}

int
cdf_ptr9001(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)⌷╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢⍵)╠⌷╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳≢╠⍵╣)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+⍳╠≢╣⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-1+⍳╠≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1+╠⍳╣≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-1+╠⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)-1╠+╣⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[329] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[329] reverse_last←{(⊂(≢⍵)-╠1╣+⍳≢⍵)⌷⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂(≢⍵)-╠1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢⍵)╠-╣1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(≢╠⍵╣)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[329] reverse_last←{(⊂(╠≢╣⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂╠(≢⍵)╣-1+⍳≢⍵)⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{(⊂╠(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[329] reverse_last←{(╠⊂╣(≢⍵)-1+⍳≢⍵)⌷⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[329] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)╣⌷⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[329] reverse_last←{╠(⊂(≢⍵)-1+⍳≢⍵)⌷⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[329] reverse_last←╠{(⊂(≢⍵)-1+⍳≢⍵)⌷⍵}╣");
	return err;
}

int
cdf_ptr9002(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[330] rotate_last←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[330] rotate_last←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[330] rotate_last←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[330] rotate_last←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[330] rotate_last←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr9003(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[330] rotate_last←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[330] rotate_last←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[330] rotate_last←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[330] rotate_last←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[330] rotate_last←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr9004(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[334] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←╠⍵╣ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[334] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
cdf_ptr9005(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[334] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[334] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
cdf_ptr9006(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[334] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←╠⍵╣ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[334] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
cdf_ptr9007(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		arr->shape[0] = 1;
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eql)
		CHK(6, cleanup, L"[334] com←{2╠=╣⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eql);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		CHK(fill_array(arr, dat), cleanup, L"[334] com←{╠2╣=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{╠2=⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[334] com←{2=╠⎕NC'⍺⍺'╣:⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':╠⍺⍺╣ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ╠⍺╣}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ╠⍺⍺╣ ⍺}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵╣ ⍺⍺ ⍺}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[334] com←{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ╠⍵ ⍺⍺ ⍺╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[334] com←╠{2=⎕NC'⍺⍺':⍺⍺ ⋄ ⍺←⍵ ⋄ ⍵ ⍺⍺ ⍺}╣");
	return err;
}

int
cdf_ptr9008(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[337] map←{⍺←╠⊢╣ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[337] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
cdf_ptr9009(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺╣ veach ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺╣ veach ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[337] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
cdf_ptr9010(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[337] map←{⍺←╠⊢╣ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[337] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
cdf_ptr9011(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_moper_box *cdf_veach;
		struct cell_func_box *cdf_chk_scl;
		struct cell_func_box *cdf_max_shp;
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	loc->cdf_s = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_chk_scl->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ ╠chk_scl╣ ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_chk_scl->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ╠⍺╣ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ╠⍺ chk_scl ⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ╠⍵╣: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ╠⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_max_shp->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ ╠max_shp╣ ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_max_shp->value);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺╣ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←╠⍺ max_shp ⍵╣ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_veach->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ ╠veach╣ ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_veach->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺╣ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ╠⍺⍺ veach╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺╣ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ╠⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze╣ ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴╠squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s╠⍴╣squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s╣⍴squeeze ⍺ ⍺⍺ veach ⍵}");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[337] map←{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ ╠s⍴squeeze ⍺ ⍺⍺ veach ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[337] map←╠{⍺←⊢ ⋄ ⍺ chk_scl ⍵: ⋄ s←⍺ max_shp ⍵ ⋄ s⍴squeeze ⍺ ⍺⍺ veach ⍵}╣");
	return err;
}

int
cdf_ptr9012(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_c;
		struct cell_array *cdf_zs;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_s = NULL;
	loc->cdf_c = NULL;
	loc->cdf_zs = NULL;
	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[341] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[341] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_zs);
	loc->cdf_zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_identity_ibeam)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_identity_ibeam);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9013, cdf_ptr9014, 2), cleanup, 
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->cdf_op);
		k->fv[1] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[343] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[343] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[343] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[340] reduce←╠{╣");
	return err;
}

int
cdf_ptr9013(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9014(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9015(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_c;
		struct cell_array *cdf_zs;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_s = NULL;
	loc->cdf_c = NULL;
	loc->cdf_zs = NULL;
	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[341] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[341] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_zs);
	loc->cdf_zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_identity_ibeam)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_identity_ibeam);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9016, cdf_ptr9017, 2), cleanup, 
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->cdf_op);
		k->fv[1] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[343] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[343] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[343] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[340] reduce←╠{╣");
	return err;
}

int
cdf_ptr9016(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9017(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9018(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_c;
		struct cell_array *cdf_zs;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_s = NULL;
	loc->cdf_c = NULL;
	loc->cdf_zs = NULL;
	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[341] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[341] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_zs);
	loc->cdf_zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_identity_ibeam)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_identity_ibeam);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9019, cdf_ptr9020, 2), cleanup, 
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->cdf_op);
		k->fv[1] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[343] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[343] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[343] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[340] reduce←╠{╣");
	return err;
}

int
cdf_ptr9019(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9020(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9021(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_s;
		struct cell_array *cdf_c;
		struct cell_array *cdf_zs;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_s = NULL;
	loc->cdf_c = NULL;
	loc->cdf_zs = NULL;
	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢╠⍴╣⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢╠⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[341] 	0≡╠≢╣⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡╠≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[341] 	0╠≡╣≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	╠0╣≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	╠0≡≢⍴⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[341] 	0≡≢⍴╠⍵╣:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,╠⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_cat)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇╠,╣⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_cat);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:∇╠,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_self)
				CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:╠∇╣,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
			*stkhd++ = retain_cell(cdf_self);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[341] 	0≡≢⍴⍵:╠∇,⍵╣ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓s");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴╠⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴╣⍵ ⋄ c←⊃s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←╠⍴⍵╣ ⋄ c←⊃s ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_s);
	loc->cdf_s = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃╠s╣ ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_dis)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃╣s ⋄ zs←1↓s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_dis);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←╠⊃s╣ ⋄ zs←1↓s");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_c);
	loc->cdf_c = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_s)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1↓╠s╣");
	
	*stkhd++ = retain_cell(loc->cdf_s);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←1╠↓╣s");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		CHK(fill_array(arr, dat), cleanup, L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1╣↓s");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[341] 	0≡≢⍴⍵:∇,⍵ ⋄ s←⍴⍵ ⋄ c←⊃s ⋄ zs←╠1↓s╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_zs);
	loc->cdf_zs = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0╠≡╣c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	╠0╣≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	╠0≡c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡╠c╣:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 1;
				int16_t dat[] = {0};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				arr->shape[0] = 0;
			
				CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)╠⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_identity_ibeam)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺╠('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_identity_ibeam);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴╠⍺⍺╣('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)╣⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴╠⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs╠⍴╣⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:╠zs╣⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:╠zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬╣ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1╠≡╣c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		CHK(fill_array(arr, dat), cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1╣≡c:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ ╠1≡c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡╠c╣:zs⍴⍵ ⋄ op←⍺⍺¨");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴╠⍵╣ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_prim.cdf_rho)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs╠⍴╣⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_rho);
			
			if (!loc->cdf_zs)
				CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs╣⍴⍵ ⋄ op←⍺⍺¨");
			
			*stkhd++ = retain_cell(loc->cdf_zs);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:╠zs⍴⍵╣ ⋄ op←⍺⍺¨");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[342] 	0≡c:zs⍴⍺⍺('identity_ibeam'⌶⌶)⍬ ⋄ 1≡c:zs⍴⍵ ⋄ op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)╠⌷╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-╠1╣)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c╠-╣1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(╠c╣-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)╣⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←╠(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵╠⊣╣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵╣⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂╠⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂╣⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨╠⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}╠¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9022, cdf_ptr9023, 2), cleanup, 
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	
		k->fv[0] = retain_cell(loc->cdf_op);
		k->fv[1] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}¨╣⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-╠1╣){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c╠-╣1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_c)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳╠c╣-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_c);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳╠c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[343] 	z⊣(⌽╠⍳╣c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣(⌽╠⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[343] 	z⊣(╠⌽╣⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1)╣{⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣╠(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z╠⊣╣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[343] 	╠z╣⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	╠z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[340] reduce←╠{╣");
	return err;
}

int
cdf_ptr9022(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9023(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)op ╠z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷⍵)╠op╣ z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺⌷╠⍵╣)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(⍺╠⌷╣⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←(╠⍺╣⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)╣op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣z⊢←╠(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣z╠⊢╣←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z╣⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){⍬⊣╠z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[343] 	z⊣(⌽⍳c-1){⍬╠⊣╣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		arr->shape[0] = 0;
	
		CHK(fill_array(arr, dat), cleanup, L"[343] 	z⊣(⌽⍳c-1){╠⍬╣⊣z⊢←(⍺⌷⍵)op z}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[343] 	z⊣(⌽⍳c-1){╠⍬⊣z⊢←(⍺⌷⍵)op z╣}¨⊂⍵⊣z←(c-1)⌷⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[343] 	z⊣(⌽⍳c-1)╠{⍬⊣z⊢←(⍺⌷⍵)op z}╣¨⊂⍵⊣z←(c-1)⌷⍵");
	return err;
}

int
cdf_ptr9024(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_rc;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_rc, NULL), cleanup, L"Init mutable variable: loc->cdf_rc");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[345] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[345] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[345] nwreduce←{op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[346] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[346] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rc->value);
	loc->cdf_rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array_box *bx = loc->cdf_rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9025, cdf_ptr9026, 3), cleanup, 
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->cdf_rc);
		k->fv[1] = retain_cell(loc->cdf_op);
		k->fv[2] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[347] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[347] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[345] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
cdf_ptr9025(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9026(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9027(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_rc;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_rc, NULL), cleanup, L"Init mutable variable: loc->cdf_rc");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[345] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[345] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[345] nwreduce←{op←╠⍺⍺¨╣");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[346] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[346] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rc->value);
	loc->cdf_rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array_box *bx = loc->cdf_rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9028, cdf_ptr9029, 3), cleanup, 
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->cdf_rc);
		k->fv[1] = retain_cell(loc->cdf_op);
		k->fv[2] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[347] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[347] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[345] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
cdf_ptr9028(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9029(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9030(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_rc;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_rc, NULL), cleanup, L"Init mutable variable: loc->cdf_rc");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[345] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[345] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[345] nwreduce←{op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[346] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[346] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rc->value);
	loc->cdf_rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array_box *bx = loc->cdf_rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9031, cdf_ptr9032, 3), cleanup, 
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->cdf_rc);
		k->fv[1] = retain_cell(loc->cdf_op);
		k->fv[2] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[347] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[347] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[345] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
cdf_ptr9031(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9032(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9033(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_rc;
		struct cell_array_box *cdf_z;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_is_integer;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_func_box(&loc->cdf_op, NULL), cleanup, L"Init mutable variable: loc->cdf_op");
	CHK(mk_array_box(&loc->cdf_rc, NULL), cleanup, L"Init mutable variable: loc->cdf_rc");
	CHK(mk_array_box(&loc->cdf_z, NULL), cleanup, L"Init mutable variable: loc->cdf_z");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[345] nwreduce←{op←⍺⍺╠¨╣");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[345] nwreduce←{op←╠⍺⍺╣¨");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[345] nwreduce←{op←╠⍺⍺¨╣");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_op->value);
	loc->cdf_op->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!lex->cdf_is_integer->value)
		CHK(6, cleanup, L"[346] 	0≡╠is_integer╣ ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(lex->cdf_is_integer->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡╠is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[346] 	0╠≡╣is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	╠0╣≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	╠0≡is_integer ⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ╠⍺╣:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {11};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL ╠11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL╣ 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:╠⎕SIGNAL 11╣ ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢╠⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢╣⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+╠≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1╠+╣≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1╣+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←╠1+≢⍵╣ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_rc->value);
	loc->cdf_rc->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc╠<╣⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc╣<⍺:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ ╠rc<⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<╠⍺╣:⎕SIGNAL 5 ⋄ rc-←|⍺");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				CHK(fill_array(arr, dat), cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL ╠5╣ ⋄ rc-←|⍺");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL╣ 5 ⋄ rc-←|⍺");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:╠⎕SIGNAL 5╣ ⋄ rc-←|⍺");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←|╠⍺╣");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|╣⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc-←╠|⍺╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ rc╠-╣←|⍺");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!loc->cdf_rc->value)
		CHK(6, cleanup, L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc╣-←|⍺");
	
	*stkhd++ = retain_cell(loc->cdf_rc->value);
	
	{
		struct cell_array_box *bx = loc->cdf_rc;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[346] 	0≡is_integer ⍺:⎕SIGNAL 11 ⋄ rc←1+≢⍵ ⋄ rc<⍺:⎕SIGNAL 5 ⋄ ╠rc-←|⍺╣");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_drp)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)╠↓╣⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_drp);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-╠1╣)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺╠-╣1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(╠⍺╣-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)╣↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←╠(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_z->value);
	loc->cdf_z->value = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵╠⊣╣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵╣⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂╠⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂╣⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╠⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╠¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9034, cdf_ptr9035, 3), cleanup, 
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		k->fv[0] = retain_cell(loc->cdf_rc);
		k->fv[1] = retain_cell(loc->cdf_op);
		k->fv[2] = retain_cell(loc->cdf_z);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨╣⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[347] 	z⊣(⌽⍳⍺-╠1╣){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺╠-╣1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳╠⍺╣-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳╠⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽╠⍳╣⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽╠⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rot)
		CHK(6, cleanup, L"[347] 	z⊣(╠⌽╣⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1)╣{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣╠(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[347] 	z╠⊣╣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!loc->cdf_z->value)
		CHK(6, cleanup, L"[347] 	╠z╣⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(loc->cdf_z->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	╠z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[345] nwreduce←╠{op←⍺⍺¨╣");
	return err;
}

int
cdf_ptr9034(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9035(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_rc;
		struct cell_func_box *cdf_op;
		struct cell_array_box *cdf_z;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)op ╠z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	if (!lex->cdf_op->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷⍵)╠op╣ z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_op->value);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)⌷╠⍵╣)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_sqd)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳rc)╠⌷╣⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sqd);
	
	if (!lex->cdf_rc->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+⍳╠rc╣)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_rc->value);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳╣rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺+╠⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂⍺╠+╣⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺╣+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((⊂╠⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_par)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z⊢←((╠⊂╣⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_par);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←(╠(⊂⍺+⍳rc)╣⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)╣op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){z⊢←╠((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){z╠⊢╣←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	if (!lex->cdf_z->value)
		CHK(6, cleanup, L"[347] 	z⊣(⌽⍳⍺-1){╠z╣⊢←((⊂⍺+⍳rc)⌷⍵)op z}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
	*stkhd++ = retain_cell(lex->cdf_z->value);
	
	{
		struct cell_array_box *bx = lex->cdf_z;
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *orig = bx->value;
	
		CHK((fn->fptr_dya)(&bx->value, x, y, fn), cleanup,
		    L"[347] 	z⊣(⌽⍳⍺-1){╠z⊢←((⊂⍺+⍳rc)⌷⍵)op z╣}¨⊂⍵⊣z←(⍺-1)↓⍵");
	
		release_array(orig);
		release_array(x);
		release_func(fn);
		stkhd -= 2;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[347] 	z⊣(⌽⍳⍺-1)╠{z⊢←((⊂⍺+⍳rc)⌷⍵)op z}╣¨⊂⍵⊣z←(⍺-1)↓⍵");
	return err;
}

int
cdf_ptr9036(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_moper_box *cdf_nwreduce;
		struct cell_moper_box *cdf_reduce;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[349] rdf←{⍺←╠⊢╣ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[350] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_reduce->value)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->cdf_reduce->value);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_nwreduce->value)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_nwreduce->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[349] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
cdf_ptr9037(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_moper_box *cdf_nwreduce;
		struct cell_moper_box *cdf_reduce;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[350] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_reduce->value)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->cdf_reduce->value);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_array(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_nwreduce->value)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_nwreduce->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[349] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
cdf_ptr9038(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_moper_box *cdf_nwreduce;
		struct cell_moper_box *cdf_reduce;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[349] rdf←{⍺←╠⊢╣ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[350] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_reduce->value)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->cdf_reduce->value);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_nwreduce->value)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_nwreduce->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[349] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
cdf_ptr9039(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct lex_vars {
		struct cell_moper_box *cdf_nwreduce;
		struct cell_moper_box *cdf_reduce;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺╠⊣╣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺╣⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴╠⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴╣⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢╠⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢╣⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<╠≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_lth)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1╠<╣≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lth);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ ╠1╣<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ ╠1<≢⍴⍺⊣0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣╠0╣:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {4};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL ╠4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL╣ 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:╠⎕SIGNAL 4╣ ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_lft)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺╠⊣╣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_lft);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺╣⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,╠⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,╣⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢╠,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢╣,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢╠≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1╠≢╣≢,⍺⊣0:⎕SIGNAL 5");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1╣≢≢,⍺⊣0:⎕SIGNAL 5");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ ╠1≢≢,⍺⊣0╣:⎕SIGNAL 5");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣╠0╣:⎕SIGNAL 5");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			{
				struct cell_array *arr;
			
				enum array_type typ = ARR_SINT;
				unsigned int rnk = 0;
				int16_t dat[] = {5};
			
				CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				CHK(fill_array(arr, dat), cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL ╠5╣");
			
				*stkhd++ = arr;
			}
			
			if (!cdf_prim.cdf_q_signal)
				CHK(6, cleanup, L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL╣ 5");
			
			*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[349] rdf←{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:╠⎕SIGNAL 5╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡╠⍺╣ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[350] 	1≡╠⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[350] 	1╠≡╣⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		CHK(fill_array(arr, dat), cleanup, L"[350] 	╠1╣≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	╠1≡⍺ 1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[350] 	1≡⍺ ╠1╣:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ╠⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!lex->cdf_reduce->value)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ ╠reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(lex->cdf_reduce->value);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[350] 	1≡⍺ 1:╠⍺⍺╣ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *x = stkhd[-1];
				struct cell_moper *op = stkhd[-2];
				struct cell_func *dst;
			
				CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce╣ ⍵ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(x);
				release_moper(op);
				stkhd -= 2;
			
				*stkhd++ = dst;
			}
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[350] 	1≡⍺ 1:╠⍺⍺ reduce ⍵╣ ⋄ ⍺(⍺⍺ nwreduce)⍵");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ nwreduce)╠⍵╣");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!lex->cdf_nwreduce->value)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(⍺⍺ ╠nwreduce╣)⍵");
	
	*stkhd++ = retain_cell(lex->cdf_nwreduce->value);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺(╠⍺⍺╣ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ⍺╠(⍺⍺ nwreduce)╣⍵");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺╣(⍺⍺ nwreduce)⍵");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[350] 	1≡⍺ 1:⍺⍺ reduce ⍵ ⋄ ╠⍺(⍺⍺ nwreduce)⍵╣");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[349] rdf←╠{⍺←⊢ ⋄ 1<≢⍴⍺⊣0:⎕SIGNAL 4 ⋄ 1≢≢,⍺⊣0:⎕SIGNAL 5╣");
	return err;
}

int
cdf_ptr9040(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[354] rpf←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[354] rpf←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[354] rpf←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[354] rpf←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[354] rpf←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr9041(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {16};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[354] rpf←{⎕SIGNAL ╠16╣}");
	
		CHK(fill_array(arr, dat), cleanup, L"[354] rpf←{⎕SIGNAL ╠16╣}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[354] rpf←{╠⎕SIGNAL╣ 16}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[354] rpf←{╠⎕SIGNAL 16╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[354] rpf←╠{⎕SIGNAL 16}╣");
	return err;
}

int
cdf_ptr9042(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array_box *cdf_X;
		struct cell_array_box *cdf_fn;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_array_box(&loc->cdf_X, NULL), cleanup, L"Init mutable variable: loc->cdf_X");
	CHK(mk_array_box(&loc->cdf_fn, NULL), cleanup, L"Init mutable variable: loc->cdf_fn");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,╠⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[357] scf←{X←╠,╣⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←╠,⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_X->value);
	loc->cdf_X->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←╠⍺⍺╣ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	release_cell(loc->cdf_fn->value);
	loc->cdf_fn->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳╣≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1╠+╣⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}╠¨╣1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9043, cdf_ptr9044, 2), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fn);
		k->fv[1] = retain_cell(loc->cdf_X);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨╣1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)╠⍴╣{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴╠⍵╣)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (╠⍴╣⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)╣⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze╣ (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←╠{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}╣");
	return err;
}

int
cdf_ptr9043(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9044(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9045(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array_box *cdf_X;
		struct cell_array_box *cdf_fn;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_array_box(&loc->cdf_X, NULL), cleanup, L"Init mutable variable: loc->cdf_X");
	CHK(mk_array_box(&loc->cdf_fn, NULL), cleanup, L"Init mutable variable: loc->cdf_fn");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,╠⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[357] scf←{X←╠,╣⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←╠,⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_X->value);
	loc->cdf_X->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←╠⍺⍺╣ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	release_cell(loc->cdf_fn->value);
	loc->cdf_fn->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳╣≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1╠+╣⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}╠¨╣1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9046, cdf_ptr9047, 2), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fn);
		k->fv[1] = retain_cell(loc->cdf_X);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨╣1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)╠⍴╣{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴╠⍵╣)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (╠⍴╣⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)╣⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze╣ (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←╠{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}╣");
	return err;
}

int
cdf_ptr9046(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_array_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9047(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_array_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9048(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array_box *cdf_X;
		struct cell_func_box *cdf_fn;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_array_box(&loc->cdf_X, NULL), cleanup, L"Init mutable variable: loc->cdf_X");
	CHK(mk_func_box(&loc->cdf_fn, NULL), cleanup, L"Init mutable variable: loc->cdf_fn");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,╠⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[357] scf←{X←╠,╣⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←╠,⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_X->value);
	loc->cdf_X->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←╠⍺⍺╣ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	release_cell(loc->cdf_fn->value);
	loc->cdf_fn->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳╣≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1╠+╣⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}╠¨╣1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9049, cdf_ptr9050, 2), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fn);
		k->fv[1] = retain_cell(loc->cdf_X);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨╣1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)╠⍴╣{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴╠⍵╣)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (╠⍴╣⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)╣⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze╣ (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←╠{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}╣");
	return err;
}

int
cdf_ptr9049(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9050(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9051(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array_box *cdf_X;
		struct cell_func_box *cdf_fn;
	} loc_frm, *loc;

	loc = &loc_frm;

	struct lex_vars {
		struct cell_func_box *cdf_squeeze;
	} *lex;

	lex = (struct lex_vars *)cdf_deldel->fv;

	CHK(mk_array_box(&loc->cdf_X, NULL), cleanup, L"Init mutable variable: loc->cdf_X");
	CHK(mk_func_box(&loc->cdf_fn, NULL), cleanup, L"Init mutable variable: loc->cdf_fn");
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,╠⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[357] scf←{X←╠,╣⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←╠,⍵╣ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_X->value);
	loc->cdf_X->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←╠⍺⍺╣ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	release_cell(loc->cdf_fn->value);
	loc->cdf_fn->value = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢╣⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳╠≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳╣≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+╠⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_add)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1╠+╣⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_add);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1╣+⍳≢⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨╠1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}╠¨╣1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	{
		struct cell_func *k;
	
		CHK(mk_func(&k, cdf_ptr9052, cdf_ptr9053, 2), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	
		k->fv[0] = retain_cell(loc->cdf_fn);
		k->fv[1] = retain_cell(loc->cdf_X);
	
		*stkhd++ = k;
	}
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨╣1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)╠⍴╣{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴╠⍵╣)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (╠⍴╣⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)╣⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze ╠(⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!lex->cdf_squeeze->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze╣ (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_squeeze->value);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ ╠squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←╠{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳⍵]}¨1+⍳≢⍵}╣");
	return err;
}

int
cdf_ptr9052(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;

	cdf_alpha = NULL;

	struct lex_vars {
		struct cell_func_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9053(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;

	struct lex_vars {
		struct cell_func_box *cdf_fn;
		struct cell_array_box *cdf_X;
	} *lex;

	lex = (struct lex_vars *)cdf_self->fv;

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[⍳╠⍵╣]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳╣⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X[╠⍳⍵╣]}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[⍳⍵]╣}¨1+⍳≢⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿X╠[╣⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!lex->cdf_X->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X╣[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_X->value);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn⌿╠X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{fn╠⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!lex->cdf_fn->value)
		CHK(6, cleanup, L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn╣⌿X[⍳⍵]}¨1+⍳≢⍵}");
	
	*stkhd++ = retain_cell(lex->cdf_fn->value);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿╣X[⍳⍵]}¨1+⍳≢⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴{╠fn⌿X[⍳⍵]╣}¨1+⍳≢⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[357] scf←{X←,⍵ ⋄ fn←⍺⍺ ⋄ squeeze (⍴⍵)⍴╠{fn⌿X[⍳⍵]}╣¨1+⍳≢⍵}");
	return err;
}

int
cdf_ptr9054(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9055(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9056(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9057(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵╣¨ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨╣ ⍵}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9058(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9059(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rpf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rpf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9060(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9061(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵¨ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ⍵⍵╠¨╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵╣¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{⍺⍺⌿⍺ ╠⍵⍵¨╣ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[360] dot←{⍺⍺⌿╠⍺╣ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[360] dot←{⍺⍺⌿╠⍺ ⍵⍵¨ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rdf)
		CHK(6, cleanup, L"[360] dot←{⍺⍺╠⌿╣⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rdf);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[360] dot←{╠⍺⍺╣⌿⍺ ⍵⍵¨ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[360] dot←{╠⍺⍺⌿╣⍺ ⍵⍵¨ ⍵}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[360] dot←{╠⍺⍺⌿⍺ ⍵⍵¨ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[360] dot←╠{⍺⍺⌿⍺ ⍵⍵¨ ⍵}╣");
	return err;
}

int
cdf_ptr9062(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_y;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_y = NULL;
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,╠⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,╣⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←╠≢╣,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←╠≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_y);
	loc->cdf_y = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)╠×╣y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,╠⍺╣)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢╠,╣⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢╠,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(╠≢╣,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[363] oup←{i←╠⍳╣(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←╠⍳(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|╠i╣]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y╠|╣i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y╣|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y|i╣]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[╣y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,╠⍵╣)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(╠,╣⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)╣[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺╠¨╣(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺╣¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺¨╣(,⍵)[y|i]}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷╠y╣]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i╠÷╣y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i╣÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊╣i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[╣⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,╠⍺╣)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(╠,╣⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)╣[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)╠⍴╣(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴╠⍵╣)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴╣⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴╠⍺╣),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((╠⍴╣⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[363] oup←╠{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}╣");
	return err;
}

int
cdf_ptr9063(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_y;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_y = NULL;
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,╠⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,╣⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←╠≢╣,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←╠≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_y);
	loc->cdf_y = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)╠×╣y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,╠⍺╣)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢╠,╣⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢╠,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(╠≢╣,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[363] oup←{i←╠⍳╣(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←╠⍳(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|╠i╣]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y╠|╣i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y╣|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y|i╣]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[╣y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,╠⍵╣)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(╠,╣⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)╣[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺╠¨╣(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺╣¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_am, op->fptr_ad, x), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺¨╣(,⍵)[y|i]}");
	
		release_array(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷╠y╣]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i╠÷╣y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i╣÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊╣i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[╣⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,╠⍺╣)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(╠,╣⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)╣[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)╠⍴╣(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴╠⍵╣)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴╣⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴╠⍺╣),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((╠⍴╣⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[363] oup←╠{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}╣");
	return err;
}

int
cdf_ptr9064(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_y;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_y = NULL;
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,╠⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,╣⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←╠≢╣,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←╠≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_y);
	loc->cdf_y = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)╠×╣y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,╠⍺╣)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢╠,╣⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢╠,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(╠≢╣,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[363] oup←{i←╠⍳╣(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←╠⍳(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|╠i╣]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y╠|╣i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y╣|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y|i╣]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[╣y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,╠⍵╣)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(╠,╣⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)╣[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺╠¨╣(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺╣¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺¨╣(,⍵)[y|i]}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷╠y╣]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i╠÷╣y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i╣÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊╣i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[╣⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,╠⍺╣)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(╠,╣⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)╣[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)╠⍴╣(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴╠⍵╣)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴╣⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴╠⍺╣),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((╠⍴╣⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[363] oup←╠{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}╣");
	return err;
}

int
cdf_ptr9065(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_moper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];

	struct {
		struct cell_array *cdf_y;
		struct cell_array *cdf_i;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_y = NULL;
	loc->cdf_i = NULL;
	err = 0;
	stkhd = &stk[0];

	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,╠⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,╣⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢╠,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←╠≢╣,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←╠≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_y);
	loc->cdf_y = retain_cell(stkhd[-1]);
	
	if (!cdf_prim.cdf_mul)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)╠×╣y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_mul);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,╠⍺╣)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢╠,╣⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢╠,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_nqv)
		CHK(6, cleanup, L"[363] oup←{i←⍳(╠≢╣,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_nqv);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)╣×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳╠(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_iot)
		CHK(6, cleanup, L"[363] oup←{i←╠⍳╣(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_iot);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←╠⍳(≢,⍺)×y←≢,⍵╣ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	release_cell(loc->cdf_i);
	loc->cdf_i = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|╠i╣]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	if (!cdf_prim.cdf_res)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y╠|╣i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_res);
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y╣|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[╠y|i╣]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[y|i]╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)╠[╣y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,╠⍵╣)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(╠,╣⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)╣[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨╠(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_map)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺╠¨╣(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_map);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺╣¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_moper *op = stkhd[-2];
		struct cell_func *dst;
	
		CHK(apply_mop(&dst, op, op->fptr_fm, op->fptr_fd, x), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]╠⍺⍺¨╣(,⍵)[y|i]}");
	
		release_func(x);
		release_moper(op);
		stkhd -= 2;
	
		*stkhd++ = dst;
	}
	
	if (!loc->cdf_y)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷╠y╣]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_y);
	
	if (!cdf_prim.cdf_div)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i╠÷╣y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_div);
	
	if (!loc->cdf_i)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i╣÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(loc->cdf_i);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊╠i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_min)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊╣i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_min);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[╠⌊i÷y╣]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		arr->shape[0] = 1;
		CHK(alloc_array(arr), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_brk)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[╣⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_brk);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,╠⍺╣)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(╠,╣⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)╣[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴╠(,⍺)[⌊i÷y]╣⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)╠[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)╠⍴╣(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴╠⍵╣)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴╣⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),╠⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_cat)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺)╠,╣⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_cat);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴╠⍺╣),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	if (!cdf_prim.cdf_rho)
		CHK(6, cleanup, L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((╠⍴╣⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rho);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ (╠(⍴⍺)╣,⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)╣⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[363] oup←{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ╠((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[363] oup←╠{i←⍳(≢,⍺)×y←≢,⍵ ⋄ ((⍴⍺),⍴⍵)⍴(,⍺)[⌊i÷y]⍺⍺¨(,⍵)[y|i]}╣");
	return err;
}

int
cdf_ptr9066(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[366] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9067(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[366] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9068(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[366] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9069(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[366] pow←{0╠≡╣⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {0};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{╠0╣≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{0≡╠⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9070(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9071(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		arr->shape[0] = 3;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
		dat[2] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *arr, **dat;
	
		CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		arr->shape[0] = 2;
		CHK(alloc_array(arr), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
		dat = arr->values;
	
		dat[0] = *--stkhd;
		dat[1] = *--stkhd;
	
		*stkhd++ = arr;
	}
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_aam, op->fptr_aad, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9072(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←╠⊢╣ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	release_cell(cdf_alpha);
	cdf_alpha = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9073(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	err = 0;
	stkhd = &stk[0];

	CHK(2, cleanup, L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[366] pow←{╠0≡⍵⍵╣:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:╠⍵╣ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	if (!cdf_omega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ╠⍵╣}");
	
	*stkhd++ = retain_cell(cdf_omega);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ╠⍺⍺╣ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺╣ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢╠⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_prim.cdf_rgt)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢╣⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_rgt);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)╠⊢⍺ ⍺⍺ ⍵╣}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {1};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		CHK(fill_array(arr, dat), cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-╠1╣)⊢⍺ ⍺⍺ ⍵}");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_sub)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-╣1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_sub);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵╠-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_omegaomega)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(╠⍵⍵╣-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_omegaomega);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇╠(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	if (!cdf_deldel)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺╠∇∇╣(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_deldel);
	
	if (!cdf_alphaalpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺╣∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alphaalpha);
	
	{
		struct cell_func *x = stkhd[-1];
		struct cell_doper *op = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_func *dst;
	
		CHK(apply_dop(&dst, op, op->fptr_afm, op->fptr_afd, x, y), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ╠⍺⍺∇∇(⍵⍵-1)╣⊢⍺ ⍺⍺ ⍵}");
	
		release_func(x);
		release_doper(op);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	if (!cdf_alpha)
		CHK(6, cleanup, L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺╣ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}");
	
	*stkhd++ = retain_cell(cdf_alpha);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[366] pow←{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ╠⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵╣}");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[366] pow←╠{0≡⍵⍵:⍵ ⋄ ⍺←⊢ ⋄ ⍺ ⍺⍺∇∇(⍵⍵-1)⊢⍺ ⍺⍺ ⍵}╣");
	return err;
}

int
cdf_ptr9074(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 4;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
				dat[3] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9075(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 2, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 4;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
				dat[3] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				arr->shape[0] = 3;
				CHK(alloc_array(arr), cleanup,
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
				dat[2] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9076(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9077(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_array *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ⍺⍺ ╠⍵⍵╣");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:⍵ ╠⍺⍺╣ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_omega)
				CHK(6, cleanup, L"[374] 	type≡0 3 2:╠⍵╣ ⍺⍺ ⍵⍵");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9078(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9079(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_array *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *arr, **dat;
			
				CHK(mk_array(&arr, ARR_NESTED, STG_HOST, 1), cleanup, 
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				arr->shape[0] = 2;
				CHK(alloc_array(arr), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺╣ ⍵⍵ ⍵");
				dat = arr->values;
			
				dat[0] = *--stkhd;
				dat[1] = *--stkhd;
			
				*stkhd++ = arr;
			}
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9080(struct cell_array **z,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_array *cdf_alpha;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_alpha = NULL;
	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));
	release_array(cdf_alpha);

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

int
cdf_ptr9081(struct cell_array **z,
    struct cell_array *cdf_alpha,
    struct cell_array *cdf_omega,
    struct cell_func *cdf_self)
{
	void *stk[128];
	void **stkhd;
	int err;
	struct cell_doper *cdf_deldel;
	struct cell_func *cdf_alphaalpha;
	struct cell_func *cdf_omegaomega;

	cdf_deldel = cdf_self->fv[0];
	cdf_alphaalpha = cdf_self->fv[1];
	cdf_omegaomega = cdf_self->fv[2];

	struct {
		struct cell_array *cdf_type;
	} loc_frm, *loc;

	loc = &loc_frm;

	loc->cdf_type = NULL;
	err = 0;
	stkhd = &stk[0];

	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[370] 	type←╠⎕NC'⍺' '⍺⍺' '⍵⍵'╣");
	
		*stkhd++ = arr;
	}
	
	release_cell(loc->cdf_type);
	loc->cdf_type = retain_cell(stkhd[-1]);
	
	release_cell(*--stkhd);
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {2, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[371] 	type╠≡╣2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[371] 	╠type╣≡2 3 3:⍺ ⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[371] 	╠type≡2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[371] 	type≡╠2 3 3╣:⍺ ⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:⍺ ⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:⍺ ╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			if (!cdf_alpha)
				CHK(6, cleanup, L"[371] 	type≡2 3 3:╠⍺╣ ⍺⍺ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alpha);
			
			{
				struct cell_array *x = stkhd[-1];
				struct cell_func *fn = stkhd[-2];
				struct cell_array *y = stkhd[-3];
				struct cell_array *dst;
			
				CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
				    L"[371] 	type≡2 3 3:╠⍺ ⍺⍺ ⍵⍵ ⍵╣");
			
				release_array(x);
				release_func(fn);
				release_array(y);
			
				stkhd -= 3;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[372] 	type╠≡╣0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[372] 	╠type╣≡0 3 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[372] 	╠type≡0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[372] 	type≡╠0 3 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[372] 	type≡0 3 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[372] 	type≡0 3 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 2, 3};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[373] 	type╠≡╣0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[373] 	╠type╣≡0 2 3:⍺⍺ ⍵⍵ ⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[373] 	╠type≡0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[373] 	type≡╠0 2 3╣:⍺⍺ ⍵⍵ ⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			if (!cdf_omega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ⍵⍵ ╠⍵╣");
			
			*stkhd++ = retain_cell(cdf_omega);
			
			if (!cdf_omegaomega)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵╣ ⍵");
			
			*stkhd++ = retain_cell(cdf_omegaomega);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:⍺⍺ ╠⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			if (!cdf_alphaalpha)
				CHK(6, cleanup, L"[373] 	type≡0 2 3:╠⍺⍺╣ ⍵⍵ ⍵");
			
			*stkhd++ = retain_cell(cdf_alphaalpha);
			
			{
				struct cell_func *fn = stkhd[-1];
				struct cell_array *y = stkhd[-2];
				struct cell_array *dst;
			
				CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
				    L"[373] 	type≡0 2 3:╠⍺⍺ ⍵⍵ ⍵╣");
			
				release_func(fn);
				release_array(y);
			
				stkhd -= 2;
				*stkhd++ = dst;
			}
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 1;
		int16_t dat[] = {0, 3, 2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		arr->shape[0] = 3;
	
		CHK(fill_array(arr, dat), cleanup, L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_eqv)
		CHK(6, cleanup, L"[374] 	type╠≡╣0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_eqv);
	
	if (!loc->cdf_type)
		CHK(6, cleanup, L"[374] 	╠type╣≡0 3 2:⍵ ⍺⍺ ⍵⍵");
	
	*stkhd++ = retain_cell(loc->cdf_type);
	
	{
		struct cell_array *x = stkhd[-1];
		struct cell_func *fn = stkhd[-2];
		struct cell_array *y = stkhd[-3];
		struct cell_array *dst;
	
		CHK((fn->fptr_dya)(&dst, x, y, fn), cleanup,
		    L"[374] 	╠type≡0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
		release_func(fn);
		release_array(y);
	
		stkhd -= 3;
		*stkhd++ = dst;
	}
	
	{
		struct cell_array *x = *--stkhd;
	
		TRC(guard_check(x), L"[374] 	type≡╠0 3 2╣:⍵ ⍺⍺ ⍵⍵");
	
		release_array(x);
	
		if (err > 0)
			goto cleanup;
	
		if (!err) {
			CHK(2, cleanup, L"[374] 	type≡0 3 2:╠⍵ ⍺⍺ ⍵⍵╣");
			
			*z = *--stkhd;
			goto cleanup;
			
			err = -1;
			goto cleanup;
		}
	
		err = 0;
	}
	
	{
		struct cell_array *arr;
	
		enum array_type typ = ARR_SINT;
		unsigned int rnk = 0;
		int16_t dat[] = {2};
	
		CHK(mk_array(&arr, typ, STG_HOST, rnk), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		CHK(fill_array(arr, dat), cleanup, L"[375] 	⎕SIGNAL ╠2╣");
	
		*stkhd++ = arr;
	}
	
	if (!cdf_prim.cdf_q_signal)
		CHK(6, cleanup, L"[375] 	╠⎕SIGNAL╣ 2");
	
	*stkhd++ = retain_cell(cdf_prim.cdf_q_signal);
	
	{
		struct cell_func *fn = stkhd[-1];
		struct cell_array *y = stkhd[-2];
		struct cell_array *dst;
	
		CHK((fn->fptr_mon)(&dst, y, fn), cleanup,
		    L"[375] 	╠⎕SIGNAL 2╣");
	
		release_func(fn);
		release_array(y);
	
		stkhd -= 2;
		*stkhd++ = dst;
	}
	
	*z = *--stkhd;
	goto cleanup;
	
	err = -1;

cleanup:
	release_env(stk, stkhd);
	release_env((void **)loc, (void **)(loc + 1));

	if (err)
		return err;

	TRC(chk_array_valid(*z), 
	    L"[369] jot←╠{╣");
	return err;
}

EXPORT int
cdf_q_signal(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_q_signal;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_q_signal_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_q_signal, z, l, r, L"cdf_q_signal");
}

EXPORT int
cdf_q_dr(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_q_dr;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_q_dr_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_q_dr, z, l, r, L"cdf_q_dr");
}

EXPORT int
cdf_squeeze(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_squeeze->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_squeeze_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_squeeze, z, l, r, L"cdf_squeeze");
}

EXPORT int
cdf_is_simple(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_simple->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_simple_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_simple, z, l, r, L"cdf_is_simple");
}

EXPORT int
cdf_is_numeric(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_numeric->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_numeric_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_numeric, z, l, r, L"cdf_is_numeric");
}

EXPORT int
cdf_is_char(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_char->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_char_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_char, z, l, r, L"cdf_is_char");
}

EXPORT int
cdf_is_integer(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_integer->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_integer_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_integer, z, l, r, L"cdf_is_integer");
}

EXPORT int
cdf_is_bool(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_bool->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_bool_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_bool, z, l, r, L"cdf_is_bool");
}

EXPORT int
cdf_is_span(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_is_span->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_is_span_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_is_span, z, l, r, L"cdf_is_span");
}

EXPORT int
cdf_max_shp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_max_shp->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_max_shp_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_max_shp, z, l, r, L"cdf_max_shp");
}

EXPORT int
cdf_has_nat_vals(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_has_nat_vals->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_has_nat_vals_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_has_nat_vals, z, l, r, L"cdf_has_nat_vals");
}

EXPORT int
cdf_chk_scl(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_chk_scl->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_chk_scl_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_chk_scl, z, l, r, L"cdf_chk_scl");
}

EXPORT int
cdf_chk_valid_shape(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_chk_valid_shape->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_chk_valid_shape_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_chk_valid_shape, z, l, r, L"cdf_chk_valid_shape");
}

EXPORT int
cdf_both_simple(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_both_simple->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_both_simple_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_both_simple, z, l, r, L"cdf_both_simple");
}

EXPORT int
cdf_both_numeric(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_both_numeric->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_both_numeric_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_both_numeric, z, l, r, L"cdf_both_numeric");
}

EXPORT int
cdf_both_integer(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_both_integer->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_both_integer_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_both_integer, z, l, r, L"cdf_both_integer");
}

EXPORT int
cdf_both_char(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_both_char->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_both_char_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_both_char, z, l, r, L"cdf_both_char");
}

EXPORT int
cdf_both_bool(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_both_bool->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_both_bool_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_both_bool, z, l, r, L"cdf_both_bool");
}

EXPORT int
cdf_any(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_any->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_any_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_any, z, l, r, L"cdf_any");
}

EXPORT int
cdf_idx_shp_check(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_idx_shp_check;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_idx_shp_check_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_idx_shp_check, z, l, r, L"cdf_idx_shp_check");
}

EXPORT int
cdf_idx_rng_check(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_idx_rng_check;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_idx_rng_check_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_idx_rng_check, z, l, r, L"cdf_idx_rng_check");
}

EXPORT int
cdf_set(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_set;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_set_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_set, z, l, r, L"cdf_set");
}

EXPORT int
cdf_brk(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_brk;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_brk_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_brk, z, l, r, L"cdf_brk");
}

EXPORT int
cdf_rgt(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_rgt;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_rgt_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_rgt, z, l, r, L"cdf_rgt");
}

EXPORT int
cdf_lft(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lft;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lft_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lft, z, l, r, L"cdf_lft");
}

EXPORT int
cdf_reshape(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_reshape;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_reshape_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_reshape, z, l, r, L"cdf_reshape");
}

EXPORT int
cdf_rho(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_rho;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_rho_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_rho, z, l, r, L"cdf_rho");
}

EXPORT int
cdf_cat(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_cat;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_cat_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_cat, z, l, r, L"cdf_cat");
}

EXPORT int
cdf_depth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_depth;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_depth_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_depth, z, l, r, L"cdf_depth");
}

EXPORT int
cdf_same(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_same->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_same_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_same, z, l, r, L"cdf_same");
}

EXPORT int
cdf_eqv(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_eqv;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_eqv_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_eqv, z, l, r, L"cdf_eqv");
}

EXPORT int
cdf_nqv(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_nqv;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_nqv_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_nqv, z, l, r, L"cdf_nqv");
}

EXPORT int
cdf_sqd_vec(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_sqd_vec->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_sqd_vec_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_sqd_vec, z, l, r, L"cdf_sqd_vec");
}

EXPORT int
cdf_sqd(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_sqd;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_sqd_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_sqd, z, l, r, L"cdf_sqd");
}

EXPORT int
cdf_index_gen(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_index_gen;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_index_gen_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_index_gen, z, l, r, L"cdf_index_gen");
}

EXPORT int
cdf_index_of(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_index_of;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_index_of_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_index_of, z, l, r, L"cdf_index_of");
}

EXPORT int
cdf_iot(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_iot;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_iot_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_iot, z, l, r, L"cdf_iot");
}

EXPORT int
cdf_dis(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_dis;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_dis_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_dis, z, l, r, L"cdf_dis");
}

EXPORT int
cdf_enclose(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_enclose;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_enclose_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_enclose, z, l, r, L"cdf_enclose");
}

EXPORT int
cdf_par(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_par;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_par_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_par, z, l, r, L"cdf_par");
}

EXPORT int
cdf_conjugate(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_conjugate;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_conjugate_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_conjugate, z, l, r, L"cdf_conjugate");
}

EXPORT int
cdf_add(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_add;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_add_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_add, z, l, r, L"cdf_add");
}

EXPORT int
cdf_sub(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_sub;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_sub_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_sub, z, l, r, L"cdf_sub");
}

EXPORT int
cdf_sign(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_sign;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_sign_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_sign, z, l, r, L"cdf_sign");
}

EXPORT int
cdf_mul(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_mul;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_mul_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_mul, z, l, r, L"cdf_mul");
}

EXPORT int
cdf_div(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_div;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_div_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_div, z, l, r, L"cdf_div");
}

EXPORT int
cdf_absolute(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_absolute;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_absolute_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_absolute, z, l, r, L"cdf_absolute");
}

EXPORT int
cdf_residue(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_residue;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_residue_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_residue, z, l, r, L"cdf_residue");
}

EXPORT int
cdf_res(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_res;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_res_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_res, z, l, r, L"cdf_res");
}

EXPORT int
cdf_floor_array(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_floor_array;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_floor_array_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_floor_array, z, l, r, L"cdf_floor_array");
}

EXPORT int
cdf_min(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_min;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_min_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_min, z, l, r, L"cdf_min");
}

EXPORT int
cdf_ceil_array(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_ceil_array;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_ceil_array_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_ceil_array, z, l, r, L"cdf_ceil_array");
}

EXPORT int
cdf_max(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_max;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_max_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_max, z, l, r, L"cdf_max");
}

EXPORT int
cdf_exp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_exp;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_exp_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_exp, z, l, r, L"cdf_exp");
}

EXPORT int
cdf_log(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_log;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_log_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_log, z, l, r, L"cdf_log");
}

EXPORT int
cdf_pitimes(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_pitimes;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_pitimes_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_pitimes, z, l, r, L"cdf_pitimes");
}

EXPORT int
cdf_trig(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_trig;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_trig_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_trig, z, l, r, L"cdf_trig");
}

EXPORT int
cdf_cir(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_cir;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_cir_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_cir, z, l, r, L"cdf_cir");
}

EXPORT int
cdf_binomial(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_binomial;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_binomial_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_binomial, z, l, r, L"cdf_binomial");
}

EXPORT int
cdf_fac(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_fac;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_fac_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_fac, z, l, r, L"cdf_fac");
}

EXPORT int
cdf_notscl(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_notscl;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_notscl_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_notscl, z, l, r, L"cdf_notscl");
}

EXPORT int
cdf_without(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_without;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_without_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_without, z, l, r, L"cdf_without");
}

EXPORT int
cdf_not(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_not;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_not_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_not, z, l, r, L"cdf_not");
}

EXPORT int
cdf_logand(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_logand;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_logand_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_logand, z, l, r, L"cdf_logand");
}

EXPORT int
cdf_and(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_and;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_and_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_and, z, l, r, L"cdf_and");
}

EXPORT int
cdf_logor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_logor;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_logor_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_logor, z, l, r, L"cdf_logor");
}

EXPORT int
cdf_lor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lor;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lor_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lor, z, l, r, L"cdf_lor");
}

EXPORT int
cdf_nan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_nan;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_nan_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_nan, z, l, r, L"cdf_nan");
}

EXPORT int
cdf_nor(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_nor;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_nor_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_nor, z, l, r, L"cdf_nor");
}

EXPORT int
cdf_lessthan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lessthan;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lessthan_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lessthan, z, l, r, L"cdf_lessthan");
}

EXPORT int
cdf_lth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lth;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lth_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lth, z, l, r, L"cdf_lth");
}

EXPORT int
cdf_lesseql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lesseql;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lesseql_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lesseql, z, l, r, L"cdf_lesseql");
}

EXPORT int
cdf_lte(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_lte;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_lte_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_lte, z, l, r, L"cdf_lte");
}

EXPORT int
cdf_eql_vec(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_eql_vec->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_eql_vec_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_eql_vec, z, l, r, L"cdf_eql_vec");
}

EXPORT int
cdf_equal(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_equal;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_equal_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_equal, z, l, r, L"cdf_equal");
}

EXPORT int
cdf_eql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_eql;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_eql_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_eql, z, l, r, L"cdf_eql");
}

EXPORT int
cdf_greatereql(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_greatereql;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_greatereql_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_greatereql, z, l, r, L"cdf_greatereql");
}

EXPORT int
cdf_gte(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_gte;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_gte_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_gte, z, l, r, L"cdf_gte");
}

EXPORT int
cdf_greaterthan(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_greaterthan;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_greaterthan_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_greaterthan, z, l, r, L"cdf_greaterthan");
}

EXPORT int
cdf_gth(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_gth;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_gth_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_gth, z, l, r, L"cdf_gth");
}

EXPORT int
cdf_firstocc(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_firstocc;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_firstocc_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_firstocc, z, l, r, L"cdf_firstocc");
}

EXPORT int
cdf_neq_vec(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_neq_vec->value;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_neq_vec_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_neq_vec, z, l, r, L"cdf_neq_vec");
}

EXPORT int
cdf_noteq(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_noteq;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_noteq_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_noteq, z, l, r, L"cdf_noteq");
}

EXPORT int
cdf_neq(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_neq;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_neq_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_neq, z, l, r, L"cdf_neq");
}

EXPORT int
cdf_mix(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_mix;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_mix_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_mix, z, l, r, L"cdf_mix");
}

EXPORT int
cdf_take(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_take;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_take_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_take, z, l, r, L"cdf_take");
}

EXPORT int
cdf_tke(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_tke;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_tke_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_tke, z, l, r, L"cdf_tke");
}

EXPORT int
cdf_split(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_split;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_split_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_split, z, l, r, L"cdf_split");
}

EXPORT int
cdf_drop(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_drop;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_drop_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_drop, z, l, r, L"cdf_drop");
}

EXPORT int
cdf_drp(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_drp;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_drp_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_drp, z, l, r, L"cdf_drp");
}

EXPORT int
cdf_reverse_last(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_reverse_last;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_reverse_last_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_reverse_last, z, l, r, L"cdf_reverse_last");
}

EXPORT int
cdf_rotate_last(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_rotate_last;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_rotate_last_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_rotate_last, z, l, r, L"cdf_rotate_last");
}

EXPORT int
cdf_rot(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_rot;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_rot_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_rot, z, l, r, L"cdf_rot");
}

EXPORT int
cdf_rpf(struct cell_array **z, struct cell_array *l, struct cell_array *r)
{
	struct cell_func *self;
	struct cdf_prim_loc *loc;
	int err;

	CHKFN(cdf_prim_init(), fail);

	loc = &cdf_prim;
	self = loc->cdf_rpf;

	if (l == NULL) {
		CHKFN(self->fptr_mon(z, r, self), fail);
	} else {
		CHKFN(self->fptr_dya(z, l, r, self), fail);
	}

fail:
	return err;
}

EXPORT int
cdf_rpf_dwa(void *z, void *l, void *r)
{
	return call_dwa(cdf_rpf, z, l, r, L"cdf_rpf");
}

